All done and verified. Final report:

## 批 CW：导入预览（iris 独有）— Complete

**Commit**: (see below) `feat(table): grid 批 CW——导入预览（iris 独有）` — 9 files, +254/−6

### Files changed

| File                                                                                          | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/props.ts`                                                | `importPreview?: boolean` top-level prop (after `zoomConfig`) — single-line boolean, named JSDoc (scanner hygiene clean)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/Table.tsx`                                               | destructure · module helper `previewColumnsFromRows` (first-row key order, pure + DOM-free) · local state `importPreviewRows` (null = closed) + **gate split exactly at `toolbar.onImport?.(rows)`** (parse/row-build unchanged; off → byte-identical) · `confirmImportPreview` (onImport with ALL rows, then close) / `cancelImportPreview` · open-only window Escape listener (zoom precedent) · fixed centered modal (Dialog backdrop + `--iris-z-modal` token precedent) in the toolbar fragment: title, first-5-rows table (`data-iris-import-preview-*`, headers in CSV order), `table.total` note when >5, 取消/确认 footer |
| `packages/react/src/primitives/table/test/import-preview.test.tsx`                            | **NEW, 12 tests, 253 lines** (≤500) — synchronous FileReader fixture (`vi.stubGlobal`, per-call text so re-imports carry the new file)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `packages/core/src/i18n.ts`                                                                   | +3 en keys: `table.importPreview.title/confirm/cancel` (total note reuses existing `table.total`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/plugin-locale-zh/src/core/index.ts`                                                 | +3 zh keys: `导入预览` / `确认导入` / `取消`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/manifest/{manifest.json,llms.txt}`                                                  | regenerated — `propCount 182→183` / `eventCount 32` unchanged (baseline prediction exact)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `docs/vxe-grid-comparison.md` · `DECISIONS.md` · `batch-cw-baseline.md` · `batch-cw-adapt.md` | 构建状态 tail + test-count line + stage bookkeeping                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

### Tests added (12)

① preview shows exactly 5 of 7 rows + `Total 7` note + CSV-ordered headers + truncation (spec ①) ② confirm → `onImport` once with ALL 7 rows, then closes (spec ②) ③ cancel → zero calls + closed (spec ③) ④ off → direct import byte-identical, zero modal nodes ⑤ Esc closes, zero calls ⑥ backdrop pointerdown closes (PointerEvent-absent fallback) ⑦ single-line CSV silent no-op ⑧ blank second line → one empty-string row, confirm imports it raw ⑨ total-note boundary (5 → none; 6 → `Total 6`) ⑩ re-import after cancel shows the NEW file (fresh per selection) ⑪ quoted RFC-4180 fields (embedded comma + newline) survive as raw strings in preview AND payload ⑫ zh locale chrome (`导入预览` / `共 6 条`)

### Verification (all green)

- **core: 1559/1559** — zero core logic changes (only 3 default-message keys); framework-free invariant intact (0 framework imports)
- **react typecheck** clean · **react test: 2648/2648** (2636 + 12) · **lint: 0 errors** (1 pre-existing `IrisTable` complexity warning)
- **spec: 0 violations** (1416 files) · **check:manifest** up to date · **check:docs-reference** up to date · **prettier** clean

### Deviations from the baseline design

1. **`onImport([])` (header-only) test dropped as infeasible**: `parseCsv` never yields a body-less parse with `length >= 2` — every pushed row has ≥1 field (blank lines push `['']`), so `rows` is never `[]` when the gate opens. The header-only CSV (`"a,b"` / `"a,b\n"`) is exactly the `<2`-line silent no-op (fiat preserved, tested in ⑦); the blank-second-line case (test ⑧) covers the closest reachable shape (`[{a:'',b:''}]`).
2. **Total note mounts beside the modal footer** per the `--iris-z-modal` Dialog precedent rather than the `--iris-z-popover` batch-edit-panel precedent (fixed overlay, not an anchored popover) — same toolbar-fragment render site.

### What's left

- The preview table shows the first 5 rows only (spec); no pagination/scroll inside the modal.
- Esc/backdrop/cancel all zero-call; confirm is the single commit path — no partial import UI (spec doesn't ask).
- Prior-stage dirt remains untracked: `docs/vxe-grid/batch-cv-gate.md` (belongs to the CV gate stage, not this batch).

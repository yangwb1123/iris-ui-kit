# Batch DY — 列显隐动画 `columnFade?: boolean`（iris 独有）

Done. Batch DY complete at adapt stage — implementation + tests + manifest regenerated + this report.

## Report

**Files changed (4 source + 1 test + manifest generated + this doc):**

| File                                                               | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/props/layout.ts`              | `columnFade?: boolean` after `onColumnVisibilityChange` (202→203 props, 33 events unchanged; zero i18n/core/type changes)                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/table-css.ts`                 | 3 new `TABLE_ROW_CSS` rules: `[data-iris-column-fade]` opacity transition · `[role="row"]` grid-template-columns transition (both `var(--iris-duration-md, 200ms)`) · reduced-motion freeze gate (`transition: none !important`) — both scoped by `[data-iris-column-fade-active]` so later drag-resizes never lag                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/Table.tsx`                    | Fade machine (layout-effect diff on `columnVisibility`; mount records base only; `effectiveColumnVisibility` overlay keeps mid-fade columns in `displayColumns` — **no useTableColumns signature change**; double-rAF pending→run flip; ONE 200ms commit timer; SSR/reduced-motion fail-closed) · `fadeByLeaf` expansion (grouped parents → all leaf tracks/cells) · track `Wpx↔0px` in `gridTemplateColumns` · `data-iris-column-fade` + inline `opacity: 0` on body/grouped-header/flat-header/totals cells · root `data-iris-column-fade-active` |
| `packages/react/src/primitives/table/summary-row.tsx`              | `TableSummaryRowProps` gains `columnFadeAttr`/`columnFadeStyle` (single-line function props; named exported interface) → summary cells carry the fade surface                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/react/src/primitives/table/test/batch-dy.test.tsx` (NEW) | **402 lines / 17 tests** (≤500 ✓)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/manifest/{manifest.json,llms.txt}`                       | Regenerated via `gen:manifest`: IrisTable **202→203 props / 33 events unchanged**, 155×4 components / 86 tokens unchanged                                                                                                                                                                                                                                                                                                                                                                                                                           |

**Tests added (17)** — 属性/样式 axes: ①② fail-closed (no prop → instant; mount-hidden never animates + 0-cell invariant) · ③④ hide/show full phase walk (pending = mounted at full/0 width without/with inline opacity 0, run = `opacity: 0` + `0px` track, commit = unmount/attr drop) · ⑤ multi-column under ONE commit window · ⑥⑦ mid-fade reversals (restart in new direction, column never unmounts) · ⑧ opacity-key absence when not fading · ⑨ template restore by string identity (= never-shown table) · ⑩ non-numeric `1fr` tracks graceful degrade · ⑪ header cells · ⑫ summary-row cells (global summary path) · ⑬ column-totals cells · ⑭ grouped parent → every leaf cell + leaf track · ⑮ stylesheet (token-driven transitions, no hardcoded duration, freeze gate) · ⑯ reduced motion JS skip (matchMedia stub, instant, zero timers) · ⑰ settle invariants (exactly ONE pending timer during run → zero after commit; re-toggle re-bases).

**Counts:** react 251→**252 files / 2890→2907 tests** · core **106 files / 1641** unchanged · manifest **203 props / 33 events**.

**Verification (all green):**

- `@iris-ui-kit/core test`: 106 files / **1641/1641** ✓
- `@iris-ui-kit/react typecheck`: clean ✓
- `@iris-ui-kit/react test`: 252 files / **2907/2907** ✓
- `@iris-ui-kit/react lint`: **0 errors** (2 pre-existing complexity warnings in Table.tsx, untouched)
- `iris-ui-spec.py --mode all --json`: **0 violations / 1546 files** (all new CSS is `--iris-*` token driven)
- `pnpm gen:manifest` regenerated (202→203) ✓ · `pnpm check:manifest` up to date ✓ · manifest tests 69 ✓ · prettier clean ✓

**Fiats / documented limitations:** footerMethod/footerData inline rows in footer-stack.tsx are NOT per-cell faded (their `[role="row"]` grid-template still animates the collapse; 4-file budget kept footer-stack untouched) · `spanMethod`-covered cells collapse with their origin column · non-numeric tracks fade via `0px` hop (grid-template interpolation is CSS-level) · a skin overriding `--iris-duration-md` stretches the CSS transition past the JS commit window · responsive tail-trim interplay not covered.

**What is left:** nothing within scope — React-only, additive, core untouched. `check:docs-reference` reports stale `apps/docs/components.md` — pre-existing (the file is gitignored/untracked; verified failing on a HEAD-clean stash too, unrelated to this batch). Pre-existing working-tree dirt (grid-refactor, `docs/*`, `core/src/index.ts`, plugin exports, script baselines, `aero-platform-console`, `pi-batch.yaml`, `pipeline.grid-refactor.yaml`) deliberately left uncommitted/untouched.

Batch CC complete — all gates green on the committed state.

## Report

**Commits** (2, following CB/CA convention):

- `feat(table): grid 批 CC——编辑自动换行（iris 独有）` — source + manifest + comparison doc + baseline doc
- `docs(table): batch CC adapt report`

**Files changed** (per baseline file map, exactly):

| File                                                         | Change                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/props.ts`               | `editAutoHeight?: boolean` after `onAutosave` (single-line prop, scanner JSDoc convention; manifest propCount 167→168)                                                                                                                                                                                                                                                                                                                               |
| `packages/react/src/primitives/table/Table.tsx`              | destructure `editAutoHeight` · `EditorSurfaceProps` + `editAutoHeight?: boolean` · module helpers (`EDITOR_AUTO_MAX_ROWS=6` / `EDITOR_AUTO_FALLBACK_LINE_HEIGHT=16` / exported pure `autoHeightSize` / `measureEditorLineHeight` session cache / `applyEditorAutoHeight`) · textarea branch `rows={editAutoHeight ? 1 : 3}` + `onInput` · mount-measure effect (pre-filled multi-line drafts) · both call sites (row mode + cell mode) pass the prop |
| `packages/react/src/primitives/table/edit-autowrap.test.tsx` | **NEW** — 10 tests (pure 5 + integration 5)                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/manifest/{manifest.json,llms.txt}`                 | regenerated via `gen:manifest` (propCount 167→168, eventCount 31 unchanged)                                                                                                                                                                                                                                                                                                                                                                          |
| `docs/vxe-grid-comparison.md`                                | persistState row + 构建状态 line (comparison doc 2 处)                                                                                                                                                                                                                                                                                                                                                                                               |

**Tests added**: 10 — pure `autoHeightSize` 5 (增长 40→40px / 上限 300→120px+auto / 边界 120→hidden / 收缩 60→60px / 单行下限 0→20px) · integration 5 (默认 off 回归锁批 I `rows=3` + 无 inline height / on rows=1 + onInput 增长接线 40px / 上限接线 96px+auto / 交互不变量 Enter·Shift+Enter·Escape·commit / row-mode 共享同一 surface). jsdom 无布局故 scrollHeight 由 `Object.defineProperty` mock（F9），数学全在纯函数（lineHeight 20→maxHeight 120；jsdom 集成线 lineHeight 回退 16→maxHeight 96）。

**Counts**: react 2399→**2409** · manifest propCount 167→**168** · eventCount 31 unchanged · spec 0 violations (1415 files).

**Verification** (all ✅): core test 1533/1533 · react typecheck clean · react test 2409/2409 · react lint 0 errors (1 pre-existing complexity warning on IrisTable) · `iris-ui-spec.py --mode all` 0 violations · `gen:manifest` regenerated + committed (`check:manifest` up-to-date, `check:framework-parity` green, `check:docs-reference` up to date — components.md is a gitignored generated artifact) · react build + prettier clean. Zero changes: core / types.ts / styles.ts / i18n / TableAnnotatePanel 批注 textarea（独立 surface，spec 范围外）/ vue/solid/svelte.

**What is left**: runner's review/gate stage; working-tree `DECISIONS.md`/`batch-cb-gate.md` are prior-stage leftovers (untouched, per CB precedent); baseline open questions deferred — O1 initial rows=1 (vs 3) chosen per spec「单行起步」, O2 per-column control deferred (table-level prop per spec), O3 configurable cap deferred (spec fixes max 6); F6 semantics documented — with fixed rowHeight/virtual rows the editor scrolls within its own maxHeight (expected, not a defect).

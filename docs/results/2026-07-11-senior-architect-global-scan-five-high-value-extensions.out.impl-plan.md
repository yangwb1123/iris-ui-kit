已创建分析文档并保存到 `/home/u1/iris-ui/docs/requirements/2026-07-11-senior-architect-global-scan-five-high-value-extensions.out.md`。

---

## 关键发现摘要

### 🔍 源码验证发现（勘误）

在分解任务前，我直接阅读了 core 源码验证文档中的 claims，发现**原文 2 个方向的基础设施已被近期提交覆盖**：

| 方向               | 原文断言                       | 源码事实                                                                                                                     |
| ------------------ | ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| **② 动态表单数组** | "四框架都没有 `useFieldArray`" | **四框架全有** `useFieldArray.ts`，桥接 `createFormStore.arrayPush/Insert/Remove/Swap/Move`                                  |
| **③ 撤销/重做**    | "`commands.ts` 无 undo/redo"   | `core/undo.ts` 已有 `createUndoStack`；`createFormStore` 已有 `undo()/redo()/canUndo()/canRedo()`；React 已有 `useUndoStack` |

### 📋 任务分解结果

| 方向           | 任务数 | 工时     | 实际状态                             |
| -------------- | ------ | -------- | ------------------------------------ |
| ① 实时数据     | 9      | 25.5h    | 从头新建                             |
| ② 动态表单数组 | **1**  | **6h**   | 几乎完成（仅缺 renderless 原语组件） |
| ③ 撤销/重做    | 6      | 18h      | 引擎就绪，缺上层集成                 |
| ④ 跨容器拖放   | 13     | 42h      | 从头新建（最大工程）                 |
| ⑤ 状态持久化   | 12     | 27.5h    | 从头新建                             |
| **总计**       | **41** | **119h** | ≈ 3 人周                             |

### 🎯 推荐执行优先级

基于真实工作量把脉后的建议：

1. **P0 → ⑤ 状态持久化**（27.5h）— 与现有 Store 正交叠加，立竿见影
2. **P0 → ① 实时数据**（25.5h）— 差异化最高，DataSource 接口向后兼容
3. **P1 → ③ 撤销/重做集成**（18h）— 引擎已完成，仅需补齐集成
4. **P1 → ② IrisFormArray**（6h）— 快速收益
5. **P2 → ④ 跨容器拖放**（42h）— 最大工程，建议作为 v0.2 大特性

文档包含完整 Mermaid 依赖图、甘特图时间线、每个任务的验收标准、技术风险矩阵、性能测试需求和代码审查要点。

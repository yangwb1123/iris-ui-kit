Batch BB (annotation editing) complete and committed (working tree clean).

## Report

**Files changed** (4 mandated edits + 1 new test + 1 docs + 2 generated + 2 runner bookkeeping):

| File                                                                        | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/props.ts`                              | `annotationEditing?: boolean` + `onAnnotationsChange?: (next: Record<string, string>) => void` — both **single-line** declarations (manifest-scanner hygiene); manifest captures both (`propCount` 144→146)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/Table.tsx`                             | 3 reserved keys (`ANNOTATE_MENU_KEY`/`ANNOTATE_EDIT_MENU_KEY`/`ANNOTATE_REMOVE_MENU_KEY`) + panel state/handlers (`openAnnotate`/`saveAnnotation`/`removeAnnotationKey` — save with empty text removes the key, non-empty sets it; all writes via `onAnnotationsChange`, inert without it) + context-menu append **after the summary item** (add when no note, edit+remove when one — existence = `annotations[cellId(rowKeyOf(row, idx), col.key)]`, same dedupe guard) + onSelect interception + panel render + **module-level `TableAnnotatePanel`** (distribution/summary mold: useFloating + useDismiss + portal + virtual cursor anchor + seq remount, Esc/outside/scroll close; textarea `data-iris-annotate-input` seeded from `annotations[cellKey]` + 保存 `data-iris-annotate-save` + 删除 `data-iris-annotate-remove` only when a note exists; token-only styles). **Deviation from the baseline file map**: the panel lives inline in `Table.tsx` (batch-AL batch-edit-panel precedent) instead of a new `AnnotatePanel.tsx` — the task hard-constraint caps changed files at 4 + tests. |
| `packages/core/src/i18n.ts` · `packages/plugin-locale-zh/src/core/index.ts` | `table.annotate` / `table.annotate.edit` / `table.annotate.remove` / `table.annotate.save` — en (`Add annotation` / `Edit annotation` / `Remove annotation` / `Save`) + zh (`添加批注` / `编辑批注` / `删除批注` / `保存`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/react/src/primitives/table/annotate-editing.test.tsx`             | **NEW** — 11 tests, 380 lines (≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `docs/vxe-grid-comparison.md`                                               | iris 独有 table row (批注编辑 `annotationEditing`/`onAnnotationsChange`) + 批 BB batch row + 构建状态 + counts (react 2087→2098) + 用法示例 snippet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `manifest.json`/`llms.txt`                                                  | regenerated via `pnpm gen:manifest` (propCount 144→146, both props single-line); `check:manifest` up-to-date; `check:docs-reference` up-to-date (components.md regenerated, gitignored)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Tests added — 11** (`annotate-editing.test.tsx`): menu items vary by the cell's existing note (add when none, edit+remove when one) · annotate items append AFTER the built-in 摘要 item (valueDistribution+nlSummary ordering) · save writes the key through `onAnnotationsChange` (end-to-end: badge appears) · save with empty/whitespace text removes the key (textarea seeded from the existing note) · panel 删除 button removes the key (button only when a note exists) · menu 删除批注 item deletes directly (no panel) · no `onAnnotationsChange` → items still show but save/remove are inert (documented; panel stays open, no crash) · without `annotationEditing` → no built-in items · Esc / outside pointer-down close the panel · reserved keys never reach the user onSelect (user items still fire) · user item with a reserved key is not duplicated (still routes to the panel).

**Counts:**

- `typecheck` (react) — clean
- `test` — react **2098 passed** (186 files, +11 this batch) · core **1438 passed** · plugin-locale-zh **6 passed** (zh drift guard covers the 4 new keys)
- `lint` (react) — **0 errors** (1 pre-existing `IrisTable` complexity warning, grew 248→250 additive — pre-existing at HEAD per batch-BA report)
- `iris-ui-spec.py --mode all --json` — **0 violations**, exit 0
- `gen:manifest` — regenerated & committed; `check:manifest` + `check:docs-reference` up-to-date
- `prettier` — clean on all touched files

**What is left:**

- The pipeline gate stage (full-repo `turbo run test typecheck lint build`, 27-package pack-install, browser E2E/visual regression, `pnpm bench`) — outside this task's scope, per the baseline
- Deviation flagged for review: `TableAnnotatePanel` is inline in `Table.tsx` (module-level) instead of the baseline's proposed new `AnnotatePanel.tsx` file, to honor the ≤4-changed-files hard constraint; also the baseline's flagged fiat #10 (items gate on `annotationEditing && onAnnotationsChange`) was overridden by the task's IMPLEMENT spec — items gate on `annotationEditing` alone, and "no callbacks → items still show but save is inert" is implemented + tested + documented

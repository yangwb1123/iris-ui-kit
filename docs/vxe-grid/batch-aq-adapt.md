All done and committed. Final report:

## Report — batch AQ (drag fill `rangeFill`) ✅

**Commit `c77a9048`** — `feat(react): add IrisTable drag fill (rangeFill, batch AQ, iris 独有)`

### Files changed (10: 4 source+test, 3 docs, 2 generated)

| File                                 | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `props.ts`                           | `rangeFill?: boolean` — single-line prop (scanner-safe), multi-line JSDoc (iris 独有 text flows into manifest → generated docs)                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `Table.tsx`                          | destructure `rangeFill = false`; 4 **module-level pure helpers** (kept the cell-render arrow at its eslint complexity budget — verified 78→baseline); `fillTarget` state + `isRangeFillTarget` + pointer down/move/up handlers + `fillRangeFromHandle` (cyclic fill via `getCellValue`, formula columns + keyless rows skipped, **ONE `commitRowList`**, then `extendRange` to the drag end); **window-capture suppress listener** so the floating toolbar's `useDismiss` doesn't clear the range on a handle press; root `onPointerMove/Up` extended `\|\| rangeFill`, new `onPointerCancel` |
| `styles.ts`                          | `RANGE_FILL_HANDLE_STYLE` (6px primary square, absolute right/bottom 2px, crosshair, zIndex 3) + `RANGE_FILL_TARGET_BG` — `--iris-*` tokens only                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **NEW** `range-fill.test.tsx`        | **13 tests**, 324 lines (≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `docs/vxe-grid-comparison.md`        | iris 独有 section +`rangeFill` row, 构建状态 +批 AQ, batch table +批 AQ row                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `manifest.json` + `llms.txt`         | regenerated via `gen:manifest` (155×4, `rangeFill` extracted native)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `DECISIONS.md` + `batch-aq-adapt.md` | stage log + adapt report (baseline doc also committed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

### Tests added (13)

Handle render (no rangeFill / no range / 6px-token-placement) · press-is-not-a-click (range survives toolbar dismissal) · cyclic down-fill + range extension + handle relocation · A/B/A multi-row cycle · right-column fill · L-shape target highlight (source excluded, cleared on release) · up/left ignored (no fill, no growth) · pointercancel drops highlight without committing · bounds clamp · formula-column skip · keyless-row no-op · exactly one `onDataChange` per fill.

### Gates

- `typecheck` ✅ · `test` ✅ **1978 passed** (175 files, +13, zero regressions) · `lint` ✅ **0 errors** (1 pre-existing `IrisTable` complexity warning, 219→223 — the arrow callback stayed at budget) · `iris-ui-spec.py --mode all --json` ✅ **0 violations** · `gen:manifest` ✅ regenerated + committed; `check:manifest` + `check:docs-reference` ✅ (components.md regenerated — it's gitignored/generated)

### What is left / notes

- **types.ts untouched** — `IrisTableProps` lives in props.ts, so nothing to add there (kept within the 4-file cap).
- **aria-label omitted** on the handle (non-focusable pointer-only span — axe-safe; the baseline's i18n key plan would have added a 5th source file, excluded by the hard cap).
- **Documented simplifications**: hit-testing via `elementFromPoint` resolves rendered cells only (virtual-scroll off-screen rows unreachable); Escape mid-drag clears the range like any Escape; down/right-only semantics (`max(pointer, range.end)` + bounds clamp) in the prop JSDoc + comparison doc.

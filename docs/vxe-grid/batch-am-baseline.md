Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-am-baseline.md` (105 lines). No source files touched — `git status` shows only the new doc.

**What the baseline establishes** (all verified against current code):

**a) `IrisTableColumn.suggest?: boolean | Array<string | number>`** — hooks into `EditorSurface`'s shared text/number `<input>` branch (Table.tsx:383). Since text and number share one element, the `list` attr + `<datalist data-iris-edit-suggest>` is gated on `col.editor !== 'number'` — **text editor only**, select/textarea/number unaffected. Suggestions computed in Table.tsx as a memo over `[bodyData, leafColumns]` (only `suggest === true` columns, array values pass through with zero scan) and passed down as `suggestOptions` so EditorSurface stays free of bodyData. `true` → distinct `String` values (null/'' excluded), sorted, max 50; array → used directly. `datalistId` from `React.useId()` (repo pattern). "Only while editing" is automatic — EditorSurface mounts only during a session.

**b) `valueDistribution?: boolean`** — decision documented: **separate prop appending a built-in context-menu item, not a toolbar button** (per-column scoping — the right-click already carries `IrisTableContextMenuParams.column`; a toolbar button would need an implicit "current column" that doesn't exist; toolbar already crowded). Item `{ key: '__iris-distribution', label: t('table.distribution') }` appended after user items in `handleContextMenu`; intercepted at the onSelect wiring (Table.tsx:6042) so built-in keys never reach the user callback; reserved-key dedupe guard. New `DistributionPanel.tsx` (FilterPanel/ContextMenu pattern, dedicated virtual anchor) shows top 20 + "其余 N 个" (`table.distribution.others`), computed via the core function over `bodyData` with the `dataIndex ?? key` indirection.

**c) Core `valueDistribution(rows, key)`** — new `value-distribution.ts` + barrel export (rangeStats precedent): null/undefined/`''` excluded, `String` coercion, count desc, first-appearance tie order, `[]` for empty/all-empty.

File map: 2 new core files, 2 new react files, 5 edits (core index.ts/i18n.ts, table types.ts/props.ts/Table.tsx). Untouched: ContextMenu.tsx, TableRow.tsx, useTableState.ts, batch-AL undo machinery (datalist pick and distribution are read-only — no new mutation funnels). ~18 tests across core (8) and react (10).

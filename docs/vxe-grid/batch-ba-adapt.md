Done. Batch BA (version history) is implemented, verified, and committed (`1cc821d0`, working tree clean).

## Files changed (15 — 4 new + 7 mandated edits + 2 generated + 2 runner bookkeeping)

| File                                                                        | Change                                                                                                                                                                                                                                                                                                                                                         |
| --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/version-history.ts` **NEW**                              | `createVersionHistory<Row>({ max? })` — audit-ring mold 1:1: default max 20 (0 unlimited), newest-first, monotonic `index` never reset by `clear()`, `list()` entry copies (rows shared refs), `get(index)` copy/undefined, `subscribe`/`getVersion`                                                                                                           |
| `packages/core/src/index.ts`                                                | barrel export + types                                                                                                                                                                                                                                                                                                                                          |
| `packages/core/src/i18n.ts` · `packages/plugin-locale-zh/src/core/index.ts` | `table.history` / `table.history.empty` en + zh                                                                                                                                                                                                                                                                                                                |
| `packages/react/src/primitives/table/props.ts`                              | `versionHistory?: { max?: number }` — **single-line** (manifest-scanner hygiene) + doc                                                                                                                                                                                                                                                                         |
| `packages/react/src/primitives/table/Table.tsx`                             | ref-once controller (max from first render) + `historyEnabledRef` gate + `recordHistory` (pushes `auditRowsRef` PRE-change rows + the batch-AT type hint **before** recordAudit's eager sync) + `historySuppressRef` + `restoreVersion` (commitRowList `'undo'`, suppressed) + toolbar gate/trigger (⏱) + panel render + handle `getVersions`/`restoreVersion` |
| `packages/react/src/primitives/table/VersionHistoryPanel.tsx` **NEW**       | floating panel: newest-first `#index + formatClock + type label`, click entry → restore + close, Esc/outside/scroll close, `useSyncExternalStore` self-refresh, empty state, deliberately no clear button                                                                                                                                                      |
| `packages/react/src/primitives/table/types.ts`                              | `IrisTableVersionEntry` (lightweight — no rows) + 2 handle methods                                                                                                                                                                                                                                                                                             |
| `docs/vxe-grid-comparison.md`                                               | iris 独有 table row + 批 BA batch row + 构建状态 + counts + 用法示例                                                                                                                                                                                                                                                                                           |
| `manifest.json`/`llms.txt`                                                  | regenerated via `pnpm gen:manifest` (propCount 143→144, versionHistory captured)                                                                                                                                                                                                                                                                               |

## Tests added — 23

- **core `version-history.test.ts` — 14 tests**: empty start · pre-change rows+type+index/at fill · default `edit` · default max 20 · ring trim · max 0 unlimited · newest-first · `get` copy/shared-rows · `get` unknown/trimmed/cleared → undefined · `list` copies · clear never resets index · empty clear no-notify · subscribe/unsub/version bump · no-subscriber inert
- **react `version-history.test.tsx` — 9 tests** (217 lines): edit (updateRow) pushes lightweight version (index 0, no rows) · insert/remove types + monotonic index · inline cell edit (commitValue) does **not** push · panel newest-first + click restores + closes without re-pushing · handle restore + next commit resumes monotonic index · unknown index no-op · undo replay pushes `'undo'` · Esc/outside close · no prop → inert

## Verification

- core test **1438 passed** (+14) · react test **2087 passed** (+9) · typecheck (core + react) clean · react lint **0 errors** (1 pre-existing IrisTable complexity warning, grew 241→248 additive) · `iris-ui-spec.py --mode all --json` **0 violations** · `check:manifest` up to date · prettier clean

## What is left

- `@iris-ui-kit/core/version-history` **own subpath** (package.json exports + tsup entry) — skipped to stay within the file-count budget; barrel export covers tests/adapters (flagged for the review stage)
- Full-repo gate (turbo all-packages, 27-package pack-install, E2E/visual, bench) — outside this task's scope per the baseline
- `max` is read once at first render (ref-once, documented); panel has no clear button (restore is the wipe, per baseline F7)

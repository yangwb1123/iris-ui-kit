Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-aw-baseline.md` (155 lines). `git status` confirms only the new doc — zero source files touched.

**Key findings (every claim verified against current code):**

- **The batch-AM distribution chain is the exact mirror to clone** — reserved key `__iris_distribution` (Table.tsx:580–583) → appended in `handleContextMenu` after user items (Table.tsx:2648–2654, dedupe-guarded) → intercepted at `onSelect` (Table.tsx:6997–7004) → `openDistribution` snapshots the menu's virtual cursor anchor (Table.tsx:2579–2587) → `TableDistributionPanel` (DistributionPanel.tsx) rides `useFloating` + `useDismiss` + capture-scroll + portal. The summary item replicates this five-point chain: `SUMMARY_MENU_KEY = '__iris-summary'`, `nlSummary` push after the 值分布 item, panel cloned in `SummaryPanel.tsx`.
- **`valueDistribution` is directly reusable for the categorical branch** — its semantics (null/undefined/`''` excluded, `String` coercion, count desc, first-appearance ties) are exactly the top3 ranking needed. Design: extract the counting loop into `countDistinctValues` (value-distribution.ts, public API byte-identical), `summarizeColumn` imports it.
- **i18n is a two-file hard gate**: en in core `defaultMessages` (i18n.ts:124–125), zh in `plugin-locale-zh` (index.ts:62–63) — the zh plugin's `translates EVERY built-in key` parity test fails `make ci` if `table.summary` is added to only one side.
- **Manifest is generated and gated** (`pnpm gen:manifest` / `check:manifest`); `IrisTable` is at 139 props with `valueDistribution` at index 115 → `nlSummary` bumps propCount to 140.

**Design locked:** (a) core `summarizeColumn(values, label)` — ≥60% (inclusive) of non-empty values numeric → `label：共 N 个值，范围 min–max，平均 avg，M 个缺失` (all `.toFixed(1)`, single rounding point); else categorical top3 with integer `Math.round` percentages + `，其余 R 个` only when R > 0; empty → `label：无数据`; (b) `nlSummary?: boolean` appends 摘要 after 值分布, panel `data-iris-summary-panel` (title header + muted summary text, `--iris-*` tokens only, Esc/outside/scroll closes); (c) `table.summary` en `'Column summary'` / zh `'摘要'`.

**File map:** 4 new files (2 core incl. tests, SummaryPanel.tsx, summary-panel.test.tsx) + 6 edits (value-distribution extraction, core index barrel, core i18n, zh plugin, props.ts, Table.tsx 5 touch points) + manifest regen. Untouched: `ContextMenu.tsx`, `TableRow.tsx`, `useTableState.ts`, `DistributionPanel.tsx`, `types.ts`. 12 numbered fiats (F1–F12) included for gate arbitration, covering the threshold boundary, rounding rules, top3 truncation, item order, dismissal set, and the en+zh parity gate.

Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-z-baseline.md` (174 lines). `git status` confirms the only change is the new doc — no source files touched.

**Key findings baked in:**

1. **Vue edit machinery is a bespoke singleton session** (`editingCellId/editingDraft/editError` + `beginEdit/commitEdit/finishCommit/cancelEdit`, L948–1055) — no core `createCellEdit` bridge (React uses per-column `CellEdit` sessions). Row mode design extends the bespoke refs into a reactive `Map<cellId, {draft, error}>` with a shared commit core, keeping cell mode byte-identical. Vue `editConfig` has `trigger` but **no `mode`**.
2. **Vue HAS `getSubRows`** — tree mode exists (flattenTree + expansion model + caret render + virtualScroll support). `lazyLoad` is purely additive: `treeMode` widens, `lazyChildrenOf` wins over `getSubRows`, reactive `lazyLoading` set drives `data-iris-tree-loading`, plus the React M2 epoch fix (data re-feed clears cache + drops stale callbacks).
3. **contextMenu**: vue `useFloating` has **no virtual-anchor type** — but React's is typed `HTMLElement` too and it just casts a fake cursor-rect element at the call site; `computePosition`/`autoUpdate` accept `VirtualElement` at runtime, so the same cast works with **zero hook changes**. New `ContextMenu.ts` = useFloating (flip/shift off) + useDismiss + capture-scroll close + `Teleport` to body (React uses createPortal) + seq remount key.
4. **filter panel**: i18n keys `table.filter/filterConfirm/filterClear` already exist in core; header trigger goes in both flat + grouped leaf headers; `filteredData` gains an OR-match sets channel AND-ed with the text channel; remote merge = `mergeFilterValues(mergeFormFilters(...), filterValues)` comma-join — matching React's `mergeFilterValues` exactly.
5. **Tests**: 4 new vitest files mirroring React's `row-edit-mode`/`lazy-tree`/`context-menu`/`filter-panel` shapes; 7 open questions for the adapt stage (manual-trigger × row mode, `clearFilter` surface, editor right-click parity, etc.).

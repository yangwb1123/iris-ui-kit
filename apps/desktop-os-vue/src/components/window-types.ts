/** Type barrel for Window.vue — the core window-manager types plus the
 *  IrisMovable/IrisResizable payload shapes, in one import. */
export type { DesktopWindow, SnapZone } from '@iris-ui-kit/core/window'
export type { IrisMovablePosition, IrisResizableSize } from '@iris-ui-kit/vue'

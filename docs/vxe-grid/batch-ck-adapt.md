# Adapt report — batch CK：单元格文本搜索高亮（iris 独有）

Done. All verification gates pass; committed as `d208c8fc`.

## Files changed (10 in commit, 7 source + 3 tests)

| File                                                            | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/search-highlight.ts`                         | **NEW** pure framework-free splitter `splitSearchHits(text, query): string[] \| null` — case-insensitive LITERAL substring (no regex — metacharacters match themselves, fnr parity), non-overlapping forward scan (fnr replace-all `gi` parity — `'aaa'`+`'aa'` = one hit), alternating `[plain, hit, plain, hit, …]` segments (leading empty string when text starts with a hit, so adapters mark odd indices unconditionally); `null` on empty query/empty text/no-match (the `detectAutoLink` null contract — adapter fast-path returns the original string byte-identical). Never throws. |
| `packages/core/src/search-highlight.test.ts`                    | **NEW**, 11 tests (hit segments / case-insensitivity preserving casing / multi-hit / non-overlap `aaa`+`aa` / adjacent hits / no-match null / empty query null / empty text null / CJK / literal metacharacters / whole-text single hit)                                                                                                                                                                                                                                                                                                                                                      |
| `packages/core/src/index.ts`                                    | barrel: `export { splitSearchHits } from './search-highlight'` (next to `detectAutoLink`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/react/src/primitives/table/props.ts`                  | `searchHighlight?: string` after `fnr` (baseline-指定位置, JSDoc `*/` on its own line — the batch-CJ P3 lesson)                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/react/src/primitives/table/styles.ts`                 | **NEW** `SEARCH_HIT_STYLE` (token-only: `--iris-surface-selected` fallback — the SAME token `fnrCellStyle` uses; `color: inherit`, radius-sm, space-xxs padding)                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `packages/react/src/primitives/table/Table.tsx`                 | 4 touch points: core import `splitSearchHits` + styles import `SEARCH_HIT_STYLE` · module helper `applySearchHighlight` next to `renderAutoLinkCell` (row-arrow complexity budget flat — pure function, fail-closed: non-string nodes / null segments / empty query pass through untouched) · destructure `searchHighlight` · formatter branch + raw fallback call sites (the exact text chain autoLink consumes: mask → formatter ?? raw)                                                                                                                                                    |
| `packages/react/src/primitives/table/search-highlight.test.tsx` | **NEW**, 11 tests (T1/T2 fail-closed no-prop + empty-query / T3 高亮 mark with matched substring, cell text byte-identical / T4 case-insensitive preserving casing / T5 multi-hit / T6 numeric untouched / T7 formatter-output highlight / T8 render+html untouched / T9 link label untouched (link wins) / T10 **fnr coexistence** — same cell carries `data-iris-fnr-match` AND the inline mark, the spec's 与 fnr 区分 proof / T11 rerender clears)                                                                                                                                        |
| `packages/manifest/manifest.json` + `llms.txt`                  | regenerated via `gen:manifest` (IrisTable props **170→171**, events 31 unchanged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## Tests added / counts

- core `search-highlight.test.ts`: **+11** → core suite **1559 passed** (was 1548; baseline ✓)
- react `search-highlight.test.tsx`: **+11** → react suite **2503 passed** (was 2492; baseline ✓)

## Verification (all green)

- `core test`: 1559 ✓ · `react typecheck`: clean ✓ · `react test`: 2503 ✓ · `react lint`: 0 errors (1 pre-existing `IrisTable` complexity warning) ✓
- `iris-ui-spec.py --mode all --json`: **0 violations** (1416 files) ✓
- `gen:manifest` regenerated + committed: IrisTable props **170→171** (searchHighlight after fnr), events 31 unchanged, tokens 86 unchanged ✓
- `check:manifest` up to date ✓ · manifest suite 69 passed ✓ · prettier clean on all 7 source/test files ✓
- `arch-check` ratchet: pre-existing failure on clean HEAD `bdc437a9` (stale `arch-baseline.json` since d7d281de — Table.tsx already 10117 > baseline 1255 without my change; my +31 lines don't change pass/fail) — outside this batch's verify gates

## Scope notes

- **File count**: 5 source files (baseline file map exactly: new core `search-highlight.ts` + barrel `index.ts` — needed for react to resolve the new export, per batch-CJ precedent — + Table.tsx + props.ts + styles.ts) + 2 tests.
- Manifest hygiene: new prop is a single-line `searchHighlight?: string` (no function props); no new interfaces (reuses `IrisTableProps`); no new core types (`splitSearchHits` is a function export); no `as any`/TODO.
- Zero changes to types.ts, i18n, events, audit-enum, and the other three frameworks (react-only bridge, per the established 批次 convention).

## What's left (subsequent stages)

- Review/gate stages: `docs/vxe-grid/batch-ck-review.md` / `-gate.md` reports + `docs/vxe-grid-comparison.md` batch entry (iris 独有 单元格文本搜索高亮 `searchHighlight` row + 构建状态 批 CK entry — prior batches update the comparison doc in the gate stage).
- `docs/vxe-grid/DECISIONS.md` + `batch-cj-gate.md` were pre-existing dirty before this batch — left untouched.

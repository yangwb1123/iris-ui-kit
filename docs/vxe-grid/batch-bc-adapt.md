All done and committed (`bdbc472d`, working tree clean).

## Report

**Batch BC — 跨表引用公式 (cross-table formula refs) complete.**

### Files changed (4 mandated + 1 barrel line + 2 test files + docs)

| File                                                          | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/core/src/formula.ts`                                | `!` bang token in TOKEN_RE · `parsePrimary` ident branch resolves `table!field` via new `resolveTableField` (own-property-only, fail-closed: missing tables arg / unknown table / **empty table** / missing field → whole formula null; known nullish coerces, Excel parity) · `evaluateFormula` optional 3rd `formulaTables` param (backward-compatible) · `memoizedFormulaValue` cache key → (row, tables identity, formula) nested WeakMaps + module sentinel for 2-arg calls (AO byte-compatible) · new exported `FormulaTables` type + module doc |
| `packages/core/src/index.ts`                                  | barrel re-export of `type FormulaTables` (1 line, required for the type to be package-reachable — scanner hygiene "export new types")                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/props.ts`                | `formulaTables?: Record<string, Row[]>` — single-line prop, single-line doc (manifest hygiene); propCount 146→147                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/react/src/primitives/table/Table.tsx`               | **Dual-channel**: render-scoped module slot `currentFormulaTables` (assigned top-of-render, serves ~30 `getCellValue` sites + `querySortedData` comparator) + `formulaTablesRef` mirror passed **explicitly** to the two mount-time CSV export handles via `withComputedFormulaCells(…, tables)`                                                                                                                                                                                                                                                       |
| `packages/react/src/primitives/table/useTableSort.ts`         | `formulaTables` option threaded into `buildSorter` (both `va`+`vb`), single + multi comparators with dep-array updates — 4th evaluation point                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/core/src/formula-tables.test.ts`                    | **NEW** — 19 tests, 162 lines                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/react/src/primitives/table/formula-tables.test.tsx` | **NEW** — 12 tests, 239 lines                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| docs ×3 + manifest/llms.txt                                   | comparison row + batch row + build status + counts + snippet; regenerated (propCount 147, `check:manifest`/`check:docs-reference` pass)                                                                                                                                                                                                                                                                                                                                                                                                                |

### Tests added (31)

- **Core (19):** first-row取值 · arithmetic/function-arg composition · string value passthrough · **missing table** · **missing field** · empty table · 2-arg feature-off · nullish coercion · illegal `!` positions (`a!`/`!a`/`a!1`) · whitespace tolerance · prototype-name safety · malformed payloads · no local fallback · memo key trinity (tables identity / 2-arg sentinel / new row)
- **React (12):** render · fail-closed empty cells · sort asc+desc · filter · both CSV handles · display-only · summary sum · immutable recompute (new tables object) · multi-table isolation · grouped + dataIndex-override

### Counts

- core **1457 passed** (+19) · react typecheck clean, **2110 passed** (+12) · lint **0 errors** (pre-existing IrisTable complexity warning 250, unchanged) · spec audit **0 violations** · prettier clean · manifest 155×4, propCount 147

### What is left

- The pipeline gate stage (full-repo turbo, pack-install, E2E/visual, bench) — out of scope per baseline, as in prior batches.
- Documented fiats (in `batch-bc-adapt.md`): fail-closed poison semantics; dual-channel rationale (render slot vs explicit handle args; event-handler reads follow last completed render on multi-table pages); the +1 barrel line beyond the 4 mandated files (type export mandate).

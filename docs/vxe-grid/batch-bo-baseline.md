Baseline complete. **`/home/u1/iris-ui/docs/vxe-grid/batch-bo-baseline.md`** (17 lines) — the only file created; `git status` confirms the pre-existing BM/BN-stage entries untouched, zero source files modified.

## What's in the baseline

**Exact design** (every anchor verified against current code — `widthsControlled`/`setColumnWidth` :2453–2464, grid template override :4480, pinned `widthOf` :4500, colTrack/footer :5389, :8182 — all read `columnWidths[col.key] ?? col.width`):

- **Prop** — `columnWidthsReset?: boolean` at props.ts after `onColumnWidthsChange` (:199, 列宽家族 grouping — BL `perfStats` feature-family precedent), before `onRowClick`. Additive, unset = byte-identical current behavior.
- **Reset semantics = empty map** — click calls `onColumnWidthsChange?.({})`; `{}` is the spec's「默认映射」: zero overrides → **every existing render path falls back to the column-declared width** (`col.width` number / `'auto'` / `1fr`) through the CURRENT `??` fallback — zero new render logic. Uncontrolled mode also `setWidthsInternal({})` (same dual channel as `setColumnWidth` :2460–2464 — callback fires unconditionally).
- **One throat** — `resetColumnWidths` handler directly after `setColumnWidth` (:2464); button JSX `data-iris-table-toolbar-reset-widths=""` inserted **after the batch-edit block, immediately before the zoom button** (`{zoomConfig?.showButton ? (` :7422), same ghost-button style family (↻⇪⇩☰⛶), glyph `⇔`, `aria-label`/`title` = `t('table.resetColumnWidths')`, `color: var(--iris-muted)` font-size-md.
- **Button lives in the toolbar** — toolbar render gate (:6848–6854) **unchanged**: like `zoomConfig`, `columnWidthsReset` alone does NOT create a toolbar (zoom test precedent locked in zoom-layouts-visiblemethod.test.tsx :63–69).
- **i18n** — +1 key: en `'table.resetColumnWidths': 'Reset column widths'` (core i18n.ts after `table.zoomOut` :139) + zh `'重置列宽'` (plugin-locale-zh after :75).

**8 fiats** for gate arbitration: `{}` = canonical 默认映射 (spec's "undefined 或默认映射" — `{}` needs no `onColumnWidthsChange` widening to `(next?)` nor the controlled→uncontrolled prop-drop contract; undefined option explicitly rejected); reset target = column-DECLARED widths, NOT the `defaultColumnWidths` mount seed (React uncontrolled initial-value semantics, documented); dual channel fires callback even uncontrolled (`setColumnWidth` precedent); button independent of `resizableColumns` (controlled widths resettable too — resizableColumns only gates drag handles); not in toolbar gate; placement batch-edit→before-zoom; zero handle methods / zero core / zero types.ts (propCount 155→156); persistState needs no special case — reset fires onColumnWidthsChange → snapshot saves `{}` → remount restores `{}` = defaults (AG machinery verbatim).

**File map**: props.ts +1 · Table.tsx 3 touch points (destructure :1656, handler :2464, button :7422) · NEW `test/reset-column-widths.test.tsx` · i18n 2 files (+1 key each) · comparison doc 3 sites (iris-only row after :68, 构建状态 :283, test count :341) · manifest regenerated (propCount 155→156) · zero: core/types/styles/vue/solid/svelte.

**Test plan**: ~9 cases, spec-mandated pair first — **按钮** (toolbar + columnWidthsReset → `[data-iris-table-toolbar-reset-widths]` with aria-label 'Reset column widths' + `⇔`; no prop → absent; prop without toolbar → no button, zoom precedent) · **重置回调** (controlled `columnWidths={{name:150,age:80}}` + spy → click → called with `{}` exactly once; parent rerender `{}` → gridCols falls back to declared widths) — plus 非受控 (`defaultColumnWidths` seed + ArrowRight resize 116 → reset → back to declared), 混合列 (number/auto/1fr all restore), 点击不触发排序/其他回调, persistState 快照存 `{}` 重挂载即默认宽.

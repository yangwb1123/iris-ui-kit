我准备好了。请提供以下输入文档以便我开始代码实现：

1. **架构设计文档**（.arch.md）
2. **实现计划文档**（.impl-plan.md）
3. **原始需求文档**

---

### 当前项目上下文摘要（iris-ui）

根据 `AGENTS.md`，`iris-ui` 是一个 **四框架**（React/Vue/Solid/Svelte）、**token-driven**、**插件可扩展** 的 UI 基础设施。核心原则：

- **逻辑下沉 core，适配器做薄桥**
- **跨框架不跨设计** — 同一套 core 工厂，四条薄桥
- **Token 杠杆** — 组件一行 ≈ 30 token vs 裸 HTML/CSS ≈ 800
- **渐进式复杂度** — 从 Button 起步，主题/皮肤/Behaviors/引擎/插件按需接入

我有 `read`、`bash`、`edit`、`write` 等工具可用，可以查看现有代码结构、读取设计文档、执行编译和测试命令。

---

请提供上述文档，我将严格按照架构设计和实现计划编写代码，并遵循项目的所有约定（命名规范、目录结构、测试要求等）。

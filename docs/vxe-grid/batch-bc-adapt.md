Batch BC complete. Implementation per `batch-bc-baseline.md` (all 6 fiats honored).

## Report

**Files changed** (4 mandated edits + 1 barrel line + 2 new test files + docs):

| File                                                          | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/formula.ts`                                | `!` added to TOKEN_RE as a new `bang` token; `parsePrimary`'s ident branch resolves `table!field` → new `resolveTableField` (own-property-only lookups, **fail-closed**: missing tables arg / unknown table / EMPTY table / unknown field → whole formula null; known nullish field still coerces) — illegal `!` positions (`a!` / `!a` / `a!1`) are parse errors → null, never throws. `evaluateFormula` gains an optional 3rd `formulaTables?: FormulaTables` param (backward-compatible). `memoizedFormulaValue` cache key extended to (row, tables-object identity, formula) via nested WeakMaps + a module sentinel for 2-arg calls (AO byte-compatible). New exported `FormulaTables` type + module doc updated |
| `packages/core/src/index.ts`                                  | barrel re-export of `type FormulaTables` (1 line — the new type must be reachable by adapters per manifest hygiene)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/react/src/primitives/table/props.ts`                | `formulaTables?: Record<string, Row[]>` — **single-line** prop (manifest-scanner hygiene), single-line doc; propCount 146→147                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/react/src/primitives/table/Table.tsx`               | **Dual-channel threading**: module-level `currentFormulaTables` slot assigned at the top of every render serves the ~30 render-time `getCellValue` call sites + `querySortedData` comparator (React's synchronous render walk guarantees isolation); `formulaTablesRef` mirror (same pattern as `filteredDataRef`) serves the two mount-time CSV export handles, which pass it **explicitly** to `withComputedFormulaCells` (now takes an optional 3rd param) — on-demand calls never race. `useTableSort` options gain `formulaTables`                                                                                                                                                                               |
| `packages/react/src/primitives/table/useTableSort.ts`         | `UseTableSortOptions<Row>` gains single-line `formulaTables?`; `buildSorter` threads it into `memoizedFormulaValue` (both `va` and `vb`); single + multi comparators pass it with dep-array updates — the **4th evaluation point** (hook usable standalone)                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/core/src/formula-tables.test.ts`                    | **NEW** — 19 tests, 162 lines (≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/formula-tables.test.tsx` | **NEW** — 12 tests, 239 lines (≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `docs/vxe-grid-comparison.md`                                 | iris 独有 table row + 批 BC batch row + 构建状态 + counts + 用法示例 snippet (all `--iris-*`-free doc text)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `manifest.json`/`llms.txt` / `apps/docs` generated            | regenerated (`gen:manifest` + `gen:docs-reference`), propCount 146→147, prop single-line; `check:manifest` + `check:docs-reference` up to date                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

**Tests added (31):**

- **Core (19):** 首行取值（`rates!rate`、`=rates!rate`、多行只读首行）· 组合运算（`price * rates!rate`、括号）· 函数实参（SUM/AVG/MAX/COUNT + COUNT 内未知本地字段仍 poison）· 字符串值流转与双侧字符串 `+` 拼接（非数值字符串入数值运算 → null）· **缺失表** → null · **缺失字段** → null · **空表** → null · 缺 tables 参（2-arg）与空对象 feature-off · 已知 nullish 强制转换（Excel parity）· 非法 bang 位置（`a!`/`!a`/`a!!b`/`a!1`/`a!+1`/`1+!`）· bang 旁空白容忍 · 原型名（`toString!length`/`rates!toString`）own-property 安全 · 畸形载荷（null 行/原始值行/空行对象）· 无本地字段回退 · memo 键三分（tables 对象同一性/2-arg 哨兵槽/新行引用）
- **React (12):** 渲染首行值 · 缺失表/缺失字段/无 prop → 空格 · 排序 asc + desc（useTableSort 4th point）· 筛选匹配计算值 · exportCurrentViewCsv 影子行物化 · exportSelectionCsv 选中行物化 · editable 公式列 display-only · summary 合计聚合计算值 · 新 tables 对象重算（immutable 契约）· 多表页隔离（模块槽不串）· 分组表头 + dataIndex 被 formula 覆盖

**Counts:** core typecheck + **1457 passed** (+19) · react typecheck clean + **2110 passed** (+12) · lint **0 errors** (pre-existing IrisTable complexity warning 248→250 unchanged) · spec audit **0 violations** · prettier clean.

**What is left:** the pipeline gate stage (full-repo turbo, pack-install, E2E/visual, bench) — out of scope per baseline. Flagged decisions (baseline fiats): (1) fail-closed poison semantics — missing table/field → whole formula null, not local 0; (2) dual-channel threading — render-time module slot (synchronous render walk guarantees per-table isolation; StrictMode double-render re-assigns idempotently; event-handler reads in multi-table pages follow the last completed render, documented) vs explicit-arg CSV handles; (3) one extra barrel line in `core/src/index.ts` beyond the 4 mandated files, required so the new `FormulaTables` type is package-exported (scanner hygiene: export new types); (4) types.ts formula doc left untouched (4-file constraint; the props.ts doc is the reference).

All verification complete. Here is my QA lead review.

---

# QA Lead Review — Plugin Design-Token Unification (calendar / dashboard / kanban / pro-table)

**Revision**: `main` @ `4b984c3` + uncommitted working tree (314 modified files; the 17-file plugin diff is a subset). **Not committed.**

## 1. Test inventory and commands actually run (this revision)

| Command                                                                                                     | Result                                                | Status                              |
| ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ----------------------------------- |
| `python3 /home/u1/ai-batch-runner/projects/iris-ui/scripts/iris-ui-spec.py --mode all --pkg plugins --json` | 0 violations / 115 files                              | **Verified — but false-clean (F1)** |
| `pnpm --filter @iris-ui-kit/plugin-calendar test`                                                           | 54/54 (36+9+9)                                        | Verified                            |
| `pnpm --filter @iris-ui-kit/plugin-kanban test`                                                             | 52/52 (35+8+9)                                        | Verified                            |
| `pnpm --filter @iris-ui-kit/plugin-dashboard test`                                                          | 54/54 (36+9+9)                                        | Verified                            |
| `pnpm --filter @iris-ui-kit/plugin-pro-table test`                                                          | 72/72 (46+13+13)                                      | Verified                            |
| `npx tsc -p tsconfig.json --noEmit` (plugin-kanban)                                                         | exit 0                                                | Partial (1 of 12 typecheck legs)    |
| `pnpm exec prettier --check` on 17 changed files                                                            | clean                                                 | Verified                            |
| Independent bare-hex scan (python, all 17 files)                                                            | 0 hits in sources (3 in test fixtures — event colors) | Verified                            |
| grep/`git diff` forensics (attribution of each finding)                                                     | see §3                                                | Verified                            |
| **Not run**: svelte-check, remaining tsc legs, `pnpm build`, repo CI (`.github/workflows/ci.yml`)           | —                                                     | Missing                             |

Report claims verified: test counts match exactly (232 total), prettier clean, grid-gap lockstep change confirmed (`packages/plugin-calendar/src/core/index.ts:123` `'1px'`→`'var(--iris-space-xxs, 4px)'` with all 4 consumers at `, 4px)`), bare-hex claim consistent, svelte a11y warnings pre-existing (IrisEventCalendar.svelte:88/116, untouched lines).

## 2. Requirement-to-test matrix

| Requirement                                  | Evidence                                                                                                   | Status                                                                                |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| R1 fallback palette == `light.ts`            | colors in `light.ts`/`dark.ts` **unchanged at HEAD**; matched values `#6366f1`/`#64748b`/`#e2e8f0` correct | Verified (gate-blind, F1)                                                             |
| R2 no unknown tokens                         | audit + grep; `--iris-space-xxs/xs` exist in dirty tree                                                    | Verified* (*registrations are uncommitted, F7)                                        |
| R3 spacing on token scale                    | audit claims 0                                                                                             | **False-clean — 12 kanban sites `5px`/`10px` + react `gap: 6` invisible (F1)**        |
| R4 no bare hex outside `var()` fallbacks     | audit + independent scan                                                                                   | Verified                                                                              |
| R5 4-framework parity                        | diff inspection                                                                                            | **Broken — react kanban header left at `gap: 6` while 3 frameworks → 8px token (F1)** |
| R6 dark-mode contrast preserved              | no test; manual token analysis                                                                             | **Regression — `#0b1020` text on user colors (F2)**                                   |
| R7 "No API change" / override channel intact | no test                                                                                                    | **Broken — 3 plugin-private tokens lost all consumers (F3)**                          |
| R8 gates (test/typecheck/prettier)           | run above                                                                                                  | Verified (tests/prettier); Partial (typecheck)                                        |
| R9 constraints: only plugin files touched    | `git status`                                                                                               | Partial — plugin diff is scoped, but tree carries 297 other modified files (F7)       |

**No unit test covers any changed line.** Renderer tests contain zero style/token/`toHaveStyle` assertions (kanban react test: 0 hits; calendar react test: 0 hits). The 232 tests exercise core state logic (month nav, eventsForDate, subscriptions) and registration **identity only** — e.g. `index.test.ts:164` asserts `tokens['--iris-cal-today-bg']` round-trips the plugin's own registration, which would pass even if the value or consumers were wrong. The entire visual surface is gated solely by an out-of-repo Python audit.

## 3. Findings (severity order)

| #   | Sev                                             | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Evidence (repo-relative)                                                     | Exact test to add                                                                                                                                   | Acceptance assertion                                                                                                                             |
| --- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| F1  | **High**                                        | **Audit gate is false-clean.** `_SPACING_STYLE_RE`/`_SPACING_CSS_RE` (`projects/iris-ui/scripts/iris-ui-spec.py:81-95`) match only a _quoted single-value_ `'Npx'`: shorthand `'8px 10px'` and `'var(--iris-space-xxs, 4px) 5px'` are invisible; React numeric `gap: 6` invisible. 12 off-scale kanban sites: react:164,182,228; solid:170,188,234; svelte:148,159,184; vue:146,193,213 — 3 of them on lines this change half-normalized (`1px 5px`→`var(--iris-space-xxs, 4px) 5px`). React kanban header `gap: 6` (react:146) left numeric while the change unified solid/vue/svelte to `var(--iris-space-xs, 8px)` — **change-introduced cross-framework divergence**, invisible to the gate. Kanban chips now inconsistent with calendar's `4px 4px` chip. | grep + diff hunks above                                                      | Extend both regexes to parse multi-value shorthands and numeric (unquoted) style values; add fixture file with `'8px 10px'`, `gap: 6`, `'4px) 5px'` | Audit flags ≥1 violation on the fixture and on all 12 sites + `gap: 6` after re-run                                                              |
| F2  | **Medium**                                      | **Change-introduced dark-mode contrast regression + semantic token misuse.** `--iris-primary-foreground` = `#0b1020` (near-black, committed `dark.ts:14`, designed for dark primary `#818cf8`) is now used on user-supplied `event.color` chips (calendar react:166, solid:164, svelte:122, vue:112) and `--iris-warning` WIP badge (kanban react:162, svelte:148, vue:144, solid:168). Replaces hardcoded `#fff`; dark theme + colored event chip = unreadable. Light WIP `#fff` on `#f59e0b` ≈ 2.45:1 fails AA (pre-existing, now "blessed" as a token).                                                                                                                                                                                                     | grep sites; `dark.ts:14,19`                                                  | Computed-color contrast test under dark theme tokens for user color chips + warning badge (jsdom, token fixture)                                    | Contrast ≥ 4.5:1 (AA) for both; or introduce `--iris-on-color` (#fff both themes) / `--iris-warning-foreground`                                  |
| F3  | **Medium**                                      | **Override channel silently removed — "No API change" (decision #2) is inaccurate.** `--iris-cal-today-bg` (calendar core:121), `--iris-dashboard-widget-bg` (dashboard core:115), `--iris-kanban-card-bg` (kanban core:133) still registered but **zero consumers** (grep: registrations + test identity asserts only). Host apps overriding these tokens now no-op. Inconsistent with decision #1 (`--iris-cal-grid-gap` kept indirection).                                                                                                                                                                                                                                                                                                                  | grep; core test asserts only identity                                        | Register token → render consumer → assert override changes output                                                                                   | Overriding the token changes the rendered background (nested-var fallback `var(--iris-cal-today-bg, var(--iris-primary, #6366f1))` preserves it) |
| F4  | **Medium** (pre-existing, in unification scope) | **Installed vs standalone dual-path drift**: `--iris-kanban-gap`/`--iris-dashboard-gap` registered `var(--iris-gap-md)` (=8px, light.ts:32) but consumers fall back `16px` (kanban react:106, solid:103, vue:295, svelte:42; dashboard react:158, solid:123, vue:253, svelte:103). Same bug class the change fixed for `--iris-cal-grid-gap` (decision #1) — left inconsistent. Untested: standalone resolution.                                                                                                                                                                                                                                                                                                                                               | grep registrations vs consumers                                              | Unit test resolving each plugin token against empty registry vs tokens registry                                                                     | Computed values equal on both paths (register as `var(--iris-gap-lg)` or align fallbacks to 8px)                                                 |
| F5  | Low (pre-existing)                              | pro-table selected row has no visible style: `data-selected` set, no renderer consumes `--iris-pro-table-selected-bg` (react:142, svelte:443, vue:257, solid:134)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | grep                                                                         | Renderer test toggling selection                                                                                                                    | `tr[data-selected]` gets the token background in all 4 renderers                                                                                 |
| F6  | Low (pre-existing)                              | Off-scale radii (calendar chip `3px` ×4, kanban card `6px`, today badge `22px`); no 三态 (empty/loading/error) in any plugin data region (calendar/dashboard/kanban: 0 hits; pro-table: comment only)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | grep                                                                         | follow-up backlog items                                                                                                                             | n/a                                                                                                                                              |
| F7  | Info                                            | **Tree hygiene / commit-order risk**: 297 non-plugin modified files (incl. `packages/tokens` typography/`iris.space.*` additions the plugin's `var(--iris-space-*)` depend on, `packages/vue` deps pinned in `pnpm-lock.yaml`). Env note "no tracked files changed" doesn't match tree state. If `iris.space.*` additions aren't committed together, `--iris-space-*` has no registered value (fallback masks it). CI runs `pnpm install --frozen-lockfile`; the lockfile is already modified.                                                                                                                                                                                                                                                                 | `git status`; `git diff pnpm-lock.yaml` (vue importer `workspace:*`→`0.2.0`) | —                                                                                                                                                   | Commit tokens + plugins atomically; confirm lockfile/package.json consistency before merge                                                       |

**Non-issues checked**: no props/events/DOM changes (diff-verified); no async/state introduced → no race/recovery surface in this change (DnD touch/mouse dual-path untouched except outline color); jsdom tests deterministic (single `new Date()` in calendar core defaults test — month-boundary flake negligible; no fake timers/random); `--iris-cal-grid-gap` lockstep is the correct pattern to replicate.

## 4. Prioritized scenario list

**P0 (blocking this change):**

- S1 Dark theme + user-supplied `event.color` chip — text must remain readable (F2)
- S2 Standalone (no tokens package) resolution of every changed `var()` — installed == standalone (F3, F4)
- S3 Host override of `--iris-cal-today-bg`/`--iris-dashboard-widget-bg`/`--iris-kanban-card-bg` still renders (F3)

**P1 (before merge, cheap):**

- S4 Audit gate honesty: multi-value shorthand + numeric spacing flagged (F1)
- S5 4-framework parity: same kanban config → identical rendered spacing (react `gap: 6` vs 8px) (F1)
- S6 WIP badge contrast in both themes (light 2.45:1 fail / dark 11.5:1 pass) (F2)

**P2 (follow-ups, tracked):**

- S7 pro-table selected-row visibility (F5); S8 nested-var mid-chain fallback; S9 calendar grid-gap 1px→4px visual delta (documented decision — needs a screenshot/baseline if plugin screenshots exist); S10 三态 (F6)

## 5. CI/manual-suite gaps, flake risks, fixtures, exit criteria

**Gaps (High):** The only enforcement of the ui-spec (spacing/hex/fallback) is an **out-of-repo script** (`/home/u1/ai-batch-runner/projects/iris-ui/scripts/iris-ui-spec.py`) — `ci.yml` runs no ui-spec audit, and no plugin test asserts styles. The gate that reported "76→0" is neither repo-committed nor complete. No visual/screenshot baseline exists for plugins (visual_reviewer judged code, not pixels — its PASS cannot certify contrast).

**Flake risks:** Low. Tests deterministic (jsdom, no timers); the earlier `MODULE_NOT_FOUND` was environmental (stale node_modules vs lockfile) and CI's `--frozen-lockfile` prevents recurrence **only if the already-modified lockfile is consistent** — verify before commit.

**Fixtures needed:** (a) token-resolution fixture (registry with/without tokens package) for S2/S3; (b) dark/light theme token fixture for contrast assertions; (c) audit negative-fixture file for F1.

**Exit criteria:** audit regex extended (multi-value + numeric) and re-run → flags the 12 sites + `gap: 6`; F2 fixed via `--iris-on-color`/`--iris-warning-foreground` with AA assertions; F3 override indirection restored with consumer test; F4 gaps aligned; 232 tests + full typecheck + prettier green; tokens committed atomically with plugins.

**VERDICT: FAIL** — The unification work itself is real and verified (fallback matching, hex cleanup, lockstep grid-gap), and I independently confirm the report's test counts. But the headline "76 → 0 violations" is a false-clean from a regex-blind audit (12 off-scale paddings in the very plugin the change normalized), the change introduces a dark-mode contrast regression on user-colored event chips, and it silently removes the plugin-token override channel while claiming "No API change" — all three are small, mechanical fixes with exact tests specified above. Re-run the extended audit and the P0 scenarios before acceptance.

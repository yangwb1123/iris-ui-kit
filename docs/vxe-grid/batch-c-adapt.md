All gates green. Implementation complete.

## Report

### Files changed (6 — all explicitly required by the task spec)

| File                                                             | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/remote-table.ts`                              | **NEW** — `createRemoteTableSource<Row>` (167 lines): state `{ data, total, loading, error, params }`, options `{ query, autoLoad (default true), initialParams }`, methods `request(params?)` / `refetch()` / `setParams(partial)` / `subscribe` / `getState` / `destroy`. Sort/filter changes reset page to 1 (vxe); latest-wins via the data engine's epoch token; `setParams` with unchanged params is a no-op (dedupes). Implemented as a thin projection over `createDataSource` (the same unified engine `createResourceController` uses) — zero logic duplication, zero framework imports. |
| `packages/core/src/remote-table.test.ts`                         | **NEW** — 12 unit tests: initial load, initialParams, autoLoad=false deferral, setParams reload + page reset, no-op dedupe, latest-wins concurrency, request(partial), error state + refetch recovery, refetch params, subscriber notifications, destroy abort.                                                                                                                                                                                                                                                                                                                                    |
| `packages/core/src/index.ts`                                     | +7 lines: barrel exports `createRemoteTableSource` + 4 types.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/react/src/primitives/table/props.ts`                   | +37 lines: `IrisTableProxyConfig<Row>` (`query`, `autoLoad`, `remoteSort`, `remoteFilter`, `pageSize`, `defaultPage`, `onPageChange`) + additive `proxyConfig?` prop.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `packages/react/src/primitives/table/Table.tsx`                  | +122 lines: controller in a ref (once), `useSyncExternalStore` subscription, loading/error driven from controller state (reusing existing rendering; retry → `refetch()`), data flows into `liveData` (edit write-back keeps working; next refetch replaces it), remoteSort routes `cycleSort`/controlled-prop changes to `setParams` (no local sort), remoteFilter routes `filters` prop to `setParams` (no local hiding), pager renders below the body wired to page/pageSize/total + `onPageChange`, autoLoad kicked from an effect + destroy on unmount.                                       |
| `packages/react/src/primitives/table/test/proxy-config.test.tsx` | **NEW** — 8 integration tests: loading→rows, error UI + retry refetch, remoteSort re-query (asc/desc), page-change → `{page:2}` + `onPageChange`, autoLoad=false no mount query (first setParams loads), edit write-back coexists (local edit replaced by next refetch), local sort preserved when flags off, remoteFilter hands filter map to query.                                                                                                                                                                                                                                              |

### Test counts

- **Core: 1213 passed (79 files)** — 12 new.
- **React: 1525 passed (141 files)** — 8 new.
- React typecheck clean; react lint **0 errors** (1 pre-existing `IrisTable` complexity warning, explicitly acceptable).
- `iris-ui-spec.py --mode all --json`: **0 violations** (no new CSS beyond existing `--iris-*` tokens).
- Husky filesize gate (simulated with everything staged): **6 files, 0 failed**; all new files ≤500 lines (167/214/219). Core logic lines: 174 ≤ 300 budget.
- `grep "from '(react|vue|solid|svelte)'" packages/core/src` → **0** (invariant holds).

### Deviations from the baseline design

1. **Option A rejected in favor of the task's exact contract**: the baseline recommended an alias over `createResourceController` (rows/load naming); the task mandated a distinct state shape (`params` nested, `data`) and methods (`request(params?)`, `setParams(partial)`, `refetch`). I kept the baseline's spirit — the controller is a thin projection over the shared `createDataSource` engine, not a duplicated implementation.
2. **Pager wasn't pre-wired** (contrary to the prompt's assumption): Table.tsx had no pager, so I rendered the existing `IrisPagination` primitive below the body.
3. **6 files vs the 5-file target**: all 6 are explicitly required by the task's IMPLEMENT list; the repo's change-budget gate flags this as a non-blocking warning only (`|| true` in husky).
4. **autoLoad semantics**: `setParams` always fires a request (deduped, no-op when params unchanged); `autoLoad` only gates the creation-time request — required so an `autoLoad=false` table can still load on first interaction (per "no initial request until the first setParams").
5. **`arch-check --ratchet` is pre-existing red on HEAD** (Table.tsx 1848 > stale baseline 1255, solid too) — not introduced here; the actual husky/commit gate (`check-filesize --diff`, which exempts Table.tsx) passes.
6. **Manifest untouched** per constraint — `pnpm gen:manifest` should be run by the pipeline to pick up the new core exports.

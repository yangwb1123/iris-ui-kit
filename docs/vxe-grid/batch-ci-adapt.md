All done. Complete report:

## Report

**Commit**: `c82e02c9 feat(table): grid 批 CI——行克隆 cloneRow（iris 独有）` (+ baseline/adapt docs)

**Files changed** (2 source + 1 type + 2 test; ≤4 + tests met):

| File                                                                               | Change                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/table-rows.ts` (+26)                                            | New pure `cloneRowInList(rows, rowKeyField, key, index?)` — reuses `nextAutoId`; same contract as insert/remove/update trio (immutable, original-reference no-op on missing key). Clone = `{ ...source, [rowKeyField]: max+1 }` — shallow-copies all fields onto a new object; fresh key keeps selection/dirty/key addressing sound. Default insert = **right after source (sourceIndex+1)**; explicit index clamped `[0, length]`. |
| `packages/core/src/index.ts`                                                       | `cloneRowInList` added to export line                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/react/.../table/types.ts`                                                | `cloneRow: (key, index?) => void` in `IrisTableHandle` (single-line, JSDoc)                                                                                                                                                                                                                                                                                                                                                         |
| `packages/react/.../table/Table.tsx` (+6)                                          | Import + wiring: `next = cloneRowInList(...); if (next !== rows) commitRowList(next, 'insert')` — one funnel: onDataChange once, undoable, audit `type:'insert'`, not selected                                                                                                                                                                                                                                                      |
| `packages/core/src/table-rows.test.ts` (+9 tests)                                  | 克隆内容 / 默认插入位置（源后、末行追加、首行 index 1）/ 显式 index 钳制 / 缺键 no-op / 身份保持 / 字符串 key / 非 max 源行                                                                                                                                                                                                                                                                                                         |
| NEW `packages/react/.../test/clone-row.test.tsx` — 14 tests / **226** lines (≤500) | Spec ①克隆内容 → T1/T2/T13/T14; ②插入位置 → T3/T4/T5; + 缺键静默、onDataChange 恰一次、选择不动、undo、audit insert、脏点语义、update/remove 集成                                                                                                                                                                                                                                                                                   |

**Counts**: core 1533→**1542** (+9, exact baseline) · react 2467→**2481** (+14 — baseline's prose predicted +13 but its own enumeration sums to 14, which this adapt matches) · manifest propCount **169** / eventCount **31** unchanged (zero diff, as baseline predicted) · spec **0 violations**.

**Verification** (all ✅ on committed state): core test 1542/1542 · react typecheck · react test 2481/2481 · react lint **0 errors** (1 pre-existing IrisTable complexity warning, unchanged at 284) · `iris-ui-spec.py --mode all --json` **0 violations** · `gen:manifest` regenerated (zero diff → nothing to stage) · prettier clean · framework-free invariant holds (0 framework imports in core).

**Note (pre-existing, not introduced here)**: `audit:tokens` exits 1 with only long-standing warnings (component-local scoped vars / documented cross-framework divergences) — batch CI adds zero CSS/tokens, so 0 new warnings.

**What is left**: runner's review/gate stage. Pre-existing, untouched: `arch-check:ratchet` red on committed baseline (stale baseline per prior reports), prior-stage doc dirt (`DECISIONS.md`, `batch-ch-gate.md` — left uncommitted). No baseline open questions.

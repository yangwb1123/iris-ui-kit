Baseline complete → `/home/u1/iris-ui/docs/vxe-grid/batch-y-baseline.md` (467 lines). `git status` confirms the only change is the new doc; no source files touched.

**Key findings baked in:**

1. **Current Vue pipeline (post batch X)** — `tableData (L496) → useTableSort → sortedData (L530) → filteredData (L702, batch-X form filter stage already implements the exact substring/case-insensitive/''-ignored semantics) → bodyData (L719)`. Header (flat L1647 + grouped L1516), body cells (`renderRow` L1798), and summary (L2255) all consume `leafColumns`/`headerMatrix` — so the columnVisibility design reduces to: one `displayColumns` computed + the flat header loop switching from `props.columns` to `leafColumns`. **No new watchers; all computeds/render-site changes.**
2. **Core bridge is free**: `createSortable`/`closestCenter` (pure, DOM-free) and `mergeFormFilters` are already exported; `remote-table.ts` has **no setData** — so `loadData` writes `proxyCtrl.liveData` (the Vue analogue of React's `commitRowList`), with a new `localRowsOverride` ref for local mode (Vue has no mutable data store). `setCellValue` needs no bridge — edit write-back already lands via `finishCommit` (batch X).
3. **Deliberate deviation from React** (flagged in §4.1): React omits seq/drag tracks from `gridTemplateColumns` and relies on CSS auto-placement with no header placeholders. Vue design renders **explicit tracks + header/summary `__seq`/`__drag` placeholders** and extends `colTrack` (L1115 hardcodes only expand+selection today) — deterministic alignment in every combination including `columnVirtualization` and grouped headers.
4. **Skipped to batch Z** per the task: `filterValues` OR-match, `col.filterMethod`, `seqMethod`, proxy cumulative seq, `columnOrder`/toolbar column panel, footer spans. Also found `TableBody.ts`/`TableHeader.ts` are orphaned (unimported leftovers) — flagged, not built on.
5. **Tests**: 5 new files (~32 cases) mirroring react's `vxe-parity`/`proxy-methods` shapes; 5 open questions for the adapt stage (localRowsOverride lifetime, proxy-mode filters page reset, grouped reorder mapping, etc.).

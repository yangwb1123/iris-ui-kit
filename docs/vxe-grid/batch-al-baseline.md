Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-al-baseline.md` (257 lines). No source files touched — `git status` shows only the new doc.

**Key verified findings:**

- **Two mutation funnels, not one**: `commitRowList` (Table.tsx:2389) covers row ops/paste/fnr/range-clear/loadData, but cell+row edit commits go through `commitValue` (1595) which calls `setLiveData` **directly** — never `commitRowList`. The task's "push on every commitRowList call" would silently miss cell edits; the baseline wires `recordUndo()` at **both** funnels (flagged correction, same style as batch-ak's setCellValue correction).
- **Stack design**: ref-created `createUndoStack<Row[]>` (`maxHistory: 100`, `initial: [...data]` → canUndo after one mutation), snapshot = row list **before** the change (`[...externalDataRef.current]`), `restoringRef` guard so replay through `commitRowList` never re-pushes, baseline refresh on external re-feeds only while the stack is untouched (interaction-scoped, vxe undoRedoHistory parity).
- **Selection pruning** on undo AND redo (keys that vanish are dropped via one `selModel.set` after `rebaseToProp`, mirroring the removeRows/clearSelection patterns).
- **Keyboard**: window keydown gated on `undo === true`, mirroring the clipConfig guard (root-contains + skip INPUT/TEXTAREA/SELECT/editor) — self-contained, clipConfig untouched. Ctrl+Z / Ctrl+Y (+meta).
- **Buttons**: `data-iris-table-undo`/`data-iris-table-redo` as glyph buttons first in the right-side toolbar group, `disabled` from `canUndo()/canRedo()` (fresh on the same render as every `setLiveData`), i18n `table.undo`/`table.redo`.
- **Batch edit**: `toolbar.batch.edit?: boolean` — when true the batch button (multi + selection ≥ 1) opens a toolbar-anchored panel (`data-iris-batch-edit-panel`): column select over `leafColumns.filter(c => c.editable)` (exactly the inline-edit gating at 2219/1738) + value input + 应用 (`data-iris-batch-edit-apply`, `table.batchEdit.apply`). Apply = one `commitRowList` → undo snapshot + one `onDataChange`, selection stays, Esc/outside closes.
- **File map**: 3 edited files (core i18n.ts, props.ts, Table.tsx), 1 new test file (`undo-batch-edit.test.tsx`, ~19 cases), 0 new source files, `useTableUndo` untouched. 12 fiats flagged, including the cell-edit funnel correction, editRules bypass on batch apply (paste parity), panel-closes-on-apply, and the a11y `table.batchEdit.column` key.

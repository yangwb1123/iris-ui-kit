Complete. All verification gates green on the committed state.

## Report

**Commits** (2, per CB/CA convention):

- `feat(table): grid 批 CE——复制选中高亮（iris 独有）`
- `docs(table): batch CE adapt report`

**Files changed** (2 source + 1 test + 1 report; ≤4 + tests):

| File                                                      | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/Table.tsx`           | `writeClipboardText` `Promise<void>` → `Promise<boolean>` (true iff any of the 3 channels succeed — core `copyText` / `navigator.clipboard.writeText` resolve / `execCommand('copy')` boolean; the 3rd call site stays `void`-prefixed, widening is call-site-compatible) · `COPY_FLASH_MS = 600` · `copyFlashRange` snapshot state + timer ref + `flashCopyFeedback` (re-copy restarts the clock; unmount cleanup effect) · both consumption points wired success-gated: window Ctrl/Cmd+C handler + `copyActiveRange` (range toolbar 复制) · module-scope `inCopyFlashRange` / `copyFlashCellAttr` / `copyFlashCellStyle` (row-render arrow complexity budget untouched) · render: `data-iris-copy-flash="true"` attr + `backgroundColor` longhand after `fnrCellStyle`/`rangeFillCellStyle`, before `lockedRender.style` (BE discipline: never clobbers background-image; locked stripes coexist) |
| `packages/react/src/primitives/table/styles.ts`           | `COPY_FLASH_BG` = `color-mix(in srgb, var(--iris-primary) 25%, var(--iris-background))` — token-only (compare-view rows precedent), distinct from the persistent selection tint                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/react/src/primitives/table/copy-flash.test.tsx` | **NEW** — 11 tests, 271 lines (≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/manifest/{manifest.json,llms.txt}`              | regenerated via `gen:manifest` — **zero diff** (no new props/components/types; propCount 168, components 155 already current)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `docs/vxe-grid/batch-ce-adapt.md`                         | report                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Tests added**: 11 — spec's two mandatory blocks explicitly mapped: ① flash appears on success (Ctrl+C flashes the exact 2×2 rect with `data-iris-copy-flash="true"` + the token `backgroundColor`, nothing outside it) ② removed at 600ms. Plus: range toolbar 复制 flashes the same rect · clipboard reject + no execCommand → NO flash (spec's "复制成功后" gate) · execCommand-only success → flash via the legacy channel · re-copy restarts the clock (outlives the original 600ms expiry) · snapshot semantics (selection moved mid-flash → highlight stays on the copied rect) · `clipConfig.copy:false` regression (no copy, no flash) · no live range → no-op · unmount clears the pending timer · `copyFormat` set still flashes (dispatcher unchanged). Fake timers + `act` microtask flush; jsdom's missing `execCommand` defined via `Object.defineProperty` for the legacy-channel case (deleted in afterEach).

**Counts**: react 2420→**2430** · core 1533 unchanged · manifest propCount 168 · spec 0 violations.

**Verification (all ✅)**:

- core test: 1533/1533
- react typecheck: clean · react test: 2430/2430 (copy-flash 11/11)
- react lint: 0 errors (1 pre-existing IrisTable complexity warning — the row-render arrow stayed at its baseline 70)
- `iris-ui-spec.py --mode all --json`: 0 violations
- `gen:manifest`: regenerated twice (pre- and post-prettier) — zero diff, prettier clean

**What is left**: runner's review/gate stage. Working-tree `DECISIONS.md`/`batch-cd-gate.md` mods + untracked `batch-ce-baseline.md` are prior-stage leftovers (untouched, per CB precedent). No baseline open questions — the design was fully specified (success gate, two consumption points, snapshot semantics, 600ms, token color).

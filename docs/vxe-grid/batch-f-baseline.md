Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-f-baseline.md` (178 lines; the only change — no source files touched).

**Key findings baked in:**

- **vxe semantics** (grounded in table.d.ts): `SortConfig.multiple`/`chronological`/`remote` (L1273–1344) with `SortCheckedParams.sortTime` backing trigger-sequence ordering (L6838); sort-change events carry the full `sortList` (L6849); grid `getProxyInfo()` exposes **both** `sort` and `sorts` (grid.d.ts L524–527) — direct evidence for the additive `sorts` proxy field. `ValidConfig.showMessage` is **deprecated** in favor of `showErrorMessage` (L2718) — flagged as Q1. `expandAll` is one-shot-at-init in all three configs (tree L1742 / expand L1652 / group L479). The requested selection method names (`clearSelection`/`selectAll`/`toggleRowSelection`) are **not in the extracted types** — v4 canonical names are `clearCheckboxRow`/`setAllCheckboxRow`/`toggleCheckboxRow`; iris adopts the requested names with v4 semantics (documented deviation).
- **iris evidence**: `useTableSort` is strictly single-column (comparator memoized on one sortState; `sortedData` stable-sorted); core `SortState` is shape-identical to `IrisTableSortState` (array reuse is free); the error div and `aria-invalid` are independently rendered (showMessage=false can drop only the message); `selModel.set/toggle` + `rebaseToProp` give the handle methods a ready pattern; expansion model is created **before** data arrives (proxy async) so `expandAll` must seed one-shot on first data, walking `sortedData` via `getSubRows` (not the flattened rows — chicken/egg).
- **Design**: (a) multiSort array state with append/cycle/remove click semantics, sequence-number badge on non-primary columns, chained stable comparator, additive `sorts` proxy param; (b) `validConfig.showMessage=false` keeps the commit block + `aria-invalid`; (c) three additive handle methods; (d) `expandAll` one-shot seeding via `expansion.merge`.
- **One flagged call**: passing `sorts` through the query pipe needs a ~5-line additive optional field in core `RemoteTableParams` (the pipe is core-typed; react-only alternatives are casts or abusing `filters`) — listed as Q6 alongside 6 other open questions.

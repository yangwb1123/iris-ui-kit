Done. Batch BE (单元格锁定) is complete and fully verified.

## Report

**Files changed (3 source + 1 test + adapt doc — additive, react-only, core untouched):**

| File                                                        | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/types.ts`              | `IrisTableColumn.locked?: boolean \| ((row: Row, column: IrisTableColumn<Row>) => boolean)` after `editor` — single-line function prop (manifest scanner hygiene), `true` = 整列锁, predicate 忽略 column 即行级锁                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/react/src/primitives/table/Table.tsx`             | Module `isCellLocked` (single truth next to `isEditableColumn`) + `lockedCellRender` helper (keeps renderRow under complexity budget). Guards at every write entry: `beginEdit` (covers dblclick/click-trigger/F2/Tab-nav/pendingNav in one edit, early-return before `onEditStart`), `beginRowEdit` filter, row re-open branch, `moveRowEditOnTab` (+row param), `moveEditOnTab`, Delete shortcut, paste (both branches), fill, range-clear, FNR replace + replace-all, `applyBatchEdit` (changed-flag → all-locked = zero commit, panel still closes). Render: `data-iris-cell-locked="true"` + cursor `not-allowed` |
| `packages/react/src/primitives/table/styles.ts`             | 45° stripes in `TABLE_ROW_CSS` via `--iris-muted-subtle` only (background-image — hover/selected tints, dirty dot, note badges stay visible)                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/locked-cells.test.tsx` | new — **309 lines, 11 tests**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `docs/vxe-grid/batch-be-adapt.md`                           | adapt report                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

**Tests added (11):** 整列锁 attr+cursor · 行级锁 · 组合锁 (column-aware predicate) · dblclick no-op (no editor, no `onEditStart`) · click-trigger no-op · F2 no-op · Delete no-op · row-mode skip + Tab skips locked · 批量编辑跳过 (mixed, one commit) · 全选 + 全锁零提交 (no `onDataChange`, panel closes) · paste skips locked.

**Counts/verification (all pass):**

- core: **1457 passed** (94 files) — zero core diff
- react typecheck ✓ · react test **2130 passed** (189 files, +11) ✓
- react lint **0 errors** (1 pre-existing complexity warning: 250 → 250; no new warning — extracted helper)
- `iris-ui-spec.py --mode all --json` → **0 violations**
- `gen:manifest` regenerated → **byte-identical** (IrisTable propCount stays 148 — `locked` is a column field, batch-AY precedent; 155 components × 4 unchanged); prettier clean; react build + manifest tests (69) ✓

**What is left:** nothing. Fail-inert per baseline: selection/range/copy/export/FNR-find/`data-editable` all unchanged; locking guards writes only. Pre-existing (not mine): `DECISIONS.md`/`batch-bd-gate.md` `M` entries (BD stage), and `arch-check:ratchet` is red on HEAD already (17 pre-existing violations, same 17 after — no new ones).

Batch CM complete and committed as `8da5a58f`.

## Report — 列合计行粘性 `summaryRowStyle`（iris 独有）

**Files changed** (9 files, +281/−4, committed):

| File                                                                                          | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/props.ts`                                                | `summaryRowStyle?: 'default' \| 'sticky'` after `footerAlign` (footer family), multi-line JSDoc                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/react/src/primitives/table/Table.tsx`                                               | Helper `summaryStickyAttr` · destructure `summaryRowStyle = 'default'` · 2 injection points: `renderSummaryRow` row div (`=== 'sticky' && groupKey === undefined` — groupKey distinguishes global vs per-group) + `renderFooterStack` footerMethod replacement rows (global slot by construction) · live `TABLE_ROW_CSS` +1 rule `[data-iris-table-fixed-height] [data-iris-summary-sticky="true"] { position: sticky; bottom: 0; z-index: 1 }` (header z2 / pinned z1 tiering; `--iris-*` tokens only) |
| `packages/react/src/primitives/table/summary-sticky.test.tsx`                                 | **NEW — 8 tests, 159 lines** (≤500 ✓)                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/manifest/manifest.json` + `llms.txt`                                                | Regenerated — IrisTable props **172→173**, events 31 unchanged, enum `["default","sticky"]`                                                                                                                                                                                                                                                                                                                                                                                                             |
| `docs/vxe-grid-comparison.md` · `DECISIONS.md` · `batch-cm-adapt.md` · `batch-cm-baseline.md` | Comparison row 85 + build-status tail + stage records                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

**Tests added**: 8 (fail-closed default double-check / explicit default / sticky attr + aggregate unchanged / stylesheet selector + sticky + bottom:0 + z-index / footerMethod slot gets attr + footerData excluded / group summaries excluded / virtual scroll preserves / invalid value fail-closed)

**Counts**: core **1559/1559** (zero changes, framework-free invariant 0 framework imports) · react **2519/2519** (2511→+8) · typecheck ✓ · lint **0 errors** (1 pre-existing complexity warning) · **iris-ui-spec 0 violations** (1416 files) · check:manifest up to date · prettier clean

**Design notes**: additive/fail-closed; zero state/effect/core/i18n; two explicit fiats — group summaries never stick (bottom-edge contention), footerData never sticks (contractually below the summary). Virtual scroll unaffected (footer stack lives outside the virtualizer).

**What's left**: review/gate stages (`batch-cm-review.md`/`batch-cm-gate.md` + full repo gate). Pre-existing dirt `docs/vxe-grid/batch-cl-gate.md` left uncommitted per convention. One intermittent flake observed in `CommandPalette` A1 (10k-item render vs 5s timeout under parallel load) — reproduced once on the branch, confirmed green on clean HEAD and on final branch run; unrelated to this batch. `arch-check` ratchet staleness is pre-existing.

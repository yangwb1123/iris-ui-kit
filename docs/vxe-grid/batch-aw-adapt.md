Batch AW complete. Committed as `ef23aaee`.

## Report

**Files changed** (committed `ef23aaee` — `feat(table): grid 批 AW——NL 列摘要`):

| File                                                              | Change                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **NEW** `packages/core/src/summary.ts`                            | `summarizeColumn(values, label)` — ≥60% (inclusive) numeric → `label：共 N 个值，范围 min–max，平均 avg，M 个缺失` (min/max/avg numeric-only, single `.toFixed(1)`); else categorical top3 `Math.round` % + `，其余 R 个` only when R>0; empty → `label：无数据`. Categorical branch reuses the extracted `countDistinctValues`. |
| `packages/core/src/value-distribution.ts`                         | Counting loop extracted into public `countDistinctValues`; `valueDistribution` API byte-identical (existing tests untouched).                                                                                                                                                                                                    |
| `packages/core/src/index.ts` · `i18n.ts`                          | Barrel exports + en `table.summary` = `Column summary`.                                                                                                                                                                                                                                                                          |
| `packages/plugin-locale-zh/src/core/index.ts`                     | zh `摘要` — zh parity test green.                                                                                                                                                                                                                                                                                                |
| `packages/react/src/primitives/table/props.ts`                    | Single-line `nlSummary?: boolean` (manifest-scanner hygiene).                                                                                                                                                                                                                                                                    |
| `packages/react/src/primitives/table/Table.tsx`                   | 5 touch points: `SUMMARY_MENU_KEY='__iris-summary'` · destructure · push after distribution item (dedupe guard) · summary state + `openSummary` · `onSelect` interception + panel render.                                                                                                                                        |
| **NEW** `packages/react/src/primitives/table/SummaryPanel.tsx`    | Distribution-panel clone (`useFloating`+`useDismiss`+portal+virtual anchor+capture-scroll), `data-iris-summary-panel`, `--iris-*` only.                                                                                                                                                                                          |
| **NEW** `packages/core/src/summary.test.ts`                       | 12 tests.                                                                                                                                                                                                                                                                                                                        |
| **NEW** `packages/react/src/primitives/table/nl-summary.test.tsx` | 11 tests (3 core-direct + 8 react).                                                                                                                                                                                                                                                                                              |
| Docs                                                              | `docs/vxe-grid/batch-aw-adapt.md` (NEW), `docs/vxe-grid-comparison.md` (iris 独有 row + 批 AW row + counts), `DECISIONS.md`, `apps/docs/components.md` (gitignored generated — lives in tree for docs build).                                                                                                                    |
| `packages/manifest/*`                                             | Regenerated: propCount 139→140, 155×4.                                                                                                                                                                                                                                                                                           |

**Tests added:** 23 (12 core + 11 react). **Counts:** core 1409 (+12) · react 2045 (+11, 0 regressions) · zh plugin 6.

**Verification (all green):**

- core test ✓ · react typecheck ✓ · react test ✓ · react lint **0 errors** (1 pre-existing `IrisTable` complexity warning) ✓ · spec script **0 violations** (1413 files) ✓ · `gen:manifest` regenerated + committed ✓ · prettier clean ✓

**Constraints honored:** core framework-free (react only bridge) · additive only · no dist/tgz/node_modules edits (core `dist` rebuilt locally for resolution, not committed) · test files well under 500 lines · 3 new source + 3 new test files, 6 edits, docs, manifest.

**What is left:** vue/solid/svelte wiring for `nlSummary` (core material ready; "react only" scope per baseline) — plus the review/gate stages of the workflow (full turbo run, E2E/visual regression, size, RSC) by the harness.

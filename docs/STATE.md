# STATE

> Compression snapshot — refreshed every ~5 iterations. Continue from this to minimize context re-reads.

**当前架构 (Architecture):** 5 layers (tokens → theme → core controllers → 4 adapters → plugins). Behavior in `@iris-ui/core`; adapters are thin reactivity bridges. See `ARCHITECTURE.md`. 149 components ×4 at full parity, 25 packages, CI + changesets, **126/126 gates green**, 0 skipped tests, packages at 0.0.0 (pre-first-publish).

**已完成 (Done) — factory cycle, condensed:**

- **iters 1–14** (earlier; see CHANGELOG for detail): `/docs/` memory system; Table feature parity ×4 (treegrid a11y, column virtualization + resizing); REAL bugs fixed via verification sweeps (Select `aria-haspopup`, manifest `export class` discovery, 58 zh i18n keys); **7 CI-enforced invariant GUARD tests** (barrel reachability, 4-fw name parity, module test-coverage, CSS-var validity, token-export completeness, zh coverage+placeholders, manifest class-discovery) + 10 backfilled tests + solid `isolate:true` flake fix. **(iter-32 added an 8th guard: contract-coverage — every core `*Scenario` replayed by all 4 adapters, negative-tested.)**
- **iters 15–30 — TWO interleaved threads (the productive autonomous mode this cycle):**
  - **(A) Contract-harness expansion 4→15 scenarios (13 components).** Leaf controls: segmented/toggle-group[single+multiple]/slider/radio/number-input/rating/pagination/stepper. Flagship DATA component: **Table sort** (aria-sort cycle none→asc→desc→none, iter-29) + **Table multi row-select** (aria-selected per row, iter-30). Each: author core scenario + React reference inline → fan out 3 parallel agents (vue/solid/svelte) to mirror → full gate → commit. Adapter split: React/Solid uncontrolled `defaultValue`/`defaultPage`; Vue `v-model`/direct harnesses; Svelte controlled `$state` shared `ContractsHarness.svelte`, with DEDICATED `*ContractHarness.svelte` files for components that collide on a generic role/selector in the shared harness (rating + slider both `role="slider"`; multi-toggle-group + single share `[data-iris-toggle-group-item]`; Table = many DOM nodes). Harness gotchas: a component whose accessible node is its ROOT (rating) needs the Vue harness to wrap it in a host `<div>` (driver `queryAll` matches descendants only); radio/table-select click the inner native `<input>` (master selection checkbox is index 0, rows follow).
  - **(B) a11y behavioral-parity audits → 6 real fixes.** Tree keyboard: solid had NONE (18), svelte lacked Home/End+aria-level (19). Select keyboard: solid lacked Home/End (20), svelte lacked arrows+open-focus (21). Pagination: solid uncontrolled `aria-current` frozen by `<For>` identity-memoization + plain-bool `active` → thunk fix (24). **Table row-selection a11y (30): vue/solid/svelte rows only set `data-state="selected"`, no `aria-selected` → invisible to screen readers; added `aria-selected` ×3, guarded by the table-select contract.** Both pagination (24) + table-select (30) gaps were SURFACED BY contracts in thread (A) — the harness paid for itself twice.

**关键经验 (Key lessons this cycle):** (1) **source-level cross-framework BEHAVIORAL audits are high-yield** — diffing the interaction surface (keyboard, aria, focus) of all 4 impls finds real bugs name-parity is blind to. (2) **The contract harness pays for itself** — caught real solid bugs (pagination aria-current; table aria-selected) that per-fw unit tests missed (theirs used CONTROLLED mode / only data-state). (3) **Audit a wrapper's COMPOSED primitives, not just its own file** (vue-Select-via-IrisList false alarm). (4) **Solid `<For>` + identity-stable primitive items = stale callbacks** — per-row reactive state must be thunks, never plain values captured at map time. (5) **ALWAYS read source before concluding a gap** — surface greps gave 3+ false alarms this cycle (vue toast object-literal `role:`; vue Select via IrisList; vue Table aria-selected via different attr). When a grep says "missing," confirm it's not just different syntax/delegation.

**未完成 (Open) — contract+audit thread now largely COMPLETE; remaining candidates are lower-value or blocked:**

- **Contract expansion COMPLETE at 17 scenarios.** Cleanly-testable click/key/select controls + flagship Table's full trio (sort/multi-select/row-expand) + **Tree keyboard (iter-33, previously deferred — unblocked via the roving-tabindex insight: target `[role="treeitem"][tabindex="0"]` so a bubbling keydown reaches both the container handler (react/solid) and the per-item handler (vue/svelte))**. Only remaining contract candidate = Table column-resize (needs pointer drag with coords — jsdom-awkward; low value). **DEFERRED-by-design (still):** portal-gated Select/Menu/Combobox/Dialog (content portals outside the container-scoped driver; portal-disable props inconsistently named ×4 → uniform scenario needs a public-API change = Blocker). The roving-tabindex trick does NOT help here — the issue is the portal boundary, not handler attachment.
- **Further behavioral-parity audits** — diminishing returns (7 clean : 6-fixed ratio is now mostly clean). Not-yet-audited, lower-probability: listbox `aria-activedescendant` vs roving, accordion/disclosure keyboard, menu typeahead. Audit only when a concrete signal appears; don't grind.
- **Audits already run CLEAN this cycle (no gaps — do not re-audit without new signal):** tabs keyboard, menu keyboard, combobox keyboard, dialog + drawer focus-trap, **form-field error wiring** (iter-26: all 4 use `ids.join(' ')` for `aria-describedby` combining hint+error, `invalid: error ? true : undefined`, matching `data-iris-form-field-state`), **toast** (iter-26: all 4 attach `onPointerEnter/Leave` hover-pause + pointer down/move/up swipe-dismiss + identical `role={variant==='error'?'alert':'status'}` / `aria-live` polite/assertive). 7 clean audits total; 5 real fixes found earlier (tree ×2, select ×2, pagination). **LESSON re-confirmed twice in iter-26:** surface greps gave false alarms (vue toast "missing role" — actually object-literal `role:` syntax; vue Select "no keyboard" iter-20 — delegated to IrisList). ALWAYS read source before concluding a gap.
- **Portal-gated overlay contracts (Select/Menu/Combobox) — DEFERRED with rationale (iter-26 investigation):** their dropdown content renders in a portal (document.body), outside the container-scoped per-fw driver's `queryAll`. Disabling the portal per-test is blocked by **inconsistent prop names** (`portalTarget` solid/react-menu, `inline`, `teleport` vue-menu, svelte-menu has none) → no uniform scenario. A document-scoped driver would risk cross-test contamination. Not worth the fragility: these components already have per-fw unit tests + VERIFIED keyboard parity (iters 20-21 Select, menu audit clean). Revisit only if portal-disable props are first unified ×4 (itself a public-API change = Blocker).

**技术债 (Debt):** all Low/by-design — props.ts interface-only extractor misses IrisToggleGroup union-type props; skeleton templates hardcode example aria-labels. (Possible tiny follow-up: svelte tree could gain `data-iris-tree-node={id}` to match react/solid's per-id selector — cosmetic, only if a test needs it.) See TODO.

**GATED (need user / external):** publish v0.1.0 (irreversible — will NOT run `npm publish` autonomously; maintainer's call); Tauri E2E demo (no Rust toolchain in sandbox); new product direction (Angular/Qwik adapter, new plugin, deeper MCP codegen).

**下一步推荐 (Next):** Sustainable autonomous mode = (1) source-level behavioral-parity audits of stateful components → fix real gaps (P1 correctness); (2) lock verified parity into the contracts harness or a guard test (P5 automation); (3) clean click-based contract additions. Both need no user input. Avoid marginal/cosmetic churn; do NOT run irreversible publish.

**Iteration count since last STATE refresh:** 6 (iter-37; full refresh at iter-30 boundary — refresh due). Contract harness **18 scenarios / 15 components** (added Calendar iter-37, which caught a 3rd real bug: solid calendar uncontrolled-selection, fixed iter-36). Plus iter-32 contract-coverage guard + iter-35 source-hygiene guard (9 CI guards total). Earlier this refresh-window: 17 scenarios / 14 components (incl. flagship Table's sort+select+expand trio AND the Tree keyboard contract — the last deferral, unblocked iter-33). 8 CI guards (incl. iter-32 contract-coverage, which auto-validated tree ×4). 6 real a11y fixes + 7 clean audits this cycle; harness caught 2 bugs itself. **Contract+audit thread now genuinely COMPLETE** — only Table column-resize (low-value) + portal-gated overlays (Blocker-gated) remain. 126/126 gates green throughout, 0 skipped tests.

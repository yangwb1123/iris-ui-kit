Baseline complete — `/home/u1/iris-ui/docs/vxe-grid/batch-ba-baseline.md` (28 lines). `git status` confirms only the new doc; zero source files touched.

**Key findings (every claim verified against current code):**

- **`commitRowList` (Table.tsx:2958) is the single mutation channel** — `(next: Row[], type: AuditLogType = 'edit')` → `recordUndo` → `recordAudit` → `setLiveData` → `externalDataRef` → `onDataChange`. It already carries the batch-AT type from 8 call sites (insert :3005, remove :3016/:3041, paste :3928, fill :4185, undo :3961, redo :3968, default `'edit'` for updateRow/fnr/range-clear/batch-edit :3044/:3830/:4251). The snapshot hooks here — same funnel, same type.
- **One deliberate scope difference vs audit**: batch AT hooks BOTH funnels (`commitValue` :2068-2083 covers inline cell/row edits); the spec hooks only `commitRowList` — inline edits don't create versions. Documented, not a gap: restore replaces the whole row list, so row-level commits are the coherent unit.
- **Suppress-flag precedent exists**: `restoringRef` (:1404, flipped in `applyUndoSnapshot` :2996-2999) already stops undo replay from re-pushing. Version history mirrors it with its own `historySuppressRef`. Restore side effects are desirable and consistent: `commitRowList(rows, 'undo')` → audit records `'undo'` (replay IS a user-visible change) and the restore itself is undoable; no new version is pushed.
- **Ring mold**: `createAuditLog` (audit-log.ts, 131 lines) — newest-first `unshift` + `pop` trim, monotonic seq never reset by `clear()`, `subscribe`/`getVersion`/`depth`. Clone 1:1 with monotonic `index` in place of `seq`. **Panel mold**: `TableAuditPanel` (useFloating + portal, Esc/outside/scroll close, `useSyncExternalStore`) minus diff context and clear button.
- **Slots verified**: handle `getAuditLog`/`clearAuditLog` (types.ts:561-563, impl :3196-3197); toolbar gate :5768; audit trigger :6371; panel render :7196; prop `auditLog` props.ts:574; i18n en :136-138 / zh plugin :74-76; core export :433-441.

**Design locked:** (a) core `createVersionHistory<Row>({ max? })` — push returns monotonic index (never resets on clear), `list()` newest-first copies, `get(index)` copy/undefined, default max **20**, `0` unlimited; (b) `versionHistory?: { max?: number }` one prop → ref-once controller + push in `commitRowList` with the batch-AT type + toolbar trigger/panel (restore suppresses via flag); (c) handle `getVersions()` (lightweight, no rows) + `restoreVersion(index)`; (d) `table.history` + `.empty` en/zh. 4 new files + 7 edits + 10 numbered fiats (F1–F10) for gate arbitration, including the flagged calls: restore routes as `'undo'` (F1), no initial seed — first commit is version 0 (F3), full `AuditLogType` union incl. `'remove'` (F8), no panel clear button (F7).

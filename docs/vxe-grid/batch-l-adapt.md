All green. The `docs/vxe-grid/DECISIONS.md` change is the batch runner's own bookkeeping (pre-existing before my work); core/manifest dist rebuilds are gitignored artifacts.

## Report — vxe-grid Batch L (proxy seq + toolbar export + cell link)

**Files changed (6, all required by spec):**

| File                                                                                       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/types.ts`                                             | `IrisTableColumn.link?: (value: unknown, row: Row) => { href; label?; target? } \| string \| null` — one line, placed after `html`                                                                                                                                                                                                                                                                                                                                |
| `packages/react/src/primitives/table/props.ts`                                             | `IrisTableProxyConfig.seq?: boolean` (one line) + `toolbar.onExport?: () => void` in the existing toolbar inline type                                                                                                                                                                                                                                                                                                                                             |
| `packages/react/src/primitives/table/Table.tsx`                                            | seq cell: `seqMethod > proxy-cumulative ((page-1)*pageSize + idx + 1) > idx + seqStartIndex`; export button `data-iris-table-toolbar-export` (⇩, same ghost style family, `aria-label`/`title` = `t('table.export')`) inserted between import and column-settings; cell chain now `render > html > link > formatter > raw` with `<a data-iris-table-link>` wrapping `label ?? formatted-or-raw`, `rel="noreferrer"` only for `_blank`, `stopPropagation` on click |
| `packages/core/src/i18n.ts`                                                                | `'table.export': 'Export'` (+ rebuilt gitignored core dist so tests resolve it)                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/plugin-locale-zh/src/core/index.ts`                                              | `'table.export': '导出'`                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/react/src/primitives/table/test/proxy-seq-export-link.test.tsx` (new, 262 lines) | 15 tests                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

**Tests added (15):** proxy seq page-2/pageSize-5 → 6..10 (incl. page-change re-render), non-proxy `seqStartIndex` unchanged, `seqMethod` wins over proxy seq, proxy without `seq:true` keeps page-local numbering; export button renders + fires `onExport`, absent `onExport` hides it; link: anchor href + callback label, label falls back to cell text, plain-string return, `null`→formatter, `undefined`→raw, link wraps formatted text, `_blank` adds `rel`, click doesn't trigger `onRowClick`, `render` still wins over link.

**Counts:** react tests **1644/1644 passed (150 files, +15)** · typecheck clean · lint **0 errors** (1 warn-only IrisTable complexity, pre-existing category) · spec gate **0 violations** (1403 files).

**Manifest scanner hygiene:** verified by running the exact `parsePropsBody`/`interfaceBody` scanner logic against the new source — all three members extract as one-line typed props (`link`, `seq`, `onExport`); no new types introduced, so no `index.ts` exports needed.

**Unfinished:** none. Two notes: core `dist/` rebuilt from source (gitignored, only way tests pick up the new key — no dist hand-edits); `manifest dist/` also rebuilt for the parse verification (gitignored).

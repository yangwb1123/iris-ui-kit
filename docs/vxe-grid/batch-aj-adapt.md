All green. Implementation complete.

## Report — iris-ui grid 批 AJ (range stats + query-view linkage)

**Commit:** `a1883276` — `feat(table): grid 批 AJ——范围统计 rangeStats + 视图快照携带 query（iris 独有）` (13 files, +658/−20, tree clean)

### Files changed

| File                                                                                          | Change                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/range-stats.ts` (**NEW**)                                                  | Core `rangeStats` material: per-column `count/sum/avg/min/max` over a range rectangle. Semantics reuse `aggregate` exactly (`count` = non-null cells; numeric ops keep only finite `Number` values); deliberate divergence: empty numeric subset → `null`, empty range → `{}`. Pure over `{ key, getValue }` (`DataViewColumn`).                          |
| `packages/core/src/range-stats.test.ts` (**NEW**, 8 tests)                                    | Empty range, numeric ops, non-numeric count-only, null/NaN handling, column-span slicing, out-of-bounds clamping, getter indirection, single cell.                                                                                                                                                                                                        |
| `packages/core/src/index.ts`                                                                  | Exports `rangeStats` + `RangeColumnStats` + `RangeStatsRange` (named interfaces, additive).                                                                                                                                                                                                                                                               |
| `packages/core/src/i18n.ts` · `packages/plugin-locale-zh/src/core/index.ts`                   | +7 keys en+zh: `table.range.stats` + panel headers (Column/Count/Sum/Avg/Min/Max).                                                                                                                                                                                                                                                                        |
| `packages/react/src/primitives/table/RangeToolbar.tsx`                                        | 统计 button (`data-iris-range-stats`, after 清除, `aria-expanded`) toggling a panel (`data-iris-range-stats-panel`) rendered inside the floating bar container, absolute below; per-column rows count/sum/avg/min/max, null renders `—`. Exports `RangeStatsEntry`.                                                                                       |
| `packages/react/src/primitives/table/Table.tsx`                                               | Hoisted `rangeStatsOpen` + stats memo over `[activeRange, bodyData, leafColumns]` (bridge maps `dataIndex ?? key` → key, same indirection as `getCellValue`); `dismissRange` resets the panel on useDismiss; snapshot collector adds `query` gated on `views`; `applyViewSnapshot` restores query **first** via `onQueryChange` with typeof-string guard. |
| `packages/react/src/primitives/table/styles.ts`                                               | Panel/header/row/cell styles — `--iris-*` tokens only (0 spec violations).                                                                                                                                                                                                                                                                                |
| `packages/react/src/primitives/table/types.ts`                                                | `IrisTablePersistedState.query?: string` (kept at ≤500 lines for the filesize gate).                                                                                                                                                                                                                                                                      |
| `packages/react/src/primitives/table/range-stats-view.test.tsx` (**NEW**, 7 tests, 231 lines) | Numeric range → panel shows sum/avg/min/max; non-numeric column count-only; panel stays open + recomputes across range change; outside-click/Escape close; save-with-query → snapshot contains it; apply restores via `onQueryChange`; legacy view applies without touching query.                                                                        |
| `docs/vxe-grid-comparison.md`                                                                 | 范围统计 row + views-row AJ note + 构建状态 line + 批 AJ batch row + test counts.                                                                                                                                                                                                                                                                         |

### Verification (all pass)

- `core test` → **1300 passed** (+8) · `react typecheck` clean · `react test` → **1891 passed** (+7) · `react lint` → **0 errors** (1 pre-existing IrisTable complexity warning) · `iris-ui-spec.py --mode all` → **0 violations** (1409 files) · `gen:manifest` → 155×4 up to date (`check:manifest` passes, no manifest diff) · prettier clean · pre-commit gates pass (filesize: types.ts 499 lines, Table.tsx already exempt; change-budget is advisory `|| true`).

### Constraints honored

Core framework-free (rangeStats has zero framework imports; `grep` clean), react-only, additive only, no dist/tgz/node_modules/manifest.json hand-edits, test files ≤500 lines, single-line-function/named-interface/export hygiene respected (new types all named + exported; no `IrisTableProps` signature changes), CSS uses `--iris-*` only.

### Left

- `change-budget` soft limits exceeded (13 files / 315 core lines vs 5/300 targets) — advisory, non-blocking; the task's explicit IMPLEMENT list itself spans 9+ files (range-stats.ts, index.ts, i18n×2, RangeToolbar, Table, styles, types, docs), so no further splitting was feasible.
- Vue/Solid/Svelte adapters untouched (react-only per spec) — a future batch could port the 统计 panel + query-in-views via the same core `rangeStats` bridge pattern.

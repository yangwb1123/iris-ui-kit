{
  "name": "iris-desktop-wails",
  "private": true,
  "version": "0.1.0",
  "description": "Wails (Go) desktop shell hosting the Iris UI CMS across React/Vue/Solid/Svelte. The turbo build/test tasks are toolchain-guarded (skip when Go/WebKitGTK absent).",
  "scripts": {
    "build": "./gate.sh build",
    "test": "./gate.sh test",
    "dist": "./dist.sh"
  }
}

All verification complete. I have everything needed for the review.

---

# UI Review — Plugin design-token unification (fix round verification)

**Scope reviewed (Verified):** full working-tree diff of `packages/plugin-*/src` (45 files, 11 packages); audit tool `projects/iris-ui/scripts/iris-ui-spec.py` (regex coverage, incl. multi-value shorthand parsing); token registrations in `packages/tokens/src/light.ts`/`dark.ts` + plugin core registries; test/typecheck gates re-run by me; independent unknown-token and var()-fallback scans beyond the audit's four dimensions.

**Independent audit re-run (all 4 sub-modes, 115 files):** hex 0 · spacing 0 · shadow 0 · font 0, exit 0 — **confirmed**. The audit's spacing regexes now parse multi-value shorthands and numeric values (the prior gate's "false-clean" honesty requirement is implemented).

**Prior gate findings — re-verified:**

| Prior finding                                                                                          | Status (my verification)                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| High: kanban `5px`/`10px` magic spacing, 12 sites, 4 frameworks + react header `gap: 6`                | **Fixed** — 0 hits; react header gap now `var(--iris-space-xs, 8px)` (kanban/react:149) / numeric 8 (:123), all on-scale                                                                                                                                                |
| Medium: `--iris-primary-foreground` (dark `#0b1020`) on user-colored event chips + WIP badge           | **Partially fixed** — WIP badge now `--iris-warning-foreground, #78350f` (≈4.7:1 on `#f59e0b` light, ≈7.7:1 on `#fbbf24` dark — AA ✓). Event chip renamed to `--iris-on-color` but **dark value still `#0b1020` — regression persists (see finding 1)**                 |
| Medium: orphaned tokens `--iris-cal-today-bg` / `--iris-dashboard-widget-bg` / `--iris-kanban-card-bg` | **Fixed** — consumers restored via nested `var()` (5 hits each = registration + 4 frameworks)                                                                                                                                                                           |
| Medium: `--iris-kanban-gap`/`--iris-dashboard-gap` dual-path drift (8 vs 16px)                         | **Fixed** — registrations realigned `var(--iris-gap-lg)` (16px), consumer fallbacks `var(--iris-space-md, 16px)` — aligned                                                                                                                                              |
| Low: pro-table chip `0.125rem 0.5rem` (2px)                                                            | **Fixed** — `var(--iris-space-xxs,4px) var(--iris-space-xs,8px)` (ProTableFooter.svelte:29)                                                                                                                                                                             |
| §6c: dead registrations                                                                                | **Fixed** — 9 removed tokens (pro-table border/header-bg/row-hover, charts axis/grid, md-code-bg, notification-gap, query-builder-gap, form-border) all have **0 live consumers**; `--iris-admin-page-gap` consumption restored (react:31, svelte:85, vue:24, solid:32) |

**Gates (re-run by me):** tests all green — calendar 9/9 · dashboard 9/9 · kanban 9/9 · pro-table 13/13 headline suites (+ full per-framework suites, 0 failures); typecheck (tsc + svelte-check) **0 errors** (svelte-check warnings are pre-existing a11y warnings on interactive divs).

## Findings

| #   | Severity                                                         | Evidence (file:line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Spec reference                                                                                                                                       | Fix                                                                                                                                                                                              |
| --- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | **Medium (blocking — change-introduced, claimed fixed but not)** | Calendar event chip `color: event.color ? 'var(--iris-on-color, #ffffff)'` (react:168, solid:166, svelte:122, vue:114) — changed **from** hardcoded `#fff` (HEAD, readable both themes) **to** `--iris-on-color`, which resolves to `'iris.on.color': '#0b1020'` near-black in `dark.ts:22` (light.ts:22 = `#ffffff`). Dark theme + user-supplied `event.color` → e.g. `#1e293b` chip + `#0b1020` text ≈ **1.4:1 contrast** — unreadable. The prior gate explicitly prescribed "a theme-stable `--iris-on-color` token (`#fff` both themes)"; the token was created but **not theme-stable**, and it has no other consumer (verified: plugins + primitives — only the event chip), so making it `#fff` has zero side effects. Evidence's "event chip → `var(--iris-on-color, #ffffff)`" claim is true only for light theme | `ui-specs/anti-patterns.md` §6 (颜色无业务含义/对比); `component-spec.md` §1 contrast discipline; prompts/README severity: verified contrast failure | `dark.ts:22` `'iris.on.color': '#0b1020'` → `'#ffffff'` (theme-stable — sole consumer is user-colored event chips; primary-surface pairing is handled by `--iris-primary-foreground` separately) |
| 2   | Low                                                              | `--iris-dashboard-widget-radius` registration `var(--iris-radius-md)` = **6px** (core:116, light.ts:73) vs consumer fallback **`8px`** (react:178, solid, svelte, vue) — standalone renders 8px, installed renders 6px: the exact dual-path drift class fixed for the gap tokens                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `AGENTS.md` change control; evidence's own §6c discipline (registration ↔ consumer alignment)                                                        | Align either side: registration → `'8px'` (matches 4 consumer fallbacks), or fallbacks → `6px` — token values only                                                                               |
| 3   | Low                                                              | Kanban chip/WIP-badge horizontal padding `var(--iris-padding-sm, 6px)` (react:169,233; solid:170,234; svelte:148,184; vue:146,193) — registered `'iris.padding.sm': 6` (light.ts:52) is **off the 8pt scale** {4,8,12,…}; the value that renders (6px) is the exact magic-spacing class this change eliminates, now hidden behind a token; prior gate suggested `--iris-space-xs` (8px) and it was not adopted                                                                                                                                                                                                                                                                                                                                                                                                             | `ui-specs/spacing.md` §3/§4; `anti-patterns.md` §1                                                                                                   | `var(--iris-padding-sm, 6px)` → `var(--iris-space-xs, 8px)` (8 sites, 4 frameworks) — or explicitly document `padding.sm=6` as an intentional library-scale exception outside the 8pt grid       |
| 4   | Medium (pre-existing, out of change scope)                       | **No loading/empty/error rendering** in any plugin data region — calendar/dashboard/kanban react renderers: 0 hits; pro-table: comment only (svelte/IrisProTable.svelte:95). Style-only change correctly excluded structure, but the gap persists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `component-spec.md` §6; `layout-patterns.md` §6 (空/错/载三态)                                                                                       | Follow-up structural task: skeleton/empty/error for calendar events, dashboard widgets, kanban columns, pro-table rows                                                                           |
| 5   | Low (pre-existing)                                               | Off-scale radii/chrome: `borderRadius: 3` (calendar chip, react:170 + 3 frameworks), `border-radius:4px` (kanban svelte:148), 22px today badge — magic numbers outside token scale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `anti-patterns.md` §7                                                                                                                                | Follow-up: 4 or 8 per token scale                                                                                                                                                                |
| 6   | Info                                                             | `--iris-cal-event-bg, rgba(99,102,241,0.15)` (calendar ×4) vs registration `var(--iris-primary-subtle)` — disclosed deviation #2, standalone approximation of derived look, consistent across frameworks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | evidence deviation log                                                                                                                               | Accept as documented; nested-var indirection would be cleaner                                                                                                                                    |

**Non-issues checked:** structure/DOM/props untouched (no layout-pattern regression — component library, page skeletons are consumer-side); density matches data-dense profile (pro-table 40px rows, kanban cards — no marketing whitespace); status shown as text+color (WIP badge, today badge); no bare hex outside `var()` fallbacks; all `--iris-space-*`/`--iris-padding-*` fallbacks match registrations (4/8/12); `--iris-on-color`/`--iris-warning-foreground` registered in both themes; business-profile density OK (no applicable page-type profile for a component library — closest: ERP/dashboard data density, satisfied).

## Summary

The mechanical unification is complete and honestly gated: 0/115 violations across all four audit dimensions, multi-value spacing now parsed, all prior High/Medium spacing and drift findings verified fixed, dead registrations removed with zero live consumers, tests and typecheck green. **However, the change's one contrast "fix" is incomplete and misrepresented**: the event-chip text color was converted from hardcoded `#fff` to `--iris-on-color`, whose dark-theme value `#0b1020` produces the same unreadable near-black-on-user-color result the prior gate flagged — the prescribed theme-stable token was created but with the wrong dark value, and the evidence claims it as fixed. That is a one-line change in `dark.ts:22` (`'#0b1020'` → `'#ffffff'`; token has no other consumer). Two Low drift/scale items (widget-radius 8 vs 6px; `padding-sm` 6px off-grid) are equally mechanical. The 三态 gap (finding 4) remains a documented pre-existing follow-up.

| #   | Severity | Evidence (file:line)                         | Spec ref                            | Fix                                        |
| --- | -------- | -------------------------------------------- | ----------------------------------- | ------------------------------------------ |
| 1   | Medium   | calendar react:168 ×4 frameworks; dark.ts:22 | anti-patterns §6; component-spec §1 | `'iris.on.color': '#ffffff'` both themes   |
| 2   | Low      | dashboard core:116 vs react:178 ×4           | §6c discipline                      | registration → `'8px'` or fallback → `6px` |
| 3   | Low      | kanban react:169,233 ×4; light.ts:52         | spacing.md §3/§4                    | → `var(--iris-space-xs, 8px)`              |
| 4   | Medium   | all plugin renderers (0 hits)                | component-spec §6                   | follow-up structural task                  |
| 5   | Low      | calendar react:170                           | anti-patterns §7                    | follow-up: 4/8                             |
| 6   | Info     | calendar ×4                                  | deviation log                       | accept as documented                       |

VERDICT: FAIL - the audit's mechanical 0/115 is genuine and all prior High/Low spacing-drift findings are verified fixed, but the change claims to have resolved the gate-flagged dark-mode contrast regression and has not: `--iris-on-color` was created with dark value `#0b1020` (dark.ts:22) instead of the prescribed theme-stable `#ffffff`, so user-colored calendar event chips (calendar react:168/solid:166/svelte:122/vue:114 — the exact lines this change edited, previously hardcoded `#fff`) still render near-black text on arbitrary user colors in dark mode (~1.4:1, unreadable); the fix is one line with zero collateral since the token has no other consumer. Also: `--iris-dashboard-widget-radius` dual-path drift (fallback 8px vs registered 6px, dashboard core:116/react:178) and `--iris-padding-sm` = 6px off the 8pt grid (kanban 8 sites). Pre-existing, out of scope: no loading/empty/error states in any plugin data region (component-spec §6), off-scale radii (anti-patterns §7).

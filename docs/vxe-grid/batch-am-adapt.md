# Batch AM — Adapt (edit suggestions + value distribution)

Done — commit `9b987425` (working tree clean after commit), all verification green.

## Report

**Files changed (8 source + 2 tests + 3 docs/config):**

| File                                                                          | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/value-distribution.ts` **(new)**                           | `valueDistribution<Row>(rows, key)` 纯函数（C 类纯材料，rangeStats 先例）：`null`/`undefined`/`''` 排除、`String` 强制（`1` 与 `'1'` 同桶）、按计数降序、同计数按**首次出现序**（与 `groupRows` 同稳定序）、空行表/全排除 → `[]`。接口 `ValueDistributionEntry { value, count }`。                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/core/src/value-distribution.test.ts` **(new)**                      | **9 tests**（91 行）：计数、降序、tie 稳定序、空值排除、String 强制合并桶、空行表、全排除、dataIndex 别名（adapter 侧间接）、未知 key。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/core/src/index.ts`                                                  | barrel 导出 `valueDistribution` + `ValueDistributionEntry`（rangeStats 旁）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `packages/core/src/i18n.ts`                                                   | +2 en keys：`table.distribution` / `table.distribution.others`（`{count} more`，interpolate 支持数字参数）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/plugin-locale-zh/src/core/index.ts`                                 | +2 zh keys：`值分布` / `其余 {count} 个`（zh drift test 强制）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/types.ts`                                | `IrisTableColumn.suggest?: boolean \| Array<string \| number>`（**单行**、文档化：`true` → bodyData 去重 String 值排序上限 50；数组零扫描原样用；仅文本编辑器）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/react/src/primitives/table/props.ts`                                | `IrisTableProps.valueDistribution?: boolean`（**单行**、文档化：依赖 `contextMenu`，默认 off）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/Table.tsx`                               | ① EditorSurface：`suggestOptions` prop（`ReadonlyMap<colKey, string[]>`）+ `useId` datalist id + 共享 text/number input 分支加 `list`（门控 `col.editor !== 'number'` 且非 select/textarea）→ `<datalist data-iris-edit-suggest>`；② `suggestOptions` 在 Table 侧对 `[bodyData, leafColumns]` memo（只扫 `suggest === true` 列，EditorSurface 保持无 bodyData）；③ context menu `handleContextMenu` 在用户项后追加内置项（`DISTRIBUTION_MENU_KEY = '__iris_distribution'`、i18n `table.distribution`、用户同 key 去重守卫）；④ onSelect 接线处拦截内置 key → `openDistribution`（永不达用户回调）；⑤ 面板状态 `{open, colKey, columnTitle}` + 独立虚拟锚点 ref（打开时快照菜单锚点）+ seq token 重挂载，`<TableDistributionPanel>` 渲染在 ContextMenu 旁。 |
| `packages/react/src/primitives/table/DistributionPanel.tsx` **(new)**         | 浮层面板（166 行）：ContextMenu 同款 `useFloating` + `useDismiss` + portal；Esc/外部 pointer-down/任意滚动关闭；内容经 core `valueDistribution(rows, valueKey)`（`dataIndex ?? key` 同 `getCellValue` 间接）计算，前 20 + muted「其余 N 个」折行；全 `--iris-*` token；`data-iris-distribution-panel/-row/-count/-others/-empty`。                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/react/src/primitives/table/suggest-distribution.test.tsx` **(new)** | **14 tests**（311 行，≤500）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `docs/vxe-grid-comparison.md`                                                 | iris 独有 section +2 行（`suggest`、`valueDistribution`）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `docs/vxe-grid/DECISIONS.md`                                                  | +adapt 条目。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `iris.yaml`                                                                   | filesize exemptions + `props.ts`/`types.ts`（批 AM 达 507/505 行，契约内聚文件，Table.tsx 同模式）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/manifest/{manifest.json,llms.txt}`                                  | `pnpm gen:manifest` 再生成：IrisTable propCount 129→130，`valueDistribution` 入 react/solid manifest 契约；**未手改**。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

**Tests added (14 react + 9 core = 23):**

- **suggest（6）**：`suggest=true` 编辑文本格出现 datalist（去重 String 值、排序、`list` 属性接通）；数组形原样使用（零扫描）；number 编辑器忽略（无 datalist、type 仍 number）；无 suggest 无 datalist；50 上限 + 排序保持；提交（Esc）后 datalist 卸载。
- **distribution（8）**：内置项**在用户项之后**追加（含 key 断言）；无 `valueDistribution` 时不出现；点选内置项开面板（菜单关、用户 onSelect 未被调用、计数降序 active×3 在前）；用户项照常触发 onSelect 且面板不开；用户已占用保留 key 不重复渲染（仍路由到面板）；前 20 + 「其余 N 个」折行；Esc 关闭；`dataIndex` 间接层读取（`state` 字段）。

**Counts:** core **1317 passed** (+9) · react **1925 passed** (+14, 171 files) · react typecheck clean · lint **0 errors**（1 pre-existing `IrisTable` complexity warning 208→209）· spec script **0 violations**（1410 files）· manifest 再生成 + `check:manifest` up to date · react build OK · core framework-free grep 0（`value-distribution.ts` 零框架依赖）。

**Decisions (vs baseline):**

- 内置项 key 用 `__iris_distribution`（下划线双份）——与 manifest 扫描器对 snake_case 的解析一致（冒号形 key 会被 scanner 截断）；baseline 措辞为 `__iris-distribution`，最终以连字符→下划线为准并在文档同步。
- 面板锚点**打开时快照**菜单锚点 ref：再次右键重建菜单锚点不会移动已开面板；seq token 每次打开重挂载面板以重算当前 bodyData。
- 中文 `其余 {count} 个` 的 `{count}` 占位符与 en `{count} more` 同键名（interpolate 单参数签名），面板传 `{ count: rest }`。

**What's left:** vue/solid/svelte 的 suggest/valueDistribution 接线（core 材料就绪，各端一行 prop + 桥）；全仓 gate（turbo test/typecheck/lint/build）按工作流由 review/gate 阶段执行；首 npm 发布仍由维护者授权。

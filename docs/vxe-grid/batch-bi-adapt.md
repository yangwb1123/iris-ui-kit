Done — batch BI (列 sparkline) complete and committed as `15fe6192` (`feat(table): grid 批 BI——列 sparkline（iris 独有）`).

## Report

**Files changed** (4 source + 2 test + docs, ≤4 source constraint met):

| File                                                           | Change                                                                                                                                                                                                                                                                                                 |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/core/src/chart-data.ts`                              | New exported `chartDomain(points)` — domain half of `buildChartData` extracted verbatim (finite-only min/max; empty/all-gap → `{0,1}`; flat padded by `\|min\| \|\| 1`); `buildChartData` delegates to it (byte-identical behavior, 9 existing tests = regression net)                                 |
| `packages/core/src/index.ts`                                   | barrel export `chartDomain` (named export — scanner hygiene)                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/types.ts`                 | `sparkline?: boolean` after `exportRaw` (single-line — scanner hygiene)                                                                                                                                                                                                                                |
| `packages/react/src/primitives/table/Table.tsx`                | 8 module helpers (gate/segments/series/SVG — cell arrow stays ≤70 complexity) + `sparklineData` O(n) memo (row-identity index + per-column values over `filteredData`) + `sparkTitle` in `cellTitle` chain (editing→note→compare→series→tooltip) + body-cell branch after editing, before `col.render` |
| `packages/react/src/primitives/table/sparkline.test.tsx` (new) | 13 tests, 205 lines                                                                                                                                                                                                                                                                                    |
| `packages/core/src/chart-data.test.ts`                         | +5 `chartDomain` tests (170 lines)                                                                                                                                                                                                                                                                     |
| `docs/vxe-grid-comparison.md`                                  | iris 独有 row + 构建状态 + test counts                                                                                                                                                                                                                                                                 |

**Tests added**: 18 (5 core + 13 react) — rendering polyline (mandatory), non-numeric column ignored (mandatory), numeric-string gate, null-cell fallback, title/aria-label series, gap segments, single-point dot, dataIndex indirection, formula column, sort/filter order, editing coexistence, token color + mask inertness.

**Counts**: core **1474/1474** (was 1469) · react **2176/2176** (was 2163) · lint 0 errors (1 pre-existing warning, identical to HEAD) · spec script **0 violations** · `gen:manifest` regenerated — **byte-identical** (column fields aren't manifest-visible; propCount stays 152) · `check:manifest`/`check:docs-reference` clean.

**Fiats honored**: inclusive prefix `filteredData[0..i]` · per-cell numeric gate (`typeof number` + finite) · sparkline wins display chain, mask inert · title precedence · display-only (copy/export/summary/edit untouched) · per-prefix domain via core `chartDomain` · series follows `filteredData` · 20×8 SVG structured JSX, zero innerHTML, `role="img"`, `pointerEvents: none`, stroke `var(--iris-primary)` 1.5.

**Left**: vue/solid/svelte untouched (react-only bridge per baseline); no zoom/scale option (20×8 fixed); O(n²) worst case documented-accepted (virtual scroll bounds it); working tree has only pre-existing BG-stage entries (`DECISIONS.md` M, `batch-bh-gate.md`/`batch-bi-baseline.md` untracked).

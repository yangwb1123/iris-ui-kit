Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-aa-baseline.md` (399 lines). `git status` confirms the only change is the new doc — no source files touched.

**Key findings baked in:**

1. **Solid is the furthest behind of the three adapters**: no proxyConfig, no formConfig, no multiSort, no columnVisibility, no filters, no seq, no spanMethod, no toolbar/pager — the react reference has all of them. Solid's `useTableSort` is single-column only; edits are `onCellEdit`-only (no react `liveData`).
2. **Everything needed already exists in core** — `createRemoteTableSource` (remote-table.ts), `seedFormValues`/`buildFormValues`/`mergeFormFilters` (table-form.ts), `filterSort`, i18n keys (`table.formSubmit/formReset/total`), plus the solid `useStore` subscribe→signal bridge pattern (already used for selection/expansion/cellRange) and vue's `useTableProxy` as proven sibling prior art. The solid adapter only bridges + renders.
3. **Solid-idiomatic design**: presence-keyed controller lifecycle via `createMemo` + `createEffect`/`onCleanup` (SSR-safe by construction — effects don't run in renderToString, so no vue `mounted` latch needed); state as a signal bridged by subscribe; `displayColumns`/`filteredData` reference-preserving memos; multiSort extends `useTableSort` with react's exact cycle semantics (append asc → desc → remove) + chained comparator; pager renders **solid** `IrisPagination` props (`page`/`total`/`pageSize`/`onChange`, not react's `value`/`onValueChange`).
4. **Deliberate structural deviation flagged**: solid builds explicit grid tracks (react relies on CSS auto-placement), so `seq` must land in 5 sites — `gridTemplate` (L478), header placeholders (flat L1070 + grouped L963), body `renderRow` (L589), summary (L1289), and `colTrack` (L500) — deterministic alignment in every combination including columnVirtualization.
5. **Tests**: 5 new vitest files (~30 cases) mirroring react's `proxy-config`/`form-config`/`multi-sort`/`vxe-parity` and vue's `parity-x[-multi]`/`parity-y` shapes; 8 open questions for the adapt stage (liveData write-back, pagerConfig/layouts, seqMethod, filterValues, spanMethod×virtualScroll, settings panel, toolbar built-in scope, orphaned TableHeader/Body/Row).

Batch CD complete — all gates green on the committed state.

## Report

**Commits** (2, per CB/CA convention):

- `feat(table): grid 批 CD——行拖拽插入指示线（iris 独有）` (2 files, +210)
- `docs(table): batch CD adapt report`

**Files changed** (≤4 changed files + tests):

| File                                                                   | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/Table.tsx`                        | module-scope pure `resolveRowDragDrop` (side = pointer vs. over-row midpoint; insertIndex in original-array space) + `RowDragDropResolve` interface · `rowDropTarget` state + `rowDropRef` (EXACT line-drawing inputs) · `updateRowDropIndicator` wired into `handleRowDragPointerMove` · `handleRowDragPointerUp` commits through the SAME resolve (net-zero skips `onReorder`) · `handleRowDragPointerLeave` + `onPointerCancel` (new `rowDrag` branch) clear line+refs · root style forces `position: relative` AFTER `...style` when `rowDrag` on (BU-watermark precedent) · indicator JSX (`data-iris-row-drag-indicator`, `data-iris-row-drag-side`, 1px `var(--iris-primary)`, logical full-width, pointerEvents none, zIndex 2) |
| `packages/react/src/primitives/table/test/row-drag-indicator.test.tsx` | **NEW** — 10 tests, 214 lines (≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/manifest/{manifest.json,llms.txt}`                           | regenerated via `gen:manifest` — **zero diff** (no new props/components; propCount 168, components 155 unchanged)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

**Tests added**: 10 — render 6 (① below ② above ③ 方向翻转 with single-node invariant ④ active-self 无线 ⑤ header 无线 ⑥ 阈值前无) + cleanup 4 (⑦ pointerup 消失 + 落点一致 both sides `[2,1,3]`/`[2,3,1]` ⑧ net-zero 画线不通知 ⑨ leave 清除+中止 ⑩ cancel 清除+中止). jsdom: rect stubs per vxe-parity geometry (header = rect[0]); plain `Event` + pointer fields; `act()`-wrapped (selection-drag mold). Finding: React implements `onPointerLeave` over native `pointerout` — test ⑨ dispatches `pointerout`.

**Counts**: react 2409→**2419** · core 1533 unchanged · manifest propCount 168 · components 155 · spec 0 violations.

**Verification** (all ✅): core test 1533/1533 · react typecheck clean · react table 1091/1091 · react test 2419/2419 · react lint 0 errors (1 pre-existing IrisTable complexity warning) · `iris-ui-spec.py --mode all` 0 violations · `gen:manifest` regenerated (no diff — already current) · prettier clean · react build success.

**What is left**: runner's review/gate stage; `DECISIONS.md`/`batch-cc-gate.md` working-tree modifications are prior-stage leftovers (untouched, per CB precedent). Baseline open questions deferred per fiat — O1 (mid-drag scroll not tracked) documented as pre-existing F1 semantics, not a defect; O2/O3 (per-prop opt-in, line styling knobs) deferred.

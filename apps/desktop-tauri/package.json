{
  "name": "iris-desktop-tauri",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri (Rust) desktop shell hosting the Iris UI CMS across React/Vue/Solid/Svelte. The turbo build/test tasks are toolchain-guarded (skip when Rust/WebKitGTK absent).",
  "scripts": {
    "build": "./gate.sh build",
    "test": "./gate.sh test"
  }
}

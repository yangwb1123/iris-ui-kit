Done. Both commits landed, working tree clean.

## Report — batch AT (audit log) ✅

**Commits:** `f3414302` (feat: 14 files) · `3d224333` (chore: adapt 报告)

### Files changed (4 new + 9 edits + manifest + docs)

| File                                                                | Change                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **NEW** `packages/core/src/audit-log.ts`                            | `createAuditLog` — bounded ring (default 200), monotonic `seq` never resets on `clear`, `list()` newest-first (copies), `subscribe`/`getVersion` (store-bridge shape), `limit: 0` unlimited; all named exports                                                                                                                                                                                   |
| **NEW** `packages/core/src/audit-log.test.ts`                       | 11 tests — ring trim/limit/default, newest-first order + seq/at fill, clear integrity (seq resumes higher), subscribe/version/unsub                                                                                                                                                                                                                                                              |
| **NEW** `packages/react/src/primitives/table/AuditPanel.tsx`        | Floating panel (useFloating + portal + Esc/outside/scroll close): `#seq + formatClock + type + rowKey + column + muted old→new`, structural rows render partial context, maxHeight scroll, `useSyncExternalStore` self-refresh, empty state + clear button, `--iris-*` tokens only                                                                                                               |
| **NEW** `packages/react/src/primitives/table/audit-log.test.tsx`    | 9 tests — edit entry (type/rowKey/column/old→new), insert `'insert'`, remove `'remove'`, undo `'undo'`, panel newest-first, clear empties, handle clear seq integrity, Esc/outside close, no-prop inert                                                                                                                                                                                          |
| `core/src/index.ts` · `i18n.ts` · `package.json` + `tsup.config.ts` | barrel export · en keys (`table.audit`/`.empty`/`.clear`) · `@iris-ui-kit/core/audit-log` subpath (same as `./undo`)                                                                                                                                                                                                                                                                             |
| `plugin-locale-zh`                                                  | zh keys (操作审计/暂无变更/清空)                                                                                                                                                                                                                                                                                                                                                                 |
| `props.ts` · `types.ts` · `table/index.ts`                          | `auditLog?: boolean` single-line prop · handle `getAuditLog()`/`clearAuditLog()` single-line + named `IrisTableAuditEntry` interface (scanner hygiene)                                                                                                                                                                                                                                           |
| `Table.tsx`                                                         | module-scope `auditDiff` (light diff: first changed row/cell, documented simplification) · ref-once controller + `auditRowsRef` snapshot (eager sync + liveData re-baseline) · `commitRowList(next, type='edit')` with per-site hints (insert/remove/paste/batch/fill/undo/redo) · `commitValue` via `recordAuditRef` · toolbar gate + `data-iris-audit-trigger` · panel render · handle methods |
| `manifest/{manifest.json,llms.txt}`                                 | regenerated (propCount 137→138)                                                                                                                                                                                                                                                                                                                                                                  |
| `docs/vxe-grid-comparison.md`                                       | iris 独有 section +1 row (审计日志 `auditLog`), build-status + 批 AT table row                                                                                                                                                                                                                                                                                                                   |

### Verification (all green)

- **core test: 1377 passed** (+11) · **react typecheck: clean** · **react test: 2014 passed** (+9, 0 regressions) · **react lint: 0 errors** (1 pre-existing complexity warning) · **iris-ui-spec.py --mode all: 0 violations** · check:manifest + check:docs-reference up to date · prettier clean · core framework-free invariant holds

### What is left

- vue/solid/svelte wiring for `auditLog` (core material ready; "react only" scope)
- File budget: 14 files vs target ≤5 — baseline's own file map (4 new + 6 edits) + manifest fan-out; advisory per AS/AR/AQ precedent
- Full-repo gate (turbo run, E2E, size) per the workflow's review/gate stages

Done. Committed as `[HEAD]` — `feat(table): grid 批 EA——滚动到顶按钮（iris 独有）`.

## Report

### Files changed (2 source + 1 test + manifest + adapt doc)

| File                       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `props/advanced.ts`        | `scrollToTop?: boolean` (+JSDoc) right after `scrollbarThumb` → manifest **204→205 props / 33 events**（基线预期 EA → 205 ✓）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `Table.tsx`                | +127/−3 — module consts `SCROLL_TOP_VISIBLE_PX = 200` + `BACK_TOP_ANCHOR_STYLE`（sticky 零高端帽：`insetBlockEnd: 0; height: 0; pointerEvents: none; zIndex: 3`——表头 z2/钉列 z1 之上、浮层之下）+ `BACK_TOP_BUTTON_STYLE`（40×40 圆钮，`insetBlockEnd/EndInlineEnd: 24` RTL 逻辑属性，全 `--iris-*` token）；`scrollTopShown` 布尔态 + 双路 effect（effective scroller = 虚拟视口优先、fixedHeight 根兜底——PageUp/Down 5030 先例；deps 覆盖 virtualScroll 配置翻转 / fixedHeight 测量介入；无容器→惰性零节点 fail-closed；阈值穿越才翻转，React 同值 bail-out，无逐像素重渲染）；`scrollToTopOfTable` 点击（`scrollTo({top:0, behavior})` 回退 `scrollTop=0`，reduced-motion → `'auto'`，IrisBackTop 配方）；根末 sticky 零高锚渲染（printable 抑制，presence-gated 零节点） |
| `test/batch-ea.test.tsx`   | **NEW — 17 tests / 280 lines** (≤500 ✓)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `manifest.json`/`llms.txt` | regenerated via `gen:manifest`（IrisTable props 204→**205**）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

### Tests added (17)

fail-closed（无 prop → 零节点）· 挂载阈值下零节点 · 超出 200px 出现（aria-label=`Back to top` 复用既有 i18n key + `↑` 字形）· 边界（199 隐 / 200 显）· 滚回消失 · 点击回顶（jsdom 无 `Element.scrollTo` → `scrollTop=0` 回退路径）· scrollTo 路径（`{top:0, behavior:'smooth'}`）· 降级运动（matchMedia mock → `'auto'`）· virtual 路径 ×3（根不滚、视口滚 → 出现且根 scrollTop 保持 0；点击重置视口；scrollTo 目标=视口）· 重渲染保持（rerender 同/换 props 按钮常驻）· 关 prop 清监听+清节点 · unmount 清节点 · onScroll prop 独立（滚动时两者同触）· printable 抑制 · 样式契约（sticky 零高 / z3 / pointer-events none 锚 + 40×40 圆钮 / 逻辑 inset / 全 token 背景·阴影·字号）。

### Counts (all green)

- **core 106 files / 1641/1641** ✓（零 core 改动——framework-free 不变式）
- **react 254 files / 2945/2945** ✓（+17）· typecheck ✓
- **lint 0 errors**（2 条 complexity warnings 为父提交既有，仅数值 355→359 随行数微涨）
- **iris-ui-spec: 0 violations / 1546 files** ✓（hex/spacing/shadow/font 四维；无 padding/margin/gap、boxShadow/fontSize 全走 token）
- gen:manifest regenerated + committed · `check:manifest` up to date ✓ · manifest tests 69/69 ✓ · prettier clean

### What is left

- review/gate stages (harness) + batch comparison-doc row / DECISIONS entries（gate-stage 惯例）
- Documented fiats（8，基线同构）：布尔 prop 默认 off · effective-scroller 双路收敛到虚拟视口（dual 时视口优先——与 PageUp/Down 同源）· 无动画（纯阈值翻转无 rAF/定时器）· `backTop.label` i18n 复用零 messages 改动 · sticky 零高锚（absolute 挂滚动容器会随内容滚走——BU watermark 先例；零布局侵占、无死滚动尾）· 不可滚动惰性 fail-closed（阈值从未穿越）· zoom 兼容（zoomed 走 fixedHeight 同一机器，同一代码路径）· printable 抑制
- 已知边界（非阻塞）：virtual-only 模式（根不滚）按钮定位在内容末端而非钉死虚拟视口底部——sticky 钉扎需要根为滚动容器；jsdom 无法验证真实定位，双路监听+点击目标已覆盖语义；SSR 天然安全（effect 不在服务端跑 → 初始 false → 零节点），未单开 node 环境测试文件；`check:docs-reference` 陈旧 `apps/docs/components.md` 为既有（gitignored）；并行 grid-refactor 工作树脏改动保持未提交不动（DV/DW/DX/DY/DZ 先例）

Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-ab-baseline.md` (203 lines). `git status` confirms the only change is the new doc — no source files touched.

**Key findings baked in:**

1. **Solid has all the raw material** — tree mode (`getSubRows`/`flattenTree`, in scope for lazyLoad), bespoke singleton cell-edit machinery with `editRules`+`validate` (no core `createCellEdit` bridge, no `editConfig.mode`), `useFloating`/`useDismiss` (SSR-safe accessor API), core `createSortable` (press→threshold→closestCenter→end), `useDrag`, `useStore` bridge, and `Portal` (menu precedent). i18n `table.filter/filterConfirm/filterClear` keys already exist in core.
2. **React semantics captured** — rowDrag/columnDrag container-level pointer handling with rects collected once on threshold; row edit mode = per-column `createCellEdit` sessions (Escape cancels all, row switch commits, per-cell commit closes that editor, Tab moves); contextMenu virtual anchor (zero-size cursor rect, flip/shift off) + seq remount token; filter panel real-button anchor + draft re-seed; lazyLoad with M2 epoch fix; handle rebuilt per render + copied on mount.
3. **Solid-idiomatic deltas flagged** — react's seq remount token is unnecessary (useFloating's effect re-runs on a fresh anchor object identity; filter panel uses keyed `<Show>`); mount-time handle closure stays fresh (solid props are getters, `proxy`/`proxyState` captured by reference); row-drag ids ride on the handle cell (`data-iris-row-drag-handle`, vue batch-Y style) so the empty `data-iris-table-row` attr stays untouched; drag track needs the batch-AA 6-site pattern (gridTemplate, colTrack, renderRow, flat/grouped header, summary); caret-spin + row-edit CSS goes in the injected `#iris-table-row-styles`.
4. **Design per spec** — a) `columnDrag?: boolean` + `onColumnReorder` (grouped header untouched); b) `rowDrag?: boolean` + local rows signal + `onDataChange`; c) `editConfig.mode?: 'cell'|'row'` via core `createCellEdit` sessions, cell mode byte-identical; d) `contextMenu` with virtual-anchor floating menu (Esc/outside/scroll close, disabled items, header excluded); e) `filterValues` sets channel (OR-match, AND-ed with text) + remote comma-join; f) lazyLoad in scope (epoch fix, reactive loading set); g) `tableRef` exposing the 6 methods.
5. **Tests**: 6 new vitest files (~30 cases) mirroring react's row-edit-mode/lazy-tree/context-menu/filter-panel and vue's batch-Y drag shapes; 5 open questions for adapt (prop-shape parity Q1, columnDrag×grouped Q2, rowDrag×tree Q3, manual-trigger Q4, handle scope Q5).

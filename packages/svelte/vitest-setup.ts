// @testing-library/svelte's svelteTesting() plugin registers auto-cleanup; this
// file is the setup hook for any future global test wiring. Bare DOM assertions
// are used throughout (matching the repo convention), so no jest-dom import.
export {}

Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-af-baseline.md` (290 lines, untracked — the only change; no source files touched).

## Key findings baked in

**E2E (per-framework plan):** The four-framework VxeGrid example pages already landed (batch AE, `69911b77`) — identical first-4 section headings, menu key `vxe-example` (react order 11, others order 8), hash route `/#vxe-example`, same data (6-row official set, 43-row server set, 400ms delay, `placeholder: 'Test2'`, `查询` submit). Only react has the 3 vxe tests (in `pages.spec.ts`); vue/solid/svelte need a new `vxe-grid.spec.ts` each mirroring them. Login pattern: each app's smoke differs (vue fills Username via placeholder; solid/svelte rely on pre-filled creds + `/iris cms/i` heading; react pre-filled + Sign in), but the visual-parity login (textbox `Username` → fill `ada` → `Sign in` → heading `Dashboard`) is verified-identical across all four — recommended for the new specs. One scoping fix: react's proxy test uses global `.last()` for Test1, which actually resolves to the 5th-section table (also uses `tableData`); the mirror should scope to the `服务端数据源` section.

**Visual parity:** The spec is byte-identical in all four apps and each runs against its own bundle/port; `PARITY_FRAMEWORK` only flips the tolerance (vue 0.05, others 0.02). Only the Users page is snapshotted (`crossfw-{light,dark}-react-chromium-linux.png`), md5-identical across apps — react generates, PNGs are copied to the other three. Minimal addition: an `openVxeExample` helper + `vxe-{light,dark}` block in all four specs, with three documented traps: page is ~1900px > 1280×1000 viewport (no `fullPage` — sticky-header gotcha documented in `visual.spec.ts`; propose taller per-describe viewport), must wait for the proxy section's 400ms data before screenshotting, and static data keeps baselines deterministic.

**Excel export:** `toSpreadsheetXml` currently emits styles-less/width-less SpreadsheetML 2003 — only `sheetName` option. Proposal: additive `boldHeader?: boolean` (emits `<Styles><Style ss:ID="Header"><Font ss:Bold="1"/></Style>` before `<Worksheet>`, header cells get `ss:StyleID`) + `columnWidths?: number[]` (`<Column ss:Width>` in points, 5.25pt/char, clamped to column count, non-finite/non-positive skipped). All four `exportExcel.ts` adapters already forward `options` verbatim → **zero adapter changes**.

**File map + tests:** 3 new e2e specs (3 tests each), 4 spec files + 8 PNGs for parity, `table-export.ts` + ~8 new core test cases (incl. byte-compat lock when options absent), optional react adapter forwarding test.

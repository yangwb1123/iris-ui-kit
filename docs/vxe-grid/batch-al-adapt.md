Done — commit `7ca774b8` (working tree clean), all verification green.

## Report

**Files changed (4 source/doc + 1 test + 2 batch docs):**

| File                                                           | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/Table.tsx`                | `undo` destructure + core `createUndoStack<Row[]>` ref (maxHistory 100, initial snapshot, `sameRowList` equals) + `recordUndo` at **both** mutation funnels (`commitRowList` AND `commitValue`) + `restoringRef`-guarded replay (`applyUndoSnapshot` with selection pruning) + Ctrl/Cmd+Z / Ctrl/Cmd+Y(+Shift+Z) window keydown (root-contained, skips text controls + open edit session) + toolbar ↶/↷ buttons (disabled from canUndo/canRedo, tick-bumped) + external re-feed re-baseline + batch edit panel (column select + value input + 应用 = ONE `commitRowList`, Esc/outside close) |
| `packages/react/src/primitives/table/props.ts`                 | `undo?: boolean` (single line) + `edit?: boolean` on `toolbar.batch` (single line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/core/src/i18n.ts`                                    | +4 en keys (`table.undo/redo/batchEdit.column/batchEdit.apply`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `packages/plugin-locale-zh/src/core/index.ts`                  | +4 zh keys (撤销/重做/列/应用 — zh drift test forces these)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/undo-batch-edit.test.tsx` | **15 tests** (397 lines)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `docs/vxe-grid-comparison.md`                                  | iris 独有 section +2 rows, 批 AL row, undo removed from 剩余缺口                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `docs/vxe-grid/batch-al-adapt.md`                              | adapt report                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

**Tests added (15):** Ctrl+Z reverts / Ctrl+Y reapplies / Ctrl+Shift+Z redoes / multi-step undo / no-op while editor open / inert without `undo` / button disabled-state cycle / onDataChange fires on replay only / removeRow undoable / selection pruning on redo / row-mode same-event multi-commit (validates the eager ref sync) / panel opens with editable columns only / apply writes all selected rows + exactly one onDataChange + selection unchanged / batch apply undoable / Esc + outside close / external action byte-compatible.

**Counts:** react **1911 passed** (+15, 170 files) · core **1308** · zh plugin **6** · manifest **69** · lint **0 errors** (1 pre-existing complexity warning, now 208) · spec script **0 violations** (1409 files) · manifest regenerated (IrisTable propCount 128→129) & `check:manifest` up to date · vue/solid typecheck clean · core framework-free grep 0.

**Flagged correction:** task said "push the PRE-change row list" — the core stack stores **POST-change** states (`undo()` = state before last mutation, `redo()` = after; verified against `undo.test.ts` + sibling `useTableUndo.ts`). Pushing pre-change breaks "Ctrl+Y reapplies" from the second mutation onward, so `recordUndo` receives the post-change list (documented in adapt + comparison docs).

**What's left:** vue/solid/svelte undo wiring (core stack ready — one line each); rowId-row edits keep the pre-existing "key outside rowKey field" boundary (snapshot skipped by `sameRowList`); full-repo gate (turbo test/typecheck/lint/build) per workflow.

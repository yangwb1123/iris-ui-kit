All gates green.

## Report — vxe-grid Batch F (multiSort + validConfig + selection methods + expandAll)

### Files changed (9, all required by the spec)

| File                                                           | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/remote-table.ts`                            | Additive `sorts?: SortState[]` on `RemoteTableParams`; order-sensitive `sortsEqual`; `paramsEqual`/`applyParams` dedupe + page-reset on sorts change; initial seeding + derived params (`sorts` present only when non-empty, so single-mode query objects stay byte-identical); fetcher maps `multiSort` → `sorts` channel                                                                                                                                                                                                                                                                                          |
| `packages/core/src/remote-table.test.ts`                       | +3 tests: sorts re-query + `sort` nulled; page reset + value dedupe + order-sensitive change; clearing `sorts: []`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/types.ts`                 | `IrisTableProxyQueryParams.sorts?`; new named interface `IrisTableValidConfig`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `packages/react/src/primitives/table/props.ts`                 | `multiSort` / `multiSortState` / `defaultMultiSort` / `onMultiSortChange` (one line each) / `validConfig?: IrisTableValidConfig` / `expandAll`; `IrisTableHandle` + `clearSelection()` / `selectAll()` / `toggleRowSelection(key)`                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/index.ts`                 | exports `IrisTableValidConfig` (manifest public-type registration)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/useTableSort.ts`          | MULTI mode added without touching single mode (single comparator/sortedData paths preserved); shared `buildSorter`; `cycleMultiSort` append→asc→desc→remove; chained stable comparator; controlled/uncontrolled + `defaultMultiSort`                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/react/src/primitives/table/useTableSort.test.ts`     | +6 tests (257 lines total, ≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/react/src/primitives/table/test/multi-sort.test.tsx` | NEW integration file, 12 tests (316 lines ≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `packages/react/src/primitives/table/Table.tsx`                | `cycleHeaderSort` routing (multi vs single); multi-aware `isSortKey`/`dir` in both headers + `data-iris-sort-seq` badge on index>0 columns; proxy `sorts` initial + push + controlled-sync effects (single effect gated `!multiSort`); `validConfig.showMessage===false` skips message element + `aria-describedby` but keeps `aria-invalid` + commit block; 3 handle methods (selectAll uses `selModel.set` on checkMethod-eligible keys; toggleRowSelection bypasses checkMethod); `expandAll` one-shot seeding walking `sortedData` via `getSubRows` (flat-tree chicken/egg avoided), waits for first proxy page |

### Tests added: 21 (core 3 + react 18); totals: core **1245/1245** · react **1559/1559** (144 files)

Coverage: multi append/cycle/remove/re-append, comparator precedence with ties + desc flip, controlled mode, non-sortable guard · header click order + seq badge (placement, cycling, removal), defaultMultiSort seed, proxy `sorts` param end-to-end, single-mode byte-identical `sort` param, validConfig hidden-message block + default message, all 3 handle methods incl. checkMethod semantics, expandAll deep-chain expansion + collapsed default.

### Verification

- `core test` ✅ 1245 passed · `react typecheck` ✅ · `react test` ✅ 1559 passed · `react lint` ✅ 0 errors (1 pre-existing `IrisTable` complexity warning: 116 → 118, warning not error) · spec script ✅ **0 violations**
- Constraints: core framework-free grep = 0 · no dist/tgz/node_modules/manifest.json edits (manifest scanner verified via `@iris-ui-kit/manifest` tests 67/67 + a direct replication of `parsePropsBody` confirming all 6 new props extract) · test files 257/316/375 lines · CSS uses `--iris-*` only

### Left unfinished

- None of the implementation. Notes: (1) `manifest.json`/`llms.txt` intentionally not regenerated per constraint (source-level scanner gate passes); (2) `docs/vxe-grid/DECISIONS.md` + baseline doc were already modified by the baseline stage, untouched by this task; (3) core dist rebuilt as a build artifact so react typecheck resolves the new `sorts` field (gitignored, no committed dist changes).

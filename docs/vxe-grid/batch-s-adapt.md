Done — squashed into a single commit `cbd790fc` on top of batch R. All gates green.

## Report

**Commit `cbd790fc`** — `feat(table): vxe-grid 批 S——自定义列面板（搜索/拖拽排序/可见性切换/重置，react only）` — 11 files, +632/−55.

### Files changed

| File                                                              | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/props.ts`                    | `columnOrder?: string[]` + `onColumnOrderChange?: (order: string[] \| undefined) => void` (single lines); `toolbar.customConfig?: IrisTableCustomConfig`; `resetText` option                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `packages/react/src/primitives/table/types.ts`                    | New exported `IrisTableCustomConfig` interface (named, indexed for scanner)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/react/src/primitives/table/Table.tsx`                   | `displayColumns` sorted by `columnOrder` (unknown keys ignored, unnamed keys keep relative order after ordered ones, reference-preserving without the prop); full panel: search (display-only), drag-sort rows (batch-2 `createSortable` press/tryStart/moveOver/end flow, 4px threshold), live visibility toggles, confirm (commits draft via `onColumnOrderChange` + closes), reset (first-open visibility snapshot + `onColumnOrderChange(undefined)` + re-seeds draft), Esc closes without applying; panel replaces the old checkbox menu under the same `data-iris-table-column-settings` container (vxe-parity test still green) |
| `packages/react/src/primitives/table/index.ts`                    | Exports `IrisTableCustomConfig`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `packages/core/src/i18n.ts`                                       | `table.customConfig.search` / `table.customConfig.reset`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `packages/plugin-locale-zh/src/core/index.ts`                     | 搜索列 / 重置                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/react/src/primitives/table/test/custom-config.test.tsx` | **11 tests, 302 lines** (≤500): order sorting + unknown keys + parent updates; panel open/close; search filter; visibility toggles; drag→confirm fires `onColumnOrderChange`; confirm w/o drag; reset (visibility + order + draft re-seed); Esc no-apply; `resetText` override                                                                                                                                                                                                                                                                                                                                                         |
| `packages/manifest/{manifest.json,llms.txt}`                      | Regenerated + committed (155 components × 4; new props with full descriptions)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `docs/vxe-grid/DECISIONS.md` + `batch-s-baseline.md`              | Baseline-stage evidence (written by the runner before this session)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### Verification (all pass)

- `pnpm --filter @iris-ui-kit/react typecheck` ✓ (also core + plugin-locale-zh)
- `pnpm --filter @iris-ui-kit/react test` ✓ **1750/1750** (11 new custom-config, 15 vxe-parity)
- `pnpm --filter @iris-ui-kit/react lint` ✓ **0 errors** (1 pre-existing complexity warning on `IrisTable`)
- `iris-ui-spec.py --mode all --json` ✓ **0 violations**
- `pnpm gen:manifest` ✓ + committed; pre-commit gates (prettier, filesize ≤500, change-budget) ✓ (budget warning non-blocking, multi-framework fan-out)

### Cleanup from the timed-out prior attempt

- Removed 5 debug `console.log` lines from the drag handlers; deleted junk `scratch-trace.test.tsx`.
- `props.ts` was 525 lines (filesize gate ≤500) → moved the new interface to `types.ts` (spec's own file map names props.ts + types.ts) and tightened JSDoc → 491 lines.
- Manifest scanner drops JSDoc content on the `/**`/`*/` lines — converted 4 prop comments to single-line style so descriptions survive; verified in the regenerated manifest.

### Unfinished

- None in scope. Documented simplifications (per baseline): search is display-only, drag edits a draft committed on confirm (not per-drop), reset has no per-column `defaultVisible` (visibility snapshot only), grouped tables order top-level columns only.

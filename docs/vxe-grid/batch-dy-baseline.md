Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-dy-baseline.md` (184 lines). No source files modified — `git status` shows only the new doc (existing dirt untouched).

**Verified** (HEAD `39638487` 批 DX, manifest 202 props / 33 events, react 251 files / 2890 tests):

- `columnVisibility`/`onColumnVisibilityChange` at `props/layout.ts`, filtered in `useTableColumns.ts:162` (`displayColumns`), zero-render invariant pinned by `vxe-parity.test.tsx:283` (hidden column → 0 cells).
- Key mechanical fact: `gridTemplateColumns` (Table.tsx:4667) only interpolates when track count is stable — so hidden columns must be kept mounted during the fade, then committed away.
- Existing infra: `usePrefersReducedMotion` hook (motion/index.ts), `TABLE_ROW_CSS` injection (table-css.ts → Table.tsx:630, id `iris-table-row-styles`), batch-CL reduced-motion + `--iris-duration-md` token precedent, `resolvedColumnWidth`/`DEFAULT_PINNED_WIDTH` width-chain, all `leafColumns` consumer maps (body :6531, headers :8779/:8979, summary :7081→summary-row.tsx:92, totals :8350).

**Design** (in the doc): `columnFade?: boolean` (props/layout.ts, after `onColumnVisibilityChange`, 202→203 props, 33 events unchanged; zero i18n/core/type changes). A transient two-phase per-column machine — layout-effect diff on `columnVisibility` (mount-hidden never animates), `effectiveColumnVisibility` overlay keeps fade-out columns in `displayColumns` (no useTableColumns signature change), double-rAF phase flip, single 200ms commit timer (`FADE_DURATION_MS` aligned to the token fallback). Load-bearing surface for tests: root `data-iris-column-fade-active`, per-cell `data-iris-column-fade="in|out"` + inline `opacity: 0`, track `Wpx↔0px`, 3 new stylesheet rules with the fade-in-flight-scoped grid-template transition (resize never lags) + CSS reduced-motion freeze gate on top of the JS skip. 10 explicit fiats (reversal, non-numeric tracks graceful degrade, responsive tail-change window, pinned-offset chain, spanMethod limitation, etc.) documented.

**File map**: 4 source edits + 1 new test + manifest + comparison doc. **Test plan**: 18 cases over 属性/样式 axes — attr-phase walk for hide/show/multi/reversal, style assertions (0px track, template restore by string identity, opacity key absence when not fading, token-driven durations, transition-inert guard), reduced-motion & fail-closed parity, and settle invariants. Fake-timer recipe (16ms×2 rAF → 200ms commit) matches vitest conventions.

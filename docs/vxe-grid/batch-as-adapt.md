Done. All work committed as `8056e75e`.

## Report

**Commit:** `8056e75e` — `feat(table): grid 批 AS——自动刷新 autoRefresh + 新鲜度 freshness（iris 独有）`

### Files changed (10: 6 source + 2 tests, 1 doc, 2 generated — manifest counts within the feat commit)

| File                                            | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/core/src/date.ts`                     | `formatClock(date)` — local-time 24h `HH:MM:SS` (padStart, `formatLocalISO` mold, deliberately not Intl — hour12 variance)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/core/src/date.test.ts`                | 2 tests: midnight `00:00:00` / `23:59:59`; single-digit padding `09:05:30` / `13:07:03`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `packages/core/src/i18n.ts`                     | en `table.freshness: 'Updated at {time}'`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/core/src/index.ts`                    | barrel export `formatClock` (required — react typechecks against core dist)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `packages/plugin-locale-zh/src/core/index.ts`   | zh `更新于 {time}`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `packages/react/src/primitives/table/props.ts`  | `autoRefresh?: { intervalMs: number }` + `freshness?: boolean` — **both single-line declarations** (manifest-scanner-safe), multi-line JSDoc (loading-flash documented, proxy-only, ≤0 fail-closed, scalar-keyed restart)                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `packages/react/src/primitives/table/Table.tsx` | import `formatClock`; destructure both props; **auto-refresh effect** after the proxy lifecycle effect — keyed `[hasProxy, intervalMs]`, tick = `proxyRef.current?.refetch()` (the built-in ↻ path), `intervalMs ≤ 0` fail-closed, cleanup on unmount/change; **freshness state+effect** keyed `[freshness, liveData]` (after the sync effect — every setLiveData funnel: initial arrival, refetch, edit commit, commitRowList ops, undo/redo); toolbar gate admits `freshness`; `data-iris-freshness` stamp after the title (`t('table.freshness', { time: formatClock(new Date(freshnessAt)) })`, hidden when `liveData.length === 0`, `--iris-font-size-xs`/`--iris-muted` tokens only) |
| **NEW** `auto-refresh-freshness.test.tsx`       | **12 tests, 306 lines (≤500)** — see below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `docs/vxe-grid-comparison.md`                   | iris 独有 section + 2 rows (`autoRefresh` / `freshness`), 构建状态 + 批 AS, batch table + 批 AS row, test counts updated                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/manifest/{manifest.json,llms.txt}`    | regenerated via `gen:manifest` — `autoRefresh`/`freshness` extracted native, propCount 135→137                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

### Tests added (12)

**autoRefresh (7)**: interval fires refetch (query re-called each tick, fake timers) · unmount clears the timer (no queries after teardown) · no prop → no timer (autoLoad only) · `intervalMs` change restarts the timer (old 1000ms timer torn down, new 2000ms not due at +1000) · `intervalMs ≤ 0` fail-closed · **loading-flash locked in** (deferred refetch → `data-iris-table-row="loading"` visible until resolve — the standard refetch path, no silent option) · non-proxy `data` table inert.

**freshness (5)**: initial data stamps `Updated at 09:05:30` (setSystemTime + formatClock) · an edit commit re-stamps (`09:05:30` → `09:06:00`) · proxy first arrival stamps (hidden until rows exist) · hidden while no rows · no prop → no element.

### Gates

- core test ✅ **1366 passed** (87 files, +2 formatClock)
- react typecheck ✅ clean
- react test ✅ **2005 passed** (177 files, +12, zero regressions)
- react lint ✅ **0 errors** (1 pre-existing `IrisTable` complexity warning, advisory)
- `iris-ui-spec.py --mode all --json` ✅ **0 violations**
- `gen:manifest` ✅ regenerated + committed; `check:manifest` ✅ up to date; `check:docs-reference` ✅ (components.md regenerated — gitignored/generated); prettier clean on all touched files

### What is left / notes

- **File budget**: 8 files in the feat commit (target ≤5) — the fan-out matches the baseline's own file map (core date.ts + core i18n.ts + zh + core barrel + props.ts + Table.tsx + 1 new test + manifest), same class as batch AR/AQ noted as advisory; no dist/tgz/node_modules edits.
- **vue/solid/svelte wiring** for `autoRefresh`/`freshness` — core material ready (`formatClock`), one prop + bridge per adapter, same as prior iris-only batches (explicitly out of scope: "react only").
- **Loading behavior documented, not suppressed**: the core source has no silent-refresh option; each tick flips `loading` true for the request duration (locked in by a test).
- **Interval semantics**: keyed on the scalar `intervalMs` (inline object identity never resets the timer); `intervalMs ≤ 0` fail-closed; non-proxy tables inert (nothing to refetch).
- Full-repo gate (`turbo run test typecheck lint build`, browser E2E, size) per the workflow's review/gate stages.

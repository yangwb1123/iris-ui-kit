Baseline written to `/home/u1/iris-ui/docs/vxe-grid/batch-ax-baseline.md` (287 lines). `git status` confirms only the new doc — zero source files touched.

**Key findings (every claim verified against current code):**

- **The merge site is exactly one line.** The body cell style object (Table.tsx:4945–4970) spreads `cellStyle?.(row, col, idx)` LAST (line 4968). Conditional styles slot in as one new spread immediately after it → rules win. It inherits the same documented override latitude `cellStyle` already has (including structural props like pinned `position` — not a new risk).
- **The value is free.** `const raw = getCellValue(row, col)` is already computed per cell — the batch-AO choke point resolving `dataIndex ?? key` + formula columns. Rules' `value` argument is this exact `raw`: formula-aware, zero extra reads.
- **Editing is unaffected by construction.** `EditorSurface` renders _inside_ the same container div with its own inline background/border; the container keeps its styles during edit (only padding switches to `4px 8px`). Rule highlights show as cell background under the overlay.
- **The cell render has an eslint complexity budget** — every batch extracts per-cell logic into module-scope helpers (`dirtyCellState`, `fnrCellStyle`). The rule merge must do the same (`conditionalCellStyle` helper), which I locked as F11.
- **Manifest is a hard gate.** IrisTable is at 140 props (batch AW appended `nlSummary`); `conditionalStyles` bumps propCount to 141 — `pnpm gen:manifest` / `check:manifest` must run or CI fails. Docs are manifest-driven (`gen:docs-reference`), so the prop JSDoc flows into the generated reference + `llms.txt` automatically.
- **Core conventions mirror value-distribution.ts exactly**: pure `{ row, columnKey, value }` evaluator, adapter indirection at the bridge, co-located vitest tests with the `batch AX, iris 独有` stamp, barrel export.

**Design locked:** (a) core `matchConditionalStyles<Row>` in new `conditional-styles.ts` (+ 5 tests: column filter / multiple / none / column-less / input transparency); (b) single-line `conditionalStyles` prop after `cellStyle` in props.ts, rule type `IrisTableConditionalStyle<Row>` in types.ts, one spread at the merge site, inline per-cell evaluation with documented cost = visibleCells × rules (no memo — virtual scroll bounds it; callers memoize the rules array); (c) JSDoc + new guide page `apps/docs/guide/table-conditional-formatting.md` with a token-safe example. 12 numbered fiats (F1–F12) included for gate arbitration; no i18n strings this batch (no en/zh parity gate).

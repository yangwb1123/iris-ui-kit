Done. Batch AO complete — two commits (`16377f3d` feature, `58b259d7` adapt report), working tree clean.

## Report

**Files changed (8 source/test + 4 docs + 2 generated):**

| File                                         | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/core/src/formula.ts`               | **NEW (321 lines)** — `evaluateFormula` regex-scan + recursive-descent parser (no `eval`/`Function`): field refs, `+ - * / %`, parens/comma, whitelist SUM/AVG/MIN/MAX/COUNT (case-insensitive), optional `=`; unknown field / div-mod-0 / syntax / non-finite → `null`, never throws; 512-char/32-depth bounds. Plus `columnLetter` (bijective A→Z→AA) and `memoizedFormulaValue` (per-(row,formula) WeakMap memo, documented immutable-row contract) |
| `packages/core/src/formula.test.ts`          | **NEW (228 lines, 21 tests)**                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/react/.../Table.formula.test.tsx`  | **NEW (230 lines, 14 tests)**                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/core/src/index.ts`                 | barrel export                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `packages/react/.../types.ts`                | `IrisTableColumn.formula?: string` (single line)                                                                                                                                                                                                                                                                                                                                                                                                       |
| `packages/react/.../props.ts`                | `showCellRefs?: boolean` (single line)                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `packages/react/.../Table.tsx`               | `getCellValue` formula routing (render/filter/group/summary/range-stats/clipboard/distribution all inherit), explicit sort routing in `querySortedData`, CSV shadow-row export, `isEditableColumn` guards at all 10 edit entry points, `seq                                                                                                                                                                                                            |     | showCellRefs` flips all 6 track sites + body + both headers, letter badges (`ci`/`colStart-1`), row-ref column |
| `packages/react/.../useTableSort.ts`         | `buildSorter` formula branch (single + multi sort by computed value, honors sortType)                                                                                                                                                                                                                                                                                                                                                                  |
| `docs/vxe-grid-comparison.md`                | iris 独有 table +2 rows, usage example, 构建状态 row                                                                                                                                                                                                                                                                                                                                                                                                   |
| `packages/manifest/{manifest.json,llms.txt}` | regenerated (IrisTable 131→132 props)                                                                                                                                                                                                                                                                                                                                                                                                                  |

**Counts:** core **1354 passed** (+21) · react **1952 passed** (+14, 173 files) · typecheck **clean** · lint **0 errors** (1 pre-existing IrisTable complexity warning 211→215 — same tolerated pattern as AN's 209→211; I extracted `isEditableColumn`/`editableLive` to keep the body-cell arrow under the complexity budget) · spec script **0 violations** (1410 files) · prettier clean · `check:manifest` + `check:docs-reference` up to date · core framework-free grep **0**.

**Constraint notes:** no eval/Function anywhere; tests ≤500 lines (228/230); manifest scanner hygiene kept (single-line `formula?: string` / `showCellRefs?: boolean` — `formula` lands on `IrisTableColumn` like the existing `preset`, outside the manifest's `Iris<Name>Props` schema). Change-footprint: 13 paths total (11 by the budget hook, non-blocking warning — same magnitude as batch AN's 13); the "≤4" reading I took as keep-it-tight given the baseline's own "3 new files, 6 edits" plan.

**What's left:** vue/solid/svelte wiring for `formula`/`showCellRefs` (core material is framework-free and ready — one prop + a thin bridge per adapter); full-repo turbo gate in the review/gate stage; first npm release remains maintainer-authorized.

Batch BB complete and committed (`ac8d9acc`, working tree clean).

## Report

**Files changed** (4 mandated edits + 1 new test + 1 docs + 2 generated + 2 runner bookkeeping):

| File                                                                        | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/react/src/primitives/table/props.ts`                              | `annotationEditing?: boolean` + `onAnnotationsChange?: (next: Record<string, string>) => void` — both **single-line** (manifest-scanner hygiene)                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `packages/react/src/primitives/table/Table.tsx`                             | 3 reserved keys (`__iris-annotate` / `__iris-annotate-edit` / `__iris-annotate-remove`) + panel state/handlers + menu append **after the 摘要 item** (add when no note; edit+remove when one — existence = `annotations[cellId(rowKeyOf(row,idx), col.key)]`, dedupe guard) + onSelect interception (remove deletes directly; add/edit open the panel) + module-level `TableAnnotatePanel` (distribution/summary mold: useFloating+useDismiss+portal+virtual cursor anchor, Esc/outside/scroll close; textarea seeded from `annotations[cellKey]` + 保存 + 删除 only when a note exists; token-only styles) |
| `packages/core/src/i18n.ts` · `packages/plugin-locale-zh/src/core/index.ts` | `table.annotate` / `.edit` / `.remove` / `.save` — en + zh (`添加批注`/`编辑批注`/`删除批注`/`保存`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `packages/react/src/primitives/table/annotate-editing.test.tsx`             | **NEW** — 11 tests, 313 lines (≤500)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `docs/vxe-grid-comparison.md`                                               | iris 独有 table row + 批 BB batch row + 构建状态 + counts + 用法示例 snippet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `manifest.json`/`llms.txt`                                                  | regenerated (`gen:manifest`), propCount 144→146, both props single-line; `check:manifest` + `check:docs-reference` up to date                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Tests added (11):** menu items vary by existing note · order after the 摘要 item · save writes the key end-to-end (badge appears via controlled map) · empty/whitespace text removes the key (textarea seeded) · panel 删除 button (only when a note exists) · menu 删除批注 deletes directly · no `onAnnotationsChange` → items still show but save/remove **inert** (documented) · no prop → no items · Esc/outside close · reserved keys never reach user onSelect · reserved-key dedupe still routes to panel.

**Counts:** typecheck clean · react **2098 passed** (+11) · core 1438 · zh 6 · lint **0 errors** (pre-existing IrisTable complexity warning 248→250) · spec audit **0 violations** · prettier clean.

**What is left:** the pipeline gate stage (full-repo turbo, pack-install, E2E/visual, bench) — out of scope per baseline. Two flagged decisions: (1) `TableAnnotatePanel` is inline in Table.tsx (batch-AL precedent) instead of the baseline's proposed new file, to honor the ≤4-changed-files constraint; (2) the baseline's fail-closed fiat (items gated on `annotationEditing && onAnnotationsChange`) was overridden by the task spec — items gate on `annotationEditing` alone, with inert save/remove documented + tested.

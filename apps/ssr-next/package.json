{
  "name": "ssr-next",
  "version": "0.0.0",
  "private": true,
  "description": "Next.js App Router smoke app — the canonical SSR/RSC-compat proof for @iris-ui/react (ROADMAP v3 R14)",
  "scripts": {
    "dev": "next dev -p 5180",
    "build": "next build",
    "start": "next start -p 5180",
    "test": "echo \"(ssr-next) build IS the test — next build server-renders every route\" && exit 0",
    "typecheck": "tsc --noEmit",
    "lint": "eslint app",
    "clean": "rm -rf .next .turbo"
  },
  "dependencies": {
    "@iris-ui/core": "workspace:*",
    "@iris-ui/react": "workspace:*",
    "@iris-ui/theme": "workspace:*",
    "@iris-ui/tokens": "workspace:*",
    "next": "14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.16.10",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.6.3"
  }
}

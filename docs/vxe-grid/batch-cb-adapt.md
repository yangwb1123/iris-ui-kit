All green on the committed state. Batch CB is complete.

## Report

**Commits** (2, following CA/BZ convention):

- `feat(table): grid 批 CB——最近筛选（iris 独有）` (10 files)
- `docs(table): batch CB adapt report`

**Files changed**

| File                                                          | Change                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/core/src/recent-filters.ts`                         | **NEW** — framework-free `createRecentFilters` (createAuditLog 1:1 模具): ring of 10, `record(key, values)` pushes `{ key, values, ts }` newest-first + `ensureBound` trims oldest; same key + same values-SET de-dupes to top (MRU, ts re-bumped); `list/clear/subscribe/getVersion/depth` full API; deep-copy list; `limit: 0` disables bound         |
| `packages/core/src/recent-filters.test.ts`                    | **NEW** — 8 tests, 87 lines                                                                                                                                                                                                                                                                                                                             |
| `packages/core/src/index.ts`                                  | barrel export `createRecentFilters` + `RecentFilterEntry`/`RecentFilters`/`RecentFiltersOptions` types                                                                                                                                                                                                                                                  |
| `packages/core/src/i18n.ts`                                   | +1 key `table.recentFilters` = `Recent filters` (after `table.filter`)                                                                                                                                                                                                                                                                                  |
| `packages/plugin-locale-zh/src/core/index.ts`                 | +1 key `table.recentFilters` = `最近筛选`                                                                                                                                                                                                                                                                                                               |
| `packages/react/src/primitives/table/props.ts`                | `recentFilters?: boolean` after `autoLink` (scanner JSDoc convention, single-line prop)                                                                                                                                                                                                                                                                 |
| `packages/react/src/primitives/table/Table.tsx`               | 5 touch points: core import · destructure `recentFilters = false` · ref-once `createRecentFilters` controller + `recentEnabledRef` gate (auditRef mold) · record point in `applyFilterValues` (`values.length > 0` only, controlled-irrelevant) · `applyRecentFilter` (apply + close) + panel props `recent`/`onApplyRecent`/`columns`                  |
| `packages/react/src/primitives/table/FilterPanel.tsx`         | genericized `TableFilterPanel<Row>` + 3 props (`recent` snapshot / `onApplyRecent` / `columns` label resolution); recent section above options — muted title `data-iris-filter-recent-title` + entry buttons `data-iris-filter-recent={i}`; label = column title + option labels (unknown column → raw values join, fail-inert); `--iris-*` tokens only |
| `packages/react/src/primitives/table/recent-filters.test.tsx` | **NEW** — 7 tests, 278 lines                                                                                                                                                                                                                                                                                                                            |
| `packages/manifest/{manifest.json,llms.txt}`                  | regenerated via `gen:manifest`                                                                                                                                                                                                                                                                                                                          |
| `docs/vxe-grid-comparison.md`                                 | persistState row + 构建状态 line (comparison doc 2 处 per baseline)                                                                                                                                                                                                                                                                                     |

Zero changes: `types.ts` / `styles.ts` / core `package.json` (no new subpath — tsup entry list unchanged, barrel-only export) / vue / solid / svelte. Strictly additive (default off → panel byte-identical, no recording).

**Tests added**: 15 — core 8 (newest-first / MRU de-dupe same set / distinct same-key sets / ring bound 10 / deep-copy list / clear+notify / subscribe+unsubscribe / getVersion+depth) · react 7 (empty-set never recorded / section above options + title / cross-column apply + close / label resolution + raw fallback / re-confirm MRU bump / no-prop lazy / records without handler).

**Counts**: core 1525→**1533** · react 2392→**2399** · manifest propCount 166→**167**, eventCount 31 unchanged · spec `0 violations` (1415 files) · check:manifest + check:docs-reference + check:framework-parity up to date.

**Verification** (all ✅): core test 1533/1533 · react typecheck clean · react test 2399/2399 (212 files) · react lint 0 errors (1 pre-existing complexity warning 282, CA precedent 280) · core lint 0 errors (7 pre-existing complexity warnings) · `iris-ui-spec.py --mode all` 0 violations · `gen:manifest` regenerated + committed · react build clean · prettier clean · `check:manifest` / `check:docs-reference` / `check:framework-parity` green.

**What is left**: runner's review/gate stage; working-tree `DECISIONS.md`/`batch-bz-gate.md`/`batch-ca-adapt.md` are prior-stage leftovers (untouched, per BZ precedent); open questions from baseline — O1 清空入口 (recent entries have no explicit clear affordance; ring is bounded and MRU self-prunes, documented), O2 persistState 第 10 块候选 (recent filters intentionally NOT persisted — session-local UX, deferred); vue/solid/svelte alignment deferred (react-only scope).

Review complete. **Verdict: FAIL** — written to `docs/vxe-grid/batch-d-review.md`.

## Summary

**All functional checks (1–9, 11) verified working** — every command re-run:

- Core **1230/1230** · React **1535/1535** (+7 form-config) · typecheck clean · lint 0 errors (pre-existing complexity warning 92→112→113) · audit **0 violations/300 files** · zh-locale 6/6 · core framework-free grep clean
- Extra adversarial scratch test (deleted after): formConfig + proxyConfig(remoteFilter, pageSize 2) + editConfig together — real page-2 → submit → query `{page:1, filters:{status:'active', name:'Al'}}`, edits survive. No crash, no lost edits.
- Check 10 (props.ts hygiene): batch D's additions are single-line; `IrisTableProps` extraction output byte-identical HEAD vs working tree — **zero new scanner noise**.

**Findings** (numbered, severity, file:line, fix — all in the verdict):

| #   | Sev                | Issue                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | HIGH, pre-existing | `manifest` framework-contracts test FAILS: 4 multi-line function params in `props.ts:101-124` (cellClassName/cellStyle/headerCellStyle/footerCellStyle) → phantom required props `row/column/rowIndex`. Reproduced identically at HEAD, 42d52293, cb4d1e95 — batch C re-broke what `0da543da` had fixed; `81bd6446`'s "单行化" fix was incomplete. Blocks the merge gate. Fix: single-line the 4 hooks or teach `parsePropsBody` to skip `(`-continuations. |
| F2  | LOW                | Proxy reset double-queries (`Table.tsx:443-447`): `setParams` fires, then unconditional `refetch()` fires a second identical request. Fix: `setParams` returns boolean; refetch only on no-op.                                                                                                                                                                                                                                                              |
| F3  | LOW                | Reset re-queries the server but never notifies the parent (no `onSearch`/`onReset`), unlike submit. Fix: add `onReset` to `IrisTableFormConfig`.                                                                                                                                                                                                                                                                                                            |
| F4  | LOW/MED            | `remoteFilter` sync effect (`Table.tsx:396-399`) overwrites controller filters with the bare prop on the next prop change, silently dropping form values while the draft still shows them. Fix: merge `formApplied` into that effect.                                                                                                                                                                                                                       |
| F5  | test gap           | Test 7's title claims formConfig+proxyConfig+edit coexist but never renders `formConfig`.                                                                                                                                                                                                                                                                                                                                                                   |
| F6  | info               | `manifest.json`/`llms.txt` not regenerated (batch C committed its regen; batch D defers to pipeline).                                                                                                                                                                                                                                                                                                                                                       |

Working tree left untouched (scratch test removed; only `batch-d-review.md` added).

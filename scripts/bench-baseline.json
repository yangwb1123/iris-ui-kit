{
  "_comment": "Performance baselines. Values are ops/sec (higher is better).",
  "form": {
    "10k sequential field updates (keystroke sim)": 10000,
    "validate 10 fields × 1k iterations": 700,
    "insert + remove 1k rows": 500,
    "10k nested path field updates": 3000,
    "500 swaps (reorder simulation)": 300
  },
  "resilience": {
    "10k unique fetches (no cache hits)": 400,
    "10k repeated fetches (all cache hits)": 1000,
    "10k state checks (no-op while closed)": 5000,
    "10k tryRemove (all succeed with large capacity)": 8000,
    "10k cache hit (resilient path)": 1000,
    "10k enqueue (all succeed immediately)": 2
  },
  "scale": {
    "construct + initial sync load": 100000,
    "setSort (full re-sort + page)": 600,
    "setFilter (full scan + page)": 2500,
    "100 scroll steps (O(log n) windowing each)": 600,
    "1000 measures (O(log n) point updates)": 500,
    "bulk set 100k keys": 150,
    "100k isSelected lookups (Set-backed O(1))": 100,
    "toggleAll → partial toggle → toggleAll again": 400,
    "post 1000 notifications": 400
  }
}

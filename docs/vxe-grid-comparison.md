# vxe-grid 功能清单 vs iris-ui 现状与构建路线

> 来源：vxe-table@4.20.10 / vxe-pc-ui@4.16.27 类型定义与文档（2026-08 提取）
> 目标：用 iris-ui 逐步构建同等功能的可编辑 Table（四框架），并实现组合接口。

## 最终覆盖总结（2026-08-11，react）

**vxe-grid 功能面全部移植完成（react，批 W 收官）**：`tableProps` ~90 项 + 表格方法（handle）+ 事件 + grid 特有 `layouts`/`zoomConfig`/`visibleMethod` 全部对齐（react）。`customConfig`（列自定义面板，批 S）：工具栏 `columnSettings` 按钮打开完整面板（搜索 + 拖拽排序 + 可见性切换 + 重置），确认后经 `columnOrder`/`onColumnOrderChange` 受控提交，分组表仅作用于顶层列。`zoomConfig`（批 U）：工具栏 ⛶/✕ 切换 + fixed 覆盖层 + Esc 退出，缩放时工具栏提升至覆盖层之上（✕ 退出可达）；`layouts`：form/toolbar/pager 各节 `hidden` 抑制；`visibleMethod`：列可见谓词（与 columnVisibility AND，谓词 veto 优先）。`loadData`/`reloadData`/`commitProxy`/`getProxyInfo`（批 V）：代理方法对齐（loadData 走 liveData 写回通道，代理 state 总数在下次 query 前不变——core remote source 无 setData，文档化）；`onEditStart`/`onEditClosed`（批 V，cell 模式）+ `onScroll`（批 V）+ `onSelectAllChange`（批 V 增量——vxe 无此 emit）。`getFilteredData`（批 W）：当前筛选+排序视图快照副本；`exportCurrentViewCsv`（批 W）：导出当前视图（筛选/隐藏列/分组叶列均正确，ref 镜像最新态）；`showHeaderOverflow`/`showFooterOverflow`（批 W）：表头/表尾溢出省略号开关（默认保持，false → wrap + visible，body 不受影响）。

**vue 对齐（批 X）**：`proxyConfig`（服务端数据源：remoteSort/remoteFilter/分页/autoLoad/编辑回写）、`formConfig`（搜索表单：草稿/应用双态、代理合并 filters、本地过滤）、`multiSort`（多列排序：点击追加/原地升→降/移除 + 代理 `sorts`）、工具栏扩展（代理生命周期/状态/pager/写回）四块与 react **同名同语义**，共用 core 同一控制器；SSR 安全（首查 `onMounted` 触发，`renderToString` 不发请求）、受控 `sort`/`multiSortState` 变更推送代理、代理模式本地过滤仅 `filters` prop（React 对齐）。

**vue 对齐（批 Y）**：`columnVisibility`（+ `update:columnVisibility` emit）、`filters` 文本筛选（substring 大小写不敏感忽略空值，与 formConfig AND，远程代理合并 filters）、`seq`/`seqStartIndex` 序号列、`spanMethod` 合并单元格（resolveSpan + 占用集，被覆盖格跳过）、`columnDrag` 列拖拽（core createSortable 桥，分组表头不支持——文档化）、`rowDrag` 行拖拽（代理 liveData / 本地 override 重排 + `onDataChange`/`onReorder`）、`expose` 代理方法（`loadData`/`reloadData`/`commitProxy`/`getProxyInfo`）。

**vue 对齐（批 Z）**：`editConfig.mode='row'` 行编辑模式（会话 `Map<cellId,{draft,error}>` + 共享提交核心，cell 模式字节一致；点击整行开编辑、逐列 Enter/blur 提交、Escape 整行取消、行切换先提交、同步校验失败阻断切换、异步校验后取消丢弃提交）、`contextMenu` 右键菜单（虚拟光标锚点 + Teleport body，Esc/外部/滚动关闭，禁用项 inert，表头/序号/选择/汇总格排除）、`filterValues` 筛选面板（checkbox 面板 + OR 匹配通道与文本 filters AND，远程代理逗号合并）、树形 `lazyLoad` 懒加载（lazyChildrenOf 优先于 getSubRows，epoch 防陈旧回填）。**均 additive、core 零改动**（review 修复：初始代理 params 的 filters 逗号合并改按 `remoteFilter` 门控并合并文本 filters，React 对齐）。其余 react-only 能力（批 2/5/J–W 剩余）为 **vue/solid 剩余缺口**，**svelte 核心批 AC 已交付、交互批 AD 为下一轮**（见构建状态表下方）。

**solid 对齐（批 AA，核心批）**：`columnVisibility` 列显隐（`displayColumns` 驱动扁平/分组表头、body、summary、gridTemplate 全部渲染路径，引用保持）、`filters` 文本筛选（substring 大小写不敏感忽略空值、`filterMethod` 覆盖、与 formConfig AND、远程代理合并）、`seq`/`seqStartIndex`/`seqMethod` 序号列（含 `proxyConfig.seq` 代理累计序号）、`spanMethod` 合并单元格（每趟占用集，键控 bodyEntries 引用 + 方法身份重建，gridColumnEnd span n + rowspan 覆盖格跳过）、`proxyConfig` 服务端数据源（remoteSort/remoteFilter/分页/autoLoad/编辑回写/累计序号 + `pagerConfig` pageSizes/showTotal + solid `IrisPagination`，presence-keyed 生命周期 + SSR 安全 + 去重防双请求）、`multiSort` 多列排序（点击追加/原地升→降/移除 + 链式比较器 + 序号徽标 + 受控/非受控 + 代理 `sorts`）、`formConfig` 搜索表单（草稿/应用双态、提交→`{filters, page:1}`、重置 noop 重查）、工具栏扩展（refresh 代理重查/export/buttons/batch-with-keys）八块与 react/vue **同名同语义**，共用 core 同一控制器；SSR 安全、**core 零改动、纯 additive**（review 修复：spanMethod 占用集按方法身份键控重建——换函数不换数据时被覆盖格恢复；manifest 提取器对多行 spanMethod 签名的解析 artifact，solid props 改 react 同款内联对象形）。**solid 对齐（批 AB，交互批）**：`rowDrag` 行拖拽（core createSortable 桥：4px 阈值 → rects 一次收集 → closestCenter → 重排，tap 无位移取消 + 释放清理，本地 override + `onDataChange`/`onReorder` 双通道）、`columnDrag` 列拖拽（分组表头仅叶列）、`editConfig.mode='row'` 行编辑模式（整行开编辑、逐列 Enter/blur 提交、Escape 整行取消、行切换先提交、同步校验失败阻断、Tab 逐列提交导航；会话 epoch 守卫——异步校验 pending 中 Escape 取消不写回、双击 Enter 单次提交）、`contextMenu` 右键菜单（虚拟光标锚点 + portal，Esc/外部/滚动关闭，禁用项 inert，表头排除）、`filterValues` 筛选面板（触发不开排序 + checkbox OR 匹配与文本 filters AND + 远程代理逗号合并 + core i18n 键）、代理方法（`loadData`/`reloadData`/`commitProxy`/`getProxyInfo` + `clearSort`/`clearFilter`；loadData 本地 override 在下一次查询结果 ref 变化时整页替换——分页切换无陈旧行）。**均 additive、core 零改动**（review 修复：异步提交 epoch 守卫对齐 core sessionGen、代理 loadData 页切换替换、rowMode 优先于 cellRange 点击、同行双击重开整行；gate 修复：行编辑内联编辑器模板 SSR 水合对齐）。其余 react-only 能力同 vue 剩余缺口（见构建状态表下方）。

**svelte 对齐（批 AC，核心批）**：`columnVisibility` 列显隐（`displayColumns`/`leafColumns` 驱动扁平/分组表头、body、summary、gridTemplate 全部渲染路径，`!== false` 语义与 react 一致，挂载后隐藏/恢复反应式生效）、`filters` 文本筛选（substring 大小写不敏感忽略空值、`filterMethod` 覆盖、与 formConfig AND、远程代理合并，`{name:''}` 不重查）、`seq`/`seqStartIndex` 序号列（含 `proxyConfig.seq` 代理累计序号）、`spanMethod` 合并单元格（`buildSpanPlan` 占用集纯计划——跨虚拟窗口正确，与 react 渲染期 ref 同语义）、`proxyConfig` 服务端数据源（presence-keyed `$effect` 控制器生命周期——内联身份不重建、absent/卸载销毁；remoteSort/remoteFilter/分页/autoLoad/编辑回写 + 互斥 sort/sorts 通道 + pager = svelte `IrisPagination`）、`multiSort` 多列排序（点击追加/原地升→降/移除 + 链式比较器 + 徽标 + aria-sort + 代理 `sorts`，受控父拒绝无乐观翻转）、`formConfig` 搜索表单（草稿/应用双态、签名键控重播种、提交→合并 filters + 页 1、重置 noop 重查）、工具栏扩展（title/refresh/export/buttons/batch-with-keys）八块与 react/vue/solid **同名同语义**，共用 core 同一控制器；**core 零改动、纯 additive、零硬编码 hex**（review：`onColumnVisibilityChange` 事件为契约内延后——react 亦仅设置面板触发；`grouped` 由 `displayColumns` 计算与 solid/vue 一致）。**svelte 交互批 AD 为下一轮**。

**批 AE（虚拟树 + 四框架 example）**：react 虚拟树/详情行修复——`virtualScroll` 门控放开（原 flat+detail / tree / tree+detail × virtual 组合丢详情行），`BodyPlanEntry.kind='detail'` 每展开行一个详情槽（内容超高时槽内滚动，源码+设计文档已注明），展开/收缩经 `expandedKeys → plan → items.length` 全链路重钳制（layout effect 预绘同步 scrollTop，无空白帧，外部折叠深滚动时 DOM+state 双重钳制）；新增 `virtual-tree.test.tsx` 5 测试（flat 不变 / 树窗口化 / 展开增 itemCount 保滚动 / 外部折叠重钳制 / tree+detail 组合）+ 更新 2 处旧 pin。**四框架 example**：vue/solid/svelte CMS 各新增 `VxeGridExamplePage`（5 节镜像 react：basic / 行编辑 / 服务端代理 / 搜索表单 / 行操作，`checkMethod`/`tableRef.insertRow` 为 react-only 已 grep 验证，行操作走本地响应式 state）+ 菜单 `vxe-example`（order 8）+ Shell/PageHost 注册 + menu-leaf 契约测试 allowlist。**均 additive、core 零改动、manifest 无 diff**（155×4）。交接：vue/solid/svelte 的 flat+detail+virtual 旧守卫（vue `TableBody.ts:52` / solid `TableBody.tsx:115` / svelte `IrisTable.svelte` `useVirtual`）留待后续批。

**批 AF（三端 example e2e + 视觉 parity + Excel 样式）**：vue/solid/svelte CMS 各新增 `e2e/vxe-grid.spec.ts`（3 测试镜像 react `pages.spec.ts`：官方 basic-usage 渲染/排序/行编辑、proxyConfig 服务端数据源等出数 + 远程分页 6 页、formConfig 搜索过滤；proxy 断言限定「服务端数据源」区——react 旧 `last()` 解析到第 5 节表的 scoping 缺陷在镜像中修正；svelte 行编辑因适配器无 `editConfig.trigger` 支持改用 dblclick）。**视觉 parity**：四框架 `e2e/visual-parity.spec.ts` 各加 `vxe-{light,dark}` 块（1280×2000 视口单屏容纳 ~1913px 页面，刻意不用 fullPage——sticky header 拼接陷阱；截图前等代理区 400ms 模拟延迟；同阈值 vue 0.05 / 其余 0.02）+ 8 张基线 PNG（四框架 × 双主题，md5 同源）。**Excel 导出**：core `toSpreadsheetXml` 增 `headerStyle?: boolean`（`<Styles><Style ss:ID="Header"><Font ss:Bold="1"/></Style></Styles>` + 表头格 `ss:StyleID="Header"`）与 `columnWidths?: number[]`（`<Column ss:Width>` 点数，5.25pt/char，钳到列数，非有限/非正跳过）——缺省输出字节不变（byte-compat 锁测试）；react `exportExcel` 本就透传 `options`，零适配器改动。**react 表头 track 对齐**（跨框架 parity 修复，batch AF）：`IrisTable` gridTemplateColumns/colTrack/表头补齐 `rowDrag`/`seq` 前置 track（原缺失致 seq 列换行，react-only 与 vue/solid/svelte 不一致）。均 additive（core 改动仅 additive，缺省字节不变）、manifest 无 diff。**gate 实测（批 AF）**：三端 `vxe-grid.spec.ts` 3/3（react/vue/solid/svelte 各 3/3）· 四框架 vxe 视觉 parity 8/8（react/solid/svelte @0.02、vue @0.05）· review P1 基线 PNG 按修复后 app 重生成后全过。交接：vue/solid/svelte 组合护栏同批 AE。

## iris 独有（批 AG，react —— vxe 没有的内置能力）

| 能力                      | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `persistState` 状态持久化 | vxe 无内置持久化（官方做法是手动监听事件 + 本地存储）。iris 把它做成声明式：`persistState={{ storage, key, include }}` 挂载时从存储读回快照（经对应 change 回调回放：sort → onSortChange、multiSortState → onMultiSortChange、filters → onFiltersChange、filterValues → onFilterValuesChange、columnVisibility → onColumnVisibilityChange、columnOrder → onColumnOrderChange、columnWidths → onColumnWidthsChange），每次变更把**当前 props** 序列化写回。表格受控，该能力是纯 LOADS/SAVES 协调器（零新增内部状态）；`pageSize` 是文档化特例——无回调（代理 `onPageChange` 是通知），仅在 `proxyConfig.onPageChange` 存在时恢复（`onPageChange(1, restored)` 在首次 query **之前**应用，避免默认 10 的双请求），无代理则整体跳过。`storage: false` 完全禁用（不读不写）；默认键 `'iris-table-state'`；`include` 默认全部 9 块；SSR 安全（window 守卫）；恢复/保存顺序契约：恢复 effect 在保存订阅之前、逐通道 skip-first ref，挂载提交永不把恢复前的值写回存储（恢复通道保留恢复值，写幂等 + JSON 去重）。**批 BY（iris 独有）**：`expandedKeys` 展开行键纳入一键持久化（`IrisTablePersistPiece` +`'expandedKeys'`、`IrisTablePersistedState` +`expandedKeys?: Array<string | number>`）——收集器仅在 `onExpandedRowsChange`**且**表可展开（renderDetail 或树模式）时保存（「可恢复的才保存」，pageSize 无代理跳过先例）；恢复经`expansion.set(value.map(String))`**全量替换**（快照是完整展开集，merge 只并集不适用），commit 路径触发`onExpandedRowsChange(keys)` —— 展开无受控 prop，是 pageSize 之后第二个无 change 回调的特例；`applyViewSnapshot`同步补块（views 共享收集器的存=恢复对称）；扁平表/无回调双向惰性；陈旧键 fail-inert（行未加载时键保持、行到达即展开，代理惰性语义）。**批 BZ（iris 独有）**：表格状态导出/导入`handle.exportStateJson()`/`importStateJson(json)`——同收集器把全部 9 块（sort/filters/filterValues/columnVisibility/columnOrder/columnWidths/pageSize/expandedKeys/query）序列化为 JSON（multiSortState 刻意不导出——spec 无此项，导入接受超集）；收集器门放宽（`persistState                     |     | views`门移除 + query 收集无条件——usePersistState`hasConfig`/ useTableViews config 门双兜底，persist 保存循环无 query 键字节不变，裸表导出`'{}'`）；导入复用 `applyViewSnapshot`逐项经回调应用（资格门 + 类型守卫 + query 先行 + pageSize`onPageChange(1, size)`+恰一次 request + expandedKeys 全量替换）；非法 JSON/非纯对象 → `false`零应用，有效 JSON 逐块惰性仍`true`；导出↔导入往返字节相等。**批 CA（iris 独有）**：单元格链接自动检测 `autoLink`（vxe 无自动链接——link 列需手配）——`autoLink?: boolean` 表级 prop（`props.ts` `compareWith`后，默认 off，manifest propCount 165→166）：core 新纯函数`detectAutoLink(text)`（`auto-link.ts`，框架无关、零依赖、**永不抛错**）——URL `/^https?:\/\/\S+$/i`（仅 http/https、大小写不敏感）+ email 与 `mask.ts`的`EMAIL_RE`**逐字节同一**，二正则**全文本锚定**（嵌入句子的 URL 不命中、尾部标点算链接一部分，fiat）；react 单一咽喉`renderAutoLinkCell` 模块 helper（`contextCellText` 旁）：显示文本链（掩码→formatter ?? raw）`typeof string`才检测，命中渲染`<a data-iris-auto-link href=text target="_blank" rel="noreferrer" onClick=stopPropagation>`（link 分支 `_blank→noreferrer`先例逐字，点击不冒泡进 onCellClick）；分支插在`col.link` 之后、`col.formatter`之前——**link 列配置并存且优先**，检测失败返回原 text 与 formatter/raw 分支字节同形；无 prop 全路径惰性；core 8 + react 7 新测试（react 2385→2392），manifest 已重新生成（propCount 165→166，eventCount 31 不变），三框架零改动、styles/i18n 零改动。**批 CB（iris 独有）**：最近筛选`recentFilters`（vxe 无「最近筛选」——筛选面板每次打开无状态）——`recentFilters?: boolean` 表级 prop（`props.ts` `autoLink`后，默认 off，manifest propCount 166→167）：core 新控制器`createRecentFilters`（`recent-filters.ts`，`createAuditLog`1:1 模具——环形 10 条 +`ensureBound`裁最旧 +`subscribe`/`getVersion` store 桥对）：`record(key, values)`压入`{ key, values, ts }` 最新优先，**同 key+同 values 集合去重置顶**（MRU，ts 重缀），`list/clear/subscribe/getVersion/depth`全 API；记录点 =`applyFilterValues`（确认咽喉）：`values.length > 0` 才记录（空集=清除语义，`mergeFilterValues` 先例），受控无关（无 handler 也记录）；面板显示 = FilterPanel 泛型化 + 3 props（`recent` 快照/`onApplyRecent`/`columns`标签解析），options 上方新增最近区（muted 标题`data-iris-filter-recent-title`+ 条目按钮`data-iris-filter-recent={i}`），label = 列标题 + 选项 label 拼接（未知列 fail-inert 回落原始值）；点击快速应用 = `onApplyRecent(entry)`→`applyFilterValues(entry.key, entry.values)`（**跨列应用**）+ 关闭 + 自动 MRU 置顶；`filterPanelSeq`remount 播种 → **零 useSyncExternalStore 订阅**；+1 i18n 键（en`Recent filters`/ zh`最近筛选`）；core 8 + react 7 新测试（core 1525→1533 · react 2392→2399），manifest 已重新生成（propCount 166→167，eventCount 31 不变），三框架零改动、types/styles/core package.json 零改动**批 CC（iris 独有）**：编辑自动换行 `editAutoHeight`（vxe 无自动高度——textarea 编辑器固定 rows=3）——`editAutoHeight?: boolean` 表级 prop（`props.ts` `onAutosave`后，默认 off，manifest propCount 167→168）：作用面唯一 = `EditorSurface`textarea 分支（批 I 模具），开时`rows` 3→1（单行起步随内容增长），`onInput`读`scrollHeight`→ 纯函数`autoHeightSize(scrollHeight, lineHeight)`（Table.tsx 模块级导出；行高 `getComputedStyle`每会话测一次缓存、fallback 16px）→ 直写 inline`height/maxHeight/overflowY`（无 setState 无重渲染循环）；上限 `EDITOR_AUTO_MAX_ROWS=6`、`maxHeight=6×lineHeight`，**严格超出才 `overflowY:auto`**（恰好 6 行无滚动条）；双向（删除收缩）+ 挂载测量（预置多行值打开即定型）；row 模式与 cell 模式同一 surface 双生效；Enter/Shift+Enter/Escape/blur/aria/错误消息/datalist 全不动；关时字节不变（`rows=3`回归锁批 I）；纯函数 5 + 集成 5 新测试（react 2399→2409），manifest 已重新生成（propCount 167→168，eventCount 31 不变），三框架零改动、core/types/styles/i18n 零改动。**批 CD（iris 独有）**：行拖拽插入指示线（vxe rowDrag 拖拽时无落点指示——只有幽灵行，用户看不见插入位置）——**零新 prop**（复用既有`rowDrag`选入）：单一纯函数`resolveRowDragDrop`（Table.tsx 模块级，move 与 up 两 handler 共用同一解析）——side = 指针 vs 悬停行中点（dnd-kit 语义）、`insertIndex`落回原数组空间；move 中`updateRowDropIndicator`画线、up 经同一 resolve 提交（**net-zero 落点跳过`onReorder`**）；leave + 新 pointercancel 分支清线与 refs（后者顺带修复既存 stuck `activeId`）；`rowDrag`开时根在`...style`后强制`position: relative`（BU 水印先例）；指示线 `data-iris-row-drag-indicator`+`data-iris-row-drag-side`，1px `var(--iris-primary)`、逻辑属性全宽 inset、pointerEvents none、zIndex 2（盖过固定列 z1）；10 新测试（react 2409→2419），manifest 零 diff（propCount 168、components 155 不变），core/types/styles/i18n 零改动。**批 CE（iris 独有）**：复制选中高亮（vxe clipConfig 复制无任何成功反馈——用户看不见复制是否成功）——**零新 prop**（完全由既有 `clipConfig` 复制路径门控）：`writeClipboardText` `Promise<void>`→`Promise<boolean>`（true iff 三通道任一成功——core `copyText`/`navigator.clipboard.writeText`resolve /`execCommand('copy')`boolean；第 3 调用点 void 前缀，签名放宽调用点兼容）；**两个范围复制消费点**都 success-gated（window Ctrl/Cmd+C handler + 范围工具栏 复制按钮`copyActiveRange`）——`buildRangeCopy(...).then(ok => ok && flashCopyFeedback(range))`（spec 的「复制成功后」门：clipboard reject / 无 execCommand → 不闪）；`copyFlashRange`快照状态（复制时刻归一化矩形，**不追 selection 变化**）+`COPY_FLASH_MS=600` 定时器（重复制重启动、卸载清理）；渲染：`data-iris-copy-flash="true"`+`backgroundColor`**长属性**（BE 纪律——不重置 background-image，locked 条纹共存），fnr/range-fill 之后、locked 之前合并；token-only 色`color-mix(in srgb, var(--iris-primary) 25%, var(--iris-background))`（compare-view 先例，区别于持久选中 tint）；11 新测试（react 2419→2430），manifest 零 diff（propCount 168、components 155 不变），core/types/i18n 零改动。**批 CF（iris 独有）**：空态动作按钮（vxe 空态只有 text 无动作——用户得离开表格另找按钮）：`emptyState`类型加宽`ReactNode | IrisTableEmptyState`（**零新 prop**，propCount 168 不变）——新**具名导出接口** `IrisTableEmptyState`（`text?: ReactNode`+`action?: { label; onClick }`），ReactNode 形字节不变（零 wrapper，既有 `emptyState`行为逐字节）；单一咽喉`isEmptyStateObject`判别器（typeof object && 非数组 && 非 React element && **非`$$typeof` React 内部标记**——review 门禁修复：React portal 带 `REACT_PORTAL_TYPE` 的 `$$typeof`，isValidElement 漏判会把 `emptyState={createPortal(...)}`误当 descriptor 渲染 fallback 文本，排除后 portal 原样渲染 + 回归测试）→`emptyTextOf`/`emptyActionOf`/`renderEmptyState`：空行单行 inline——文本 span（仅后随 action 时 12px `marginInlineEnd`，RTL-safe）+ `<button type="button" data-iris-empty-action>`（`EMPTY_ACTION_STYLE`与错误行 retry 按钮 token 对 token 镜像，全`--iris-*`）；`action`缺省 fail-closed 纯文本、非空表无按钮；react 2430→**2439**（9 新测试），manifest 已重新生成（155 组件、propCount 168 不变），core/types/styles/i18n 零改动。**批 CG（iris 独有）**：单元格字数统计`charCount`（vxe 无任何字数显示——编辑器与选区都看不见字符数）：`charCount?: boolean` 表级 prop（`props.ts` `editAutoHeight`后，默认 off，manifest propCount 168→**169**）。**编辑徽标**住共享 `EditorSurface`（cell 与 row 两模式同一渲染点）：开时编辑器右下角 chip `data-iris-char-count` `data-iris-char-count-edit`显示**实时**`String(draft).length`——经既有 session-store 订阅每次按键刷新，**零新 state**；**选区徽标** = 既有 `rangeStatsData`memo 的**纯归约**（模块 helper`rangeCharCount`：count = Σ列非空计数、sum = Σ数值列合计，无数值列 → null）——渲染在范围右下角格（`isRangeCharCountHost` 判别，`renderRangeCharCountBadge`），文本 `{count} cells`+ 有数值时`· sum {sum}`（i18n `table.charCount`/`table.charCount.range`/`table.charCount.rangeSum`en`{count} chars`/`{count} cells`/`sum {sum}`，zh `{count} 字`/`{count} 个单元格`/`合计 {sum}`，**en+zh 同批落地**）；sum 走与汇总行/选择汇总同款 0–100 `aggregateAccuracy` 舍入门（3.579→3.58）。**fail-closed 双模式**（无 prop 无徽标）；与填充句柄同角共存——句柄宿主格徽标上移（`CHAR_COUNT_HANDLE_SHIFT_STYLE` bottom 10px，让 6px 句柄可用）；chip 样式全 token（`CHAR_COUNT_STYLE`：font-size-xs/`--iris-muted`/`--iris-surface` 回落/`--iris-border`/space-xxs/radius-sm + **`insetInlineEnd`RTL 安全** +`pointer-events: none`不挡打字与点击 + zIndex 4 压过句柄 z3）。core 仅 3 i18n 默认键、react-only 桥、三框架零改动；14 新测试（react 2439→**2454**：6 编辑计数 live 增减/textarea 换行/row 模式逐列/zh`5 字`+ 8 选区计数文本`4 cells`/数值 `sum 85`/混合/Escape/fail-closed/句柄 10px 移位/默认 2px/aggregateAccuracy 舍入），manifest 已重新生成（propCount 168→169）。 |
| 可变高度虚拟化            | vxe `virtualYConfig` 的「增强模式」（可变行高）在 iris 是 `virtualScroll.itemHeight` 的函数形：`itemHeight: (index) => px`。整条管线本就支持（core `computeVirtualRange`/`buildOffsets` + `IrisVirtualScroll` offset 树），本批仅收窄类型。行按**累计偏移**定位（`translateY(cum[i])`，非 `i * h`）；函数收到的是**虚拟 plan 下标**（详情槽/树槽交错后的槽位，非 bodyData 下标——文档化陷阱，也允许应用显式给详情槽定高）；固定数字形不变（闭式窗口 + `i * h` 定位）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 命名视图 `views`          | vxe 无等价物（官方做法是手动监听 + 存储）。iris 把它做成声明式：`views={{ storage, key, label, activeKey }}` 工具栏渲染一个紧凑 select（`data-iris-table-views`）列出已保存快照 + 「＋ 保存」项（展开内联输入 `data-iris-views-save`，Enter 确认 / Escape 关闭）+ 活动视图的删除（×，`data-iris-table-views-delete`）。快照 = 与 `persistState` **同一个收集器**（同样的 8 块 + 逐回调门控）；应用 = 同一套逐块回调（`pageSize` 特例：视图应用复现挂载恢复序列 `onPageChange(1, size)` + 恰好一次 request）。挂载加载与 `persistState` 同守卫（window/JSON 损坏/`storage: false`）；同名 upsert；`activeKey` 受控-only（`views.activeKey` + `onActiveViewChange`，不持久化）；存储键 `'iris-table-views'`。**批 AJ：**快照收集器在 `views` 在场时另捕获受控 `query` 字符串（`IrisTablePersistedState` 增 `query?: string`；`persistState` 保存循环只遍历 `IrisTablePersistPiece`，故其路径字节不变）；应用时 query **先于**其他块经 `onQueryChange` 恢复（typeof-string 类型守卫，篡改存储不达回调）；遗留无 `query` 的快照照常加载、不触碰当前 query。                                                                                                                    |
| 范围浮动工具栏            | vxe 无等价物。`cellRange` 有活动范围时，一个迷你操作条浮在**首个选中单元格**上方（ContextMenu 同款虚拟锚点 = 该单元格的活 rect、placement top、flip/shift 开、portal；与右键菜单的两处有意分歧：`autoUpdate` 滚动时重定位而非关闭、无关闭按钮——可见性源自范围 store，`clearRange` 即隐藏）。三个动作 1:1 复用既有路径：复制 → 当前范围的 TSV（clipConfig 同款 `buildRangeTsv` + `writeClipboardText`）；导出 CSV → 范围矩形的**无表头** CSV（core `downloadCsv`）；清除 → 范围内每格置 `''`（pasteIntoRange 同款 byKey patch + 一次 `commitRowList`）。Esc / 外部点击（`useDismiss`）清除范围从而隐藏。批 AJ 另加 统计 按钮（见下行）。                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 范围统计 `rangeStats`     | vxe 无等价物（最接近是 Excel 状态栏）。浮动条在 清除 之后新增 统计 按钮（`data-iris-range-stats`）：点开一个小面板（`data-iris-range-stats-panel`）渲染在浮动条容器内、**绝对定位其下方**，按列列出所选矩形（anchor→active 归一化，行/列双向含端点）内 计数/求和/平均/最小/最大。数值语义与 core `aggregate` 完全一致：`count` = 非空单元格、sum/avg/min/max 仅取 `Number` 有限值（非数值忽略）；唯一分歧：无数值子集 → `null`（面板渲染 `—`，而非 `0`/`NaN`），空范围 → `{}`。面板打开态**提升到 Table**（浮动条每次范围变化 remount `key={rangeToolbarSeq}`，提升保证重算时面板保持打开）；统计 memo 依赖 `[activeRange, bodyData, leafColumns]`（bodyData = 已查询/筛选的显示行；列键走 `dataIndex ?? key` 同 `getCellValue` 间接层，core 纯于 `{ key }`）；Esc / 外部点击复用浮动条既有 `useDismiss`（清范围即隐藏 + 复位打开态）。i18n：`table.range.stats` + 面板表头 6 键（en + zh）。                                                                                                                                                                                                                                                                              |
| 自然语言查询 `query`      | vxe 无内置查询语法。iris 把它做成声明式受控输入（`query` + `onQueryChange`，工具栏标题右侧渲染 `data-iris-table-query-input`，仅当 `query !== undefined` 时出现）：core `parseTableQuery` 解析 `field op value` 语法（`= != > >= < <= contains in`、单/双引号值、`in (a, b, c)` 列表、尾部 `sort by field asc                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | desc`、字段名大小写不敏感、未知字段（传 `fields` 时）返回错误字符串而非抛出）。合并语义：`=`/`contains`→ 子串通道（与`filters` 同语义）、`in`→`filterValues`通道（OR-match）、关系算子 → core`matchesRule`的 typed-rules AND 通道；同字段`=`/`in`的 OR 折叠进`inValues`，同字段 `contains`/关系 OR 无法表达 → 解析错误（fail-closed），跨字段 OR 归一为 AND。`sort by` 仅在所有 sort prop（`sort`/`defaultSort`/`multiSort`/`multiSortState`/`defaultMultiSort`）都缺席时播种排序，用户任何排序交互立即接管。解析错误显示 muted `data-iris-query-error` 提示并**保留最后一次有效解析**（ref 模式）继续过滤。代理模式：子串/in 通道逗号合入远端 filter map（首请求即携带，`remoteFilter` 变更经 effect 推送），关系 rules 无文本序列化保持本地-only（文档化）。i18n：`table.queryPlaceholder`（en + zh「自然语言筛选，如 age > 25 and role = Test」）。 |

| 范围粘贴（选区矩形填充） | vxe 的粘贴是流式（锚点起逐格继续）；iris 批 AK 对**多格选区**改为 Excel 式**矩形精确填充**：剪贴板 TSV 从选区左上角起逐格写入，只填选区矩形本身——剪贴板小于选区 → 左上角填充、其余格保持不变；大于选区 → 裁到矩形（超出表格末行/末列同样忽略）。单格选区保持批 O 的流式行为（锚点继续），既有粘贴测试不变。两种路径都走同一个 byKey patch map + **一次 `commitRowList`**，值保持字符串。 |
| `editRules.unique` 唯一性规则 | vxe 无内置唯一性校验（需 validator 手写）。iris 批 AK 在 core `EditRule` 增加 `unique?: boolean` 内建规则：对同列做 **String 比较**（跨类型如 `1` vs `'1'` 视为重复），空值两侧豁免，编辑行自身按**引用身份**跳过（改成原值不误报）；失败消息 = 规则的 `message` 或英文默认 `'Value must be unique'`（与现有 required/pattern 等默认消息同风格）。数据上下文经 `validateEditRules`/`validateEditRulesAsync` 的**可选第 5 参** `context?: EditRuleContext<Row> { rows, columnKey }` 传入（新增**具名导出接口** `EditRuleContext`）；无 context（或无 rows/columnKey）时该规则跳过（no-op 通过），旧调用方字节兼容。react 表在 cell/row 两种编辑模式的校验桥均传入 `{ rows: externalDataRef.current, columnKey: 列 key }`（引用跳过依赖同一批行对象）；vue/solid/svelte 适配器尚未接线——同列唯一性在该三端暂为 no-op（文档化缺口）。 |
| 内置撤销/重做 `undo` | vxe 无内置 undoRedoHistory（官方示例手写快照）。iris 把它做成声明式：`undo` 布尔 prop（默认 off）开启后**每条数据变更**（行操作 insertRow/removeRow(s)/updateRow/loadData、粘贴、find&replace、范围清除、cell/row 编辑、批编辑）都把**变更后**的行列表压入 core `createUndoStack`（`maxHistory: 100` + 初始快照 + 内容相等跳过死步骤）——**两个变更漏斗都接线**（`commitRowList` 与 `commitValue`，后者直写 `setLiveData` 从不经过前者，漏掉它 undo 会静默错过每一次内联编辑；基线措辞为「push 变更前」与 core 的 redo 语义矛盾——redo() 返回的是变更后的快照，故实际 push 变更后，否则二次变更后 Ctrl+Y 无法重放，测试明确覆盖）。Ctrl/Cmd+Z 撤销、Ctrl/Cmd+Y（或 Ctrl/Cmd+Shift+Z）重做：window 监听、仅根容器内目标、跳过 INPUT/TEXTAREA/SELECT/编辑器、**编辑会话打开时不劫持**（编辑器自己的 Ctrl+Z 优先）；重放经 `restoringRef` 守卫走 `commitRowList`（setLiveData + onDataChange）**不重推**。工具栏标题右侧 ↶/↷ 按钮（`data-iris-table-undo`/`data-iris-table-redo`），disabled 由 `canUndo()/canRedo()` 实时驱动（栈非 observable store——每次 push/undo/redo bump tick 重渲染重读）。恢复后消失的选择键被修剪（`selModel.set` 过滤，同 removeRows 模式），保留键保持选中。外部重喂数据只在栈未触碰时重建基线（交互作用域，vxe parity）。i18n：`table.undo`/`table.redo`（en + zh）。 |
| 批量编辑面板 `toolbar.batch.edit` | vxe 无内置批量填值（需手写循环 + 重渲染）。iris 把既有批量按钮变成内置面板：`toolbar.batch.edit: true` 时点批量按钮（multi + 选中 ≥ 1）展开工具栏锚定面板（`data-iris-batch-edit-panel`，右对齐下拉、Esc/外部 pointer-down 关闭、再点按钮切换）：**可编辑列**下拉（`data-iris-batch-edit-column`，只列 `c.editable` —— 与内联编辑同一门控）+ 值输入（`data-iris-batch-edit-value`）+ 应用按钮（`data-iris-batch-edit-apply`，`table.batchEdit.apply`）。应用 = **一次 `commitRowList`**：每个选中行的目标列置为输入值（按 `rowKey` 字段寻址，值保持字符串、editRules 不重验——粘贴 parity），选择不变，恰好一次 `onDataChange`；该变更同样进入 undo 栈（可与 Ctrl+Z 联动）。`edit: false`/缺省时批量按钮行为字节不变（外部 `onClick`）。i18n：`table.batchEdit.column`/`table.batchEdit.apply`（en + zh）。 |
| 编辑建议 `suggest` | vxe 无等价物（官方示例是手写下拉建议）。iris 在 `IrisTableColumn` 加 `suggest?: boolean | Array<string \| number>`：`true` 时编辑该列文本格，原生 `<datalist data-iris-edit-suggest>` 列出当前 bodyData 中该列的**去重 String 值**（null/'' 排除、排序、上限 50）；数组形则**零扫描**原样使用。input 的 `list` 属性接 datalist id（`React.useId()` SSR 稳定）；**仅文本编辑器生效**（number/select/textarea 忽略），仅编辑会话挂载期间存在（EditorSurface 按会话挂载）。选项在 Table 侧对 `[bodyData, leafColumns]` memo（只扫 `suggest === true` 列），EditorSurface 保持无 bodyData。 |
| 值分布面板 `valueDistribution` | vxe 无等价物（最接近是手建筛选选项列表，看不出数据偏斜）。`valueDistribution?: boolean`（依赖 `contextMenu`，默认 off）：右键菜单**在用户项之后追加**内置项（key `__iris_distribution`，i18n `table.distribution`；用户项同 key 去重守卫，内置 key 永不达用户 onSelect——onSelect 接线处拦截改开面板）；点选后在**同一虚拟光标锚点**浮出面板（`data-iris-distribution-panel`，ContextMenu 同款 useFloating + useDismiss + portal，Esc/外部/滚动关闭）：core `valueDistribution(rows, key)` 纯函数（null/undefined/'' 排除、`String` 强制、按计数降序、同计数首次出现序、空 → `[]`）对 `bodyData` 计算，列键走 `dataIndex ?? key` 同 `getCellValue` 间接层；面板列**前 20** + muted「其余 N 个」折行（`table.distribution.others`，en + zh）。值前景色、计数 muted，全 `--iris-*` token。 |
| NL 列摘要 `nlSummary` | vxe 无等价物（最接近是手写聚合 footer——一行看不出这个列**是什么**）。`nlSummary?: boolean`（依赖 `contextMenu`，默认 off）：右键菜单在**值分布项之后**追加内置项（key `__iris-summary`，i18n `table.summary`；同款去重守卫 + onSelect 拦截）；点选后在**同一虚拟光标锚点**浮出面板（`data-iris-summary-panel`，分布面板同款 useFloating + useDismiss + portal，Esc/外部/滚动关闭）：core 新纯函数 `summarizeColumn(values, label)`（`summary.ts`，**框架无关**、零依赖）把一列值压成一句人话——非空值 ≥60%（**含**边界）为数值 → `label：共 N 个值，范围 min–max，平均 avg，M 个缺失`（N=非空数、min/max/avg 只取数值（布尔不算、非数值字符串不入范围）、M=null/undefined/'' 缺失数，min/max/avg 各经**唯一一处** `.toFixed(1)` 舍入）；否则分类 top3（`countDistinctValues` 与分布面板同计数语义：null/'' 排除、`String` 强制、计数降序、同计数首现序）整数 `Math.round` 百分比 + `，其余 R 个` 仅当 R>0；全空 → `label：无数据`。面板=标题头 + muted 摘要文本，全 `--iris-*` token。+1 i18n 键（en `Column summary` / zh `摘要`）。 |
| 列预设 `preset` | vxe 无内置列预设（需手写 formatter/align/editor）。iris 在 `IrisTableColumn` 加 `preset?: 'date' \| 'money' \| 'status' \| 'progress'`，由 core 工厂 `applyColumnPreset`（`column-preset.ts`，**框架无关**：操作最小部分列描述符 `ColumnPresetDescriptor`，结构化类型即插即用——react 的 `IrisTableColumn<Row>` 零 cast；`formatter` 为 `unknown` 是因 strictFunctionTypes 下固定元数函数类型与适配器不同元数 formatter 互不兼容，preset 填入的实为 `(value) => string` 纯函数）**定义字段级合并**：preset 只填列上为 `undefined` 的字段、**用户字段永远赢**（普通 spread 会让 `align: undefined` 杀掉默认）。预设内容：`money` = 2 位小数 + 千分位 `formatMoney`（null/undefined → ''，非数值直通）+ 右对齐 + `editor: 'number'` + 数值字符串 editRules（pattern 校验——draft 到达校验时是原始输入字符串，`{ type: 'number' }` 会拒绝一切提交）；`progress` = 0..1 → `${Math.round(v*100)}%`、否则原值加 `%` + 右对齐；`date` = `String` 直通 + 左对齐（**本地化格式化是调用方职责**，preset 必须确定）；`status` = 大写纯文本 + 居中（无徽标耦合）。表格在**一个 memo** 中解析（递归 `children`，分组表头叶列继承；零 preset 时结果即原列数组引用不变），喂给 `orderedColumns`。 |
| 表格快捷键 `tableShortcuts` | vxe keyboardConfig 无编辑/清除键（官方示例手写监听）。`tableShortcuts?: boolean`（默认 off）：F2 对**焦点格所在列** `beginEdit`（仅 `col.editable`，再守卫）、Delete/Backspace 把焦点格清为 `''`（`setCellValue(rowKey, col.key, '')` 经**一次 `commitRowList`**，undo 漏斗免费覆盖、`onCellEdit` 不触发——粘贴/批编辑 parity）。焦点格状态来自 `keyboardNavigation` 的 roving 焦点（`data-grid-row` + onFocus 只在 keyboardNavigation 下存在）；**无 keyboardNavigation 则快捷键惰性**（文档化）。内联编辑器打开时编辑器自己的键优先（gate `editing === null` 且事件目标必须是 grid 单元格——表头焦点不会误触陈旧焦点格）。事件走 root 既有 onKeyDown 门（toolbar/form/pager 在 root 之外，天然不触发）。 |
| 公式列 `formula` | vxe 无计算列概念（最近似的是 formatter——只能改**显示**，喂不了排序/筛选/汇总/导出）。iris 在 `IrisTableColumn` 加 `formula?: string`：由 core 新引擎 `evaluateFormula`（`formula.ts`，**框架无关**、零依赖、正则扫描 + 递归下降，**绝无 eval/Function**）对每行求值——字段引用 `[A-Za-z_]\w*` + 数字 + `+ - * / %`（`%` 取模，**无一元负号**）+ 括号/逗号 + 白名单函数 SUM/AVG/MIN/MAX/COUNT（大小写不敏感、逗号分隔表达式参数）；可选前导 `=`（两种形式都剥离）；上限 512 字符 / 32 层嵌套。值语义：**未知字段 → 整式 null**（fail-closed，格渲染为空）、已知 null 字段 → 0（Excel 空格当零）、`+` 任一侧为字符串 → 拼接（null → ''）、除/模 0 → null、非有限运算 → null——**永不抛错**。**计算值进入每一条数据管道**：渲染、排序（`useTableSort.buildSorter` + `querySortedData` 比较器两处显式接线）、筛选、分组、汇总、范围统计、tooltip、剪贴板 TSV/CSV、分布面板（全部经 `getCellValue` 单一咽喉，`memoizedFormulaValue` 按 (row, formula) WeakMap 记忆，表格文档化不可变行契约下安全——新行引用即重算）；CSV 导出走**影子行**映射（`toCsv` 直读 `row[dataIndex]`，react 桥把计算值物化到浅拷贝，原行不动）。`editable` 公式列**只读展示**：内联编辑（beginEdit/双击/单击触发/F2/Tab 两移动器）、行模式、批量编辑面板、`data-editable` 属性、光标全部同一守卫（`isEditableColumn`）忽略之。 |
| 行列引用 `showCellRefs` | vxe seqConfig 只有数字、无列字母。`showCellRefs?: boolean`（默认 off）：表头每个**叶列标题后**追加 muted 字母徽标（`data-iris-cell-ref`，Excel 式双射 A→Z→AA，core 纯函数 `columnLetter`；A = 第一个叶列，seq/选择/详情/拖拽列跳过，分组叶列按源码序）——扁平与分组两种表头变体都渲染；另加一个前导**行号列**（1, 2, 3…，`data-iris-row-ref`，复用 seq 列渲染）。`seq` 开启时 seq 列就是行号、**不重复**（所有 6 处轨道 + 正文 + 两表头站点翻转为合并条件 `seq || showCellRefs`）。纯 token 样式（muted/字号/间距），无 i18n。 |
| 拖拽填充 `rangeFill` | vxe 无等价物（最近似的是 Excel 拖拽填充句柄，vxe 无此交互）。`rangeFill?: boolean`（默认 off，依赖 `cellRange`）：范围活动（≥1 格）时，范围**右下角格内**渲染 6px 主色方块句柄（`data-iris-range-fill`，`var(--iris-primary)`、cursor crosshair、absolute right/bottom 小偏移；宿主格 `position: relative` + `zIndex: 2` 盖过固定列 sticky zIndex 1；pointerdown `preventDefault` 掐掉兼容 click——格 onClick 的 startRange/extendRange 永不触发；浮动范围工具栏的 useDismiss 文档级 outside-press 会清范围——window 捕获相监听先于 document 捕获把句柄按压标记为免清（`suppressRangeDismissRef`，每次 pointerdown 自同步））。拖拽**仅向下/向右**：pointermove 经 `document.elementFromPoint` → `closest('[data-iris-cell-row][data-iris-cell-col]')`（只命中叶格；表格外保持上一 drag end）算拖拽终点，目标矩形 = 范围边到 drag end（`max(pointer, range.end)` + 表格边界钳制，向上/向左拖不缩范围 → 无目标格、释放无操作）；目标格 `data-iris-range-fill-target` + surface-selected 背景（源范围格排除）。pointerup：**循环填充** `(r, c) ← 源((r - start.row) % rangeRows + start.row, (c - start.col) % rangeCols + start.col)`（源值经 `getCellValue` 咽喉），公式列跳过、无 rowKey 键行跳过，byKey patch → **一次 `commitRowList`**（undo 漏斗免费覆盖），然后 `extendRange` 把范围扩到 drag end（Excel parity，句柄随新 end 移动）。pointercancel → 只丢目标高亮、零提交。无 i18n（无文案）。 |
| 迷你图表 `chartPreview` | vxe 无等价物（最接近是手写 sparkline——永远看不到表格当前筛选视图）。`chartPreview?: boolean`（默认 off）：工具栏渲染图表触发钮（`data-iris-chart-trigger`，▤，仅 `chartPreview` 在场时门控 `(toolbar || views || query !== undefined || undo || chartPreview)` 开启工具栏）→ 浮动面板（`data-iris-chart-panel`，useFloating 锚定触发钮 + portal + Esc/外部 pointer-down/任意滚动关闭；关闭接线沿用批量编辑面板先例——列选择器的 listbox 被 Popover 门户到 body，选项按压须**选择**而非关面板，window 冒泡监听排除 `[data-iris-select-option]`）。内容：**数值叶列** IrisSelect（两条既有信号——某行 `typeof getCellValue(row, col) === 'number'`（公式列经咽喉）或 `col.summary === 'sum'`，Table 侧解析）+ bar/line 小按钮切换（`table.chart.bar`/`table.chart.line`）。SVG（viewBox 0 0 300 120）**纯结构化 JSX**（零 SVG 字符串、零 dangerouslySetInnerHTML）：core `buildChartData(rows, key)`（新纯材料，`dataDomain` 同款域语义——空/全空 → `{0,1}`、平铺按 `|v|` 两侧填充永不为零跨度、null/undefined/非有限 → 空隙点；适配器独占全部像素映射）对**当前 `filteredData`** 前 20 值（超出折叠「共 N 行」注，复用 `table.total`）；柱 = `var(--iris-primary)` rect（宽度按槽计算、负值以零基线悬挂）、线 = 多段 polyline（null 断线）+ 圆点；值标签刻意省略（迷你图，文档化）。+3 i18n 键（en+zh：table.chart/chart.bar/chart.line）。 |
| 自动刷新 `autoRefresh` | vxe 无等价物（vxe 靠外部轮询 timer 自己接）。`autoRefresh?: { intervalMs: number }`（默认 off，**代理模式 only**——非代理 `data` 表惰性，没有可 refetch 的东西，文档化）：每个 `intervalMs` 毫秒 tick 调用与内置 ↻ 按钮**同一个** `proxyRef.current.refetch()`——**标准 refetch 路径**，core 无 silent 选项（`createRemoteTableSource` 的 options 只有 query/autoLoad/initialParams），`loading` 在请求期间翻 true（文档化行为、不压制，测试锁定该 loading 闪烁）。定时器 keyed 于**标量** `intervalMs` + proxy 在场（内联对象不重置定时器）；`intervalMs ≤ 0` fail-closed（无定时器）；卸载 / 代理移除即清理（生命周期 cleanup 先 null 掉 proxyRef，迟到的 tick 永不命中已销毁源）。无 i18n（无文案）。 |
| 新鲜度 `freshness` | vxe 无等价物（看不到数据到达时间）。`freshness?: boolean`（默认 off，需工具栏渲染——门控 `(toolbar || views || query !== undefined || undo || chartPreview || freshness)` 开启工具栏）：标题右侧渲染 `data-iris-freshness` 时间戳（i18n `table.freshness`：en `Updated at {time}` / zh `更新于 {time}`，core 新纯函数 `formatClock` 本地 24h HH:MM:SS——`formatLocalISO` 同款 padStart 模具、刻意不用 Intl（hour12 漂移））。**每条 liveData 变更都重打 Date.now()**——首达（代理首页 / 挂载 data）、refetch、编辑提交（commitValue 直写）、行操作/粘贴/批编辑/范围清除（commitRowList 漏斗）、撤销/重做（applyUndoSnapshot 漏斗）——单 effect keyed `[freshness, liveData]` 全捕获；effect 挂载即跑，**首次数据到达也盖戳**；`liveData.length === 0`（无数据）时隐藏。+2 i18n 键（en+zh：table.freshness）。 |
| 审计日志 `auditLog` | vxe 无等价物（vxe 无任何审计/变更历史能力）。`auditLog?: boolean`（默认 off，需工具栏渲染——门控 `(toolbar || views || query !== undefined || undo || chartPreview || freshness || auditLog)` 开启工具栏）：core 新控制器 `createAuditLog`（`@iris-ui-kit/core/audit-log` 独立子路径，默认 limit 200 有界环形、monotonic `seq` **清空不重置**（审计完整性）、`list()` 最新优先、`subscribe`/`getVersion` 供 `useSyncExternalStore` 桥）——**每个变更提交记一条**：`commitRowList(next, type?)` 获类型提示（行操作 `insert`/`remove`、粘贴 `paste`、批编辑 `batch`、填充 `fill`、撤销/重做 `undo`/`redo`，默认 `edit` 覆盖 updateRow/查找替换/范围清除/Delete 快捷键）并对其**上一行列表**（`auditRowsRef` 快照）做**轻量 diff**（模块级 `auditDiff`：按下标逐行找首个分歧行——同键行走字段并集记首个变更格；结构变更只记 rowKey；重排读作首分歧下标结构变更，文档化简化）记首个变更行/格；`commitValue`（cell/row 编辑直写绕过 commitRowList）经 `recordAuditRef` 镜像记同一 diff（type `edit`）。工具栏 `data-iris-audit-trigger`（☰）开浮动面板 `data-iris-audit-panel`（useFloating 锚触发钮 + portal，Esc/外部/滚动关闭，图表面板同款接线；`maxHeight` + 滚动容纳整环）：`#seq + formatClock 时间 + 类型 + rowKey + 列 + muted old→new`，行级结构变更渲染部分上下文行；`table.audit.empty` 空态 + `table.audit.clear` 清空钮（`useSyncExternalStore` 订阅，面板自刷新）。handle 增 `getAuditLog()`/`clearAuditLog()`（清空后 seq 继续递增）。+3 i18n 键（en+zh：table.audit/audit.empty/audit.clear）。core additive（audit-log.ts + barrel + i18n + 子路径导出）、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 137→138） |
| 比较视图 `compareWith` | vxe 无等价物（vxe 无内置两数据集对比能力）。`compareWith?: Row[]`（默认 off，纯 additive）：core 新纯函数 `diffRows(before, after, rowKeyField)`（`diff-rows.ts`，**框架无关**、零依赖、无副作用）——按 `rowKeyField` 键控分类三种：`added`（仅 after）、`removed`（仅 before）、`changed`（同在且 ≥1 列分歧）；**逐列 `Object.is` 比较**（值同一性，刻意区别于批 AT 审计 diff 的 `!==` 真值比较——NaN≡NaN、+0≠−0 均为文档化语义），变更列按 **after 行自身键序**（再补 before 独有键）；rowKey 为 null/undefined 的行跳过（无键无 diff 身份）。返回 O(1) 查询 `status` Map（rowKey→kind）与嵌套 `cellChanges` Map（rowKey→列键→old→new），另附 `added`/`removed`/`changed` 键数组——19 核心新测试。**react 桥**：`compareWith` 单行 prop + 单 memo `diffRows(liveData, compareWith, rowKey)`（无 `compareWith` 即 null——全路径惰性；方向 per 基线：before=liveData、after=compareWith）：live 行缺于快照 → `data-iris-row-removed`、同在且分歧 → `data-iris-row-changed`（变更格 `data-iris-cell-changed` + title tooltip `旧值: X → 新值: Y`，i18n `table.compare.tooltip` en `Old: {old} → New: {new}` / zh `旧值: {old} → 新值: {new}`；**compare tooltip 覆盖 tooltipConfig**（文档化——diff 比原值更有行动价值），未变更格仍走 tooltipConfig）；列键解析走 `dataIndex ?? key` 同 `getCellValue` 间接层，公式列是计算显示值——自身不标 chang（文档化简化，引用字段格会标）。仅 compareWith 独有的行（`added`）在核心 diff 中报告但**无渲染槽位**（比较视图渲染 live 数据集，文档化）。行背景纯 token：changed → `--iris-surface-selected`、added → `color-mix(in srgb, var(--iris-success) 12%, var(--iris-background))`、removed → `color-mix(in srgb, var(--iris-danger) 12%, var(--iris-background))`（`background` 全行显色 + `--iris-cell-bg` 喂槽位列，与 hover/selected 同一高亮机制；无 color-mix 引擎回落继承背景，文档化）——`props.ts` + `Table.tsx`（memo + 行三 attr + 单元格 attr/title，helper 抽取保持单元格箭头复杂度预算）+ `styles.ts` + `compare.test.tsx` 13 react 新测试。+1 i18n 键（en+zh）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 138→139）；批 BV 增量：`IrisTableHandle.exportComparisonCsv(): string`（handle 方法，manifest 不可见）导出差异行 CSV——当前视图 removed/changed 按视图序 + 快照独有 added 尾部按快照序，每行前置 `__iris_diff` 标记列（i18n `table.compare.diff` en `Diff`/zh `差异`，值 = 英文字面量），变更格导出 `maskedOld → maskedNew` 合成串（掩码先于合成，exportRaw 双侧裸值，公式列自身不合成且从原始数据物化），无 compareWith/rowKey → `''`、全等快照仅表头 |
| 选择汇总 `selectionSummary` | vxe 无等价物（最接近的是表头勾选计数——只显示在表头且无合计）。`selectionSummary?: boolean`（默认 off）：`selectable === 'multi'` 且选中 ≥ 1 时，工具栏在**批量按钮之前**渲染 `data-iris-selection-summary`：`已选 N 行`（i18n `table.selectionSummary`，en `{count} selected` / zh `已选 {count} 行`，与表头 `table.selectedCount` 同款 `{count}` 插值）+ 每个 `summary === 'sum'` 叶列一个 `· 合计 X`（`table.selectionSummarySum`，en `sum` / zh `合计`）——**与汇总行同源材料**（core `data-view/aggregate.ts`，零新 core API）：`aggregate(selectedRows, r => getCellValue(r, col), 'sum')` + 同一 `aggregateAccuracy` 舍入点 + `String(value)` 格式化（`renderSummary` 不参与——工具栏是纯文本）；非 sum op（avg/min/max/count）不渲染。选中行按 **bodyData 序**（筛选+排序+树展平后的显示行）经 `rowKeyOf` 映射，跨页代理键不在当前页则跳过（视图绑定，与汇总行同语义）。清除按钮 `data-iris-selection-clear`（✕，`table.clearSelection` en/zh）走共享 `clearSelection` 路径（`rebaseToProp(); selModel.clear()`——handle 同款）。i18n：`table.selectionSummary`/`table.selectionSummarySum`/`table.clearSelection`（en + zh）。 |
| 选中行导出 `exportSelectionCsv` | vxe 无内置选中导出（需手动组合 getCheckboxRecords + 导出）。iris 在 `IrisTableHandle` 加 `exportSelectionCsv(): string`：选中行按 **bodyData 序** → `exportCsv(withComputedFormulaCells(rows, displayColumns), displayColumns)`——与 `exportCurrentViewCsv` **字节同形**：公式列影子行物化、隐藏列（columnVisibility/visibleMethod）排除、公式注入中和（OWASP）；空选中 → `''`（调用方用 `getSelection()` 判别）。挂载时 handle 闭包问题沿用 F1 回归模式：新增 `bodyDataRef` 镜像（同 `filteredDataRef`/`viewColumnsRef`），rerender 后仍读最新行。 |
| 单元格标注 `annotations` / `cellNote` | vxe 无等价物（vxe 的 tooltip 只能显示值本身，无单元格批注概念）。iris 批 AZ 把它做成声明式：**静态表** `annotations?: Record<string, string>` 按 `` `${rowKey}::${columnKey}` `` 键控（与内部 `cellId` 同款 `::` 定界符，查找即 `annotations[cellId(k, col.key)]`，rowKey 缺失回落 idx 同 `rowKeyOf`）；**动态回调** `cellNote?: (row, column) => string \| null` 每格计算——**动态赢静态**（两者同设时 cellNote 优先，文档化；null/'' 注记渲染零节点）。有注记的格：`data-iris-cell-note` 属性 + 右上角 6px 角标 `<span data-iris-cell-note-badge aria-hidden>`（`var(--iris-warning, var(--iris-primary))` 警告色 + primary 回落，token-only；宿主格在注记时才加 `position: relative` 供角标锚定——**固定列**的 `position: sticky` 覆盖 relative，角标仍相对 sticky 格盒定位，文档化；`inset-inline-end` 逻辑属性 RTL 镜像）+ **title = 注记**（优先于 `tooltipConfig`/`cellTooltip`，未注记格保留原 tooltip；编辑中格沿用既有豁免链——title 免设、角标/属性保留）。纯展示层，编辑/排序/筛选/导出均读原始值。`props.ts` + `Table.tsx`（模块级 `cellNoteOf` 单点解析保持单元格箭头复杂度预算）+ `styles.ts`（`CELL_NOTE_STYLE`）+ `annotations.test.tsx` 8 react 新测试。core 零改动、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 140→142） |
| 版本历史 `versionHistory` | vxe 无等价物（vxe 无任何时间旅行/版本回退能力，官方做法是手写快照）。`versionHistory?: { max?: number }`（默认 off，需工具栏渲染——门控 `(toolbar \|\| views \|\| query !== undefined \|\| undo \|\| chartPreview \|\| freshness \|\| auditLog \|\| versionHistory)` 开启工具栏）：core 新控制器 `createVersionHistory<Row>`（`version-history.ts`，**框架无关**，审计环 1:1 模具：默认 max 20 有界环形、monotonic `index` **清空不重置**、`list()` 最新优先（条目拷贝，rows 共享引用）、`get(index)` 拷贝/undefined、`subscribe`/`getVersion` 供 `useSyncExternalStore` 桥）——**每个行列表提交推一条「变更前」版本**：`commitRowList(next, type?)` 在 batch-AT 审计漏斗之前把 `auditRowsRef` 快照（真·变更前行）连**同一类型提示**（insert/remove/paste/batch/fill/undo/redo，默认 edit）推入环；**首个提交即版本 0**（无初始播种——提交 0 的变更前行就是初始状态，文档化）；`commitValue`（cell/row 内联编辑直写）**不产生版本**（文档化——restore 替换整个行列表，行级提交才是连贯单元）。工具栏 `data-iris-history-trigger`（⏱）开浮动面板 `data-iris-history-panel`（审计面板同款 useFloating + portal，Esc/外部/滚动关闭；`useSyncExternalStore` 自刷新）：**最新优先**条目 `#index + formatClock 时间 + 类型标签`（raw type 大写，审计面板同款），**点击条目即 restore**——经正常写回通道 `commitRowList(rows, 'undo')`（**可审计 + 可撤销**，与 undo/redo 重放一致）并 `historySuppressRef` 抑制自推（不产生新版本）；restore 后关面板；刻意**无清空钮**（restore 即回退，文档化）；`table.history.empty` 空态。handle 增 `getVersions()`（**轻量快照——不含 rows**，面板/调用方零搬运数据）+ `restoreVersion(index)`（未知 index 无操作）。+2 i18n 键（en+zh：table.history/history.empty）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 143→144） |
| 批注编辑 `annotationEditing` / `onAnnotationsChange` | vxe 无等价物（批 AZ 的 `annotations` 是纯展示层）。iris 批 BB 把它做成**受控编辑闭环**：`annotationEditing?: boolean`（默认 off，依赖 `contextMenu`）开启后，右键菜单在**摘要项之后**追加内置项——无注记格 → 添加批注（`__iris-annotate`，i18n `table.annotate`）；有注记格 → 编辑批注（`__iris-annotate-edit`）+ 删除批注（`__iris-annotate-remove`）（存在性 = `annotations[cellId(rowKeyOf(row, idx), col.key)]` 非空；与分布/摘要项同款保留键去重守卫）。添加/编辑项在 onSelect 接线处拦截，于**同一虚拟光标锚点**浮出编辑面板（`data-iris-annotate-panel`，分布/摘要面板同款 useFloating + useDismiss + portal + seq remount，Esc/外部/滚动关闭）：textarea `data-iris-annotate-input` 以 `annotations[cellId]` 播种 + 保存钮 `data-iris-annotate-save`（**空文本删键、非空设键**）+ 删除钮 `data-iris-annotate-remove`（仅注记存在时）；菜单删除项**直删不弹面板**。**写通道完全受控**：全部经 `onAnnotationsChange?: (next: Record<string, string>) => void` 单行 prop（`onFiltersChange` 同款形状）——表零内部注记状态，`annotations` 仍是唯一读源；**无回调时菜单项照常显示但保存/删除惰性**（文档化 fail-safe）。+4 i18n 键（en+zh：table.annotate/annotate.edit/annotate.remove/annotate.save）。core 仅 i18n 默认键、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 144→146） |
| 跨表引用公式 `formulaTables` | vxe 无等价物（vxe 公式都不存在，更无跨表）。iris 批 BC 给批 AO 的公式列补上**跨表引用**：`formulaTables?: Record<string, Row[]>`（`IrisTableProps` 单行 prop，纯 additive）——公式语法新增 `table!field` 令牌（core `evaluateFormula` 第 3 可选参，**框架无关**）：解析为 `formulaTables[table][0][field]`（**只读首行**，文档化）；**fail-closed**——缺 tables 参 / 未知表 / **空表** / 未知字段 → 整式 null（与未知行字段同契约，无静默回退）；已知 nullish 字段值照常强制转换（Excel 空格当零）；`!` 非法位置（`a!`/`!a`/`a!1`）→ 语法错 null、**永不抛错**；表/字段只查 own property（原型命中如 `toString` 不泄露 Object.prototype）。`memoizedFormulaValue` 缓存键扩为 **(row, tables 对象同一性, formula)** 嵌套 WeakMap——2 参调用共享模块哨兵槽（AO 行为字节兼容）；tables 与行同属**不可变契约**（新对象引用即重算）。react 桥**双通道**：渲染期全部 `getCellValue` 站点（~30 处）+ `querySortedData` 比较器读模块槽 `currentFormulaTables`（React 同步渲染行走保证隔离，每次 render 顶部赋值）；两条挂载期 CSV handle（`exportCurrentViewCsv`/`exportSelectionCsv`）经 `formulaTablesRef` 镜像**显式传参**（按需调用不竞态）。`useTableSort` 选项加 `formulaTables` 穿入 `buildSorter`（单/多列比较器 + dep 数组更新——**第 4 求值点**，hook 独立可用）。渲染/排序/筛选/汇总/导出/display-only 全走既有 `getCellValue` 咽喉与 `isEditableColumn` 守卫，零新管道。core 仅 additive（`FormulaTables` 类型 barrel 导出）、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 146→147） |
| 协作光标 `presence` | vxe 无等价物（vxe 无任何协作者光标共享能力，官方做法是手写 overlay 同步）。iris 批 BD 把它做成声明式：`presence?: IrisTablePresenceEntry[]`（`{ id, name, color, cellKey }`，`props.ts` 在 `onAnnotationsChange` 之后，index barrel 具名导出类型）——`cellKey` 用**规范定界符** `${rowKeyVal}::${colKey}`（与内部 `cellId` 和批 AZ `annotations` 键同款，零新键发明）。渲染走批 AZ/AQ 同款热格模式：render-scoped memo 按 cellKey 分组（每个可见格一次 Map 查找）+ 3 个模块 helper（`presenceOf`/`presenceStyle`/`renderPresenceLabels`）保持单元格箭头复杂度预算；有光标的格 `data-iris-presence="true"` + `outline: 2px solid <首条目 color>` + `position: relative`，左上角角标（`data-iris-presence-label` + `-id`/`-name`，`insetInlineStart` 逻辑属性 RTL 安全）——同格多人时 `top: i*14` 级联堆叠、首条目颜色赢；**角落预算实测零冲突**（左上角标 vs 脏点/注记徽标右上 `insetInlineEnd` vs 填充句柄右下）。**纯展示**：零 state/store/effect/handle/i18n/core 改动；受控数组契约——**新数组引用重渲**、原地突变不重渲（与 `data`/`annotations` 同契约，文档化）；未知 cellKey fail-inert（不渲染任何节点）。样式全 token（`PRESENCE_LABEL_STYLE`：font-size-xs/space-xxs 内边距/`primary-foreground` 回落，背景 = 条目 color **逐字使用**——数据例外，与 Button/Chip 同款 `var(--iris-*, #fff)` 回落模式）；标签 `pointerEvents: none` 不挡点击。react-only 桥、core 零改动、其余框架零改动；manifest 已重新生成（propCount 147→148） |
| 单元格锁定 `locked` | vxe 无等价物（vxe 无单元格级只读概念——editConfig 只有整表/整列可编辑开关，做不到列内按行锁）。iris 批 BE 在 `IrisTableColumn` 加 `locked?: boolean \| ((row, column) => boolean)`（`types.ts` 单行 prop）：`true` = 整列锁；predicate 忽略 column 参数即**行级锁**；列感知 predicate = **组合锁**（单格锁）。**单一咽喉 `isCellLocked`**（模块级，与 `isEditableColumn` 并肩）守卫**每一个写入口**：`beginEdit`（双击/单击触发/F2/Tab 导航/待导航一个入口全覆盖，早退在 `onEditStart` 之前）、行模式（`beginRowEdit` 过滤 + 重开分支 + Tab 两移动器带 row 参）、Delete/Backspace 快捷键、粘贴（矩形与流式两分支）、拖拽填充、范围清除、FNR 替换/全部替换、批量编辑（全锁 → **零提交**、面板照常关闭——与 paste/fill 同款 zero-commit 守卫）。**渲染**：`data-iris-cell-locked="true"` + cursor `not-allowed` + 45° 斜纹背景图（`--iris-muted-subtle` token 双主题都有；background-**image** 故 hover/选中底色、脏点/注记角标全部透过可见）。**fail-inert 面**：选择/范围/复制/导出/FNR 查找/`data-editable` 属性全不受影响——锁定只守写入、读通道零改动。**review 修复**（条纹曾静默不渲染——规则误落 styles.ts 死导出 `TABLE_ROW_CSS`，且 `fnrCellStyle` 无条件 `background` 简写重置 background-image）：规则移入活跃注入样式表（`#iris-table-row-styles`）+ `fnrCellStyle` 改 `backgroundColor` **长属性**（简写会重置 image 并触发 React 简写/长属性混合告警——rerender 时背景变化会静默抹掉条纹，jsdom 实测）+ 渲染尾部**再断言** inline `backgroundImage`（防 range-fill/条件格式/用户 `cellStyle` 简写）；全锁范围清除跳过空 patch 行（零伪提交，无多余 onDataChange/undo/审计）。13 react 新测试（`locked-cells.test.tsx` 11 + range-clear 零提交/混合 2）。core 零改动、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 148 不变——`locked` 是列字段，批 AY 先例） |
| 数据导出时间线 `exportVersionCsv` | vxe 无版本历史概念，更无历史导出（官方做法是手动快照 + 自行序列化）。iris 批 BF 给批 BA 的版本历史补上**按版本导出**：`IrisTableHandle.exportVersionCsv(index: number): string`（handle 方法，manifest 不可见、无 propCount/i18n/props.ts/core 改动）——`versionHistory.get(index)`（批 BA 环形控制器既有 API）→ 未知 index（越界/被裁剪/清空）或**无 `versionHistory` prop → `''`**（调用方用 `getVersions()` 探测；与 `exportCurrentViewCsv` 的永不空语义不同，文档化）；有效 index → **批 AP `exportSelectionCsv` 同款管线**：`exportCsv(withComputedFormulaCells(rows, viewColumnsRef.current, formulaTablesRef.current), viewColumnsRef.current)` ——同一序列化器/列集/掩码（批 AY）/OWASP 中和，公式列在影子行物化、隐藏列排除。**行源分歧**（review LOW，刻意保持基线决定）：导出的是环中**变更前快照**（原始行序），非 `exportCurrentViewCsv` 的**已筛选+已排序**活动视图——有筛选/排序时两者不等（文档化）；**无筛选/排序时** `restoreVersion(i)` 后 `exportVersionCsv(i) === exportCurrentViewCsv()` 字节相等（核心断言，测试 #6）。公式物化用当前公式状态（跨表引用读活表）——与 restore-再导出同近似，非 bug。**无 UI**（刻意非目标：面板不加导出钮，restore + `exportCurrentViewCsv` 覆盖面板路径）。core 零改动、react-only 桥、其余框架零改动；manifest 无 diff |
| 比较差异导出 `exportComparisonCsv` | vxe 无比较能力，更无差异导出。iris 批 BV 给批 AU 的比较视图补上**差异行导出**：`IrisTableHandle.exportComparisonCsv(): string`（handle 方法，manifest 不可见、无 propCount/i18n 键数/props.ts/core 改动——模块级纯函数 `buildComparisonCsv` + 3 个 ref 镜像 + handle 项，`types.ts` 单行方法）——差异行 = **当前视图**（`filteredDataRef`，与 `exportCurrentViewCsv` 同源）中 status ∈ {removed, changed} 的行**按视图序** + compareWith 快照独有 `added` 行**尾部按快照序**（批 AU 文档化无渲染槽位），未变更行排除（spec「差异行」明文）；每行前置**标记列**（保留键 `__iris_diff` + i18n `table.compare.diff` en `Diff`/zh `差异`，值 = 英文字面量 added/removed/changed）。**规格扩展**（fiat #4）：变更格导出 `maskedOld → maskedNew` 合成串（`→` 同批 AU tooltip；**掩码先于合成**——批 AY 导出默认掩码不因合成泄漏裸值，`exportRaw` 双侧裸值；公式列自身不合成且从**原始数据**物化——变更输入列的合成串永不漏进依赖公式）。**标记复用渲染 memo**（`compareDiffRef` 镜像，零重算、与渲染视图按构造一致；key 提取裸 `row[rowKeyField]` 同 core diffRows，无 rowId 回落）；序列化走 `buildComparisonCsv`（csvRangeCell 本地序列化器先例：公式物化 + 掩码 + core `toCsv` 同款 RFC-4180/OWASP 中和）。**空语义**：无 compareWith/rowKey → `''`；有但零差异 → 仅表头（两态区分）。**无 UI 入口**（handle-only，`exportVersionCsv` 同款）。`export-comparison-csv.test.tsx` 14 react 新测试（changed 行标记+composite/removed 现值/added 快照值/未变更排除/惰性 `''`×2/全等快照仅表头/标记列置首+i18n/隐藏列/掩码双侧+exportRaw/公式物化/行序/多变更列+null 空串/筛选视图排除）。core +1 i18n 键（en+zh）、react-only 桥、其余框架零改动；manifest 无 diff（handle+i18n 均不可见，propCount 161/eventCount 30 不变） |
| 审计时间线导出 `exportTimelineCsv` | vxe 无审计概念，更无审计导出（官方做法是手写事件日志 + 自行序列化）。iris 批 CO 给批 AT 的审计日志补上**时间线导出**：`IrisTableHandle.exportTimelineCsv(): string`（handle 方法，manifest 不可见、无 propCount/i18n 键数/props.ts/core 改动——`types.ts` 单行方法 + `Table.tsx` handle 内联）——把批 AT 审计环经 core `toCsv` 序列化为 **spec 字面 6 列** `time,type,rowKey,column,old,new`：**time** = `formatClock(new Date(e.at))`（HH:MM:SS 本地——与审计面板时间格**逐字同源**，display/export 一致原则；fiat：不带日期，面板即此粒度，原始 `at` 仍在 `getAuditLog()`）；**type/rowKey/column/old/new** 原值直通——undefined → 空串（行级结构变更 insert/remove 行尾三列空）、数字保持裸数字、字符串走 RFC-4180 引号 + **OWASP 公式中和**（审计内容是不可信数据，core `toCsv` 既有语义）。**顺序 = 环序（最新优先**，与面板/`getAuditLog` 同一视图）。**空语义 fail-closed 家族**：`auditLog` 关 → `''`（exportVersionCsv/exportComparisonCsv 先例）、开但空环 → 仅表头（`getAuditLog().length` 判别两态）；**无 UI**（面板不加导出钮，批 BF 非目标）。零新 import（`toCsv`/`formatClock` 已导入）、零 core 改动、react-only 桥、其余框架零改动；manifest 无 diff（handle 不可见） |
| 批 CP | **iris 独有增量（react，见「iris 独有」节）**：**行密度三档 `density`**（vxe 无密度概念——vxe 只有 size；iris 在 `size` 之上加第三维）：`density?: 'comfortable' \| 'compact' \| 'cozy'`（默认 comfortable）——三档行高/内边距，与 size 同写 `--iris-cell-pad-y`（compact→6px / cozy→4px，规则置于 size 预置之后、同特异性后胜，密度叠在 size 之上）；根 `data-density` 属性、comfortable 声明无规则字节不变；非法值 fail-closed 回 comfortable。**工具栏密度切换 `densityToggle`**：ghost 按钮 `data-iris-density-toggle` 循环 comfortable→compact→cozy→comfortable（zoom 先例本地 state，effective = toggle ? state : prop，`data-iris-density` 随按钮态；按钮单独开即入工具栏渲染门、layouts 隐藏同步抑制）；单元格仍读共享 `--iris-cell-pad` var 链（纯 CSS 零 inline）+ 4 i18n 键 en+zh（table.density 与三档名）；core 仅 i18n 默认键、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 174→176） |
| 批 CQ | **iris 独有增量（react，见「iris 独有」节）**：**编辑实时预览 `editPreview`**（vxe 无等价物——vxe 编辑态不展示 formatter 结果，只能提交后看格）：`editPreview?: boolean` 单行 prop（`props.ts` 在 `charCount` 之后，默认 off fail-closed）——开启后编辑态单元格下方实时渲染 **formatter 应用草稿值的结果**（`data-iris-edit-preview`，muted 小字：`--iris-font-size-xs`/`--iris-muted`/`--iris-space-xxs` gap，token-only + `pointer-events: none` 纯展示）。**零新 state**：EditorSurface 已订阅会话 store（`useStore(session.store)` → `state.draft` 每击键重渲，批 CG 先例逐字），预览只是同一订阅上的纯派生。**预览值镜像提交格显示链**：草稿先走与 commit/validate 路径**同一共享强转** `coerceEditDraft`（批 CQ review 收敛——select 编辑器解析 option 的 TYPED 值、number 编辑器 `Number()` + 非法/空回落 `getCellValue` 原值，formatter 永不收到字符串草稿崩溃/不 byte-faithful；未来新增编辑器类型不可能让预览与提交漂移），再 `applyCellMask` 掩码 → `col.formatter`（批 AY mask→formatter 契约）——与提交后格的显示逐字一致；formatter 第二参收到**正在编辑的行**（新 `row` prop 穿入 EditorSurface，两调用点行内即有）。**双门控**：`editPreview && col.formatter`——无 formatter 的列不显示（规格第二门）；cell/row 双编辑模式同源渲染，select/textarea 分支天然覆盖；提交/Enter 与 Escape 取消随编辑器一起拆除（会话归零即无预览）。**渲染位置 = 编辑器之后、校验错误之前（既有错误槽位，错误共存下方）**——批 CQ review 门禁修复：编辑态单元格 `flex-wrap: wrap` + 预览/错误 `flexBasis: 100%` 整行堆叠，Chrome 实测编辑器、预览、错误各占一行依次**下方**渲染（修复前 flex-row 并排挤压）；新增 `row` prop 于 `EditorSurfaceProps`。**零外围**：无 i18n 键（纯 formatter 输出）、零 core/types/events 改动、其余框架零改动。manifest 已重新生成（propCount 176→**177**，eventCount 31 不变） |
| 批 CR | **iris 独有增量（react，见「iris 独有」节）**：**列汇总条 `columnTotals`**（vxe 无等价物——vxe 只有汇总行，没有工具栏下方的列合计横条；Excel 状态栏对齐）：`columnTotals?: boolean` 单行 prop（`props.ts` 在 `editPreview` 之后，默认 off fail-closed）——开启后**工具栏正下方**渲染全宽网格横条 `data-iris-column-totals`（FNR 栏同款边框语言 + `COLUMN_TOTALS_STYLE` token-only：`--iris-surface`/`--iris-border`/`--iris-font-size-sm`/600 weight，共享 `gridTemplateColumns` 与正文逐轨道对齐）；每叶列一个格子 `data-iris-column-totals-cell={col.key}`（非 sum 列渲染空占位保持对齐，`justifyFor(col.align)` 对齐同汇总行），仅 `summary === 'sum'` 列经**汇总行逐字同管线**取值：`aggregate(bodyData, r => getCellValue(r, col), 'sum')` → `aggregateAccuracy` 舍入门（批 P 单一舍入点，越界忽略）→ `renderSummary ?? String`（footer-parity 可直接断言）；公式列经 `getCellValue` 咽喉自动汇入、null/NaN/Infinity 行跳过；空 body → core `aggregate` 返 0 → 条显示 `0`；数据更新实时刷新（`columnTotalsValues` useMemo 零新 state，chartNumericColumns 先例）。**栏门独立于工具栏可见性**（FNR 先例：无 toolbar 也渲染）；选择列前导占位格保持轨道对齐。**五个显式 fiat**：无 label/i18n（纯数值条）、固定轨道对齐同批 M、不感知列虚拟化、pinned 不粘、空 body 显示 `0`。零 core/types/事件/其他框架改动；manifest 已重新生成（propCount 177→**178**，eventCount 31 不变，`column-totals.test.tsx` 12 新测试） |
| 批 CS | **iris 独有增量（react，见「iris 独有」节）**：**展开滚动保持 `expandScrollPreserve`**（vxe 无等价物——vxe 展开时只保持像素 scrollTop，从不重锚**内容**：视口上方的节点/详情面板展开会把下方所有行下推插入高度，正在读的行跳走）：`expandScrollPreserve?: boolean` 单行 prop（`props.ts` 在 `columnTotals` 之后，默认 off fail-closed）——虚拟模式下，展开提交在**新 plan** 中重定位首可见行（内容锚点 + 部分偏移）并写 `newIndex × slotHeight + relativeTop`（**精确下标数学，零 delta 记账**）：树上展开 +C×36 / 收起 −C×36 / 详情 +36 / 部分偏移保留 / 折叠超界 clamp。**五显式 fiat**：变高仅像素（偏移树子内部）、非虚拟惰性、全量恢复（`expandAll`/`persistState` 重放）回退虚拟器 re-clamp、单键提交精确、lazy-load 切换 post-fill。**实现**：锚点记录两处（稳定渲染 + IrisVirtualScroll 既有 `onScroll` 接线——Table 滚动不重渲，ref 镜像 scrollTop；过渡渲染跳过重记保旧锚），过渡判定 = `prevExpandedKeysRef` 与当前**对称差恰一键**（`singleKeyDiff`），锚键 = `virtualItemKeyOf`（keyOf 提为模块 helper 单真相源，`BodyPlanEntry` 类型上提模块级共用）；钳界 = plan 总数（真实浏览器 scrollTop setter 自钳到自身范围，jsdom scrollHeight/clientHeight 0 不可用）。零 core/i18n/types/事件/三框架改动；manifest 已重新生成（propCount 178→**179**，eventCount 31 不变，`expand-scroll-preserve.test.tsx` 12 新测试） |
| 批 CT | **iris 独有增量（react，见「iris 独有」节）**：**表格标签页 `tableTabs`**（vxe 无等价物——vxe 视图切换只有工具栏下拉，无标签条）：`tableTabs?: IrisTableTab[]`（`props.ts` 在 `onActiveViewChange` 之后，默认 off fail-closed，propCount 179→**180**；types.ts 新增具名导出 `IrisTableTab`：`{ key, label, views?: string[] }`）——点击标签把 `views` 中每个名字**按序**经**同一 `selectView` 路径**应用（未知名跳过 fail-inert、重叠块后赢、工具栏下拉镜像最后应用的视图；`views` 已在工具栏门内，标签条 + 视图天然组合）。**渲染**：`role=tablist` 条 `data-iris-table-tabs`（token-only：`--iris-surface`/`--iris-border`/radius-md/`--iris-primary` 活动下划线）渲染在**工具栏上方**、独立于工具栏门（`layouts.toolbar: 'hidden'` 仍渲染标签条）；活动态内部 state——**首次点击前无任何活动标签**；`tabs` 去重 memo（重复 key 保留首个，渲染与 apply 同源）；工具栏顶角半径在标签条存在时移交（fail-closed 条件 spread，无标签路径字节不变）；打印媒体查询隐藏标签条（printable 一致）。**9 条 fail-closed 规则**：dup key 保首、空 `views` 标签渲染但零应用、无 `views` 配置点击惰性、未知名跳过、活动态内聚、无 prop 零 DOM、空数组零 DOM、半径协调、toolbar hidden 组合。**零外围**：零新 i18n 键（标签是数据不是文案）、零 core/types 事件/三框架改动。manifest 已重新生成（propCount **180**/eventCount **31** 不变，`table-tabs.test.tsx` 10 新测试） |
| 批 CU | **iris 独有增量（react，见「iris 独有」节）**：**带格式复制 `copyWithFormat`**（vxe clipboard-config 固定复制原始值，无格式保留复制）；`clipConfig.copyWithFormat?: boolean`（`props.ts` 在 `clipConfig` 内联类型加字段，默认 off fail-closed，纯 additive，propCount **180**/eventCount **31** 不变——嵌套字段不增顶层成员，clipConfig type 串更新先例）——**单一咽喉 `buildRangeCopy`** 第 3 参（**formatter 门控**：仅 `col.formatter` 列切到 `contextCellText` 显示文本链——mask → formatter → String，与右键 复制值/`cellTooltip` 同源「单元格文本」链，零新 helper）——复制**格式化文本**而非原始值；无 formatter 列字节不变（负数中和爆炸半径消除）；两消费点（Ctrl/Cmd+C 与范围工具栏 复制按钮）同传 `!!clipConfig?.copyWithFormat`。**三格式全收格式化串**：格式化结果仍走同一 tsv/csv/html 序列化器（RFC-4180 引号 + OWASP 中和对格式化文本照样生效；html 经 toHtml 转义、格式化字符串失数字右对齐 fiat）。**fiats**：formatter 输入恒为掩码值（批 AY 契约）——formatter 列 `copyWithFormat` **取代** `exportRaw` 的复制路径跳过（掩码 → formatter 恒走），导出零改动；非字符串 formatter 结果与 复制值 同款回落到 `String(掩码值)`；粘贴不对称（paste 仍按 `\t` 读原始值）；公式列经批 AO `getCellValue` 咽喉物化后照常 formatter。`types.ts` formatter 文档 1 句 + `props.ts` 文档 1 块 + `copy-with-format.test.tsx` **14 新测试**（spec ① 格式化 TSV 字节 pin `25.00` 非 `25` / ② 格式化非原始 `$25` / 工具栏同咽喉 / 无 formatter 字节回归 / 掩码→formatter 顺序 / exportRaw 组合（格式化取代 + 关时字节回归）/ 非字符串回落 / csv 引号+OWASP / html 转义+右对齐丢失 / copyFormat 正交三格式 / row 感知 formatter / 粘贴不对称 / 无范围 no-op / 公式+formatter）。零 core / 零 i18n / 零样式表 / 其余框架零改动；manifest 已重新生成（propCount 180/eventCount 31 不变，clipConfig type 串 + 描述更新） |
| 批 CV | **iris 独有增量（react，见「iris 独有」节）**：**固定列数边界拖拽 `pinnedDrag`**（vxe 固定列数由 column-config 静态声明，无边界拖拽线）：`pinnedDrag?: boolean` 单行 prop（`props.ts` 在 `pinnedLeft/pinnedRight` 之后，默认 off fail-closed）——拖拽线在 pinned 边界：**最后一个左固定叶头尾缘** 8px grip + 2px `--iris-primary` 线（sticky 随格免费继承），拖右加 pin / 拖左减 pin、松开提交 + translateX ghost、Arrow ±1；提交逐列 `setColumnPinned('left'|null)` 走**同咽喉**（onColumnPinnedChange 每列、受控无乐观翻转）+ `onPinnedCountChange(count)` 每次提交一次、no-op 零回调；**左计数 only**：零左固定无句柄、首右固定列硬上限、宽近似 pinnedOffsets 同回落链、boundary 列 resizableColumns 句柄抑制防抢手势。review 门禁修复：gapped 态 [A(left), B(null), C(left)] 预算改从**前导前缀**宽起算（resolve(0) ≡ leftPinnedCount，同 commit 的 current 概念）——pin 菜单可达间隙态下零 dx 不再误 pin B/unpin C 触发 onColumnPinnedChange ×2 + onPinnedCountChange，+T18 回归测试；propCount 180→182/eventCount 31→32，react 2618→2636，18 新测试 |
| 批 CW | **iris 独有增量（react，见「iris 独有」节）**：**导入预览 `importPreview`**（vxe ⇪ 导入无预览直接调 onImport——用户看不见导入什么）：`importPreview?: boolean` 表级 prop（`props.ts` `zoomConfig` 后，默认 off，manifest propCount 182→183/eventCount 32 不变）；解析/行构建零改动，`importPreview` 门恰在 `toolbar.onImport?.(rows)` 调用点分裂——开时 `setImportPreviewRows(rows)` 本地挂起，固定居中 modal（Dialog backdrop/`--iris-z-modal` 先例）显示**前 5 行**表格（表头 = 首行键序，`previewColumnsFromRows` 模块 helper）+ 超 5 行时 `table.total` 总数注；**确认** = `onImport(全部行)` 恰一次 + 关闭、**取消/Esc（仅开时挂 window 监听）/backdrop** = 零调用；关闭后 input value 已清 → 重选文件**每选刷新**（fresh per selection）；`<2` 行 silent no-op 保留、header-only 单行零 modal 零调用、引号字段 RFC-4180 原串直显；off 路径字节不变；+3 i18n 键 en+zh（title/confirm/cancel，总数注复用 `table.total`）；core 仅 3 i18n 默认键、react-only 桥、三框架零改动、styles/types 零改动；react 2636→2648，12 新测试 |
| 批 CX | **iris 独有增量（react，见「iris 独有」节）**：**列类型自动检测 `autoDetectTypes`**（vxe 要求调用者逐列声明 `sortType`，从不自动推断）：`autoDetectTypes?: boolean` 表级 prop（`props.ts` `footerAlign` 后，默认 off fail-closed，propCount 183→**184**/eventCount 32 不变）——**列首次数据到达时自动推断值类型**（string/number/date/boolean）→ **默认对齐 + 排序类型**（数值右对齐 + `sortType: 'number'`；string/date/boolean 左对齐 + `'string'`），仅填调用者未定义的字段（显式 `align`/`sortType` 恒赢、preset 默认存活）。**core 新纯函数 `detectColumnType`**（框架无关，`column-type.ts`）：前 50 个非空样本、全样本一致才定类型；number/boolean 按 `typeof` 定（非有限数 NaN/Infinity 仍投 number——与既有逐格数值回落 `typeof === 'number'` 一致）、date = `Date` 实例或 ISO 正则、**数值/布尔字符串保持 string**（CSV 导入全字符串不强转——异构行不误对齐）、混合 → string fail-safe；core index +2 导出。**react 桥**：one-shot effect 在首个非空 `liveData` 触发（`detectTypesRef` 守卫，重喂/编辑写回不重测；代理模式仅首页采样；SSR 安全——effect 不跑 renderToString，hydrate 后落地）+ `detectedColumns` memo 插在 preset 与 order 之间（defined-fields-only、公式列跳过——其 sortType 是计算值上的调用者契约），`orderedColumns` 消费；**闭合既有表头对齐缺口**（body/footer 已按值右对齐、表头恒左——检测填充 `col.align` 后表头随右）。12 显式 fiat：one-shot、首页代理、preset 共存、SSR 后 hydrate、`sortBy` 正交、types.ts 零改动、零 i18n。core 1559→**1569**（+10 核心测试：per-type 推断/混合/数值串/布尔串/非有限/样本上限）、react 2648→**2660**（+12 新测试 `auto-detect-types.test.tsx`：spec ① 表头右对齐 ② 异构行数值排序 ③ 类型推断 ×3，plus off 字节不变/显式字段赢/preset 共存/one-shot/异步到达/分组叶子/公式跳过/混合 fail-safe）；props.ts +1、Table.tsx 4 触点、vue/solid/svelte/types/i18n/styles 零改动；manifest 已重新生成（propCount **184**/eventCount **32** 不变） |
| 批 CZ | **iris 独有增量（react，见「iris 独有」节）**：**跳到行 `goToRow`**（vxe 无等价物——vxe scrollToRow 只滚动、无「跳到行」高亮定位反馈）：`IrisTableHandle.goToRow(key)`（`types.ts` 在 `scrollToRow` 之后新增**单行 handle 成员**；handle 方法不入 manifest 计数——propCount 185/eventCount 32 零增量，185 = 184 基线 + 批 CY 遗留 `responsive` prop 声明）——**滚动 + 短暂行高亮**：共享定位 helper `findTableRowEl`（从 scrollToRow 提取的单真相源——同一 `data-iris-table-row` 属性定位 flat/tree/grouped/virtual 行；属性值 selector 转义，jsdom 无 CSS.escape 回落属性迭代）`scrollIntoView({block:'nearest'})`；行节点置 `data-iris-row-target="true"` + `ROW_TARGET_MS=2000` 定时器移除（copyFlash 模具；`styles.ts` 在 `[data-iris-row-current]` 之后新增 token-only 规则——color-mix `--iris-primary` 18% over `--iris-background`，`--iris-cell-bg` 与 background 双设使全行着色，源序让瞬态赢 selection 后衰减回）。**单目标语义**：先标行立即清除、重呼重启时钟、卸载清理定时器（detached 节点零变更）。**fiats**：不触发 `onCurrentRowChange`（与 setCurrentRow 正交）、未知/虚拟窗未命中键 no-op（scrollToRow 先例）、header/loading/empty 键冲突继承、无 handler 也工作。零 core / 零 i18n / 零事件 / 其余框架零改动；styles.ts +1 规则、types.ts +1 成员、Table.tsx 5 触点 + `go-to-row.test.tsx` **11 新测试**（spec ① 滚动 ② 高亮 ③ 2s 移除，plus 重呼重启时钟 / A→B 单目标 / 未知键 no-op / 引号键 / 卸载清理 / 无 handler 正交 / scrollToRow 字节回归 / setCurrentRow 正交）；react 2660→**2671**，core 1580 不变；manifest 已重新生成（185 props/32 events——goToRow 零 diff） |
| 批 DA | **验证式 adapt（react，见「iris 独有」节，零源码改动）**：**单元格格式化复制增强**——spec 已被批 CU `copyWithFormat` 完整交付（选区整体按列 formatter 输出 + 掩码优先 + 14 测试），本次逐行核对磁盘代码零新增：`buildRangeCopy(range, format, copyWithFormat)`（Table.tsx:6822）三序列化器（tsv/csv/html）formatter-gated 遍历整个 rect、双喉 Ctrl/Cmd+C :7049 + 工具栏复制按钮 :7380 同穿参、`contextCellText` :2098 先 `applyCellMask` 再 formatter（掩码优先，测试钉 `M:138****8000`）、`copy-with-format.test.tsx` **14/14**（432 行 ≤500，spec 逐条 1:1 映射）；基线建议的 7 条边角测试有意不加（每句 spec 已有对应测试、文件将逼近 500 行上限）。零 source files、manifest 零 diff（propCount **180**/eventCount **31** 不变、155×4、86 tokens）；gate 实测 turbo **180/180** · react **2671/2671** · core **1580/1580** · audit **0** |
| 批 DC | **iris 独有增量（react，见「iris 独有」节，零新 prop——复用既有 `columnDrag`）**：**冻结区内拖拽排序钳制**（vxe 固定列无拖拽排序概念——vxe `fixed` 列在 columnDragConfig 重排中与普通列同等参与、无冻结区内/区间分界，拖拽可把固定列甩进自由区后粘性错位；iris 自批 BX 起 pin 是运行时状态，固定列落进自由区会**损坏 `pinnedOffsets` sticky 布局**——偏移按左固定序数累积）——**落点钳进拖拽列自己的固定区**：新模块级纯函数 `clampReorderZone(cols, from, to, zoneOf)`（Table.tsx:231，`zone = pinOf(col) ?? 'free'`——与 pinnedOffsets **同一 pinOf 咽喉**；同区落点原样通过（**零固定表字节不变**，vxe parity 回归锁）、跨区越前钳到区首、越后钳到区尾；invariant **列拖拽永不改变列的固定区**，`[left][free][right]` 分区保持——gapped 态 `[A(left),B,C(left)]` 含内，批 CV gap 处理同款）。`resolveColDrag` 重排块（Table.tsx:4521-4540）接线 + **net-zero 守卫**（钳回原位的跨区拖拽跳过 `onReorder`——rowDrag `from === insertIndex` 先例逐字，仅钳制可达；孤立固定列（区内仅己）任何跨区拖拽恒 no-op）。**正交审计**：CH 拖出固定边缘检查保持**最先**（固定手势优先于钳制）、CV 边界句柄独立通道、BX 菜单零触碰、customConfig 面板草稿重排**刻意不钳**（父拥有顺序的独立机制，fiat）、`data-iris-col-drag-over` 保持指针位置语义（仅提交时钳制，fiat）。**零 core/props/types/styles/i18n/三框架改动**（纯复用既有列拖拽机制）；manifest 零 diff（propCount **186**/eventCount **32** 不变）；react 2682→**2697**（+15 测试，`test/pinned-reorder.test.tsx` 402 行 ≤500：T1/T4 冻结区内重排 / T2/T3/T5/T15 跨区钳制（含左侧孤零净零）/ T6 零固定回归（vxe 字节同形）/ T8 net-zero 钳制不回调 / T9 钳制提交 + DOM 跟随 / T11 受控无 onColumnPinnedChange / T12 分组叶列 / T13 gapped 态 / T14 CH 拖出固定回归 + 菜单开启带内重排仍活） |
| 批 DD | **iris 独有增量（core，见「iris 独有」节——单元格数据验证增强）**：**editRules `regexp` 快捷**（vxe 校验规则只有 `pattern` 无别名命名——iris 把内建 pattern 规则补一个**纯 additive 别名**）：`EditRule` 增 `regexp?: string \| RegExp`（JSDoc 标注内建 pattern 快捷形，经 `index.ts` 类型透传 `types.ts`）——`validateRule` 计算 `rule.pattern ?? rule.regexp`（**pattern 恒胜**、regexp 为回退），**复用 `DEFAULT_MESSAGES.pattern`** 默认消息、**空值豁免保留**（与 pattern 同语义，仅非空值才正则测试）；core 同步 `validateEditRules` 与异步 `validateEditRulesAsync` 两 API 的 validator 剥离后都走**同一 `validateRule`** → 快捷形零额外接线流经 cell/row 每条提交路径。core 仅 `edit-rules.ts` + `edit-rules.test.ts` 两文件（+4 新测试 15→**19**，空值豁免并入 string-source 例——计划 5 语义全覆盖：RegExp match/reject、string 源编译 + message 覆盖 + 空值豁免、pattern 冲突时 precedence、async-API 真实提交路由）；core 1580→**1584**，react 2697 不变（纯 core 改动）；manifest 零 diff（155×4 / propCount **186** / eventCount **32** / 86 tokens 不变），Table/props/types/styles/i18n/三框架零改动 |
| 批 DE | **iris 独有增量（react，见「iris 独有」节——表格状态对比）**：**`handle.compareStates(a, b): string`**（vxe 无任何导出状态对比能力——官方导出/导入都是黑盒 JSON，不给用户看差异）：**零新 prop/事件/i18n/core**（handle 方法，manifest 不可见，propCount 186/eventCount 32 零增量）。**纯框架无关模块 `compareStates.ts`**（NEW，零 import，`exportStateJson`/`importStateJson` 收集器零改动）：`deepEqual`（**排序键 + 结构深等，顺序无关**）+ 递归 `collectDiff` 产出**字段级差异文本**——`+` 新增 / `-` 移除 / `~ old → new`（**a=前、b=后**）；对象块按**点路径** desc（`sort.direction`）、record map 按 key（`columnWidths.name`）、数组按 index（`columnOrder[0]`）；`deepEqual` → `''`；**无效 JSON → `! compareStates: invalid JSON`（永不抛错）**；迭代排序键 = 确定性输出。Table.tsx（+1 import `./compareStates` + handle 项 `compareStates: (a, b) => compareStatesDiff(a, b)`，沿用 exportCsv 独立 stdlib 先例不膨胀 god-file）、types.ts（+1 单行 JSDoc 方法，manifest 扫描友好）。新模块是纯函数非 `Iris*` 组件、非 barrel 导出——manifest 零 diff。`compare-states.test.ts` **14 断言**（T1–T11 全景 + 排序确定性 + 方向性 + fail-closed；baseline 可选的 T12 render 集成循环按先例跳过）；core 1584 不变 · react 2697→**2711**（+14）；manifest 155×4/186 props/32 events/86 tokens 零 diff，core/三框架/styles/i18n 零改动 |
| 批 DF | **iris 独有增量（react，见「iris 独有」节——批量粘贴增强）**：**溢出插行 `pasteOptions.insertIfOverflow`**（vxe clipboard-config 粘贴只忽略超界——单格流式粘贴越过末尾的行静默丢弃，不追加）：`pasteOptions?: { insertIfOverflow?: boolean }` 表级 prop（`props.ts` 单行内联对象，manifest 扫描友好，默认 off fail-closed，纯 additive）——**`pasteIntoRange` 单单元格分支**加溢出行收集（`overflowStart`/`newRows`，单元格分支专属；多格矩形粘贴保持批 O 剪裁 fiat）：超界剪贴板行经 **core `insertRowInList`** 组合追加（auto-id `max+1` 键防冲突，surplus 单元格丢弃、locked/readonly 列跳过），一次 `commitRowList(next, 'paste')` 入漏斗（undo/audit/onDataChange 恰一次）；缺省（未设）完全退化批 O 溢出丢弃行为**字节一致**；react 2697→**2711**（`clip-fnr.test.tsx` 26 通过含 8 新：溢出插行/多行 spill 顺序/exact-fit no-op/默认关回归/多格矩形剪裁/surplus 丢弃/key 冲突 max+1/locked 列跳过，498 行 ≤500），core 1584 不变；manifest 已重新生成（propCount **186**/eventCount **32** 不变、155×4/86 tokens，`pasteOptions` 进入 props 流）；core/三框架/styles/i18n 零改动。review 门禁 P2 修复：`pasteOptions` JSDoc 末段移出 `*/` 行独立成行（manifest 扫描逐行读取不再把描述截断到「…keeps batch-O」），gen:manifest 后描述完整 |
| 批 DG | **iris 独有增量（react，见「iris 独有」节——列宽自适应增强）**：**双击自适配列宽 `autoResizeColumns`**（vxe 需手动逐列声明 minWidth/maxWidth，无「双击边界按内容自适应」）：`autoResizeColumns?: boolean` 表级 prop（`props.ts` 单行，默认 off fail-closed，纯 additive，manifest propCount 187→**188**/eventCount 32 不变）——`ColumnResizeHandle` 增可选 `onAutoFit` + `span onDoubleClick`（stopPropagation，不触发表头排序）；双击**列边界**经模块级纯函数 `measureColumnContentWidth(container, colKey)`（表头+正文格 `scrollWidth` 最大值，**12px×2 padding 已内含**——单元格 nowrap+overflow:hidden）→ clamp 到**拖拽同款**钳制（`minWidth ?? 60`/`maxWidth ?? Infinity`）→ `Math.round` → `setColumnWidth` → `onColumnWidthsChange`（受控/非受控双通道，受控无乐观翻转）；测量 ≤0（jsdom scrollWidth=0）fail-closed no-op；无 `resizableColumns` 无句柄、与拖拽/排序零干扰；core/三框架/styles/i18n 零改动（复用既有手柄样式，CSS 零新）；react 2719→**2732**（`auto-resize-columns.test.tsx` 13 新测试：gating ×2/写回+栅格更新/跨格取最大/padding 内含/分钟钳/max 钳/舍入/≤0 no-op/受控无翻转+重渲/拖拽双击零干扰/双击不触发排序），core 1584 不变；manifest 已重新生成 |
| 快捷键自定义 `keymap` | vxe keyboardConfig 无重新绑定能力（官方示例手写监听替换）。iris 批 BG 给批 AN 的表格快捷键补上**声明式重绑**：`keymap?: Partial<Record<'edit' \| 'clear' \| 'undo' \| 'redo' \| 'copy' \| 'paste' \| 'fill' \| 'query', string>>`（`props.ts` 在 `tableShortcuts` 之后，单行 prop）——每个动作一个键串（`Modifier+Key`，大小写不敏感；`Ctrl`/`Cmd`/`Meta` 共用一个 **ctrl-or-meta** 旗标，与历史 `ctrlKey \|\| metaKey` 读取字节一致）。**core 新文件 `keymap.ts`**（框架无关）：`parseTableKey`（修饰符解析 + 非法串 null——`''`/`'Meta'`/`'Ctrl+'`/空白 11 种形态全拒）、`normalizeKeymap(overrides?)`（默认映射表 `DEFAULT_TABLE_KEYMAP` + 逐动作**整组替换**覆盖——`clear: 'Delete'` 会连 Backspace 别名一起换掉；**全非法覆盖 fail-closed 回落默认**）、`matchTableKey`（精确修饰符匹配——`Ctrl+Shift+Z` 必落 redo 永不落 undo、`Alt+Ctrl+Z` 惰性、裸 F2 带 Shift 不触发；键大小写归一）。**react 穿线**：`keyBindings = normalizeKeymap(keymap)` memo 接入既有 4 个硬编码键位（F2/Delete 批 AN、Ctrl+Z/Y 批 AL undo、Ctrl+C/V 批 O clip——`defaultPrevented` 首处理器优先，root 处理器先手则 window 监听退避）+ **2 个新默认键**：`Ctrl+D` **一步下拉填充**（走既有 `fillRangeFromHandle` 管线，零新变更逻辑）与 `Ctrl+K` **聚焦查询输入**（新 `queryInputRef`）。全部严格按既有 feature flag 门控（`tableShortcuts`/`undo`/`clipConfig`/`rangeFill`/`query` 存在）——**keymap 永远不能启用一个未开的特性**；root 门内编辑中 guard（内联编辑器打开时 Ctrl+D/Ctrl+K 一律跳过——编辑器自己的键优先，防未提交草稿下误填充/夺焦）；精确匹配也令历史修饰符组合（Shift+Delete/Ctrl+F2/…）惰性（文档化偏差）；`keyBindings` memo 按 JSON 序列化稳定（inline 字面量不重注册 window 监听）。**review 修复全部落地**（编辑 guard + 回归测试、注释勘误、memo 稳定化）。12 核心 + 11 react 新测试（含编辑中 guard 回归）。core additive、react-only 桥（其余框架无键盘 parity）、其余框架零改动；manifest 已重新生成（propCount 148→149） |
| 行分组折叠 `groupCollapsed` | vxe 行分组无折叠能力（官方示例手写 visibleMethod）。iris 批 BH 把批 M 的行分组补上**声明式折叠**：`groupCollapsed?: Set<string>`（受控，组键 = `String(groupBy 列值)`）+ `defaultGroupCollapsed?: string[]`（首渲种子）+ `onGroupCollapseChange?: (next: Set<string>) => void`——`props.ts` 在 `keymap` 之后，**受控模式语义**（memo `Set(value)` 仅在 value 变化时重建；value 缺省走 internal state，once-only `useState` 种子 + 受控时**无乐观翻转**——折叠请求先到回调，回调未达前 UI 保持原样，无受控表常见的闪回）。**折叠即隐藏**：单一咽喉 `groupPlan` memo（header + full count 保留；折叠组的行 **和** 其每组合计行一起跳过，flat 与 virtual 两条 body 路径同源继承）——bodyData 原索引不变（`rowKey` 寻址零漂移），选择/编辑/undo/audit 全不触碰（折叠是纯渲染层）；header 行 `data-iris-group-collapsed` + 原生 `<button data-iris-group-toggle aria-expanded>`（▸/▾，i18n `table.groupCollapse`/`table.groupExpand` en `Collapse group` / zh `折叠分组`——**+2 i18n 键**），点击 toggle 更新 Set（受控/非受控都发 `onGroupCollapseChange`）。**fiats**：隐藏行选择保持（选择汇总仍计数）、脏点存活、陈旧键惰性、无 `groupBy`/树模式惰性（该三端无 groupBy parity，vue/solid/svelte 零改动）。core 仅 i18n 默认键、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 149→152，eventCount 28→29，`onGroupCollapseChange` 入事件表）。**批 BS（iris 独有）把单列分组升级为表级多列嵌套**：`groupBy?: string[]`（`props.ts` `keymap` 后，数组元素 = 叶子列 key，顺序 = 层级）——`groupPlan` memo 重写为递归构建器（复用 `groupRows` + `getCellValue` 唯一取值漏斗），复合组键 `v0::v1::…`（`::` = cellId 同一定界符，level 0 裸值保持单列兼容），父组 count = 子树数据行总数、`group-summary` 仅最内层、组头按 depth 缩进（token 步长）+ `data-iris-group-depth`，父折叠隐整棵子树、同值异父的子组折叠互不干扰；表级数组**赢**列级 `groupBy: true` 旗标，缺省回落批 M/BH 路径字节不变（零 core/零 i18n/零事件，propCount 158→159） |
| 列 sparkline `sparkline` | vxe 无迷你趋势能力（官方做法是手写 polyline，且从不感知表格自身的筛选视图）。iris 批 BI 把数值列补上**声明式迷你趋势**：`IrisTableColumn.sparkline?: boolean`（`types.ts` 在 `exportRaw` 之后）——单元格渲染该列在 **filteredData[0..i] 含当前值**前缀上的 20×8 SVG polyline（`data-iris-sparkline`，纯结构化 JSX 零 innerHTML，`role="img"` + aria-label=序列，`pointerEvents:none`，stroke `var(--iris-primary)` 1.5 token——ChartPanel 先例）；逐格数值门（raw 须 `typeof number` + 有限，非数值列零 SVG）、gap 断线、单点前缀圆点；`title` 优先显示序列（"10, 4, 8"）覆盖 tooltip（editing/note/compare 链序之后）；域 = core 新导出 `chartDomain`（`buildChartData` 的域半部原样抽出，逐前缀域平铺/空 → {0,1} 永不除零）；序列随 filteredData（排序/筛选重排裁剪，树展开/组折叠不截断）；显示层独占——mask/编辑/复制/导出/汇总零触碰（fiats）。core additive（`chartDomain` 具名导出）、react-only 桥、其余框架零改动；manifest 已重新生成。**review 修复（随批 BJ 门禁提交）**：序列点 `Number` 强转（buildChartData 同款——数值字符串成点，null/非有限成 gap，`types.ts` 文档一致化）+ `chartDomain` finite-only 守卫（NaN/Infinity 不入域，`typeof value !== 'number' \|\| !Number.isFinite(value)` 跳过）+ 2 回归测试 |
| 单元格权限 `cellPermission` | vxe 无等价物（vxe 无单元格级权限概念——editConfig 只有整表/整列可编辑开关，做不到列内按行授权）。iris 批 BJ 在 `IrisTableColumn` 加 `cellPermission?: (row, column) => 'readonly' \| 'editable'`（`types.ts` 在 `locked` 之后、`preset` 之前，单行函数 prop）——返回 `'readonly'` 即该格只读、`'editable'`/缺省即可编辑（默认）。**与 `locked` 的区别：动态**——predicate 每次渲染重新求值，权限随行/列当前状态变化（行数据更新即翻转，零重挂载）；`locked` 是静态声明。**同一单一咽喉 `isCellReadonly`**（模块级，与 `isCellLocked` 并肩）守卫**每一个写入口**：`beginEdit`（双击/单击触发/F2/Tab 导航/待导航全覆盖，早退在 `onEditStart` 之前）、行模式（`beginRowEdit` 过滤 + 重开分支 + Tab 两移动器带 row 参）、Delete/Backspace 快捷键、粘贴（矩形与流式两分支）、拖拽填充、范围清除、FNR 替换/全部替换、批量编辑（全只读 → **零提交**、面板照常关闭——复用 paste/fill 同款 zero-commit 守卫）。**渲染**：`data-iris-cell-readonly="true"` + **点状纹理**（`radial-gradient(var(--iris-muted-subtle) 1px, transparent 1px) 0 0 / 8px 8px`，8pt 网格）——与 locked 的 45° 斜纹**视觉区分**（动态权限 vs 静态锁定）；cursor `not-allowed`（仅 editable 列）；background-**image** + 渲染尾部**再断言** inline（批 BE review 修复模式逐字复用——`background` 简写会重置 background-image）。**fiats**：locked 与 readonly 同设时 **locked 视觉赢**（条纹 + 仅 locked attr）；读通道 fail-inert（选择/范围/复制/导出/FNR 查找/`data-editable` 属性全不受影响）；动态求值每渲染一次（行数据变化即翻转）；缺省 = 可编辑；core/i18n/manifest 零改动（propCount 不变——列字段，批 BE 先例）；attr 无论列是否 editable 都渲染（只读是权限不是能力）。17 react 新测试（`cell-permission.test.tsx`：默认可编辑/只读不可编辑/视觉区分/双设 locked 赢/行级/列感知/动态重求值/click 触发/F2/Delete/行模式+Tab/批编辑跳过+全只读零提交/范围清除/粘贴/填充/FNR 替换跳过+查找仍命中）。core 零改动、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 152 不变） |
| 确定性数据生成 `generateRows` | vxe 无任何 mock 数据能力（官方示例手写 fixture，无种子/范围/生成概念）。iris 批 BK 在 core 沉淀**确定性 mock 生成器**：`generateRows(schema, count, seed?)`（新 `generate-rows.ts`，**框架无关**、零依赖、无副作用）——schema = `{ key, kind, min?, max? }[]`，kind ∈ `string \| number \| boolean \| date \| email \| phone`；PRNG 为模块内 `mulberry32(seed >>> 0)`（默认 seed **42**——规格 2 参主形零参确定性，传 seed 按需变体），**每次调用自建实例** → 同 `(schema, count, seed)` 三元组跨调用/跨进程字节稳定（无 `Math.random`/`Date.now`/时区，date 为 UTC `YYYY-MM-DD` 字符串）；**行主序抽取**（row×schema 顺序，前缀截断稳定）。**按 kind 语义**（min/max kind 相关，min>max 自动交换、NaN/非有限回落默认；小数 bound 钳到**含整数区间**——`ceil` min / `floor` max，区间内无整数时钉到 `floor(max)`，`intBetween` 永不产出界值）：`number` 整数 [0..1000] 默认 · `string` 小写 a–z 长度 [4..12] 默认 · `boolean` 50/50 · `date` epoch **ms** 界 [2020-01-01..2025-12-31] UTC 默认 · `email` local@固定假域表（local 长度 [4..12] 默认）· `phone` 纯数字、首位 1–9、位数 [7..11] 默认。**守卫全 fail-inert（永不抛错，formula 先例）**：count ≤ 0/非有限 → `[]`、小数 count 取整、空 schema → `[]`、运行时未知 kind → 键在值 null；行是全新对象、键按 schema 序。30 核心新测试（类型 6 kind/范围 7 bound——含小数钳制回归 2/确定性 4 种子/边界 5 count-schema/守卫 5/独立性 3）。core additive、react 零改动（纯 core API，无 prop → propCount 152 不变）、其余框架零改动；manifest 已重新生成（core-only → 无 diff，BF 先例）。**review 修复（随批 BK 门禁提交）**：小数 bound 曾产出界值（探针 `min: 0.5, max: 1.5` → 0/2）——`boundsOf` 改 `ceil`/`floor` 钳制 + 无整数区间钉 `floor(max)`，整数 bound 行为字节不变（seed-42 确定性不受影响）+ 2 回归测试 |
| 性能面板 `perfStats` | vxe 无任何性能统计能力（官方做法是手写 performance.now 探针）。iris 批 BL 做成声明式：`perfStats?: boolean`（默认 off，纯 additive）——开启后**每次渲染提交**采样一次：渲染顶部 `nowMs()` 打点（`performance.now()`，SSR/jsdom 无 `performance` 时 `Date.now()` 回退）+ **无依赖 `useLayoutEffect`**提交后把 `{ durationMs, rows: bodyData.length, columns: leafColumns.length, changes: audit.depth }` 推入core 新控制器 `createPerfStats`（`@iris-ui-kit/core/perf-stats` 独立子路径——**最新快照**控制器，audit 模具 1:1：`latest()/push()/subscribe()/getVersion()`）。**duration = 渲染 + layout 阶段**（layout effect 运行即打止，排除 paint，文档化）。**无回环**：push 只通知面板（独立 portal 根，`useSyncExternalStore`）——表格永不因自身测量重渲（setState-in-effect 会忙循环，AuditPanel 先例逐字复用）。**面板**（NEW `PerfPanel.tsx`，AuditPanel 模具）：工具栏 ⚡ 触发 `data-iris-perf-trigger`（历史触发器之后）→ 浮动面板 `data-iris-perf-panel`（Esc/外部/滚动关闭）4 行统计（`data-iris-perf-duration/-rows/-columns/-changes`：渲染耗时 tabular-nums、行数、**叶列数**（分组 children 平铺）、审计深度）+ **双订阅**（perf + audit 两控制器——`tableRef.clearAuditLog()` 原地刷新变更数；auditLog off → muted `—`）。off = 零成本（门控不推）。+6 i18n 键（en+zh：table.perf/perf.duration/perf.rows/perf.columns/perf.changes/perf.empty）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 152→153） |
| 批注浮动预览 `notePopover` | vxe 无等价物（vxe 的 tooltip 只能显示值本身，无单元格批注概念）。iris 批 BM 给批 AZ 的批注补上**悬停预览**：`notePopover?: boolean`（默认 off，纯 additive）——开启后**有注记的格**悬停显示浮动气泡（`data-iris-note-popover`，`role="tooltip"`、`pointer-events: none` 纯展示、`white-space: pre-wrap` 保留换行）**替代原生 title**：单咽喉 `cellTitle` 仅注记分支变 undefined（compare/sparkline/tooltipConfig 分支与编辑豁免零触碰——未注记格 title 原样；有注记+compare 变更格无 title、气泡喂注记，文档化）。虚拟锚点 = 格右上角（角标处）零尺寸 rect 快照（context-menu 虚拟锚点形状逐字），useFloating placement `top` + offset 8 + flip/shift 开 + useDismiss（Esc / 外部 pointer-down / 任意滚动关闭）+ portal；mouseleave 关闭（native-title 语义）；A→B 移动经 close→reopen 自然重挂载（**无 seq token**，气泡零内部状态，文档化）。动态 `cellNote` 同源喂入。**零 i18n（纯内容）** / 零 core / 零样式表；`props.ts` 单行 prop + `Table.tsx`（模块级 `TableNotePopover` 面板 + `notePopoverCellHandlers` 热格 helper 保持单元格箭头复杂度预算）+ `note-popover.test.tsx` 13 新测试。manifest 已重新生成（propCount 153→154） |
| 行高 `rowHeight` | vxe 的 row-height 是固定配置值，无每行/函数形行高。iris 批 BN 给表格补上**每行行高**：`rowHeight?: number \| ((index: number) => number)`（`props.ts` 在 `virtualScroll` 之后单行 prop，纯 additive）——**单一咽喉 `rowHeight ?? virtualScroll.itemHeight`** 喂三条消费路径：**虚拟路径**的槽高（数字形 → 闭式固定窗口**覆盖默认 itemHeight**；函数形 → 直接作为 `estimateSize` 喂 core 虚拟器的 **Fenwick offset 树（即前缀和）**，O(log n) 累计 top、spacer = 全量总和——批 AG 可变高度机制逐字复用，零 core 改动）、**非虚拟路径**的行内高（数字形 → 每行 `extraStyle.height`，格 stretch + flex-center 零格改动；函数形 → 按 `bodyData` 下标逐行 inline，详情包/组头保持内容高）、**PageUp/PageDown** 步长（±10 × 同一解析源，函数形取当前行高近似同批 AV）。**fiats**：`rowHeight` 与 `virtualScroll.itemHeight` 同设时 rowHeight 赢；函数形虚拟模式收**虚拟 plan 下标**（同批 AG 的 detail 槽交错陷阱——组头/合计等 plan 槽位也按 plan 下标吃同一函数）、非虚拟收 body 下标（**仅数据行**，详情包/组头保持内容高——data-row-only 作用域只属非虚拟路径）；`rowStyle` 仍是每行逃生舱（merge 在后、赢）；未设 = 字节不变既有行为。`props.ts` + `Table.tsx`（模块级 `rowHeightStyleOf` helper + 咽喉 const + 3 触点）+ `row-height.test.tsx` 13 新测试（固定非虚拟 inline / 固定+详情包内容高 / 函数非虚拟逐行 / 无 prop 零高 / rowStyle 逃生舱 / 固定虚拟闭式窗口+覆盖 itemHeight / 无 rowHeight itemHeight 回落 / 函数虚拟前缀和+累计 top / 滚动窗口边界 / 深滚动无漂移 / **plan 下标陷阱**（detail 槽交错）/ PageDown 固定步长 / PageDown 函数形当前行高）。零 core / 零 i18n / 零样式表；manifest 已重新生成（propCount 154→155） |
| 列宽重置 `columnWidthsReset` | vxe 的 columnConfig.width 只能在初始化时声明，运行期无「恢复默认宽度」概念（官方示例手写外部状态重喂）。iris 批 BO 把它做成声明式：`columnWidthsReset?: boolean`（`props.ts` 在 `onColumnWidthsChange` 之后单行 prop，纯 additive）——**工具栏重置按钮**（`data-iris-table-toolbar-reset-widths`，ghost-button 家族，glyph `⇔`，`aria-label`/`title` = i18n `table.resetColumnWidths`，`color: var(--iris-muted)` font-size-md）点击调用 `resetColumnWidths`：**`onColumnWidthsChange?.({})`**——空 map 即规格的「默认映射」：零 override → 每条既有渲染路径（gridTemplate/colTrack/pinned `widthOf`/footer）经现有 `??` 回落到**列声明宽度**（number / `'auto'` → `minmax(max-content, max-content)` / `'1fr'` 串 / 缺省 `minmax(0, 1fr)`），**零新渲染逻辑**。非受控模式同样 `setWidthsInternal({})`（与 `setColumnWidth` 同款双通道——回调无条件触发）。**fiats**：`{}` 为 canonical 默认映射（无需把回调放宽为 `(next?)` 或受控→非受控 prop-drop 契约）；重置目标是**列声明宽度**，非 `defaultColumnWidths` 挂载种子（React uncontrolled 初始值语义，文档化）；按钮独立于 `resizableColumns`（受控宽度照样可重置——resizableColumns 只门控拖拽柄）；不在工具栏渲染门（同 `zoomConfig`——prop 单独不创建工具栏）；persistState 零特例——reset 触发 `onColumnWidthsChange({})` → 快照存 `{}` → 重挂载恢复 `{}` = 默认宽（批 AG 机制逐字）。+1 i18n 键（en `Reset column widths` / zh `重置列宽`）。`props.ts` + `Table.tsx`（destructure + handler + 按钮 3 触点）+ `reset-column-widths.test.tsx` 10 新测试（按钮三态 / 受控回调 `{}` 恰一次 / 父应用 `{}` 回落 number-auto-1fr 混合 / 非受控 116→声明宽（非种子）/ 非受控回调双通道 / 无 resizableColumns 可重置 / 不触发排序 / persistState 存 `{}` 重挂载即默认宽）。零 core 逻辑 / 零样式表 / 其余框架零改动；manifest 已重新生成（propCount 155→156） |
| 单元格复制格式 `copyFormat` | vxe clipboard-config 没有复制输出格式概念（复制固定纯文本，官方示例手写序列化）。iris 批 BP 做成**声明式输出格式**：`clipConfig.copyFormat?: 'tsv' \| 'csv' \| 'html'`（`props.ts` 在 `clipConfig` 内联类型加字段，未设 = TSV 字节不变，纯 additive）——**单一咽喉 `buildRangeCopy`**（`buildRangeTsv` 升级为分发器）服务两消费点（Ctrl/Cmd+C 与范围工具栏 复制按钮），**三个序列化器全复用、零新序列化器**：`tsv` → 既有 `tsvCell`（批 O 字节不变）、`csv` → 既有 `csvRangeCell`（批 AH RFC-4180 + OWASP 中和，headerless 延续范围 fiat，与工具栏 导出 CSV 下载同序列化器）、`html` → **core `toHtml` 复用**（`leafColumns.slice(start.col, end.col+1)` 列子集 + 合成行按 toHtml 实际读取键 key 化——string dataIndex 优先、否则 key，exportCsv 影子行约定逐字；数字右对齐/XML 转义均 toHtml 契约）。掩码三格式一致（批 AY 不变量）；数字被掩成字符串即失 HTML 右对齐（fiat）。**fiats**：非法运行期值 fail-closed 回落 TSV；csv 无表头（范围是单元格矩形非表格视图）而 html 有表头（toHtml 契约）；粘贴不对称（paste 仍按 `\t` 解析，copyFormat 只影响输出）；写路径 text-only（无 ClipboardItem，writeClipboardText 三阶不变）。`props.ts` + `Table.tsx`（import toHtml、分发器、两接线）+ `copy-format.test.tsx` 13 新测试（TSV 字节 pin / 显式 tsv / csv 三要素 / csv RFC-4180 引号+公式中和 / html 全表格片段+数字右对齐 / html 列子集 / html XML 转义 / 掩码三格式一致 / 掩码数字失右对齐 / 工具栏同源 / 非法值回落 / 粘贴不对称 / 无范围 no-op）。零 core / 零 i18n / 零样式表 / 其余框架零改动；manifest 已重新生成（propCount 156 不变——嵌套字段不增顶层成员，clipConfig type 串更新） |
| 编辑自动保存 `editAutosave` | vxe editConfig 无自动保存概念（官方做法是手写 edit-closed 监听再自己拼行列表）。iris 批 BQ 做成**声明式父持久化钩子**：`editAutosave?: boolean`（特性开关）+ `onAutosave?: (rows: Row[]) => void`（`props.ts` 在 `onEditClosed` 之后，回调单行 prop）——**单一咽喉 `commitValue`**（inline 编辑提交的唯一写回漏斗：cell 模式与 row 模式两路汇合）末尾门控触发：提交成功后 fire `onAutosave`，**payload = 提交后行列表**（标准 rowKey 行靠 eager 块同步的 `externalDataRef`，rowId 行加 8 行 `autosaveRows` helper 逐字镜像 updater 的 fallback——按计算键定位行、克隆、置值）。**与 `onDataChange` 的正交性是本批关键事实**：inline 编辑**从不**触发 `onDataChange`（只有 `commitRowList` 发），`onAutosave` 精确填补父持久化钩子缺口；row-list 类写回（paste/fill/FNR/批量）不触发。**fiats**：`editAutosave` 是特性开关、`onAutosave` 单独无效果（keymap 先例）；`value === oldValue` 早退天然覆盖 no-op 不触发；校验失败/Escape 不触发；async 校验落地时触发**一次**；row 模式逐列提交**逐列触发**（载荷累积）；双回调正交（onAutosave 触发时 onDataChange 零调用）；纯回调零 UI/i18n/core。**ref 镜像**（`editAutosaveRef`/`onAutosaveRef`——commitValue 被 `cellEdit` 的 `useMemo([])` 闭包捕获，`auditEnabledRef` 同构）保证 prop 更新后不取陈旧闭包。`props.ts` + `Table.tsx`（destructure + ref 镜像 + helper/咽喉 3 触点）+ `edit-autosave.test.tsx` 12 新测试（规格两项打头：①双击→改值→Enter 触发且载荷为新行列表、②有 `onAutosave` 无 `editAutosave` 不触发；外加 no-op/校验失败/Escape/Tab/row 模式逐列/双回调正交/连续编辑累积/async 落地恰一次/rowId 分支/迟到 prop 更新仍生效）。零 core / 零 i18n / 零样式表 / 其余框架零改动；manifest 已重新生成（propCount 156→158，eventCount 29→30，`onAutosave` 入事件表） |
| 行选择拖拽 `selectionDrag` | vxe 无鼠标拖拽勾选能力（checkboxConfig 只有点击/shift-click，官方做法是手写 mouse 事件）。iris 批 BT 做成**声明式行选择拖拽**：`selectionDrag?: boolean`（`props.ts` 在 `checkboxRange` 之后单行 prop，纯 additive）——multi 模式在行选择格（`__selection`，flat/virtual/分组三路径同源）按下**不立即选中**，pointermove 超 **4px 阈值**（row-drag 对齐）进入拖拽：`document.elementFromPoint` → `closest('[data-iris-table-row]')`（range-fill 先例——组头/详情槽无该 attr、汇总/表尾键不可解析 → 天然忽略，非行区域保持上一范围）→ String 键域 `findIndex`（rowDrag/scrollToRow 同款，数字/字符串键不混型）→ **锚点与悬停行闭区间**，checkMethod 合格行**追加勾选**（`seen` 单调并集 → `rebaseToProp()` + `selModel.set([...display, ...add])`，selectAll 加法先例——反向拖拽/区间回缩**永不取消勾选**）；**pointer capture 延迟到拖拽开始**（review 门禁修复——裸按下不 capture，否则 capture 会把 pointerup→click 重定向到格 div、label 永远收不到点击、`selectionDrag` 开启后普通点击无法勾选行；超过阈值才 capture 按压格，释放出根仍收尾清理）。**点击抑制**复用 shift-click 同一机制（cell onClick `preventDefault` 取消 label→input 转发及 change——拖拽后的 trailing click 不二次翻转锚点行）；普通点击（<4px 无拖拽）照常单选一行；受控无乐观翻转（父拒收 UI 不变）。fiats：无 prop 惰性、single no-op、禁用锚点可起拖、树模式按展平行、跨页键加法保留。`props.ts` + `Table.tsx`（refs/三 handler + cell pointerdown + onClick 抑制 + 表根三 pointer 门扩为四拖拽分发）+ `test/selection-drag.test.tsx` 15 新测试（review 门禁补 capture 时序/pointercancel 清臂）。零 core / 零 i18n / 零样式表 / 其余框架零改动；manifest 已重新生成（propCount 159→160，eventCount 30 不变） |
| 表格水印 `watermark` | vxe 无任何水印能力（官方做法是手写绝对定位 overlay，且从不感知表格自身的滚动/粘性层级）。iris 批 BU 做成**声明式背景水印**：`watermark?: string`（`props.ts` 在 `showFooterOverflow` 之后、`style` 之前单行 prop，纯 additive）——非空时在根内渲染**sticky 首子旋转层**（`data-iris-watermark` 包裹层 → `data-iris-watermark-overlay` → 72 平铺 tile `data-iris-watermark-tile`，**DOM 结构与独立 IrisWatermark 原语同构**——全局 `[data-iris-watermark]` 选择器四处匹配同一元素形状；**嵌入根内而非包裹根**——原语 wrap 会破坏 fixed-height 滚动容器/sticky 表头，文档化分歧；attr/视觉复用原语 72 tile + `rotate(-22deg)` + `--iris-muted` token 色 + opacity 0.15 + `--iris-space-xl`/`--iris-font-size-lg` token 值，`aria-hidden`（overlay）+ `pointer-events`/`user-select` none 纯展示零交互）。**review 门禁修复（HIGH）**：层是根（即滚动容器）的首子元素且 `position: sticky; top: 0; height: 100%`——normal position 在内容顶端、`top: 0` 自滚动起点即锚住视口，**实测**（Chrome/Playwright）滚动 1500px 后层 top 不变、行已上移；absolute inset-0（或置于行尾的 sticky）会随内容滚走。**绘制序实测**：盖静态行/表尾/分页器，低于 sticky 表头 z2、固定列 z1、浮动面板 popover 1000。**根锚定**：`...style` 后强制 `position: relative`（zoom 健壮性先例——caller style 不能拆锚；zoom `position: fixed` 仍赢，水印随覆盖层）。空串/无 prop = presence 门控零节点（零成本）；rerender 逐 tile 更新文本。`props.ts` + `Table.tsx`（destructure + 模块 helper `renderTableWatermark` + 根 style + overlay JSX 触点）+ `styles.ts`（`WATERMARK_WRAPPER_STYLE`/`WATERMARK_OVERLAY_STYLE`/`WATERMARK_TILE_STYLE` 三常量）+ `test/watermark.test.tsx` 10 新测试（无 prop 零节点/渲染含文本+overlay 同构/aria-hidden+不可交互/rotate(-22deg)/token 色/锚定相对含 caller style 覆盖/空串门控/rerender 更新/zoom 共存/fixed-height sticky 首子锚定）。零 core / 零 i18n / 零样式表注入 / 其余框架零改动；manifest 已重新生成（propCount 160→161，eventCount 30 不变） |

| 校验统计 `validationSummary` | vxe 无等价物（vxe 校验失败只显示编辑器内错误消息，从不汇总提交结局）。`validationSummary?: boolean`（`props.ts` 在 `freshness` 之后单行 prop，纯 additive）：开启后工具栏渲染 **editRules 列提交结局账本**（muted stamp `data-iris-validation-summary`，freshness 同款样式——`--iris-font-size-xs`/`--iris-muted` token；i18n `table.validationSummary` en `Passed {ok} · Failed {fail}` / zh `通过 {ok} · 失败 {fail}`，**+1 i18n 键 en+zh**）：ok = 提交经 `editRules` 校验通过并**落地**（cell/row 两模式的 `onCommit` 包装器计数），fail = 提交尝试被**校验拒绝**（`validate` 包装器 Promise `.then` 计数）。**commit-intent 标记**（`commitWithSummaryIntent` 包装全部 5 个提交入口——cell `commitEdit` 包装器 / Tab 的 editRules 分支 / row 模式的 Enter、Tab、行切换——置位，validate 包装器**同步消费**）把提交时刻与 setDraft 打字瞬时校验分开：打字/startEdit 种子永不计数；**只计 `editRules.length > 0` 的列**（模块级 `hasEditRules` 单一咽喉）——legacy `validate` 列、paste/fill/FNR/批量面板旁路（commitRowList 直写）、Escape 取消均不计；异步 validator 恰一次（intent 在 commitEdit 内同步消费，`.then` 计数不受后续 setDraft 影响）。计数经 `validationSummaryRef` ref 镜像门控（memo 闭包不读陈旧 prop，editAutosaveRef 先例），**重新开启清零**；显示条件 = 开启且 ≥1 个结局已计（spec「提交失败时」场景被包含）；位置在 perf 钮后、custom buttons 前，工具栏门控列表加 `validationSummary`（freshness 同款——prop 单独即开工具栏）。core 仅 i18n 默认键、zh 插件 +1、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 161→162，eventCount 30 不变） |

| 右键快捷操作（复制值/清空） | vxe 无等价物（右键菜单只执行用户定义动作，从不内置常用单元格操作）。iris 批 BW 在 **每个** contextMenu 上**无条件内置**两项快捷操作（**无新 prop**）：**复制值**（key `__iris-copy-value`，i18n `table.copyValue` en `Copy value` / zh `复制值`）——把单元格**显示文本**写入剪贴板，模块 helper `contextCellText` 走 `applyCellMask` 掩码 → formatter → `String`（null/undefined → `''`）——与 `cellTooltip` 同源 = 代码库既有「单元格文本」链，复制所见即所得；经既有 `writeClipboardText` 三通道（注册 handler → `navigator.clipboard` → 隐藏 textarea `execCommand`），**无剪贴板环境安全 no-op**。**清空**（key `__iris-clear-cell`，i18n `table.clearCell` en `Clear cell` / zh `清空`）——`setCellValue(current, rowKey, k, col.key, '')` → `commitRowList(next)`（**Delete 快捷键先例逐字**，同一 undo/audit/onDataChange 漏斗），locked/readonly 格 no-op（`isCellLocked`/`isCellReadonly` 单一咽喉）；**无 onDataChange 惰性**：内部 liveData 仍更新不崩。插入点 = `handleContextMenu` 用户项 → 分布? → 摘要? → **复制值 → 清空** → 批注?（「排在批注项前」）；两键 onSelect 接线拦截**永不达用户回调** + 保留键去重守卫（分布/摘要/批注同款）。core 仅 i18n 默认键 +2、zh 插件 +2、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 162 / eventCount 30 不变） |
| 列固定快捷切换 `columnPinMenu` | vxe 无等价物（vxe 列固定是静态配置 `fixed`，从不提供运行时切换入口）。iris 批 BX 在**列头右键菜单**内置固定/取消固定（**iris 独有**，与 `contextMenu` 完全解耦——它只开在 body 格上；`columnPinMenu?: boolean` 独立门控，contextMenu 未配置也启用）：右击列头浮出菜单（复用 TableContextMenu 宿主 + 虚拟光标锚点，Esc/外点/滚动关闭，**独立浮动实例**——开其一关另一，DOM 至多一个 `data-iris-table-context-menu`），按列**当前固定态单例互斥出项**——未固定 → **固定左**（key `__iris-pin-left`，i18n `table.pinLeft` en `Pin left`/zh `固定左`）；已固定（左**或**右）→ **取消固定**（key `__iris-unpin`，`table.unpin` `Unpin`/`取消固定`；spec 无「固定右」项）。变更经 **`onColumnPinnedChange?: (key, side \| null)`** 双通道回调（`onColumnWidthsChange` 先例逐字：受控/非受控都无条件触发）+ **内部 fallback**（`pinnedColumns` 缺省时表内 `pinnedInternal` 落盘，静态 `col.pinned` 为种子回落；受控 `null` 覆盖静态声明、受控无乐观翻转、非受控立即翻转、右击零副作用不排序）。**单一咽喉 `pinOf(col)`** 替换全部 4 处 `col.pinned` 读取点（pinnedOffsets、虚拟列 union、body attr、flat 头 attr），`pinnedStyle` 按 key 天然继承——grouped 叶子头补齐固定视觉（先前 flat/grouped 不对称）。+2 i18n 键（en+zh）、core 仅 i18n 默认键、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 162→**165**、eventCount 30→**31**） |
| 列宽双栏拖拽（`columnDrag` 拖出固定） | vxe 无等价物（vxe columnDragConfig 只支持拖拽**排序**——列固定是静态配置 `fixed`，拖拽永不触发固定，官方无「拖出边缘即固定」交互）。iris 批 CH 给批 3/Y/AB 的 `columnDrag` 补上**拖出固定**：拖拽列头越过表格**左边缘外**松开 → 自动固定左（spec「与 columnPinMenu 组合」）。**零新 prop**——复用批 BX pin menu 的既有**双通道咽喉**（`pinOf` 读源 + `setColumnPinned` 双通道 + `onColumnPinnedChange` 回调），拖拽只是进入同一通道的第二个手势；零新 state、零新 i18n（`table.pinLeft`/`unpin` 已有）。**机制**（`handleColDragPointerUp` → `resolveColDrag(x, _y)`，Table.tsx 模块级 + 纯 helper `isColDragOutLeft(x, rootLeft)`）：**边缘检查优先**——释放坐标 `x < rootRect.left` → `setColumnPinned(key, 'left')`（已左 no-op、右→左翻转、**永不重排**、受控无乐观翻转）；否则既有 closestCenter 重排路径**字节不变**。**门控 `columnDrag && columnPinMenu`**——纯 `columnDrag`（vxe parity）保持字节相同、**零全局钩子**。**窗口级释放**（新 window `pointerup`/`pointermove`/`pointercancel` effect，门控 `colDragActive`、render 赋 ref 的新鲜闭包）：覆盖**根外释放**——此前拖出根外会把列拖拽控制器卡在 `activeId`（stuck-state 修复，`end()` 捕获即清），根内 `onPointerUp` 同传释放坐标。`test/column-pin-drag.test.tsx` **13 新测试**（react 2454→**2467**：受控固定+回调且不重排 / 非受控内部态 / 双 fail-closed（仅 columnDrag、仅 columnPinMenu）/ 已左 no-op / 右→左翻转 / 带内重排回归 / 同列 no-op / 窗口 pointerup 含 stuck 修复 / tap 取消 / pointercancel 中止 / 受控无乐观翻转 / 分组头仅叶列）。core **零改动**（framework-free 不变量：core 0 框架 import）、styles/i18n/types 零改动、其余框架零改动；manifest 已重新生成（propCount **169** / eventCount **31** 不变——`columnDrag` 描述文本 only，双生成器位置）。**批 DC（iris 独有）**把落点**钳进拖拽列自己的固定区**（`clampReorderZone` 纯函数，`zone = pinOf ?? 'free'`，同 pinOf 咽喉）——冻结列只能区内重排、跨区落点钳到区首/区尾（净零跳过 `onReorder`），`[left][free][right]` 分区恒定，零固定表字节不变（vxe 重排 parity 不受影响） |
| 行克隆 `cloneRow` | vxe 无等价物（vxe 行操作只有内置 insert/remove 按钮与 `insertRow`/`removeRow` 方法，**无克隆行**——复制行只能手工读字段再插入）。iris 批 CI 给 `IrisTableHandle` 补上 `cloneRow(key: string | number, index?: number): void`（单行 prop 形，manifest 不可见，handle 方法先例）。**core**：新纯函数 `cloneRowInList(rows, rowKeyField, key, index?)`（`table-rows.ts`，与 insert/remove/update 三兄弟同契约——不可变、缺键返回原引用**静默 no-op**）——克隆 = `{ ...source, [rowKeyField]: max+1 }` **浅拷贝全部字段**到新对象 + 复用 `nextAutoId` 新鲜 `max+1` 键（fresh key 保 selection/dirty/key 寻址健全，不选中）；**默认插入源行之后**（sourceIndex+1）、显式 index 钳制 `[0, length]`（末行追加/首行 index 1 均测）。**react 接线**：`next !== rows` 才 `commitRowList(next, 'insert')`——**一次漏斗**：onDataChange 恰一次、可 undo、audit `type:'insert'`、更新/删除集成不破坏。`test/clone-row.test.tsx` **14 新测试**（克隆内容 / 默认插入位置 / 显式钳制 / 缺键静默 / 字符串 key / 非 max 源行 / 选择不动 / onDataChange 恰一次 / undo / audit insert / 脏点语义 / update·remove 集成，react 2467→**2481**）+ core 9 新测试（core 1533→**1542**，精确 baseline 预测）。**additive only**：6 文件（2 core + 1 react 类型 + 1 react 实现 + 2 测试），零 props/i18n/styles/events/audit-enum 变更、vue/solid/svelte 零触碰（批次既有 react-only 约定）、styles 零改动；manifest **零 diff**（propCount **169** / eventCount **31** 不变，check:manifest up to date） |
| 表格快捷键提示 `shortcutHints` | vxe 无等价物（vxe 键盘快捷键零帮助 UI——官方不提供任何快捷键提示入口，只能查文档）。iris 批 CJ 在工具栏 perf 触发之后加 **`?` 触发钮**（`data-iris-shortcut-hints-trigger`，aria/title = `table.shortcuts`）开浮动面板 **`ShortcutHintsPanel`**（AuditPanel 同款：useFloating bottom-end + portal，Esc/外点/滚动关闭 + 触发钮豁免）——列出全部 **8 个内置快捷键**的**有效键**（F2 编辑 / Delete·Backspace 清空 / Ctrl+Z·Ctrl+Y(+Shift+Z) 撤销重做 / Ctrl+C 复制 / Ctrl+V 粘贴 / Ctrl+D 一步填充 / Ctrl+K 查询聚焦，i18n 表头 + 8 动作标签 en+zh 全量覆盖 + 占位符 parity）。**单一真相源**：面板消费 `keyBindings` memo——与全部 8 个处理器匹配的**同一归一化映射**，`keymap` 重绑**实时反映**、非法覆盖 fail-closed 回落默认，列出的键**一定实际可用**；只读参考（无重绑 UI）。**core** +2 纯格式化函数（`keymap.ts` 框架无关）：`formatKeyBinding`（修饰符序 `Ctrl+`/`Shift+`/`Alt+`、`' '`→`Space`、单字母大写、`f2`→`F2`/`delete`→`Delete`、ctrl-or-meta 共用旗标 → `Ctrl`）+ `formatKeyBindings`（`' / '` 连接、空→`''`）；+9 i18n 键 en+zh。core **6** + react **11** 新测试（fail-closed / 工具栏门控 / 8 动作精确默认串 / 整组覆盖丢 Backspace 别名 / 修饰符重绑显示 / 非法覆盖 fail-closed / Esc / 外点关 + 触发钮 toggle / zh 标签 / **面板开着时 live remap** / 只读无控件）；core 1542→**1548** · react 2481→**2492**。**additive only**（源码仅 baseline 指定 1 行门编辑）、core 框架无关（0 框架 import）、react-only 桥、其余框架零改动；manifest 已重新生成（propCount **169→170** / eventCount **31** 不变 / tokens 86 不变） |
| 单元格文本搜索高亮 `searchHighlight` | vxe 无等价物（vxe 无内联搜索高亮——单元格只是纯文本，除非查找栏打开）。iris 批 CK 加 **`searchHighlight?: string`**（`props.ts` 在 `fnr` 之后单行 prop，纯 additive）：对每个文本格的**显示链**（mask → formatter ?? raw——autoLink 消费的同一条文本）做**大小写不敏感的字面子串搜索**（core `splitSearchHits`：无正则、`indexOf` 前向扫描，**非重叠**（`'aaa'` + `'aa'` = 一个命中）、奇偶段交替且**奇数下标 = 命中**、空 query/空文本/无命中 → `null` fail-closed）——每处命中渲染为内联 **`<mark data-iris-search-hit>`**（`SEARCH_HIT_STYLE` 用 **`--iris-surface-selected`** 同款 token——fnrCellStyle 同一种搜索高亮语言，区别在**机制**而非查询：fnr = 整格背景 + 替换/导航工具，searchHighlight = 纯内联 mark，无栏/无匹配状态/无回写）。**单一咽喉 `applySearchHighlight`**（Table.tsx 模块级，非字符串/null 原样直通）：**恰好两处调用**——formatter 分支与 raw 回落分支，位于 `mask → formatter ?? raw` 链尾，drop-in 替换纯文本路径（无 prop 字节相同）；render/html/link/sparkline/autoLink 单元格零触碰（autoLink 赢，文档化 fiat）。**core** +1 纯函数（`search-highlight.ts` 框架无关）+ 11 核心新测试（基础命中/大小写不敏感/`'aaa'`+`'aa'` 非重叠/相邻命中空间隙/文本以命中开头/无命中 null/空 query null/空文本 null/多命中/特殊字符字面量匹配/与 fnr 区分共存 T10——fnr 高亮格 + searchHighlight 内联 mark 同存）。core **1548→1559** · react **2492→2503**（22 新测试）。**additive only**（10 文件：2 core + 1 react 测试 + 3 react 触点 + manifest 再生成 + 2 文档）、core 框架无关（0 框架 import）、react-only 桥、vue/solid/svelte 零改动、styles/i18n/types 零新契约；manifest 已重新生成（propCount **170→171** / eventCount **31** 不变） |
| 行展开动画 `expandAnimation` | vxe 无等价物（vxe 展开无任何过渡——行瞬间出现/消失）。iris 批 CL 加 **`expandAnimation?: boolean`**（`props.ts` 在 `lazyLoad` 之后，默认 off fail-closed）：详情面板与树行**展开入场动画**——max-height `0 → var(--iris-table-expand-max, 512px)` + opacity `0 → 1`，纯 CSS 键帧（**零 state/effect/handler/i18n/core**），from/to 双端 `overflow: hidden` 动画结束回基态（内容永不永久裁切）。**token 杠杆**：时长 = motion 包四框架共用 `--iris-duration-md`（fallback 200ms）、上限 `--iris-table-expand-max`（fallback 512px）；**reduced-motion 关闭** = `@media (prefers-reduced-motion: reduce) { animation: none }` 纯 CSS 翻转（floating/animations.ts 先例）。**两个注入点共用 `expandAnimOn = expandAnimation === true && !virtualScroll`**：详情包裹（`renderBodyEntry`）`data-iris-expand-anim` + 树行（`renderRow`）**depth > 0 门**（root 行不动）——helper `expandAnimAttr`（`rangeFillTargetAttr` 先例）保行箭头复杂度预算；**两个显式 fiat**：虚拟模式惰性（滚动挂载重放动画 + 冲击热路径）、收起瞬时卸载（真正收起过渡需保持挂载，与虚拟器均高槽模型冲突——收起 affordance 沿用 caret 旋转）。纯 CSS 注入活单例 `iris-table-row-styles`（batch-BE 教训：styles.ts copy 是 dormant）。`expand-animation.test.tsx` **8 新测试**（fail-closed 双查 / detail 展开收起 / keyframes / duration token / reduced-motion 块 / 树 depth 门 / 初始展开重放 / 虚拟惰性双路径）。react **2503→2511** · core **1559 不变**（零改动）、i18n/types/事件零改动、三框架零改动；manifest 已重新生成（propCount **171→172** / eventCount **31** 不变） |
| 列合计行粘性 `summaryRowStyle` | vxe 无等价物（vxe 汇总行不可吸附——页脚是普通布局流，官方无 sticky 配置）。iris 批 CM 加 **`summaryRowStyle?: 'default' \| 'sticky'`**（`props.ts` 在 `footerAlign` 之后，footer 家族，默认 `'default'` fail-closed）：`'sticky'` 让**全局列合计行**吸附 fixed-height 滚动容器底缘（`position: sticky; bottom: 0`；CSS 规则 `[data-iris-table-fixed-height] [data-iris-summary-sticky="true"] { position: sticky; bottom: 0; z-index: 1 }`，与表头粘性 z2/钉列 z1 层级同构，注入活单例 `iris-table-row-styles`——行已有不透明 `var(--iris-surface)` 背景 + 2px 顶边，**零 inline style 改动**）。**判别 `summaryRowStyle === 'sticky' && groupKey === undefined`**（`renderSummaryRow` 行 div，groupKey 精确区分全局/组汇总——组汇总行也是 root 直接子节点，DOM 位置无法区分）；`renderFooterStack` 的 footerMethod 替换行同加（占据同一汇总槽位，无 groupKey 作用域按构造恒为全局）；**两个显式 fiat**：组汇总行不粘（多组互相争夺底缘）、footerData 不粘（契约上渲染于汇总之下）。虚拟滚动下全局汇总行在 `IrisVirtualScroll` 之外，天然不受影响。`summary-sticky.test.tsx` **8 新测试**（默认 fail-closed 双查 / 显式 default / sticky 属性+聚合不变 / 样式规则（选择器+sticky+bottom:0+z-index） / footerMethod 槽位+footerData 排除 / 组汇总排除 / 虚拟滚动保留 / 非法值 fail-closed）。react **2511→2519** · core **1559 不变**（零改动）、i18n/types/事件零改动、三框架零改动；manifest 已重新生成（propCount **172→173** / eventCount **31** 不变） |
| 批 CN | **iris 独有增量（react，见「iris 独有」节）**：**单元格拖拽移动 `cellDrag`**（vxe 无等价物——vxe 无单元格移动，仅行/列拖拽）：`cellDrag?: boolean` 单行 prop（`props.ts` 在 `rangeFill` 之后，range 家族，默认 off fail-closed）——`cellRange` 有活选区时，范围**左上角格顶缘**渲染 `data-iris-range-move` grip（12×4 主色 pill、`cursor: move`、z 3；`RANGE_MOVE_STYLE` 活样式，left/right 0 + auto margin 居中 RTL 中性——填充句柄 + charCount 徽标占右下、顶缘空闲）；grip pointerdown `preventDefault + stopPropagation`（fill AQ 先例，格 `startRange` 点击永不触发）。**拖拽流**与 fill AQ 逐行同构：`elementFromPoint` → `closest('[data-iris-cell-row][data-iris-cell-col]')` 命中、越界保持最后解析端、pointerup 重臂 range dismissal（AQ 回归修复）、pointercancel 零提交；根 pointermove/up/cancel 门拓宽 + window-capture dismissal 抑制监听拓宽 `[data-iris-range-fill], [data-iris-range-move]`。**提交 `moveRangeFromHandle`**（fillRangeFromHandle 镜像、byKey-patch 形）：**越界 clamp** `dstRow = clamp(targetRow, 0, body.length − h)` / `dstCol = clamp(…, 0, cols.length − w)`——整块恒落表内（Excel 对齐）；clamp 后 == 源 → **零提交 no-op**；**剪切两阶段 ONE `commitRowList(next, 'edit')`**（batch AL undo 漏斗）：可写源→可写目标写入 + 未覆盖源单元格清空——重叠安全原子滑动；公式列不读/写/清、locked/readonly 两阶段存活（batch BE）、keyless 行跳过；**选择跟随移动块**（`startRange`+`extendRange`+`updateRangeToolbarAnchor`，Excel 对齐）。三个显式 fiat：无 dest 预览（spec 静默）、单顶缘 grip 非四边区域、选择跟随块。`cell-drag-move.test.tsx` **16 新测试**（fail-closed 双查 / grip 渲染+按压存活 / 移动三例：单格·2×2 块·重叠下滑 / 越界三例：下·上·右 clamp / pointercancel / 按压即放 no-op / 公式列跳过 / locked 存活 / keyless no-op / undo）。core **1559 不变**（零改动）、react **2519→2535**、i18n/types/事件零改动、三框架零改动；manifest 已重新生成（propCount **173→174** / eventCount **31** 不变） |
用法示例：

```tsx
// 列预设：声明式显示默认，用户字段优先
const columns = [
  { key: 'price', title: 'Price', preset: 'money' }, // 1,234.50 · 右对齐 · number 编辑器
  { key: 'progress', title: 'Progress', preset: 'progress' }, // 42%
  { key: 'created', title: 'Created', preset: 'date' },
  { key: 'status', title: 'Status', preset: 'status' }, // ACTIVE
]

// 表格快捷键：F2 编辑 / Delete·Backspace 清空，需 keyboardNavigation 的 roving 焦点
<IrisTable
  columns={columns}
  data={rows}
  keyboardNavigation
  tableShortcuts
  onDataChange={setRows}
/>

// 公式列：total = price × qty，排序/筛选/汇总/导出全用计算值；editable 公式列只读
const formulaCols = [
  { key: 'name', title: 'Name' },
  { key: 'total', title: 'Total', formula: 'price * qty', sortable: true },
  { key: 'balance', title: 'Balance', formula: '=SUM(price, qty) * 2' },
]

// 行列引用：表头 A/B/C… 字母徽标 + 前导行号列（seq 开启时 seq 即行号，不重复）
<IrisTable columns={formulaCols} data={rows} showCellRefs />

// 跨表引用公式：`=rates!rate` 读 formulaTables['rates'] 首行的 rate（缺失表/字段/空表 → 整式 null）
const rateTable = [{ rate: 1.5 }] // 汇率表（只读首行；变更时传新对象引用即重算）
const formulaColsWithRates = [
  { key: 'name', title: 'Name' },
  { key: 'taxed', title: 'Taxed', formula: 'price * rates!rate', sortable: true },
]
<IrisTable columns={formulaColsWithRates} data={rows} formulaTables={{ rates: rateTable }} />

// 协作光标：声明式渲染远程协作者的选中格（纯展示——新数组引用即重渲，原地突变不重渲）
const remoteCursors: IrisTablePresenceEntry[] = [
  { id: 'alice', name: 'Alice', color: '#3BA7FF', cellKey: '1::name' },
  { id: 'bob', name: 'Bob', color: '#FF9A3C', cellKey: '1::name' }, // 同格堆叠：首条目描边色赢，角标级联
]
<IrisTable columns={cols} data={rows} rowKey="id" presence={remoteCursors} />

// 单元格标注：静态表按 `${rowKey}::${columnKey}` 键控；cellNote 动态回调赢静态表
<IrisTable
  columns={cols}
  data={rows}
  rowKey="id"
  annotations={{ '1::name': 'VIP customer' }} // 角标 + title = 注记（赢过 tooltipConfig）
  cellNote={(row, col) => (row.age > 30 ? `over 30 (${col.key})` : null)} // 动态优先
/>

// 版本历史：每个行列表提交推「变更前」版本，面板点击条目即回退（不产生新版本）
<IrisTable
  columns={cols}
  data={rows}
  rowKey="id"
  versionHistory={{ max: 20 }} // ⏱ 工具栏触发器 + 浮动面板；默认 20，max: 0 无限
  tableRef={r}
/>
// r.current.getVersions() // 轻量快照 [{ index, at, type }…]（无 rows）
// r.current.restoreVersion(0) // 回到首次提交前的行列表（可审计 + 可撤销）

// 批注编辑：右键菜单添加/编辑/删除批注，写通道完全受控（annotations 仍是唯一读源）
<IrisTable
  columns={cols}
  data={rows}
  rowKey="id"
  annotations={notes} // 读源：角标 + 悬停提示（批 AZ）
  annotationEditing // 右键菜单在摘要项后追加 添加/编辑/删除批注
  onAnnotationsChange={(next) => setNotes(next)} // 保存/删除写回（空文本删键）
  contextMenu={{ items: () => [], onSelect }} // 依赖 contextMenu
/>
```

以下为**有意跳过项（文档化决策，非能力缺口）**：

| 项                                 | 状态         | 决策理由                                                                                                                                           |
| ---------------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `fit`                              | 跳过（弃用） | vxe 已弃用，官方建议用 height 组合替代；iris 以 height/minHeight/maxHeight + autoResize 覆盖                                                       |
| `animat` / `delayHover` / `params` | 跳过（内部） | 无 UI 语义的内部渲染钩子/默认参数，不构成可对齐行为                                                                                                |
| 虚拟树（tree + virtual 组合）      | ✅ 已完成    | 批 AE（react 参考语义）：flat/tree × detail 全组合 × virtual 窗口正确 + 展开/收缩滚动重钳制；vue/solid/svelte 组合护栏交接见构建状态表             |
| zoom 与列设置面板同开              | 已知差异     | 列设置浮层打开时按 Esc 先关浮层、再按一次退出缩放（浮层 Esc `stopPropagation` 的预期行为）；浮层随工具栏提升，不会被覆盖层遮挡（批 U review 修复） |

## vxe-grid 功能清单（VxeTableProps + VxeGridProps）

### 1. 编辑（editConfig / editRules / validConfig）

- **editConfig**：`trigger: 'click'|'dblclick'|'manual'`、`mode: 'cell'|'row'`、
  `autoFocus`、`autoClear`（切格自动清未提交）、`showStatus`（updated/inserted 状态图标）、
  `showAsterisk`（必填星号）、`showUpdateStatus`、`cache`
- **editRules**（列级规则集）：`required` / `min` / `max` / `type: number|string|array` /
  `pattern`（正则）/ `validator`（同步|异步）/ `message` / `trigger: blur|change|manual`
- **validConfig**：校验模式（type: 'one'|'all'、错误提示位置）、`showMessage`（false 时仍阻断提交 + 保留 aria-invalid，仅隐藏错误文案，批 F ✓ react）
- **undoRedoHistoryConfig**：编辑撤销/重做（core 已有 undo 可组合）

### 2. 选择

- checkboxConfig：多选（reserve 跨页保留、range 框选、checkMethod、showHeader）
- radioConfig：单选（row、checkMethod）
- 选择方法（批 F ✓ react）：`selectAll`（全选，遵守 checkMethod）/ `toggleRowSelection`（单行切换，绕过 checkMethod）/ `clearSelection`（清空）

### 3. 滚动 / 虚拟

- scrollX / scrollY（固定高度滚动）、**virtualXConfig / virtualYConfig**（虚拟滚动，
  含增强模式 vs 固定行高）

### 4. 列能力

- resizableConfig（列宽拖拽，minWidth/maxWidth）、columnDragConfig（列拖拽排序）、
  columnConfig（visible/fixed/formatter/width 等）、visibleMethod（列可见谓词，批 U ✓ react）、seqConfig（序号列）、
  mergeCells / spanMethod（合并单元格）、mergeHeaderCells（表头合并，批 P ✓ react）、
  customConfig（列自定义面板：搜索/拖拽排序/可见性切换/重置，批 S ✓ react）

### 5. 行能力

- rowConfig（keyField/isHover/useKey）、rowDragConfig（行拖拽排序）、
  expandConfig（展开行；`expandAll` 一次性全展开，批 F ✓ react）、rowGroupConfig（行分组）

### 6. 排序 / 筛选

- sortConfig（多列、remote、sortMethod）、filterConfig（本地/远程筛选、
  resetMethod、筛选类型）、floatingFilterConfig（浮动筛选）
- multiSort（批 F ✓ react）：`multiSort` 多列排序（点击追加/原地升→降/移除，chronological 点击序，
  非主排序列序号徽标，链式稳定比较器，代理发 `sorts` 数组）

### 7. 数据代理（proxyConfig）

- query/insert/remove/save ajax 代理、响应解析、表单联动

### 8. 工具栏 / 表单 / 缩放

- toolbarConfig（内置按钮：refresh/import/export/zoom/custom）、formConfig（搜索表单）、
  zoomConfig（全屏）、layouts

### 9. 导入导出 / 打印

- exportConfig（csv/xls/xlsx/html/pdf）、importConfig（文件导入）、printConfig（打印）

### 10. 交互增强

- mouseConfig（鼠标选中/框选）、areaConfig（区域复制）、keyboardConfig（键盘导航）、
  fnrConfig（查找替换，批 O ✓ react）、menuConfig（右键菜单）、clipConfig（批 O ✓ react）、tooltipConfig（单元格，批 G ✓ react；表头/表尾提示，批 P ✓ react）、emptyText、
  loadingConfig、footerMethod（汇总）、currentRowConfig

## iris-ui 现状对比

| 类别          | vxe-grid                                                        | iris-ui IrisTable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 差距                                                                                                            |
| ------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 编辑          | editConfig+editRules 规则集                                     | editable 列 + validate 回调 + validConfig.showMessage（批 F）+ editConfig.mode='row'（批 K，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 缺规则集/click 激活/状态图标                                                                                    |
| 编辑脏标记    | editDirtyConfig                                                 | editDirtyConfig ✓（批 Q，react；commitValue 单一漏斗记录首次提交原值，`data-iris-cell-dirty` 圆点 + `indicator:false` 抑制 + `className:true` 加 `iris-table-cell-dirty` 类，提交回原值自动清除，removeRow/removeRows 剪枝脏键）                                                                                                                                                                                                                                                                                                                                                                                                                        | 对齐（react；vxe keep-fields 语义 vs 差异标记）                                                                 |
| 选择          | checkbox/radio                                                  | selectable multi ✓ + 方法 selectAll/toggleRowSelection/clearSelection（批 F，react）+ radio 单列 selectable='single'（批 T，react；原生 input[type=radio] + --iris-primary accent）                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 对齐（react）                                                                                                   |
| 虚拟          | virtualX/virtualY                                               | virtualScroll ✓                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 基本对齐                                                                                                        |
| 固定高度      | scrollX/scrollY（固定高度）                                     | height/minHeight/maxHeight ✓（批 N，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 对齐（react；root 滚动容器 + sticky 表头）                                                                      |
| 自适应高度    | autoResize                                                      | autoResize ✓（批 Q，react；RO 测量仅门控 fixedHeight 滚动机制，无显式 height 时 root `height: 100%` 填充并持续追踪父容器）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 对齐（react）                                                                                                   |
| 滚动条配置    | scrollbarConfig                                                 | scrollbarConfig ✓（批 Q，react；theme:'thin' → `data-iris-scrollbar-thin`：6px webkit 滚动条 + FF `scrollbar-width: thin`，覆盖 root 与虚拟滚动子容器）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 对齐（react；vxe 自绘滚动条 vs 原生 thin）                                                                      |
| 列宽          | resizableConfig                                                 | resizableColumns ✓ + width:'auto' 自适应（批 M，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 对齐                                                                                                            |
| 列拖拽        | columnDragConfig                                                | columnDrag ✓（批 3 react + 批 Y vue + 批 AB solid；core createSortable 桥：press → 4px 阈值 → closestCenter → 落点 → onReorder；分组表头不支持——文档化）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 对齐（react+vue+solid）                                                                                         |
| 行拖拽        | rowDragConfig                                                   | rowDrag ✓（批 2 react + 批 Y vue + 批 AB solid；手柄 `data-iris-row-drag-handle` + 容器指针事件，代理 liveData / 本地 override 重排 + onDataChange/onReorder）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 对齐（react+vue+solid）                                                                                         |
| 序号列        | seqConfig                                                       | seq ✓（批 L react + 批 Y vue + 批 AA solid + 批 AC svelte；含 proxyConfig.seq 代理累计序号）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 对齐（四框架）                                                                                                  |
| 合并单元格    | spanMethod                                                      | spanMethod ✓（批 3 react + 批 Y vue + 批 AA solid + 批 AC svelte；resolveSpan + 每趟 body 清理占用集（键控 bodyEntries 引用 + spanMethod 身份），gridRowEnd/gridColumnEnd: span n，被覆盖格跳过）                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 对齐（四框架）                                                                                                  |
| 排序          | sortConfig 多列                                                 | sort 单列 + multiSort 多列 ✓（批 F react + 批 X vue + 批 AA solid + 批 AC svelte）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 对齐（四框架；点击序/原地切换/代理 sorts）                                                                      |
| 筛选          | filterConfig                                                    | filters ✓（批 4 react + 批 Y vue + 批 AA solid + 批 AC svelte；substring 大小写不敏感忽略空值，与 formConfig 值 AND，远程代理模式 setParams 合并）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 对齐（四框架）                                                                                                  |
| 展开行        | expandConfig                                                    | expandable ✓ + expandAll ✓（批 F，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 对齐（react；一次首屏数据种子）                                                                                 |
| 树形          | treeConfig                                                      | tree ✓ + lazyLoad ✓（批 J+K react + 批 Z vue；lazyChildrenOf 优先于 getSubRows，epoch 防陈旧回填）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 对齐（react+vue；仅无子行 caret、加载后展开缓存、刷新清缓存+epoch 防陈旧回填）                                  |
| 汇总          | footerMethod                                                    | summary ✓ + footerMethod ✓（批 N，react）+ 分组汇总（批 M，react；groupBy 组内汇总 + 全局 footer 同 aggregate ops，footerMethod 替换全局 summary 行）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 对齐                                                                                                            |
| 表尾合并      | footerSpanMethod                                                | footerSpanMethod ✓（批 P，react；footerMethod 行→summary 行→footerData 行栈式 rowIndex，colspan 覆盖跳过 + gridColumnEnd，rowspan 惰性——每行独立 grid 容器）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 对齐                                                                                                            |
| 表尾合并配置  | mergeFooterItems                                                | mergeFooterItems ✓（批 R，react；与 footerSpanMethod 同坐标系的声明式合并，colspan 覆盖 + gridColumnEnd，rowspan 惰性——每行独立 grid 容器，函数优先，越界条目 no-op）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 对齐（react）                                                                                                   |
| 层级          | zIndex                                                          | zIndex ✓（批 R，react；仅设置时生效，`position: relative` 搭车，置于 `...style` 之前调用方覆盖优先）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 对齐（react）                                                                                                   |
| 自适应同步    | syncResize                                                      | syncResize ✓（批 R，react；autoResize 关闭时共享 measureRoot，数据/状态变化 + visibilitychange 重测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 对齐（react）                                                                                                   |
| 数据源保持    | keepSource                                                      | keepSource ✓（批 R，react；挂载时浅拷贝种子，后续 re-feed 保持受控交接，表格两侧均不可变）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 对齐（react）                                                                                                   |
| 行键回退      | rowId                                                           | rowId ✓（批 R，react；rowKey > rowId > 索引，选择/编辑/脏标记/树拍平同一键空间，无 rowId 时逐调用点字节一致）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 对齐（react；vxe 弃用 string 型，函数化）                                                                       |
| 列自定义面板  | customConfig                                                    | customConfig ✓（批 S，react；工具栏 columnSettings 按钮打开完整面板——搜索 + 拖拽排序 + 可见性切换 + 重置；确认提交 `columnOrder`/`onColumnOrderChange`（参考保持），重置恢复打开时快照 + 清除顺序；分组表仅顶层列；window 级释放防拖拽卡死）                                                                                                                                                                                                                                                                                                                                                                                                            | 对齐（react；vxe 面板无搜索框，搜索为 iris 增量）                                                               |
| 撤销          | undoRedoHistory                                                 | core undo（外部）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 可组合                                                                                                          |
| 工具栏        | toolbarConfig                                                   | refresh/import/export/custom ✓（批 L）+ toolbar.batch 批量按钮（批 M，react）+ zoomConfig 缩放（批 U，react）+ 工具栏扩展 ✓（批 X，vue：proxy 生命周期/状态/pager/写回 + formConfig 表单渲染；批 AA，solid：refresh/export/buttons/batch；批 AC，svelte：title/refresh/export/buttons/batch）                                                                                                                                                                                                                                                                                                                                                           | 对齐（四框架；vue 批 X 工具栏扩展）                                                                             |
| 布局          | layouts                                                         | layouts ✓（批 U，react；form/toolbar/pager 各节 `hidden` 抑制——仅隐藏不重组，默认渲染与旧行为逐节一致）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 对齐（react；仅抑制，无 vxe 的拖拽重组）                                                                        |
| 搜索表单      | formConfig                                                      | 外部组件                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 可组合                                                                                                          |
| 数据代理      | proxyConfig                                                     | proxyConfig ✓（批 C react + 批 X vue + 批 AA solid + 批 AC svelte；remoteSort/remoteFilter/分页/autoLoad/编辑回写/累计序号/受控 sort 同步，core 控制器）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 对齐（四框架，core 控制器）                                                                                     |
| 搜索表单      | formConfig                                                      | formConfig ✓（批 D react + 批 X vue + 批 AA solid + 批 AC svelte；草稿/应用双态 + 代理合并 filters + 本地过滤）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 对齐（四框架；远程合并 filters + 本地过滤）                                                                     |
| 行操作        | insertRow/removeRow/setRow                                      | tableRef ✓（批 E，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 对齐（react；core table-rows 纯函数）                                                                           |
| 批量删除      | removeRows（多行）                                              | removeRows ✓（批 J，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 对齐（react；单次 onDataChange + 选择剪枝）                                                                     |
| 勾选条件      | checkboxConfig.checkMethod                                      | checkMethod ✓（批 E，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 对齐（react；全选跳过禁用行）                                                                                   |
| 分页配置      | pagerConfig.pageSizes / showTotal                               | pagerConfig ✓（批 E，react）+ showTotal 总数（批 T，react；i18n table.total，尺寸选择器前）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 对齐（react；切换重置页码 + 总数）                                                                              |
| 单元格提示    | tooltipConfig                                                   | tooltipConfig ✓（批 G，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 对齐（react；title 轻量模式）                                                                                   |
| 表头/表尾提示 | headerTooltipConfig / footerTooltipConfig                       | headerTooltipConfig/footerTooltipConfig ✓（批 P，react；title 原生提示，空内容丢弃，覆盖扁平/分组表头 + summary/footerMethod/footerData 单元格）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 对齐（react）                                                                                                   |
| 单元格链接    | —（自定义渲染/插槽实现）                                        | column.link ✓（批 L，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 对齐（react；render > html > link > formatter > raw，_blank→rel=noreferrer）                                    |
| 分组统计      | group 行小计                                                    | groupBy ✓（批 M，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 对齐（react；core groupRows + 组内合计）                                                                        |
| 批量操作      | 工具栏批量按钮                                                  | toolbar.batch ✓（批 M，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 对齐（react；选择非空时显示）                                                                                   |
| 列宽自适应    | width: 'auto'                                                   | auto ✓（批 M，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 对齐（react；minmax(max-content)）                                                                              |
| 虚拟树        | 树 + 虚拟滚动                                                   | 内建 ✓（批 M 验证 + 批 AE 修复；flat/tree × detail 全组合窗口正确，展开增 itemCount + 滚动重钳制，每展开行一个详情槽）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 对齐（react；flatTree 扁平 + treeMeta + detail 槽位；vue/solid/svelte 组合护栏交接）                            |
| 表格方法      | getTableData/getCheckbox 等 handle                              | getData/getSelection ✓（批 M）+ scrollToRow/toggleRowExpand/clearSort/clearFilter/setCurrentRow/setCurrentColumn（批 T）+ loadData/reloadData/commitProxy/getProxyInfo（批 V react + 批 Y vue expose + 批 AB solid；代理方法：loadData 经 liveData 写回通道（solid：本地 override，下次查询整页替换）、reloadData=refetch 别名、commitProxy=setParams 合并重查、getProxyInfo=state 快照，无代理时 null；solid 另含 clearSort/clearFilter）+ getFilteredData/exportCurrentViewCsv ✓（批 W，react；当前筛选+排序视图副本 + 当前视图 CSV 导出，隐藏列排除、分组表导出叶列，ref 镜像最新态——挂载后 rerender 的 filters/data/columnVisibility 变化实时可见） | 对齐（react+vue+solid；handle 快照 + 副本）                                                                     |
| 事件          | cell-dblclick / row-dblclick / header-click / toggle-row-expand | onCellDblClick/onRowDblClick/onHeaderClick ✓ + onExpandChange/onTreeExpandChange ✓（批 T，react；双击在编辑开始后触发、表头点击在排序切换后、展开事件含懒加载分支）+ onEditStart/onEditClosed ✓（批 V，react；cell 模式，提交带值/取消 cancelled:true，行编辑模式会话不报告——文档化）+ onScroll ✓（批 V，react；虚拟列模式走 JSX handler，非虚拟原生监听）+ onSelectAllChange ✓（批 V 增量，vxe 无此 emit；表头 pre-toggle 态 + 当前选择）                                                                                                                                                                                                              | 对齐（react）                                                                                                   |
| 范围勾选      | checkboxConfig.isShiftKey                                       | shift 范围勾选 ✓（批 G，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 对齐（react；尊重 checkMethod）                                                                                 |
| 下拉编辑      | edit-render: select                                             | editor: 'select' ✓（批 H，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 对齐（react；editOptions 原生 select）                                                                          |
| 右键菜单      | @context-menu                                                   | contextMenu ✓（批 H react + 批 Z vue + 批 AB solid；虚拟光标锚点 + Teleport body，Esc/外部/滚动关闭，禁用项 inert，表头/序号/选择/汇总格排除）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 对齐（react+vue+solid；虚拟锚点浮层 + 坐标定位）                                                                |
| 范围复制      | clipConfig                                                      | clipConfig ✓（批 O，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 对齐（react；Ctrl/Cmd+C 选中范围导出 TSV，Ctrl/Cmd+V 粘贴回写，公式中和，溢出忽略，行键映射排序/筛选/代理安全） |
| 查找替换      | fnrConfig                                                       | fnr ✓（批 O，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 对齐（react；Ctrl+F 查找条 + 高亮 + Enter/Shift+Enter 步进 + 替换/全部替换单次提交，Esc 关闭）                  |
| 跨页选择      | checkboxConfig.reserve                                          | 默认保留 ✓（批 H，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 对齐（react；selection 独立于分页）                                                                             |
| 筛选面板      | filter 面板多选                                                 | filterValues ✓（批 I react + 批 Z vue + 批 AB solid；checkbox 面板 + OR 匹配通道与文本 filters AND，远程代理逗号合并，i18n filterConfirm/filterClear）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 对齐（react+vue+solid；checkbox 面板 + OR 匹配）                                                                |
| 多行编辑      | edit-render: textarea                                           | editor: 'textarea' ✓（批 I，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 对齐（react；Shift+Enter 换行）                                                                                 |
| 列格式化      | formatter                                                       | formatter ✓（批 I，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 对齐（react；仅显示层，排序/编辑不受影响）                                                                      |
| 表头/表尾对齐 | headerAlign / footerAlign                                       | headerAlign/footerAlign ✓（批 N，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 对齐（react；prop > col.align > 默认，数字右对齐保留）                                                          |
| 表头合并      | mergeHeaderCells                                                | mergeHeaderCells ✓（批 P，react；扁平表头 row 0、leaf 索引，colspan 覆盖跳过 + gridColumnEnd，rowspan 惰性，columnVirtualization 下 fail-closed）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 对齐（react；分组表头不合并）                                                                                   |
| 圆角/内边距   | round / padding                                                 | round ✓ + padding ✓（批 P，react；round 仅 bordered 时 lg 圆角，padding 走 --iris-cell-pad 变量链）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 对齐（react；padding 字符串 vs vxe boolean）                                                                    |
| 合计精度      | aggregateAccuracyConfig                                         | aggregateAccuracy ✓（批 P，react；全局+分组汇总唯一舍入点，toFixed 0–100 越界忽略，renderSummary 见舍入值）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 对齐（react）                                                                                                   |
| 悬停高亮      | highlightHoverRow                                               | highlightHoverRow ✓（批 N，react）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 对齐（react；默认开，false 关闭悬停规则）                                                                       |
| Tab 编辑导航  | keyboardConfig                                                  | Tab/Shift+Tab 编辑导航 ✓（批 J+K，react；异步校验延期落焦）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 对齐（react；提交后移动，跳过不可编辑列，异步校验成功落焦/失败停留/Escape 取消）                                |
| 行编辑模式    | editConfig.mode: row                                            | editConfig.mode='row' ✓（批 K react + 批 Z vue + 批 AB solid；会话 Map + 共享提交核心，cell 模式字节一致；solid 会话含 epoch 守卫——异步校验 pending 中 Escape 取消不写回、双击 Enter 单次提交）                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 对齐（react+vue+solid；点击整行开编辑、逐列 Enter/blur 提交、Escape 整行取消、行切换先提交；Tab 编辑导航仍缺）  |
| 导入导出      | export/import                                                   | exportCsv/Excel ✓ + exportCurrentViewCsv 导出当前视图 ✓（批 W，react；筛选/隐藏列/分组叶列正确）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 缺导入                                                                                                          |
| 打印          | printConfig                                                     | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 缺                                                                                                              |

## 构建状态（2026-08 完成，react 全量；vue 批 X–Z；solid 批 AA–AB；svelte 批 AC；批 AE 虚拟树 + 四框架示例；批 AF 三端 example e2e + 视觉 parity + Excel 样式；批 AG iris 独有增量：persistState + 可变高度虚拟化；批 AH iris 独有增量：命名视图 + 范围浮动工具栏；批 AI iris 独有增量：自然语言查询；批 AJ iris 独有增量：范围统计 + 视图快照携带 query；批 AK iris 独有增量：范围粘贴 + unique 唯一性规则；批 AL iris 独有增量：内置撤销/重做 + 批量编辑面板；批 AN iris 独有增量：列预设 `preset` + 表格快捷键 `tableShortcuts`；批 AO iris 独有增量：公式列 `formula` + 行列引用 `showCellRefs`；批 AP iris 独有增量：选择汇总 `selectionSummary` + 选中行导出 `exportSelectionCsv`；批 AQ iris 独有增量：拖拽填充 `rangeFill`；批 AR iris 独有增量：迷你图表 `chartPreview`；批 AS iris 独有增量：自动刷新 `autoRefresh` + 新鲜度 `freshness`；批 AT iris 独有增量：审计日志 `auditLog`；批 AU iris 独有增量：比较视图 `compareWith`；批 AV iris 独有增量：网格键盘增强（Tab/Shift+Tab 行主序、Enter 下移、PageUp/PageDown 虚拟滚动）；批 AW iris 独有增量：NL 列摘要 `nlSummary`（core `summarizeColumn` 自然语言一句话总结）；批 BA iris 独有增量：版本历史 `versionHistory`（core `createVersionHistory` 变更前行环形快照 + 面板点击 restore）；批 BB iris 独有增量：批注编辑 `annotationEditing` / `onAnnotationsChange`（右键菜单添加/编辑/删除批注 + 浮动编辑面板，受控写通道）；批 BC iris 独有增量：跨表引用公式 `formulaTables`（`=other!col` 读外部表首行，core 求值器 `!` 令牌 + react 双通道穿线）；批 BD iris 独有增量：协作光标 `presence`（`{ id, name, color, cellKey }` 受控数组 + `::` 规范键 + 描边/角标纯展示）；批 BE iris 独有增量：单元格锁定 `locked`（列/行/组合锁，`isCellLocked` 单一咽喉守卫全部写漏斗 + 45° 条纹视觉标记，review 修复条纹静默不渲染）；批 BF iris 独有增量：数据导出时间线 `exportVersionCsv`（`versionHistory` 按版本导出 CSV——环中变更前快照，`exportSelectionCsv` 同款管线，未知 index/无 prop → `''`）；批 BG iris 独有增量：快捷键自定义 `keymap`（8 动作声明式重绑——core `normalizeKeymap`/`matchTableKey` 默认表 + 覆盖合并 + 精确修饰符，react 四键位穿线 + Ctrl+D 一步填充 / Ctrl+K 查询聚焦）；批 BH iris 独有增量：行分组折叠 `groupCollapsed`（组头原生按钮 ▸/▾ + `aria-expanded`，`groupPlan` 单咽喉跳过折叠组行与每组合计，受控无乐观翻转，选择/编辑/索引零触碰）；批 BI iris 独有增量：列 sparkline `sparkline`（数值列单元格渲染该列在 filteredData 前序值上的 20×8 SVG polyline——core `chartDomain` 域抽出 + react 每格前缀序列 + title/aria-label 序列，纯展示 display-only）；批 BJ iris 独有增量：单元格权限 `cellPermission`（`(row, column) => 'readonly' | 'editable'` 列字段——`isCellReadonly` 单一咽喉守卫全部写漏斗 + 点状纹理视觉与 locked 条纹区分，**动态**每渲染重求值，locked 同设时赢；读通道 fail-inert）；批 BK iris 独有增量：确定性 mock 数据生成 `generateRows`（core `mulberry32` 种子 PRNG——6 kind：string/number/boolean/date/email/phone，min/max 按 kind 归一、min>max 交换、NaN 回落，count/空 schema/未知 kind 全 fail-inert，同种子跨调用字节稳定；review 修复随门禁提交：小数 bound 钳到含整数区间 + 2 回归测试）；批 BL iris 独有增量：性能面板 `perfStats`（core `createPerfStats` 最新快照控制器——渲染顶部 `nowMs()` 打点 + 无依赖 `useLayoutEffect` 提交后采样 `{ durationMs, rows, columns, changes }`，`nowMs()` 带 SSR/jsdom Date.now 回退，duration = 渲染 + layout 阶段排除 paint；工具栏 ⚡ 触发浮动面板 4 行统计 + perf/audit 双订阅原地刷新变更数，auditLog off muted —；无回环：push 只通知面板）；批 BM iris 独有增量：批注浮动预览 `notePopover`（悬停有注记格显示浮动气泡 `data-iris-note-popover` 替代原生 title——useFloating 虚拟锚点 + role tooltip + pointer-events none + Esc/外点/滚动关闭 + mouseleave，仅注记分支 title 置空、compare/sparkline/tooltipConfig 零触碰，纯展示零 i18n/core））；批 BN iris 独有增量：行高 `rowHeight`（`number \| ((index) => number)`——单一咽喉 `rowHeight ?? virtualScroll.itemHeight` 喂虚拟槽高/非虚拟行内高/PageUp/PageDown 步长三路径：数字形覆盖默认（虚拟闭式窗口、非虚拟 inline height）、函数形走前缀和（虚拟 = core Fenwick offset 树收 plan 下标、非虚拟按 body 下标逐行 inline），`rowStyle` 逃生舱赢，未设字节不变））；批 BO iris 独有增量：列宽重置 `columnWidthsReset`（工具栏 ⇔ 重置按钮——`onColumnWidthsChange({})` 空 map 即默认映射，零 override 全路径回落列声明宽度 number/auto/1fr；非受控双通道同 setColumnWidth；独立于 resizableColumns；不在工具栏门；persistState 快照存 `{}` 重挂载即默认宽，+1 i18n 键，propCount 155→156）；批 BP iris 独有增量：单元格复制格式 `copyFormat`（`clipConfig` 内联字段，未设 TSV 字节不变——单一咽喉 `buildRangeCopy` 分发器：tsv 复用 `tsvCell` / csv 复用 `csvRangeCell` RFC-4180 headerless / html 复用 core `toHtml` 表格片段，列子集 + 合成行按 toHtml 读取键 key 化，掩码三格式一致；两消费点 Ctrl+C 与工具栏 复制 同源，非法值 fail-closed 回落 TSV，粘贴不对称仍按 `\t`，propCount 156 不变）；批 BQ iris 独有增量：编辑自动保存 `editAutosave`（`onAutosave?(rows)` 父持久化钩子，单一咽喉 `commitValue` 末尾门控触发，payload = 提交后行列表，rowId 行 autosaveRows fallback；特性开关、`onAutosave` 单独惰性，no-op/校验失败/Escape 不触发，async 落地恰一次，row 模式逐列触发，与 onDataChange 正交——inline 编辑从不触发 onDataChange，propCount 156→158）；批 BS iris 独有增量：多列分组 `groupBy`（数组嵌套层级 + 复合组键 `v0::v1::…` + 组头 depth 缩进 + 合计仅最内层，表级数组赢列级旗标，propCount 158→159）；批 BT iris 独有增量：行选择拖拽 `selectionDrag`（4px 阈值进入拖拽——`elementFromPoint` → `closest` 命中行、锚点与悬停行闭区间追加勾选，checkMethod 尊重、seen 单调并集反向不取消、trailing click 抑制防二次翻转，propCount 159→160；review 门禁修复：pointer capture 延迟到拖拽开始——裸按下 capture 会把 click 重定向到格 div 致 label 无法勾选行，+pointercancel 清抑制臂，15 测试）；批 BU iris 独有增量：表格水印 `watermark`（背景水印文本——**sticky 首子旋转层** `data-iris-watermark` 包裹层 → `data-iris-watermark-overlay` → 72 平铺 tile `data-iris-watermark-tile`，DOM 与原语同构、嵌入根内（滚动容器）且是首子元素，`position: sticky; top: 0; height: 100%` 自滚动起点锚视口——review 门禁修复：absolute/行尾 sticky 会随行滚走，Chrome 实测滚动后层不动；`rotate(-22deg)` + `--iris-muted` token 色 + opacity 0.15 + space-xl/font-size-lg token 值，overlay aria-hidden + pointer-events/user-select none 纯展示；根锚定 `...style` 后强制 `position: relative`（caller style 拆不了锚，zoom fixed 仍赢）；层低于 sticky 表头 z2/固定列 z1/浮动面板；空串 presence 门控，propCount 160→161，10 测试）；批 BV iris 独有增量：比较差异导出 `exportComparisonCsv`（差异行 CSV——当前视图 removed/changed 按视图序 + 快照独有 added 尾部，前置 `__iris_diff` 标记列，变更格 `maskedOld → maskedNew` 合成串掩码先于合成，公式列原始数据物化不合成，无 compareWith/rowKey → `''`、全等快照仅表头，+1 i18n 键 table.compare.diff）；批 BR iris 独有增量：校验统计 `validationSummary`（editRules 列提交结局账本——ok = 校验通过并落地（onCommit 包装器计数，cell/row 两模式）、fail = 提交被拒绝（validate 包装器 Promise `.then` 计数）；commit-intent 标记包装 5 个提交入口 + validate 同步消费区分提交时刻与 setDraft 打字校验；`hasEditRules` 单一咽喉只计 editRules 列——legacy validate/paste/fill/FNR/批量旁路/Escape 均不计；工具栏 muted stamp `data-iris-validation-summary`，i18n en `Passed {ok} · Failed {fail}`/zh `通过 {ok} · 失败 {fail}`，位置在 perf 后、custom buttons 前，prop 单独即开工具栏；重新开启清零，propCount 161→162，12 测试）；批 BW iris 独有增量：右键快捷操作（复制值/清空——**每个** contextMenu **无条件内置**两项快捷操作，无新 prop：**复制值** `__iris-copy-value`（i18n `table.copyValue` en `Copy value`/zh `复制值`）走模块 helper `contextCellText`（`applyCellMask` 掩码 → formatter → `String`，与 cellTooltip 同源「单元格文本」链）+ 既有 `writeClipboardText` 三通道（无剪贴板安全 no-op）；**清空** `__iris-clear-cell`（i18n `table.clearCell` en `Clear cell`/zh `清空`）= `setCellValue(current, rowKey, k, col.key, '')` → `commitRowList(next)`（Delete 快捷键先例逐字，同一 undo/audit/onDataChange 漏斗），locked/readonly no-op、无 onDataChange 惰性内部仍更新；插入点 = 用户项 → 分布? → 摘要? → 复制值 → 清空 → 批注?（排在批注项前），onSelect 接线拦截永不达用户回调 + 保留键去重守卫；+2 i18n 键 en+zh，propCount 162/eventCount 30 不变，12 测试）；批 BX iris 独有增量：列固定快捷切换 `columnPinMenu`（列头右键菜单内置 固定左/取消固定——与 contextMenu 解耦独立门控、受控/非受控双通道 + 内部 fallback、`pinOf` 单咽喉 + grouped 叶子头固定视觉补齐，+2 i18n 键，propCount 162→165/eventCount 30→31，17 测试）；批 BY iris 独有增量：展开状态持久化 `expandedKeys`（`persistState` 第 9 块——展开行键纳入一键持久化，详情面板 + 树 caret 共用同一模型：收集器 `onExpandedRowsChange && (hasDetail || 树模式)` 门（可恢复的才保存，pageSize 先例），恢复经 `expansion.set` 全量替换 → commit 触发 `onExpandedRowsChange(keys)`（展开无受控 prop，pageSize 之后第二个无 change 回调特例），skip-first 挂载保护自动获得（usePersistState 零额外逻辑），views 应用对称补块，扁平表/无回调双向惰性 + 陈旧键 fail-inert；manifest 165/31 不变，core/三框架零改动，9 新测试，react 2364→2373）；批 BZ iris 独有增量：表格状态导出导入 `exportStateJson`/`importStateJson`（同收集器 9 块 JSON——sort/filters/filterValues/columnVisibility/columnOrder/columnWidths/pageSize/expandedKeys/query，multiSortState 刻意不导出、导入接受超集；收集器门放宽裸表可导出 `'{}'` + query 无条件（persist 保存循环无 query 键字节不变），导入复用 applyViewSnapshot 逐项经回调：query 先行/pageSize `onPageChange(1,size)`+恰一次 request/expandedKeys 全量替换，非法 JSON/非纯对象 false 零应用、有效 JSON 逐块惰性 true；导出↔导入往返字节相等；manifest 165/31 不变，core/三框架零改动，12 新测试，react 2373→2385）；批 CA iris 独有增量：单元格链接自动检测 `autoLink`（core `detectAutoLink` 全文本锚定 URL/email 正则、react 单元格 `<a data-iris-auto-link>`，link 列并存优先，propCount 165→166，core 1517→1525 · react 2385→2392，15 新测试）；批 CB iris 独有增量：最近筛选 `recentFilters`（core `createRecentFilters` 环形 10 条 MRU 控制器、react `applyFilterValues` 确认咽喉记录 + FilterPanel 最近区跨列快速应用，propCount 166→167，core 1525→1533 · react 2392→2399，15 新测试）；批 CC iris 独有增量：编辑自动换行 `editAutoHeight`（textarea 编辑器随内容自动增高、6 行上限，propCount 167→168，react 2399→2409，10 新测试）；批 CD iris 独有增量：行拖拽插入指示线（复用既有 `rowDrag`——单一纯函数 `resolveRowDragDrop` 解析落点（side = 指针 vs 行中点）+ 1px `var(--iris-primary)` 逻辑属性全宽指示线 z2，move 画线 / up 同解析提交 / net-zero 跳过 `onReorder` / leave+cancel 清理，零新 prop，propCount 168 不变，react 2409→2419，10 新测试）；批 CE iris 独有增量：复制选中高亮（复制成功反馈——`writeClipboardText` 成功判定 + 600ms 选中格高亮 `data-iris-copy-flash`，零新 prop，propCount 168 不变，react 2419→2430，11 新测试）；批 CF iris 独有增量：空态动作按钮 `emptyState` 类型加宽（`IrisTableEmptyState` 具名接口，`action` 按钮与错误行 retry 同款 token 样式，ReactNode/portal 零 wrapper 原样渲染，propCount 168 不变，react 2430→2439，9 新测试）；批 CG iris 独有增量：单元格字数统计 `charCount`（编辑中实时字数徽标 + 选区计数/合计徽标——共享 EditorSurface 一处渲染 cell/row 双模式、`rangeStatsData` memo 纯归约 + 汇总行同款 `aggregateAccuracy` 舍入、句柄共存上移 10px、双模式 fail-closed、+3 i18n 键 en+zh，propCount 168→169，react 2439→2454，14 新测试）；批 CH iris 独有增量：列宽双栏拖拽（`columnDrag` 拖出固定——拖列头出**左边缘外**松开即固定左，与 `columnPinMenu` 组合走同一双通道咽喉，`resolveColDrag` 边缘检查优先永不重排、窗口级 pointerup/move/cancel 修复根外释放 stuck `activeId`，纯 columnDrag vxe parity 零全局钩子，零新 prop/core/i18n，propCount 169/eventCount 31 不变，react 2454→2467，13 新测试）；批 CI iris 独有增量：行克隆 `cloneRow`（`IrisTableHandle.cloneRow(key, index?)`——core `cloneRowInList` 纯函数浅拷贝全字段 + `max+1` 新 key、默认插源行之后、显式 index 钳制、缺键原引用静默 no-op，react 接线 `commitRowList 'insert'` 一次漏斗——onDataChange 恰一次/可 undo/audit insert/不选中；**零新 prop**、零 i18n/styles/事件，vue/solid/svelte 零触碰；core 1533→1542 · react 2467→2481，23 新测试，manifest 169/31 零 diff）；批 CJ iris 独有增量：表格快捷键提示 `shortcutHints`（工具栏 `?` 触发钮开浮动面板——**单一真相源**消费 `keyBindings` memo 列出全部 8 个内置快捷键**有效键**、`keymap` 重绑实时反映、非法覆盖 fail-closed；core `formatKeyBinding`/`formatKeyBindings` 2 纯格式化 + 9 i18n 键 en+zh；core 1542→1548 · react 2481→2492，17 新测试，propCount 169→170/eventCount 31 不变）；批 CK iris 独有增量：单元格文本搜索高亮 `searchHighlight`（core `splitSearchHits` 大小写不敏感字面子串命中切分纯函数 + react `applySearchHighlight` 单一咽喉插在 `mask → formatter ?? raw` 链尾——命中渲染内联 `<mark data-iris-search-hit>`、`--iris-surface-selected` 同款 token，render/html/link/autoLink/sparkline 零触碰，无栏/无状态/无回写，与 fnr 区别在机制；core 1548→1559 · react 2492→2503，22 新测试，propCount 170→171/eventCount 31 不变）；批 CL iris 独有增量：行展开动画 `expandAnimation`（详情/树行展开 max-height+opacity 入场过渡——纯 CSS 键帧 `--iris-table-expand-max`/`--iris-duration-md` token+fallback、reduced-motion 关闭、root 行不动、虚拟惰性，零 state/effect/i18n/core，react 2503→2511，propCount 171→172/eventCount 31 不变）；批 CM iris 独有增量：列合计行粘性 `summaryRowStyle`（`'default' | 'sticky'`——全局汇总行 `position: sticky; bottom: 0` 吸附 fixed-height 容器底缘，footerMethod 替换行同槽位、组汇总/footerData 两 fiat 排除，纯 CSS additive 零 state/core，react 2511→2519，propCount 172→173/eventCount 31 不变）；批 CN iris 独有增量：单元格拖拽移动 `cellDrag`（cellRange 活选区范围左上角格顶缘 `data-iris-range-move` grip 拖拽**剪切移动**整块——越界 clamp 整块恒落表内、clamp 后 == 源零提交 no-op、剪切两阶段 ONE `commitRowList 'edit'` 重叠安全原子滑动、公式列/locked/readonly/keyless 四跳过、选择跟随移动块；fill AQ 手势模具逐行同构 + dismissal 抑制拓宽，react 2519→2535，16 新测试，propCount 173→174/eventCount 31 不变）；批 CO iris 独有增量：表格时间线导出 `exportTimelineCsv`（批 AT 审计环 CSV——spec 字面 6 列 time/type/rowKey/column/old/new，time = `formatClock` HH:MM:SS 与审计面板时间格逐字同源，undefined → 空串、数字裸值、字符串 RFC-4180 + OWASP 中和，环序最新优先与 getAuditLog 同一视图；auditLog 关 → `''`、开但空环仅表头两态；零新 prop/i18n/core/样式，react 2535→2544，9 新测试，manifest 174/31 零 diff）；批 CP iris 独有增量：行密度三档 `density`（compact→6px / cozy→4px 叠在 size 之上、comfortable 默认无规则字节不变、非法值 fail-closed）+ 工具栏密度切换 `densityToggle`（循环按钮 `data-iris-density-toggle`、effective = toggle ? state : prop、4 i18n 键 en+zh、纯 CSS 零 inline），react 2544→2556，12 新测试，propCount 174→176/eventCount 31 不变；批 CQ iris 独有增量：编辑实时预览 `editPreview`（编辑态单元格下方实时渲染 formatter 应用草稿结果 `data-iris-edit-preview`、muted 小字 token-only——草稿先走 commit/validate 共享 `coerceEditDraft` 强转再 mask→formatter 镜像提交格显示链、双门控 `editPreview && col.formatter`、零新 state 零 i18n、cell/row 双模式同源；review 门禁修复：编辑态单元格 flex-wrap + 预览/错误 flexBasis 100% 整行堆叠在编辑器**下方**——并排问题 Chrome 实测修正 + jsdom CSS 契约断言），react 2556→2570，14 新测试，propCount 176→177/eventCount 31 不变；批 CR iris 独有增量：列汇总条 `columnTotals`（工具栏下方全宽网格横条 `data-iris-column-totals`——每叶列一格子 `data-iris-column-totals-cell`，仅 `summary === 'sum'` 列经汇总行同管线 `aggregate` → `aggregateAccuracy` 舍入 → `renderSummary ?? String` 显示合计，非 sum 列空占位对齐，空 body 显示 `0`；`COLUMN_TOTALS_STYLE` token-only、栏门独立于工具栏、零新 state 零 i18n 零 core），react 2570→2582，12 新测试，propCount 177→178/eventCount 31 不变；批 CS iris 独有增量：展开滚动保持 `expandScrollPreserve`（虚拟模式重锚内容——展开提交在新 plan 重定位首可见行写 `newIndex × slotHeight + relativeTop`，树上/详情展开收起光标下内容不动，单键精确、变高/非虚拟/全量恢复三 fiat 惰性，锚点 onScroll 接线 + 稳定渲染双记录、过渡判定单键对称差、keyOf/BodyPlanEntry 模块化单真相源），react 2582→2594，12 新测试，propCount 178→179/eventCount 31 不变；批 CT iris 独有增量：表格标签页 `tableTabs`（`role=tablist` 条渲染在工具栏上方——点击标签把 `views` 名字**按序**经同一 `selectView` 路径应用（未知名跳过、后赢、工具栏下拉镜像），活动态内聚首次点击前无活动标签、dup key 保首、工具栏顶角半径条件移交、toolbar hidden 仍渲染标签条、打印隐藏；零新 i18n/core/types，propCount 179→180），react 2594→2604，10 新测试，propCount 179→180/eventCount 31 不变；批 CU iris 独有增量：带格式复制 `copyWithFormat`（`clipConfig` 内联字段，默认 off——formatter 列复制走 `contextCellText` 显示文本链（mask→formatter→String，与右键 复制值 同源）取代原始值，无 formatter 列字节不变、格式化串仍走同一 tsv/csv/html 序列化器（引号+OWASP 仍生效）、formatter 输入恒掩码取代 `exportRaw` 复制路径跳过（导出零改动）、粘贴不对称、非字符串回落同 复制值、公式列批 AO 咽喉后照常 formatter；propCount 180/eventCount 31 不变），react 2604→2618，14 新测试，propCount 180/eventCount 31 不变；批 CV iris 独有增量：固定列数边界拖拽 `pinnedDrag`（拖拽线在 pinned 边界——最后一个左固定叶头尾缘 8px grip + 2px `--iris-primary` 线（sticky 随格免费继承），拖右加 pin / 拖左减 pin、松开提交 + translateX ghost、Arrow ±1；提交逐列 `setColumnPinned('left'|null)` 走同咽喉（onColumnPinnedChange 每列、受控无乐观翻转）+ `onPinnedCountChange(count)` 每次提交一次、no-op 零回调；左计数 only：零左固定无句柄、首右固定列硬上限、宽近似 pinnedOffsets 同回落链、boundary 列 resizableColumns 句柄抑制防抢手势；review 门禁修复：gapped 态 [A(left), B(null), C(left)] 预算改从**前导前缀**宽起算（resolve(0) ≡ leftPinnedCount，同 commit 的 current 概念）——pin 菜单可达间隙态下零 dx 不再误 pin B/unpin C 触发 onColumnPinnedChange ×2 + onPinnedCountChange，+T18 回归测试；propCount 180→182/eventCount 31→32），react 2618→2636，18 新测试；批 CW iris 独有增量：导入预览 `importPreview`（vxe ⇪ 导入无预览直接调 onImport——用户看不见导入什么）——`importPreview?: boolean` 表级 prop（`props.ts` `zoomConfig` 后，默认 off，manifest propCount 182→183/eventCount 32 不变）：解析/行构建零改动，`importPreview` 门恰在 `toolbar.onImport?.(rows)` 调用点分裂——开时`setImportPreviewRows(rows)` 本地挂起，固定居中 modal（Dialog backdrop/`--iris-z-modal` 先例）显示**前 5 行**表格（表头 = 首行键序，`previewColumnsFromRows` 模块 helper）+ 超 5 行时`table.total` 总数注；**确认** = `onImport(全部行)` 恰一次 + 关闭、**取消/Esc（仅开时挂 window 监听）/backdrop** = 零调用；关闭后 input value 已清 → 重选文件**每选刷新**（fresh per selection）；`<2` 行 silent no-op 保留、header-only 单行零 modal 零调用、引号字段 RFC-4180 原串直显；off 路径字节不变；+3 i18n 键 en+zh（title/confirm/cancel，总数注复用 `table.total`）；core 仅 3 i18n 默认键、react-only 桥、三框架零改动、styles/types 零改动；react 2636→2648，12 新测试；批 CX iris 独有增量：列类型自动检测 `autoDetectTypes`（vxe 要求逐列声明 sortType 从不自动推断）——`autoDetectTypes?: boolean` 表级 prop（`props.ts` `footerAlign` 后，默认 off fail-closed，propCount 183→184/eventCount 32 不变）：列首次数据到达时经 core 新纯函数 `detectColumnType`（`column-type.ts`，前 50 非空样本全样本一致；number/boolean 按 typeof、非有限数仍投 number、date = Date 实例或 ISO 正则、数值/布尔字符串保持 string、混合 fail-safe）推断每叶列值类型 → 默认对齐 + 排序类型（number → 右对齐 + sortType number；string/date/boolean → 左 + string），仅填调用者未定义字段（显式恒赢、preset 默认存活、公式列跳过）；one-shot effect 首个非空 liveData 触发（重喂不重测、代理仅首页采样、SSR 后 hydrate 落地），detectedColumns memo 插 preset 与 order 之间闭合表头对齐缺口（body/footer 已按值右对齐）；core 1559→1569 · react 2648→2660，12 新测试，propCount 184/eventCount 32 不变；批 CZ iris 独有增量：跳到行 `goToRow`（`IrisTableHandle.goToRow(key)`——共享定位 helper `findTableRowEl` 滚动 + `data-iris-row-target` 2s 瞬态行高亮，单目标/重呼重启/卸载清理，零事件与 setCurrentRow 正交，react 2660→2671，11 新测试）；批 DA **验证式 adapt**（单元格格式化复制增强——spec 已被批 CU `copyWithFormat` 完整交付：选区整体按列 formatter 输出 `buildRangeCopy(range, format, copyWithFormat)`（Table.tsx:6822，tsv/csv/html 三序列化器 formatter-gated、双喉 Ctrl+C + 工具栏同咽喉）、掩码优先（`contextCellText` :2098 先 `applyCellMask` 再 formatter，测试钉 `M:138****8000`）、`copy-with-format.test.tsx` 14/14——零源码改动、manifest 零 diff（propCount 180/eventCount 31、155×4、86 tokens），gate 实测 react 2671/2671 / core 1580/1580 / turbo 180/180 / audit 0）；批 DB iris 独有增量：编辑历史侧边 `editSidebar`（工具栏 ⏳ 触发器开合固定右侧 360px 侧边面板——导出纯函数 `mergeEditTimeline` 双指针合并 `auditLog` 审计 + `versionHistory` 版本双源时间线（`at` 降序、同毫秒 tie audit 在前）、双 `useSyncExternalStore` 就地刷新、版本条目点击 `restoreVersion` 恢复不重推（`getVersions()` 稳定、audit 增益 undo 条目）、审计条目 `#seq+钟表+type+rowKey+column+老→新`、Esc/外点/滚动三路关闭 + 触发器豁免、双缺空态、fail-closed 5 格矩阵、+2 i18n 键 en+zh、纯视图层集成零 core/三框架改动；react 2671→2682，core 1580 不变，manifest 155×4/185 props/32 events 零 diff）；批 DC iris 独有增量：冻结区内列拖拽排序钳制 `clampReorderZone`（复用既有 `columnDrag`——落点钳进拖拽列自己的固定区，`zone = pinOf ?? 'free'` 同咽喉；同区原样 / 跨区钳区首区尾 / net-zero 守卫跳 onReorder，`[left][free][right]` 分区恒定、零固定表字节不变，零新 prop/core/types/styles/i18n/三框架改动；react 2682→2697，15 新测试，manifest 155×4/186 props/32 events 零 diff）；批 DD iris 独有增量：单元格数据验证增强（`editRules.regexp` 内建 pattern 快捷别名——`EditRule.regexp?: string | RegExp`、`validateRule` 取 `pattern ?? regexp`（pattern 恒胜）、复用 `DEFAULT_MESSAGES.pattern`、空值豁免保留、同步/异步两 API 同 `validateRule` 零接线流经每条提交路径，core 仅 `edit-rules.ts`+`edit-rules.test.ts` 两文件 +4 新测试，core 1580→1584，react 2697 不变，manifest 155×4/186 props/32 events 零 diff）；批 DE iris 独有增量：表格状态对比（`handle.compareStates(a,b)` 纯加法——react 模块 `compareStates.ts` 排序键深等 + 递归 collectDiff，`+`/`-`/`~ old → new` 点路径块/map key/数组 index，相同 `''`、无效 JSON 错误串不抛，react 2697→2711，+14 新测试，core 1584 不变，manifest 155×4/186 props/32 events 零 diff）；批 DF iris 独有增量：批量粘贴增强（单格流式粘贴溢出插行 `pasteOptions.insertIfOverflow`——core `insertRowInList` 组合 auto-id `max+1` 追加超界行为新行、surplus 丢弃、locked/readonly 跳过、一次 commitRowList 'paste'、多格矩形保持剪裁、默认关字节一致；propCount 186/eventCount 32 不变，react 2711，+8 新测试，clip-fnr 26 通过，core 1584 不变，manifest 零 diff；review P2 修复 pasteOptions JSDoc 末段行，描述不再截断）；批 DG iris 独有增量：列宽自适应增强 `autoResizeColumns`（vxe 需手动逐列声明 minWidth/maxWidth 无内容自适应——`autoResizeColumns?: boolean` 表级 prop，拖拽手柄 `onDoubleClick` 经 `measureColumnContentWidth` 测表头+正文 `scrollWidth` 最大值（12px×2 padding 内含）→ 拖拽同款 clamp（min ?? 60/max ?? ∞）→ `Math.round` → `setColumnWidth` → `onColumnWidthsChange` 受控/非受控双通道、受控无乐观翻转；无 resizable 无句柄 fail-closed、与拖拽/排序零干扰；core/三框架/styles/i18n 零改动，manifest propCount 187→188；react 2719→2732，13 新测试）

| 批    | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 状态                                                                                                                                                                                                                                                                              |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 批 1  | editRules 规则集 + editConfig（required/min/max/type/pattern/validator）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ✅ 四框架                                                                                                                                                                                                                                                                         |
| 批 2  | rowDrag（createSortable 组合）+ useTableUndo（createUndoStack 组合）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | ✅ react                                                                                                                                                                                                                                                                          |
| 批 3  | seq 序号列 + spanMethod 合并 + columnDrag 列拖拽                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ react                                                                                                                                                                                                                                                                          |
| 批 4  | columnVisibility 列显隐 + filters 筛选（core filterSort）+ toolbar                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ react                                                                                                                                                                                                                                                                          |
| 批 5  | parseCsv 导入（工具栏按钮）+ printable 打印样式                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | ✅ react                                                                                                                                                                                                                                                                          |
| 批 C  | proxyConfig 服务端数据源（remoteSort/remoteFilter/分页/autoLoad/编辑回写）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ✅ react                                                                                                                                                                                                                                                                          |
| 批 F  | multiSort 多列排序 + validConfig.showMessage + 选择方法（selectAll/toggleRowSelection/clearSelection）+ expandAll                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ✅ react                                                                                                                                                                                                                                                                          |
| 批 J  | 树形懒加载（lazyLoad）+ removeRows 批量删除 + Tab 编辑导航                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ✅ react                                                                                                                                                                                                                                                                          |
| 批 K  | editConfig.mode='row' 行编辑模式 + Tab 异步校验修复 + 懒加载缓存刷新修复                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ✅ react                                                                                                                                                                                                                                                                          |
| 批 L  | proxy 累计序号 + 工具栏导出按钮 + 列 link 单元格链接                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | ✅ react                                                                                                                                                                                                                                                                          |
| 批 M  | 行分组（groupBy 组头 + 组内汇总）+ 工具栏批量按钮 + 列宽 auto 自适应                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | ✅ react                                                                                                                                                                                                                                                                          |
| 批 N  | 表格高度（height/minHeight/maxHeight + sticky 表头）+ 表头/表尾对齐 + footerMethod 汇总方法 + 悬停高亮                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | ✅ react                                                                                                                                                                                                                                                                          |
| 批 O  | 范围复制（clipConfig，Ctrl+C/V TSV 导出/粘贴回写）+ 查找替换（fnr，Ctrl+F 查找条 + 高亮 + 替换）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ✅ react                                                                                                                                                                                                                                                                          |
| 批 P  | mergeHeaderCells 表头合并 + footerSpanMethod 表尾合并 + round/padding 圆角内边距 + aggregateAccuracy 合计精度 + header/footer 表头表尾提示                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | ✅ react                                                                                                                                                                                                                                                                          |
| 批 Q  | autoResize 自适应高度 + scrollbarConfig 滚动条配置 + editDirtyConfig 编辑脏标记（review 修复：height:100% 追踪父容器、脏键 `::` 分隔 + 移除剪枝、逻辑属性 inset-inline-end）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ✅ react                                                                                                                                                                                                                                                                          |
| 批 R  | zIndex + syncResize 自适应同步 + keepSource 数据源保持 + rowId 行键回退 + mergeFooterItems 表尾合并配置（review 修复：rowspan 惰性——覆盖格保留轨道、expandAll seed 与 flattenTree key 对齐）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ✅ react                                                                                                                                                                                                                                                                          |
| 批 S  | customConfig 列自定义面板（搜索/拖拽排序/可见性切换/重置；review 修复：label 无障碍名、重置按次打开快照、window 级 pointerup/cancel 防卡死）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ✅ react                                                                                                                                                                                                                                                                          |
| 批 T  | handle 方法（scrollToRow/toggleRowExpand/clearSort/clearFilter/setCurrentRow/setCurrentColumn）+ 事件（cellDblClick/rowDblClick/headerClick/expand）+ radio 单列 + 分页 showTotal 总数                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | ✅ react                                                                                                                                                                                                                                                                          |
| 批 U  | zoomConfig 缩放全屏（工具栏 ⛶/✕ 切换 + fixed 覆盖层 + Esc 退出）+ layouts 节布局（form/toolbar/pager hidden 抑制）+ visibleMethod 列可见谓词（与 columnVisibility AND，谓词 veto 优先；review 修复：缩放时工具栏提升至覆盖层之上、position:fixed 内联强制防 zIndex prop 拆盖）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ✅ react                                                                                                                                                                                                                                                                          |
| 批 V  | 代理方法 loadData/reloadData/commitProxy/getProxyInfo（loadData 走 liveData 写回通道、代理 total 下次 query 前不变；commitProxy=setParams 合并重查；getProxyInfo 无代理 null）+ 事件 onEditStart/onEditClosed（cell 模式，提交带值/取消 cancelled:true）+ onScroll（虚拟列 JSX handler + 非虚拟原生监听）+ onSelectAllChange（增量，表头 pre-toggle 态 + 当前选择）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ✅ react                                                                                                                                                                                                                                                                          |
| 批 W  | getFilteredData 过滤数据方法（筛选+排序视图副本）+ exportCurrentViewCsv 导出当前视图（隐藏列排除、分组表叶列导出；review 修复：ref 镜像最新态——挂载后 rerender 的 filters/data/columnVisibility 变化实时可见）+ showHeaderOverflow/showFooterOverflow 表头/表尾溢出（默认保持省略号，false → wrap + visible，body 不受影响）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ✅ react                                                                                                                                                                                                                                                                          |
| 批 X  | **vue 适配器功能对齐**：proxyConfig（remoteSort/remoteFilter/分页/autoLoad/编辑回写 + 受控 sort/multiSortState 变更推送代理，core setParams 去重）+ formConfig 搜索表单（草稿/应用双态、提交/重置代理合并 filters + 页重置、远程过滤 watch）+ multiSort 多列排序（点击追加/原地升→降/移除 + 代理 `sorts` 通道）+ 工具栏扩展（proxy 生命周期/状态/pager/写回）；review 修复：受控 sort 同步 watch（React effect 对齐）、onMounted 首查（SSR 不发请求 + hydration 不漂移）、代理模式本地过滤仅 filters prop                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ✅ vue                                                                                                                                                                                                                                                                            |
| 批 Y  | **vue 适配器功能对齐**：columnVisibility（+ update:columnVisibility emit）+ filters 文本筛选（substring 大小写不敏感忽略空值、与 formConfig AND、远程代理 setParams 合并）+ seq/seqStartIndex 序号列 + spanMethod 合并单元格（resolveSpan + 占用集）+ columnDrag 列拖拽（core createSortable 桥，分组表头不支持）+ rowDrag 行拖拽（代理 liveData / 本地 override 重排 + onDataChange/onReorder）+ expose 代理方法（loadData/reloadData/commitProxy/getProxyInfo）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ✅ vue                                                                                                                                                                                                                                                                            |
| 批 Z  | **vue 适配器功能对齐**：editConfig.mode='row' 行编辑模式（会话 Map + 共享提交核心，cell 模式字节一致；整行开编辑/逐列 Enter/blur 提交/Escape 取消/行切换先提交/异步校验-取消丢弃提交）+ contextMenu 右键菜单（虚拟光标锚点 + Teleport body，Esc/外部/滚动关闭）+ filterValues 筛选面板（OR 匹配 + 文本 AND + 远程逗号合并）+ 树形 lazyLoad 懒加载（lazyChildrenOf 优先，epoch 防陈旧回填）；review 修复：初始代理 params 的 filters 逗号合并改按 remoteFilter 门控并合并文本 filters（React 对齐）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ vue                                                                                                                                                                                                                                                                            |
| 批 AA | **solid 适配器功能对齐（核心批）**：columnVisibility 列显隐（displayColumns 驱动全部渲染路径）+ filters 文本筛选（substring 忽略空值 + filterMethod 覆盖 + 代理合并）+ seq 序号列（seqStartIndex/seqMethod + proxyConfig.seq 累计序号）+ spanMethod 合并单元格（占用集键控引用+方法身份重建）+ proxyConfig 服务端数据源（remoteSort/remoteFilter/分页/autoLoad/编辑回写/累计序号 + pagerConfig pageSizes/showTotal）+ multiSort 多列排序（追加/升→降/移除 + 代理 sorts）+ formConfig 搜索表单（草稿/应用双态 + 提交重查）+ 工具栏扩展（refresh/export/buttons/batch）；review 修复：spanMethod 占用集按方法身份键控重建、manifest 解析 artifact（react 同款内联对象形）                                                                                                                                                                                                                                                                                                                                            | ✅ solid                                                                                                                                                                                                                                                                          |
| 批 AB | **solid 适配器功能对齐（交互批）**：rowDrag 行拖拽 + columnDrag 列拖拽（core createSortable 桥：阈值→rects 一次→closestCenter→重排，tap 取消）+ editConfig.mode='row' 行编辑模式（整行开编辑/逐列 Enter/blur 提交/Escape 整行取消/行切换先提交/同步失败阻断/Tab）+ contextMenu 右键菜单（虚拟光标锚点 + Esc/外部/滚动关闭 + 禁用 inert + 表头排除 + portal）+ filterValues 筛选面板（触发不开排序 + OR 与文本 AND + 远程逗号合并 + i18n）+ 代理方法（loadData/reloadData/commitProxy/getProxyInfo/clearSort/clearFilter）；review 修复：异步提交 epoch 守卫（Escape 取消不写回 + 双击 Enter 单次提交）、代理 loadData 下次查询替换、rowMode 优先 cellRange、同行双击重开整行；gate 修复：内联编辑器模板 SSR 水合一致                                                                                                                                                                                                                                                                                               | ✅ solid                                                                                                                                                                                                                                                                          |
| 批 AC | **svelte 适配器功能对齐（核心批）**：columnVisibility 列显隐（displayColumns/leafColumns 驱动全部渲染路径，挂载后隐藏/恢复反应式）+ filters 文本筛选（substring 忽略空值 + filterMethod 覆盖 + 代理合并，空值不重查）+ seq 序号列（seqStartIndex + proxyConfig.seq 累计序号）+ spanMethod 合并单元格（buildSpanPlan 占用集纯计划，跨虚拟窗口正确）+ proxyConfig 服务端数据源（presence-keyed $effect 控制器 + 互斥 sort/sorts + pager IrisPagination + 编辑回写/累计序号）+ multiSort 多列排序（追加/升→降/移除 + 链式比较器 + 代理 sorts + 受控无乐观翻转）+ formConfig 搜索表单（草稿/应用双态 + 签名键控重播种 + 提交重查）+ 工具栏扩展（title/refresh/export/buttons/batch）；review：onColumnVisibilityChange 契约内延后（react 亦仅设置面板触发）、grouped 由 displayColumns 计算（与 solid/vue 一致）                                                                                                                                                                                                       | ✅ svelte                                                                                                                                                                                                                                                                         |
| 批 AE | **react 虚拟树/详情行修复**：virtualScroll 门控放开（flat/tree × detail × virtual 全组合），BodyPlanEntry.kind='detail' 每展开行一个详情槽（超高槽内滚动），展开/收缩 expandedKeys→plan→items.length 全链路重钳制（layout effect 预绘同步 scrollTop 无空白帧 + fixed 模式窗口钳制）；新增 virtual-tree.test.tsx 5 测试 + 更新 2 处旧 pin。**四框架 example**：vue/solid/svelte CMS 新增 VxeGridExamplePage（5 节镜像 react：basic/行编辑/服务端代理/搜索表单/行操作）+ 菜单 vxe-example（order 8）+ Shell/PageHost + menu-leaf 契约测试 allowlist（checkMethod/tableRef.insertRow react-only 已 grep 验证，行操作用本地响应式 state）；review：manifest 155×4 无 diff、core 零改动、纯 additive                                                                                                                                                                                                                                                                                                                    | ✅ react + 示例                                                                                                                                                                                                                                                                   |
| 批 AF | **三端 example e2e**：vue/solid/svelte CMS 新增 e2e/vxe-grid.spec.ts ×3（镜像 react pages.spec.ts：basic 渲染/排序/行编辑、proxy 区等出数 + 远程分页、formConfig 搜索过滤；proxy 断言限定服务端数据源区，svelte 行编辑用 dblclick）。**视觉 parity**：四框架 visual-parity.spec.ts 各加 vxe-{light,dark} 块（1280×2000 视口 + 等 400ms 出数）+ 8 张基线 PNG。**Excel 样式**：core toSpreadsheetXml 增 headerStyle（bold Font + 表头 StyleID）与 columnWidths（<Column> 5.25pt/char、钳位/跳过），缺省字节不变；react exportExcel 已透传 options 零改动。**react 表头 track 对齐**：gridTemplateColumns/colTrack/表头补齐 rowDrag/seq 前置 track（parity 修复）。改动仅 additive（core 增量缺省字节不变）、manifest 无 diff                                                                                                                                                                                                                                                                                         | ✅ 四框架 e2e + 视觉 + Excel                                                                                                                                                                                                                                                      |
| 批 AG | **iris 独有增量（react，见「iris 独有」节）**：`persistState` 状态持久化（受控表格的纯 LOADS/SAVES 协调器：挂载经 change 回调回放恢复 + 每次变更序列化当前 props 写回；`pageSize` 特例在代理首次 query 前经 `onPageChange(1, restored)` 应用且仅单次请求；`storage: false` 全禁用；SSR window 守卫；恢复先于保存订阅 + 逐通道 skip-first，挂载提交永不覆盖存储）——新 hook `usePersistState.ts` + 3 处小编辑 + 21 新测试。**可变高度虚拟化**：`IrisTableVirtualOptions.itemHeight` 扩为 `number                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ((index) => px)`（core/virtual + VirtualScroll 本就支持，零改动），行按累计偏移定位、函数收到虚拟 plan 下标；6 新测试（累计 top / 固定形不变 / 窗口边界 / plan 下标陷阱）。文档：vxe-grid-comparison.md 新增「iris 独有」节 + 本行；manifest 已重新生成。core 零改动、纯 additive | ✅ react |
| 批 AH | **iris 独有增量（react，见「iris 独有」节）**：**命名视图 `views`**（工具栏 select `data-iris-table-views` + 内联保存输入 + 删除 ×；快照 = `persistState` 同一收集器、应用 = 同一逐块回调 + `pageSize` 挂载序列复现；挂载同守卫加载 + `storage: false` 内存态 + 损坏 JSON 忽略；同名 upsert；`activeKey` 受控-only）——新 hook `useTableViews.ts` + `TableViews.tsx` + 16 新测试。**范围浮动工具栏**（`cellRange` 活动时浮于首格上方：ContextMenu 式虚拟锚点 + placement top + autoUpdate 滚动重定位 + useDismiss 清除范围即隐藏；复制 = clipConfig 同款 TSV、导出 = 无表头 CSV、清除 = byKey patch 一次 commitRowList）——`RangeToolbar.tsx` + 11 新测试。+7 i18n 键（en+zh：table.views.save/placeholder/delete + table.range.copy/export/clear/toolbar）。core 零改动、纯 additive；review 修复：cellRange+onCellClick 组合下锚点更新移入 handleCellClick（死代码 onClick 删除）、applyViewSnapshot 逐块类型守卫复用 restorePersistPiece（篡改存储不达回调）、保存哨兵名拒绝 + 读时丢弃、工具栏 aria-label 独立键 | ✅ react                                                                                                                                                                                                                                                                          |

| 批 AI | **iris 独有增量（react，见「iris 独有」节）**：**自然语言查询 `query`**（core `parseTableQuery`：`field op value` 语法 + 单/双引号值 + `in` 列表 + 尾部 `sort by` + 大小写不敏感字段 + 未知字段错误字符串；`=`/`contains` → 子串通道、`in` → filterValues OR-match、关系算子 → `matchesRule` typed-rules AND；同字段 `=`/`in` OR 折叠 `inValues`、同字段 contains/关系 OR fail-closed、跨字段 OR → AND）——新文件 `query-parser.ts` + 36 核心测试。**react 桥**：`query`/`onQueryChange` 受控-only 工具栏输入（`data-iris-table-query-input`，仅 `query !== undefined` 渲染）+ 解析 memo + 上次有效解析 ref（错误时 `data-iris-query-error` muted 提示、保留过滤）+ `sort by` 仅无任何 sort prop 时播种 + 代理模式逗号并入远端 filter map（首请求携带）——`props.ts` + `Table.tsx` 编辑 + `query-input.test.tsx` 6 测试。+1 i18n 键（`table.queryPlaceholder`，en+zh「自然语言筛选，如 age > 25 and role = Test」）。core 改动仅 additive（新导出 `matchesRule`），其余框架零改动 | ✅ react |
| 批 AJ | **iris 独有增量（react，见「iris 独有」节）**：**范围统计 `rangeStats`**（core 新文件 `range-stats.ts`：按列 count/sum/avg/min/max，语义复用 `aggregate`（count = 非空格、数值运算仅有限值），唯一分歧无数值子集 → `null`、空范围 → `{}`；react 桥列键 `dataIndex ?? key` 同 `getCellValue` 间接层、core 纯于 `{ key }`；浮动条新增 统计 按钮 `data-iris-range-stats` + 面板 `data-iris-range-stats-panel` 绝对定位浮动条下方，打开态提升 Table（范围变化 remount 时重算保持打开），复用 useDismiss（Esc/外部点击清范围即关）——`range-stats.ts` + `RangeToolbar.tsx` + `Table.tsx` + `styles.ts` + 8 核心 + 7 react 新测试）。**视图快照携带 query**（`IrisTablePersistedState` 增 `query?: string`；收集器 `views` 在场时追加受控 query——`persistState` 保存循环只遍历 `IrisTablePersistPiece` 字节不变；应用先于其他块经 `onQueryChange` 恢复 + typeof-string 守卫；遗留无 query 快照照常加载）。+8 i18n 键（en+zh：table.range.stats + 面板表头 6 键）。core additive（新导出 rangeStats/RangeColumnStats/RangeStatsRange）、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AK | **iris 独有增量（react，见「iris 独有」节）**：**范围粘贴（选区矩形填充）**（多格选区 Excel 式矩形精确填充：剪贴板从左上角逐格写入、只填矩形本身；小于 → 左上角填充其余不变、大于 → 裁到矩形与表格边界；单格选区保持批 O 流式；一次 `commitRowList`、值保持字符串——`Table.tsx` `pasteIntoRange` 分支 + 4 react 新测试）。**`editRules.unique` 唯一性规则**（core `EditRule.unique?: boolean` 内建：同列 String 比较、空值两侧豁免、编辑行引用身份跳过、默认消息 `'Value must be unique'`；`validateEditRules`/`validateEditRulesAsync` 新增可选第 5 参 `context?: EditRuleContext<Row> { rows, columnKey }`（新具名导出接口），无 context 跳过；react cell/row 两模式校验桥接线传 `{ rows: externalDataRef.current, columnKey }`——8 核心新测试）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AL | **iris 独有增量（react，见「iris 独有」节）**：**内置撤销/重做 `undo`**（core `createUndoStack<Row[]>`（maxHistory 100 + 初始快照 + 引用相等跳过）：两个变更漏斗都接线——`commitRowList` 与 `commitValue`（cell/row 编辑直写 setLiveData 从不过 commitRowList）；栈存**变更后**状态（core redo 语义，基线「变更前」措辞修正）；Ctrl/Cmd+Z / Ctrl/Cmd+Y(+Shift+Z) window 监听（根内目标 + 跳过输入控件 + 编辑会话打开不劫持）；重放 `restoringRef` 守卫经 commitRowList 不重推；工具栏 ↶/↷ 按钮 disabled 随 canUndo/canRedo；恢复后消失的选择键修剪；外部重喂仅栈未触碰时重建基线——`Table.tsx` + `props.ts` + 14 react 新测试）。**批量编辑面板 `toolbar.batch.edit`**（批量按钮改开内置面板：可编辑列下拉 + 值输入 + 应用 = 一次 commitRowList 写所有选中行、选择不变、onDataChange 恰好一次、Esc/外部关闭、`edit:false` 字节不变——`Table.tsx` + 测试）。+4 i18n 键（en+zh：table.undo/redo/batchEdit.column/batchEdit.apply）。core 仅 i18n 默认键、react-only 桥、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AN | **iris 独有增量（react，见「iris 独有」节）**：**列预设 `preset`**（core 新文件 `column-preset.ts`：`applyColumnPreset<C extends ColumnPresetDescriptor>(column, preset)` 框架无关工厂 + 纯函数 `formatMoney`/`formatProgress`/`formatDateValue`/`formatStatus`——preset 只填列上 `undefined` 的字段、用户字段永远赢；`money` = 2 位小数千分位 + 右对齐 + number 编辑器 + 数值字符串 pattern editRules、`progress` = 0..1 → 百分数 + 右对齐、`date` = String 直通 + 左对齐（本地化格式化是调用方职责）、`status` = 大写纯文本 + 居中——13 核心新测试）。**react 桥**：`IrisTableColumn.preset` 单行 prop + Table 单 memo 递归解析（分组叶列继承、零 preset 引用不变）喂 orderedColumns——`types.ts` + `Table.tsx` 编辑。**表格快捷键 `tableShortcuts`**（F2 → `beginEdit` 仅 editable 列；Delete/Backspace → `setCellValue(...,'')` 一次 `commitRowList`；焦点格来自 `keyboardNavigation` roving 焦点、无则惰性；编辑器打开时编辑键优先；事件目标须 grid 单元格）——`props.ts` 单行 prop + root onKeyDown 门扩展 + `Table.shortcuts.test.tsx` 13 新测试。core additive、react-only 桥、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AO | **iris 独有增量（react，见「iris 独有」节）**：**公式列 `formula`**（core 新文件 `formula.ts`：`evaluateFormula` 递归下降求值器——正则扫描 + 白名单函数 SUM/AVG/MIN/MAX/COUNT + 字段引用 + `+ - * / %`、可选 `=` 前缀、未知字段/除模 0/语法错 → null 永不抛错、512 字符/32 层界限；`columnLetter` 双射字母纯函数；`memoizedFormulaValue` 按 (row, formula) WeakMap 记忆——21 核心新测试。react 桥：`getCellValue` 咽喉路由 + 排序双路径（`useTableSort.buildSorter` + `querySortedData`）显式接线 + CSV 影子行 + 编辑 7 入口 `isEditableColumn` 守卫——`types.ts` 单行 prop + `Table.tsx` + `useTableSort.ts` + `Table.formula.test.tsx` 14 新测试）。**行列引用 `showCellRefs`**（表头叶列 muted 字母徽标 `data-iris-cell-ref`（扁平 `ci`/分组 `colStart-1`）+ 前导行号列 `data-iris-row-ref`；`seq || showCellRefs` 合并条件翻转全部 6 轨道站点 + 正文 + 两表头——`props.ts` 单行 prop + `Table.tsx` + 6 新测试）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AP | **iris 独有增量（react，见「iris 独有」节）**：**选择汇总 `selectionSummary`**（`selectable === 'multi'` + 选中 ≥ 1 时工具栏在批量按钮前渲染 `data-iris-selection-summary`：`已选 N 行`（i18n `table.selectionSummary`，`{count}` 插值同 `table.selectedCount`）+ 每 `summary === 'sum'` 叶列 `· 合计 X`（`table.selectionSummarySum`）——与汇总行**同源材料** `aggregate(selectedRows, r => getCellValue(r, col), 'sum')` + 同一 `aggregateAccuracy` 舍入 + `String(value)`，非 sum op 跳过；选中行按 bodyData 序（跨页代理键跳过）；清除按钮 `data-iris-selection-clear` 走共享 `clearSelection` 路径——`props.ts` 单行 prop + `Table.tsx` + `Table.selection-summary.test.tsx` 7 新测试）。**选中行导出 `exportSelectionCsv`**（`IrisTableHandle` 新方法：选中行 bodyData 序 → `exportCsv(withComputedFormulaCells(...), displayColumns)`，与 `exportCurrentViewCsv` 字节同形——公式影子行物化 + 隐藏列排除 + 空选中 `''`；新增 `bodyDataRef` 镜像（F1 回归模式）——`types.ts` + `Table.tsx` + 5 新测试）。+3 i18n 键（en+zh：table.selectionSummary/selectionSummarySum/clearSelection）。core 仅 i18n 默认键、react-only 桥、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AQ | **iris 独有增量（react，见「iris 独有」节）**：**拖拽填充 `rangeFill`**（Excel 式填充句柄：范围右下角格内 6px 主色方块 `data-iris-range-fill`，pointerdown `preventDefault` 停兼容 click、window 捕获相监听阻止浮动工具栏 useDismiss 清范围；拖拽**仅向下/向右**——`elementFromPoint` → `closest` 命中叶格算 drag end，`max(pointer, range.end)` + 表格边界钳制，向上/左不缩范围即无操作；目标格 `data-iris-range-fill-target` 高亮（源范围排除）；pointerup **循环填充** `((r - start.row) % rangeRows + start.row, …)` 经 `getCellValue` 咽喉、公式列与无键行跳过、byKey patch → **一次 `commitRowList`** + `extendRange` 扩范围（Excel parity）；pointercancel 只丢高亮零提交——`props.ts` 单行 prop + `Table.tsx`（模块级纯函数保持单元格箭头复杂度预算）+ `styles.ts`（句柄/目标样式常量）+ `range-fill.test.tsx` 13 新测试）。core 零改动、react-only 桥、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AR | **iris 独有增量（react，见「iris 独有」节）**：**迷你图表 `chartPreview`**（工具栏 ▤ 触发钮 `data-iris-chart-trigger` 开浮动面板 `data-iris-chart-panel`（useFloating 锚触发钮 + portal，Esc/外部/滚动关闭；关闭接线沿用批量编辑面板先例——列选择 listbox 门户到 body，选项按压须选择而非关面板，window 冒泡排除 `[data-iris-select-option]`）；数值叶列 IrisSelect（`typeof getCellValue(row, col) === 'number'` 或 `col.summary === 'sum'` 两条既有信号）+ bar/line 切换（`table.chart.bar/line`）；SVG viewBox 0 0 300 120 **纯结构化 JSX**（零 SVG 字符串/零 dangerouslySetInnerHTML）：core `buildChartData(rows, key)`（`dataDomain` 同款域：空/全空 → {0,1}、平铺按 |v| 填充、null/undefined/非有限 → 空隙）对**当前 filteredData** 前 20 值（超出「共 N 行」复用 `table.total`）；柱 = `var(--iris-primary)` rect、线 = 多段 polyline + 圆点、值标签省略（文档化）——`props.ts` 单行 prop + `Table.tsx` + `ChartPanel.tsx` + `chart-data.ts`（9 核心新测试）+ `chart-preview.test.tsx` 12 react 新测试）。+3 i18n 键（en+zh：table.chart/chart.bar/chart.line）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成 | ✅ react |
| 批 AS | **iris 独有增量（react，见「iris 独有」节）**：**自动刷新 `autoRefresh`**（代理模式 only、`{ intervalMs }` 标量 keyed 定时器——tick 调内置 ↻ 同款 `proxyRef.current.refetch()`（标准 refetch 路径：loading 闪烁文档化、core 无 silent 选项）、`intervalMs ≤ 0` fail-closed、卸载/改值即清理、非代理惰性——`props.ts` 单行 prop + `Table.tsx` 单 effect）。**新鲜度 `freshness`**（工具栏 `data-iris-freshness` 时间戳：core 新纯函数 `formatClock`（本地 24h HH:MM:SS，2 核心新测试）+ 单 effect keyed `[freshness, liveData]` 捕获全部 setLiveData 漏斗（首达也盖戳）、无行隐藏——`props.ts` 单行 prop + `Table.tsx` effect + 工具栏 stamp；`auto-refresh-freshness.test.tsx` 12 react 新测试：fake-timer tick 计数、卸载清理、无 prop 无定时器、intervalMs 变更重启、≤0 fail-closed、loading 闪烁锁定、非代理惰性、初始 data 盖戳、编辑提交更新盖戳、代理首达盖戳、无行隐藏、无 prop 无元素）。+2 i18n 键（en+zh：table.freshness）。core 仅 additive（formatClock + i18n 默认键）、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 135→137） | ✅ react |
| 批 AT | **iris 独有增量（react，见「iris 独有」节）**：**审计日志 `auditLog`**（core 新控制器 `createAuditLog`——`@iris-ui-kit/core/audit-log` 独立子路径：默认 200 有界环形、monotonic `seq` 清空不重置（审计完整性）、`list()` 最新优先、`subscribe`/`getVersion` 桥接——`audit-log.ts` + 11 核心新测试：环裁剪/默认 200/limit 0 无限/最新优先/seq 填充/list 拷贝/清空 seq 不重置/空清空不通知/订阅推送与取消/无订阅惰性）。**双漏斗接线**：`commitRowList(next, type?)` 类型提示（insert/remove/paste/batch/fill/undo/redo，默认 edit）+ 对 `auditRowsRef` 上一列表做轻量 diff（模块级 `auditDiff`，首个分歧行/格，结构变更只记 rowKey，文档化简化）；`commitValue` 经 `recordAuditRef` 镜像记同 diff（type edit）——`Table.tsx` + `props.ts` 单行 prop + `types.ts` handle 两方法 + **NEW `AuditPanel.tsx`**（useFloating + portal + Esc/外部/滚动关闭 + `useSyncExternalStore` 自刷新 + maxHeight 滚动 + muted old→new + 空态/清空钮）+ `audit-log.test.tsx` 9 react 新测试（编辑记一条含列与 old→new、insert 类型、remove 类型、undo 类型、面板最新优先列表现、清空钮、handle clearAuditLog seq 递增、Esc/外部关闭、无 prop 惰性）。+3 i18n 键（en+zh：table.audit/audit.empty/audit.clear）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 137→138） | ✅ react |
| 批 AU | **iris 独有增量（react，见「iris 独有」节）**：**比较视图 `compareWith`**（core 新纯函数 `diffRows(before, after, rowKeyField)`——`diff-rows.ts`，框架无关、逐列 `Object.is`（值同一性，NaN≡NaN、+0≠−0，区别于批 AT 审计 diff 的 `!==`）、变更列按 after 键序、null/undefined 键行跳过；返回 O(1) `status`/嵌套 `cellChanges` Map + added/removed/changed 键数组——`diff-rows.ts` + 19 核心新测试：三分类/变更格 old→new/按键不按下标/NaN 与 ±0 语义/全等零 diff/空前→全 added/空后→全 removed/after 键序/before 独有键→undefined/新增键从 undefined/一致 undefined 非变更/null·undefined 键跳过/数字与字符串键分立/O(1) 查询形/仅 changed 行有 cellChanges/多列同报/纯函数不变输入）。**react 桥**：`compareWith?: Row[]` 单行 prop + 单 memo `diffRows(liveData, compareWith, rowKey)`（无 compareWith 即 null 全路径惰性）：live 行缺于快照 → `data-iris-row-removed`、同在分歧 → `data-iris-row-changed`（变更格 `data-iris-cell-changed` + title `旧值: X → 新值: Y`——`table.compare.tooltip` en+zh，compare 覆盖 tooltipConfig，文档化；列键 `dataIndex ?? key` 同 `getCellValue`，公式列自身不标 chang 文档化；快照独有行 added 无渲染槽位文档化）；行背景 token-only：changed→`--iris-surface-selected`、added→success tint、removed→danger tint（`color-mix`，无引擎回落文档化）——`props.ts` + `Table.tsx`（helper 抽取保持单元格箭头复杂度预算）+ `styles.ts` + `compare.test.tsx` 13 react 新测试（changed/removed 行 attr、变更格 attr + tooltip、tooltipConfig 覆盖与未变更格保留、无 prop 惰性、同快照零 attr、无键行惰性、dataIndex 列、多变更格、null 值 tooltip 无 undefined、去 prop 后 attr 消失）。+1 i18n 键（en+zh）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 138→139） | ✅ react |
| 批 AV | **iris 独有增量（react，见「iris 独有」节）**：**网格键盘增强 `keyboardNavigation`**（`keyboardNavigation` 保持纯 boolean，零新 prop、core 零改动）：Tab/Shift+Tab 行主序移至下/上一格（模块级 `nextRowMajorCell`：`(r,c)`→`(r,c+1)`→`(r+1,0)`，边界钳制**不环绕**——末格 Tab 留在表内不溜走；编辑中 Tab 路径批 J 不变——编辑器 input 无 `data-grid-row` 早退）；Enter = ArrowDown 别名（F2 仍为编辑启动键）；Home/End 行首/行尾（core `nextGridCell` 既有）；PageUp/PageDown 跳 10 行（core 既有，钳制）+ **虚拟滚动**：target 10 行外常不在渲染窗内，`querySelector` 落空——滚 `data-iris-virtual-scroll` 视口 ±10×itemHeight（纯虚拟下 root overflow:hidden，视口即 body 滚动条；`itemHeight` 函数形取当前行高近似）钳到滚动范围，聚焦后置经 `pendingGridFocusRef` + 帧轮询 layout effect（虚拟窗在 IrisVirtualScroll 子内自 rAF→state→commit 重渲，Table effect 不重跑——轮询至目标格出现即落焦，重新导航/卸载取消 rAF，陈旧 pending 丢弃）；非虚拟滚 root ±10×实测行高。`keyboard-extra.test.tsx` 7 react 新测试（Tab 行主序/Shift+Tab 回退/首末格钳制/Home·End/Enter 下移且末行钳制/无 keyboardNavigation 全惰性/虚拟 PageDown 滚动+落焦/虚拟 PageUp 回滚）。additive、react-only、manifest 无 diff | ✅ react |
| 批 AW | **iris 独有增量（react，见「iris 独有」节）**：**NL 列摘要 `nlSummary`**（core 新纯函数 `summarizeColumn(values, label)`——`summary.ts`，框架无关：非空值 ≥60%（含）数值 → `label：共 N 个值，范围 min–max，平均 avg，M 个缺失`（min/max/avg 只取数值、布尔不算、唯一 `.toFixed(1)` 舍入点）；否则分类 top3 整数 `Math.round` 百分比 + `，其余 R 个` 仅 R>0；全空 → `label：无数据`——计数语义复用**抽出的** `countDistinctValues`（value-distribution.ts 计数循环抽出，`valueDistribution` 公开 API 字节不变），`summary.test.ts` 12 核心新测试（数值句/60% 含界/低于 60% 落分类/top3 百分比+其余/≤3 无折/单值/空/全空/缺失计数/舍入/布尔非数值/数值字符串）。react 桥：`nlSummary?: boolean` 单行 prop，右键菜单在**值分布项之后**追加内置项（key `__iris-summary`，i18n `table.summary`；同款去重守卫 + onSelect 拦截改开面板）→ **NEW `SummaryPanel.tsx`**（分布面板同款 useFloating + useDismiss + portal + 虚拟光标锚点，Esc/外部/滚动关闭；标题头 + muted 摘要文本，全 `--iris-*` token；`data-iris-summary-panel`）——`props.ts` + `Table.tsx` 五触点 + `nl-summary.test.tsx` 11 react 新测试（core 3 句 + 菜单项在分布项之后/面板文本/分类列面板/无 prop 惰性/仅分布无摘要/保留键去重/Esc 关闭/dataIndex 间接层）。+1 i18n 键（en `Column summary` / zh `摘要`）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 139→140） | ✅ react |
| 批 BA | **iris 独有增量（react，见「iris 独有」节）**：**版本历史 `versionHistory`**（core 新控制器 `createVersionHistory<Row>`——`version-history.ts`，审计环 1:1 模具：默认 max 20 有界环形、monotonic `index` 清空不重置、`list()` 最新优先条目拷贝（rows 共享引用）、`get(index)` 拷贝/undefined、`subscribe`/`getVersion` 桥接——`version-history.ts` + 14 核心新测试：空起始/变更前 rows+类型+index·at 填充/默认 edit/默认 max 20/环裁剪/limit 0 无限/最新优先/get 拷贝与共享引用/get 未知裁剪清空 undefined/list 拷贝/清空 index 不重置/空清空不通知/订阅推送取消/无订阅惰性）。**单漏斗接线**：`commitRowList(next, type?)` 在 batch-AT 审计前把 `auditRowsRef` 变更前快照 + 同一类型提示推入环（`recordHistory` 普通函数；首个提交即版本 0——无初始播种文档化）；`commitValue`（内联 cell/row 编辑）不产生版本（文档化）；restore 经 `historySuppressRef` 守卫走 `commitRowList(rows, 'undo')`——可审计 + 可撤销、**不推新版本**——`Table.tsx` + `props.ts` 单行 prop（`versionHistory?: { max?: number }`）+ `types.ts` handle 两方法（`getVersions()` 轻量无 rows + `restoreVersion(index)` 未知 index 无操作）+ **NEW `VersionHistoryPanel.tsx`**（useFloating + portal + Esc/外部/滚动关闭 + `useSyncExternalStore` 自刷新 + maxHeight 滚动 + 点击条目即 restore 并关面板 + 空态、刻意无清空钮）+ `version-history.test.tsx` 9 react 新测试（编辑推版本且轻量无 rows、insert/remove 类型与单调 index、内联编辑不推版本、面板最新优先 + 点击 restore 不重推、handle restore 后下一提交续单调 index、未知 index 无操作、undo 重放推 undo 类型、Esc/外部关闭、无 prop 惰性）。+2 i18n 键（en+zh：table.history/history.empty）。core additive、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 143→144） | ✅ react |
| 批 BB | **iris 独有增量（react，见「iris 独有」节）**：**批注编辑 `annotationEditing` / `onAnnotationsChange`**（批 AZ 展示层补上编辑闭环）：`annotationEditing?: boolean` 单行 prop——右键菜单在**摘要项之后**追加内置项（无注记格 → 添加批注 `__iris-annotate`；有注记格 → 编辑批注 `__iris-annotate-edit` + 删除批注 `__iris-annotate-remove`；存在性 = `annotations[cellId(rowKeyOf(row, idx), col.key)]` 非空；与分布/摘要项同款保留键去重守卫）；添加/编辑项 onSelect 拦截改开**浮动编辑面板**（`data-iris-annotate-panel`，分布/摘要面板同款 useFloating + useDismiss + portal + 虚拟光标锚点 + seq remount，Esc/外部/滚动关闭）：textarea `data-iris-annotate-input` 以 `annotations[cellId]` 播种 + 保存钮 `data-iris-annotate-save`（**空文本删键、非空设键**）+ 删除钮 `data-iris-annotate-remove`（仅注记存在时）；菜单删除项**直删不弹面板**。**写通道完全受控**：全部经 `onAnnotationsChange?: (next: Record<string, string>) => void` 单行 prop（`onFiltersChange` 同款形状）——表零内部注记状态（`annotations` 仍是唯一读源）；**无回调时菜单项照常显示但保存/删除惰性**（文档化 fail-safe）——`Table.tsx`（保留键三常量 + 面板 state/接线五触点 + 模块级 `TableAnnotatePanel` 组件——沿用批 AL 批量编辑面板的**内联先例**省文件数，分布/摘要面板的 useFloating+portal 模具）+ `props.ts` 两单行 prop + `annotate-editing.test.tsx` 11 react 新测试（菜单项随注记存在性变化/排在摘要项之后/保存写键端到端角标出现/空文本删键+播种/面板删除钮/菜单删除项直删/无回调惰性文档化/无 prop 无项/Esc+外部关闭/保留键不达用户回调+去重）。+4 i18n 键（en+zh：table.annotate/annotate.edit/annotate.remove/annotate.save）。core 仅 i18n 默认键、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 144→146） | ✅ react |
| 批 BC | **iris 独有增量（react，见「iris 独有」节）**：**跨表引用公式 `formulaTables`**（批 AO 公式列补跨表）：`formulaTables?: Record<string, Row[]>` 单行 prop——core `evaluateFormula` 第 3 可选参（`formula.ts`，框架无关）：TOKEN_RE 加 `!` 新 `bang` 令牌、`parsePrimary` ident 分支解析 `table!field` → 新 `resolveTableField` 读 `formulaTables[table][0][field]`（**首行 only**）——**fail-closed**（缺 tables 参/未知表/空表/未知字段 → 整式 null，own-property 查找防原型泄露，非法 `!` 位置语法错 null，永不抛错）；`memoizedFormulaValue` 缓存键扩 **(row, tables 对象, formula)** 嵌套 WeakMap + 2 参调用模块哨兵（AO 字节兼容）——`formula-tables.test.ts` 19 核心新测试（首行取值/组合运算/函数实参/字符串值流转/缺失表/缺失字段/空表/2 参 feature-off/已知 nullish 强制/非法 bang 位置/空白容忍/原型名/畸形载荷/无本地回退/memo 键三分）。react 桥**双通道**：渲染期 ~30 `getCellValue` 站点 + `querySortedData` 比较器读模块槽 `currentFormulaTables`（每次 render 顶部赋值，React 同步渲染行走隔离）；两条挂载期 CSV handle（`exportCurrentViewCsv`/`exportSelectionCsv`）经 `formulaTablesRef` 镜像**显式传参**给 `withComputedFormulaCells`（按需调用零竞态）——`props.ts` 单行 prop + `Table.tsx`（模块槽 + ref 镜像 + 咽喉穿线）+ `useTableSort.ts`（选项 `formulaTables` 穿入 `buildSorter`，单/多列比较器 + dep 数组——**第 4 求值点**）+ `formula-tables.test.tsx` 12 react 新测试（渲染首行值/缺失表与字段空格/无 prop 惰性/排序 asc+desc/筛选/两条 CSV handle/display-only/汇总合计/新 tables 对象重算/多表隔离/分组+dataIndex 覆盖）。core 仅 additive（`FormulaTables` 类型 barrel 导出）、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 146→147） | ✅ react |
| 批 BD | **iris 独有增量（react，见「iris 独有」节）**：**协作光标 `presence`**（vxe 无任何协作者光标共享）：`presence?: IrisTablePresenceEntry[]`（`{ id, name, color, cellKey }` 具名导出接口，`props.ts` 在 `onAnnotationsChange` 之后；`cellKey` = `${rowKeyVal}::${colKey}` 规范定界符——与内部 `cellId` 和批 AZ `annotations` 键同款、零新键发明；受控数组契约文档化——**新数组引用重渲、原地突变不重渲**，与 `data`/`annotations` 同契约）。**渲染**（批 AZ/AQ 热格模式克隆）：render-scoped memo 按 cellKey 分组（每个可见格一次 Map 查找）+ 3 模块 helper（`presenceOf`/`presenceStyle`/`renderPresenceLabels`）保持单元格箭头复杂度预算；有光标格 `data-iris-presence="true"` + `outline: 2px solid <首条目 color>` + `position: relative`；角标 `data-iris-presence-label` + `-id`/`-name`（左上 `insetInlineStart` RTL 安全，`pointerEvents: none`，同格多人 `top: i*14` 级联、首条目描边色赢）；**角落预算实测零冲突**（脏点/注记徽标右上 `insetInlineEnd`、填充句柄右下）。**纯展示**：零 state/store/effect/handle/i18n/core 改动；未知 cellKey fail-inert；样式全 token（`PRESENCE_LABEL_STYLE`：font-size-xs/space-xxs/`primary-foreground` 回落，背景 = 条目 color 逐字使用——数据例外）——`types.ts` + `props.ts` + `styles.ts` + `Table.tsx` + `index.ts` + `presence.test.tsx` 9 react 新测试（光标描边+角标/`::` 定界符/多格/同格堆叠首赢/无 presence 零节点/空数组零节点/新数组重渲/未知 cellKey 惰性/token+样式断言）。core 零改动、react-only 桥、其余框架零改动；manifest 已重新生成（propCount 147→148） | ✅ react |
| 批 BF | **iris 独有增量（react，见「iris 独有」节）**：**数据导出时间线 `exportVersionCsv`**（vxe 无版本历史概念，更无历史导出）：`IrisTableHandle.exportVersionCsv(index): string` handle 方法（`types.ts` 在 `restoreVersion` 之后 + `Table.tsx` handleRef 在 `restoreVersion,` 之后 +14 行）——`history.get(index)`（批 BA 环形控制器既有 API）→ 未知 index（越界/裁剪/清空）或**无 `versionHistory` prop → `''`**（`getVersions()` 探测，永不 throw）；有效 index → 批 AP `exportSelectionCsv` **同款管线**（`exportCsv(withComputedFormulaCells(...))`：同一序列化器/列集/批 AY 掩码/OWASP 中和，公式列影子行物化、隐藏列排除）。**行源** = 环中**变更前快照**（原始行序）vs `exportCurrentViewCsv` 的**已筛选+已排序**活动视图——有筛选/排序时两者不等（review LOW，按基线行源决定，JSDoc 措辞已收敛）；**无筛选/排序时** `restoreVersion(i)` 后两值字节相等（测试 #6 核心断言）。公式物化用当前公式状态（跨表引用读活表，restore-再导出同近似）。**无 UI**（面板不加导出钮——刻意非目标，restore + `exportCurrentViewCsv` 覆盖面板路径）。`export-version-csv.test.tsx` 8 react 新测试（首提交版本 0 = 初始数据/逐提交变更前语义/越界空串/环裁剪空串/无 prop 惰性/restore 后字节相等/掩码一致/公式物化+隐藏列排除）。core 零改动、react-only 桥、其余框架零改动；manifest 无 diff（handle 方法 manifest 不可见，`restoreVersion`/`exportSelectionCsv` 先例） | ✅ react |

**剩余缺口（svelte 核心批 AC 已对齐——proxyConfig/formConfig/multiSort/columnVisibility/filters/seq/spanMethod/工具栏扩展；svelte 交互批 AD 为下一轮——rowDrag/columnDrag/行编辑模式/contextMenu/filterValues 筛选面板/代理方法（loadData/reloadData/commitProxy/getProxyInfo/clearSort/clearFilter）；solid 批 AA+AB 已对齐核心批 + 交互批；vue 批 X–Z 已对齐核心批 + 交互批 + lazyLoad，其余缺口同下）**：parseCsv/printable（批 5）、removeRows/Tab 编辑导航（批 J 剩余；行编辑模式 Tab 提交导航 solid 批 AB 已含，cell 模式编辑导航仍缺）、column.link（批 L；proxy 累计序号/工具栏导出/批量按钮 solid 批 AA 已含，vue 仍缺）、行分组 groupBy/列宽 auto（批 M）、高度 height/minHeight/maxHeight/表头表尾对齐/footerMethod/悬停高亮（批 N）、clipConfig/fnr（批 O）、表头合并 mergeHeaderCells/表尾合并 footerSpanMethod/round/padding/aggregateAccuracy/表头表尾提示（批 P）、autoResize/scrollbarConfig/editDirtyConfig（批 Q）、zIndex/syncResize/keepSource/rowId/mergeFooterItems（批 R）、customConfig（批 S）、handle 方法（scrollToRow/toggleRowExpand/setCurrentRow/setCurrentColumn）/双击等事件/radio 单列/showTotal（批 T）、zoomConfig/layouts/visibleMethod（批 U）、onEditStart/onEditClosed/onScroll/onSelectAllChange（批 V 剩余）、getFilteredData/exportCurrentViewCsv/showHeaderOverflow/showFooterOverflow（批 W）、col.filterMethod/seqMethod/columnOrder 工具栏列面板（批 Y 交接、批 Z 未纳入）；批 AE 交接：vue/solid/svelte 的 flat+detail+virtual / tree+virtual 组合护栏仍为旧守卫（react 已定义参考语义，见 `TableBody.ts:52`/`TableBody.tsx:115`/`IrisTable.svelte` `useVirtual`）；三端 example e2e 已由批 AF 覆盖（`vxe-grid.spec.ts` ×3，与既有 visual-parity 同模式按 app `pnpm e2e` 运行）。

react 2648 tests（批 CW +12，import-preview；批 CV +18，pinned-drag（含 review 门禁修复：gapped 态零 dx no-op——预算从前缀宽起算 + T18 回归）；批 CU +14，copy-with-format；批 BX +17，pin-column-menu；批 BW +12，context-quick-actions；批 BR +12，validation-summary；批 BV +14，export-comparison-csv；批 BU +10，watermark（含 review 门禁修复：sticky 首子锚视口 + overlay 原语同构，10 测试锁定锚定策略）；批 BT +15，selection-drag（含 review 修复：capture 延迟到拖拽开始 + pointercancel 清臂）；批 BS +13，group-multicol；批 BQ +12，edit-autosave；批 BP +13，copy-format；批 BO +10，reset-column-widths；批 BN +13，row-height；批 BM +13，note-popover；批 BL +12，perf-stats；批 BJ +17，cell-permission；批 BI +14，sparkline（含 review 修复 2：coercion/有限域回归）；批 BH +12，group-collapse；批 BG +11，keymap；批 BF +8，export-version-csv；批 BE +13，locked-cells（含 review 修复后 range-clear 零提交/混合 2）；批 BD +9，presence；批 BC +12，formula-tables；批 BB +11，annotate-editing；批 BA +9，version-history；批 AZ +8，annotations；批 AW +11，nl-summary；批 AV +7，keyboard-extra；批 AU +13，compare；批 AT +9，audit-log；批 AR +13，chart-preview；批 AS +12，auto-refresh-freshness；BQ 遗留探针 zz-bq-probe +1，随批 BS 门禁入库）· vue 1531 tests · solid 974 tests + 34 hydration · svelte 916 tests + 31 hydration · core 1517 tests（批 BL +12，perf-stats；批 BK +30，generate-rows（含 review 修复 2：小数 bound 钳制回归）；批 BI +6，chartDomain；批 BG +12，keymap；批 BC +19，formula-tables；批 BA +14，version-history；批 AW +12，summary；批 AU +19，diff-rows；批 AT +11，audit-log；批 AS +2，formatClock）· 180/180 turbo · 审计 0 · manifest 155×4（check:manifest 通过）

## vxe-tableProps 覆盖总结（react）

- **VxeTableProps ~90 项 全部覆盖（react）**（编辑/选择/虚拟/列宽/排序/筛选/展开/树/汇总/合并/剪贴板/查找替换/提示/高度/滚动条/脏标记/列自定义面板 …）。
- **VxeGridProps（批 R 补齐）**：zIndex / syncResize / keepSource / rowId / mergeFooterItems 全部 ✓（react）。
- **无剩余功能缺口**。vxe-grid 功能面全部移植完成（tableProps ~90 项 + 方法 + 事件 + grid 特有 layouts/zoom/visibleMethod，均 react）。有意跳过项见文首「最终覆盖总结」：`fit`（弃用）、`animat`/`delayHover`/`params`（内部）、zoom 与列面板同开的两次 Esc、行编辑模式不报 onEditStart/onEditClosed、异步校验提交不报 onEditClosed——均为文档化决策；虚拟树组合（原「未来工作」）已由批 AE 完成 react 参考语义，vue/solid/svelte 组合护栏为交接项。

## 组合接口说明

- **外部能力**（resizable/movable/sortable/clickOutside/hotkey）→ `IrisCompose` 拼接
- **内部能力**（virtual/multiple/rowDrag/columnDrag/undo/编辑）→ Table prop / hook 组合
  （均基于 core 控制器：createVirtualizer/createSelectionModel/createSortable/
  createUndoStack/createCellEdit/createEditRules）

## 决策

- 逻辑下沉 core（规则引擎/拖拽/撤销）；适配器薄桥
- additive API（新配置默认关闭，零破坏）
- 组合接口优先：能用 IrisCompose / core 控制器组合的，不重复造轮子
